---
title: Steering Sparse Autoencoder Latents to Control Dynamic Head Pruning in Vision Transformers (Student Abstract)
title_zh: 引导稀疏自编码器潜变量以控制视觉 Transformer 中的动态多头剪枝（学生摘要）
authors: "Yousung Lee, Dongsoo Har"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.26743v1"
tags: ["query:cv"]
score: 9.0
evidence: 利用稀疏自编码器对视觉Transformer进行动态多头剪枝
tldr: 针对视觉Transformer（ViT）动态剪枝缺乏可解释性和可控性的问题，本文提出将稀疏自编码器（SAE）与剪枝机制结合。通过在残差嵌入上训练SAE并操纵其稀疏潜变量，实现了对剪枝决策的精准引导。研究发现，特定类别的转向策略能显著降低计算开销并提升准确率，为ViT的效率优化与机械可解释性架起了桥梁。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26743v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of dynamic head pruning before (left) and after (right) SAE latent steering, where x denotes the CLS token from the final-layer residual stream input, and x̃ denotes the steered embedding.\", \"page\": 1, \"index\": 1, \"width\": 502, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2603.26743v1/fig-002.webp\", \"caption\": \"Figure 5: Semantic structure of SAE latents, showing classwise overlap and distinct latent usage patterns.\", \"page\": 3, \"index\": 2, \"width\": 500, \"height\": 570}]"
motivation: 现有的ViT动态剪枝策略虽然能提高效率，但其决策过程往往难以解释且缺乏灵活的控制手段。
method: 在ViT末层残差嵌入上训练SAE，利用其解耦出的稀疏潜变量通过不同策略引导和干预动态剪枝决策。
result: "实验表明，按类别引导潜变量可识别出高效的类特定头子集，例如在“碗”类上实现了准确率提升6%的同时将头使用率降低了一半以上。"
conclusion: 稀疏潜变量特征能够实现对动态剪枝的类别级精准控制，有效结合了模型剪枝的效率与机械可解释性。
---

## 摘要
视觉 Transformer (ViT) 中的动态多头剪枝通过移除冗余注意力头来提高效率，但现有的剪枝策略通常难以解释和控制。在这项工作中，我们通过将稀疏自编码器 (SAE) 与动态剪枝相结合，提出了一个新颖的框架，利用其将密集嵌入解耦为可解释且可控的稀疏潜变量的能力。具体而言，我们在 ViT 的最后一层残差嵌入上训练 SAE，并通过不同的策略放大稀疏潜变量以改变剪枝决策。其中，按类别引导揭示了能够保持准确性的紧凑且特定于类别的注意力头子集。例如，“碗”类别通过注意力头 h2 和 h5，在减少注意力头使用率（从 0.72 降至 0.33）的同时，提高了准确率（从 76% 提升至 82%）。这些结果表明，稀疏潜特征能够实现对动态剪枝的类别特定控制，有效地在 ViT 的剪枝效率与机械可解释性之间架起了桥梁。

## Abstract
Dynamic head pruning in Vision Transformers (ViTs) improves efficiency by removing redundant attention heads, but existing pruning policies are often difficult to interpret and control. In this work, we propose a novel framework by integrating Sparse Autoencoders (SAEs) with dynamic pruning, leveraging their ability to disentangle dense embeddings into interpretable and controllable sparse latents. Specifically, we train an SAE on the final-layer residual embedding of the ViT and amplify the sparse latents with different strategies to alter pruning decisions. Among them, per-class steering reveals compact, class-specific head subsets that preserve accuracy. For example, bowl improves accuracy (76% to 82%) while reducing head usage (0.72 to 0.33) via heads h2 and h5. These results show that sparse latent features enable class-specific control of dynamic pruning, effectively bridging pruning efficiency and mechanistic interpretability in ViTs.
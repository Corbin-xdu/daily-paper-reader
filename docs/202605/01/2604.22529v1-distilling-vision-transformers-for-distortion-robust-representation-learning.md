---
title: Distilling Vision Transformers for Distortion-Robust Representation Learning
title_zh: 蒸馏视觉 Transformer 以实现失真鲁棒的表示学习
authors: "Konstantinos Alexis, Giorgos Giannopoulos, Dimitrios Gunopulos"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22529v1"
tags: ["query:cv"]
score: 9.5
evidence: 视觉Transformer的知识蒸馏框架
tldr: 本研究针对自监督学习在处理失真数据时的局限性，提出了一种基于预训练视觉Transformer（ViT）的非对称知识蒸馏框架。该框架通过让教师模型处理干净图像而学生模型处理对应的失真图像，并引入全局嵌入、补丁级特征和注意力图的多层级对齐，使学生模型能够学习到鲁棒的视觉表征。实验证明，该方法在多种失真环境下的图像分类任务中表现优异，显著提升了模型在恶劣观测条件下的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22529v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of our multi-target distillation framework. A frozen teacher ViT processes clean images while a student ViT of identical architecture receives their distorted counterparts, being trained to align with the teacher at three complementary levels: global embeddings, patch-level features, and attention maps. In this way, the student learns to recover clean semantic representations directly from corrupted inputs, producing robust encoders that are effective for downstream tasks on distorted images.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.22529v1/fig-002.webp\", \"caption\": \"Fig. 4. Attention maps of our distilled student models and the supervised baselines on ImageNet-100 validation images, under different distortion types. For each example, we show the original image, the distorted input, and the attention maps predicted by the supervised baseline and the encoder trained by our method.\", \"page\": 13, \"index\": 2, \"width\": 710, \"height\": 258}]"
motivation: 旨在解决自监督学习在缺乏干净观测数据或面对图像失真时难以学习到鲁棒视觉表征的问题。
method: 采用非对称知识蒸馏，通过对齐教师（处理干净图像）与学生（处理失真图像）在全局、补丁及注意力层级的特征来实现。
result: 在多个数据集的图像分类任务中，该方法在应对各种失真情况时均优于现有的替代方案。
conclusion: 证明了利用预训练模型进行多层级蒸馏可以有效提升模型对失真数据的表征能力和下游任务性能。
---

## 摘要
自监督学习在从干净数据中学习视觉表示方面取得了显著成功，但在干净观测数据稀疏或完全不可用时仍然面临挑战。在本文中，我们证明了可以利用预训练的视觉模型来学习失真鲁棒的表示，这些表示随后可以有效地应用于处理失真观测数据的下游任务。具体而言，我们提出了一种非对称知识蒸馏框架，其中教师和学生模型都初始化自同一个预训练的视觉 Transformer（Vision Transformer），但接收每张图像的不同视图：教师处理干净图像，而学生则观察其失真版本。我们引入了多级蒸馏，对齐了全局嵌入、补丁级特征和注意力图，并展示了尽管学生模型从未直接访问干净数据，但仍能够逼近干净图像的表示。我们在多个数据集和各种失真情况下的图像分类任务上评估了我们的方法，在相同的人工监督量下，其表现始终优于现有的替代方案。

## Abstract
Self-supervised learning has achieved remarkable success in learning visual representations from clean data, yet remains challenging when clean observations are sparse or not available at all. In this paper, we demonstrate that pretrained vision models can be leveraged to learn distortion-robust representations, which can then be effectively applied to downstream tasks operating on distorted observations. In particular, we propose an asymmetric knowledge distillation framework in which both teacher and student are initialized from the same pretrained Vision Transformer but receive different views of each image: the teacher processes clean images, while the student sees their distorted versions. We introduce multi-level distillation that aligns global embeddings, patch-level features, and attention maps and show that the student is able to approximate clean-image representations despite never directly accessing clean data. We evaluate our approach on image classification tasks across several datasets and under various distortions, consistently outperforming existing alternatives for the same amount of human supervision.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distilling Vision Transformers for Distortion-Robust Representation Learning》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在现实场景（如医疗影像、遥感、自动驾驶）中，获取高质量、干净的图像数据往往成本高昂或不可行，模型经常需要直接处理带有噪声、模糊或遮挡的失真图像。传统的监督学习和自监督学习模型在面对严重失真时，其语义表示能力会大幅下降。
**核心问题：** 能否在仅观察失真图像的情况下，通过利用预训练模型的先验知识，恢复出接近干净图像的语义特征空间？
**整体含义：** 本文提出将鲁棒性学习建模为一种“潜在空间中的表示恢复”问题，通过知识蒸馏让模型在不接触干净像素的情况下，学习到对失真具有不变性的鲁棒特征。

### 2. 论文提出的方法论
论文提出了一种**非对称知识蒸馏框架**，核心思想是让学生模型在失真输入下模拟教师模型在干净输入下的行为。

*   **核心架构：** 教师和学生模型均初始化自相同的预训练 ViT（如 DINO-ViT-B/16）。教师模型参数冻结，处理干净图像 $x$；学生模型参数可调，处理对应的失真图像 $\tilde{x}$。
*   **多层级蒸馏目标（Multi-Target Distillation）：**
    1.  **全局语义对齐（Global Semantic Alignment）：** 通过均方误差（MSE）对齐 `[CLS]` 标记的嵌入向量，确保学生模型捕捉到全局类别信息。
    2.  **局部空间对齐（Local Spatial Alignment）：** 对齐所有补丁标记（Patch Tokens）的嵌入向量，防止失真导致局部空间结构坍塌。
    3.  **注意力结构对齐（Attention Structure Alignment）：** 提取最后一层 Transformer 的多头自注意力图，通过 KL 散度对齐 `[CLS]` 对各补丁的注意力分布，保留模型处理空间关系的方式。
*   **总损失函数：** $L = \lambda_{cls} L_{cls} + \lambda_{patch} L_{patch} + \lambda_{attn} L_{attn}$，其中通过设置较大的 $\lambda_{attn}$ 来平衡不同量级的损失。

### 3. 实验设计
*   **数据集：**
    *   **预训练/域内评估：** ImageNet-100（100个类别的子集）。
    *   **迁移学习评估：** CIFAR-10/100、STL-10（自然图像）、RESISC45（遥感）、CAMELYON17（医疗病理切片）。
*   **失真类型：** 随机像素遮挡（75%, 90%）、高斯噪声（$\sigma=0.3, 0.5$）、高斯模糊（不同核大小）。
*   **对比方法（Baselines）：**
    1.  **Supervised Baseline：** 在失真图像上直接进行端到端监督微调。
    2.  **Contrastive Inversion (CI)：** 基于 CLIP 预训练模型的对比学习恢复方法（前人 SOTA）。

### 4. 资源与算力
*   **硬件：** 使用了 2 台 NVIDIA RTX A6000 GPU。
*   **训练细节：** 蒸馏预训练阶段进行 25 个 epoch，Batch Size 为 128。
*   **优化器：** AdamW，峰值学习率为 $10^{-5}$，采用余弦退火和混合精度训练（float16）。

### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了 6 个不同领域的数据集，测试了 3 大类、多强度的失真场景。
*   **消融实验：** 专门针对全局、局部、注意力三个蒸馏组件进行了组合消融，验证了各部分的互补性。
*   **充分性与公平性：** 实验包含了鲁棒性随失真强度增加的变化曲线，以及标签效率（Label-efficient）实验（从 0.5% 到 100% 标签）。所有对比方法均在相同的预训练权重和评估协议下进行，结果取 10 次随机实验的均值和标准差，具有较高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **性能领先：** 在大多数失真设置下，该方法显著优于监督基线和基于对比学习的 CI 方法，尤其在噪声和模糊处理上表现突出。
*   **迁移能力强：** 在 ImageNet-100 上学习到的鲁棒性可以很好地迁移到遥感和医疗等跨域任务中。
*   **标签效率极高：** 仅需 5%-10% 的标签进行微调，性能即可接近全监督模型，说明蒸馏过程确实让学生模型掌握了稳健的语义结构。
*   **注意力恢复：** 可视化分析显示，即便输入极度模糊或破碎，学生模型仍能产生与干净图像高度一致的注意力图，而监督基线则会失效。

### 7. 优点（亮点）
*   **多级对齐机制：** 不仅仅对齐最后的特征，还通过注意力图对齐捕捉了 ViT 的内部推理逻辑。
*   **无需干净数据：** 在下游任务微调阶段，学生模型完全不需要接触干净图像，非常符合实际应用场景。
*   **解释性强：** 通过注意力图可视化直观证明了模型在失真下依然能“聚焦”于核心语义区域。

### 8. 不足与局限
*   **失真特异性：** 目前的方法需要针对每种特定的失真类型训练一个专门的编码器，缺乏一个能同时处理多种未知或混合失真的统一模型。
*   **计算开销：** 虽然推理时只需学生模型，但训练阶段需要同时运行教师和学生模型，且多级蒸馏增加了计算复杂度。
*   **预训练依赖：** 效果高度依赖于初始预训练模型（如 DINO）的质量，若预训练模型本身对某些特征不敏感，蒸馏也难以补救。

（完）

---
title: "Random Cloud: Finding Minimal Neural Architectures Without Training"
title_zh: Random Cloud：无需训练即可寻找极小神经架构
authors: Javier Gil Blázquez
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26830v1"
tags: ["query:cv"]
score: 8.5
evidence: 用于寻找极小拓扑结构的无训练神经架构搜索
tldr: 本研究提出Random Cloud方法，一种无需训练的神经架构搜索技术，旨在通过随机探索和渐进式结构缩减寻找最小前馈网络拓扑。与传统的先训练后剪枝方法不同，该方法在评估阶段不使用反向传播，仅对随机初始化的网络进行拓扑缩减，最后才训练最优候选模型。实验表明，该方法在多个基准测试中优于量级剪枝，且由于避免了全量训练，其计算效率显著提高。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26830v1/fig-001.webp\", \"caption\": \"Table 1: Test accuracy (%) and parameter reduction for each method. All pruned methods use the same target topology discovered by the Random Cloud. Best pruned result in bold.\", \"page\": 3, \"index\": 1, \"width\": 582, \"height\": 693}]"
motivation: 旨在解决传统剪枝方法需要完整的“训练-剪枝-再训练”循环所导致的计算成本高昂且效率低下的问题。
method: 通过对随机初始化的网络进行随机探索和渐进式拓扑缩减，在不进行反向传播的情况下筛选出最小候选架构。
result: "在7个分类基准测试中的6个表现优于基准，其中Sonar数据集准确率提升4.9%，且计算成本仅为全量训练的0.67至0.94倍。"
conclusion: Random Cloud证明了在不经过预训练的情况下也能有效发现高效的最小神经架构，为低资源环境下的模型压缩提供了新思路。
---

## 摘要
我提出了 Random Cloud 方法，这是一种无需训练的神经架构搜索方法，通过随机探索和渐进式结构缩减来发现极小前馈网络拓扑。与需要完整的“训练-剪枝-再训练”周期的训练后剪枝方法不同，该方法在不进行反向传播的情况下评估随机初始化的网络，逐步缩减其拓扑结构，并仅在最后训练最佳的极小候选模型。我在 7 个分类基准测试中将其与量级剪枝（magnitude pruning）和随机剪枝（random pruning）基准进行了对比评估。Random Cloud 在 7 个数据集中的 6 个上达到或超过了这两个基准，在 Sonar 数据集上取得了显著的统计学提升（相比量级剪枝准确率提升 4.9 个百分点，p=0.017），同时减少了 87% 的参数。至关重要的是，该方法在 5 个数据集中的 4 个上比两种剪枝基准都快（仅为完整训练成本的 0.67-0.94 倍），因为它完全避免了对全尺寸网络的训练。

## Abstract
I propose the \emph{Random Cloud} method, a training-free approach to neural architecture search that discovers minimal feedforward network topologies through stochastic exploration and progressive structural reduction. Unlike post-training pruning methods that require a full train-prune-retrain cycle, this method evaluates randomly initialized networks without backpropagation, progressively reduces their topology, and only trains the best minimal candidate at the end. I evaluate on 7 classification benchmarks against magnitude pruning and random pruning baselines. The Random Cloud matches or outperforms both baselines in 6 of 7 datasets, achieving statistically significant improvements on Sonar ($+4.9$pp accuracy, $p{=}0.017$ vs magnitude pruning) with 87\% parameter reduction. Crucially, the method is faster than both pruning baselines in 4 of 5 datasets (0.67--0.94$\times$ the cost of full training), since it avoids training the full-size network entirely.
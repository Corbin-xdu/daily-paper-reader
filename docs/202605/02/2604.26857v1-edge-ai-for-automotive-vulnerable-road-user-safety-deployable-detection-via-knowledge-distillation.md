---
title: "Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation"
title_zh: 针对汽车弱势路面使用者安全的边缘人工智能：通过知识蒸馏实现可部署的检测
authors: "Akshay Karjol, Darrin M. Hanna"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26857v1"
tags: ["query:cv"]
score: 10.0
evidence: 知识蒸馏用于紧凑型YOLOv8学生模型并保持量化鲁棒性
tldr: "本研究针对汽车弱势群体安全，提出基于知识蒸馏的边缘AI部署方案。为解决大模型在INT8量化时精度骤降及小模型性能不足的问题，利用YOLOv8-L指导YOLOv8-S。实验表明，该方法实现3.9倍压缩并显著增强量化鲁棒性，在保持召回率的同时提升了精度并减少44%误报，为边缘端安全检测提供了有效路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26857v1/fig-001.webp\", \"caption\": \"Fig. 3. Deployment comparison: KD INT8 achieves highest precision (0.75) and lowest FAR (0.25) among all deployable configurations.\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-002.webp\", \"caption\": \"Fig. 2. Quantization impact: FP32 (solid) vs INT8 (hatched). Teacher collapses (−23% mAP, −24% precision), students retain performance. KD gains +4% precision under INT8.\", \"page\": 4, \"index\": 2, \"width\": 512, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-003.webp\", \"caption\": \"Fig. 1. BDD100K class distribution showing severe imbalance. VRU classes (red) are significantly underrepresented compared to dominant classes such as cars.\", \"page\": 3, \"index\": 3, \"width\": 514, \"height\": 356}]"
motivation: 针对边缘硬件部署，大型检测模型在INT8量化后精度会剧烈下降，而小型模型原生性能受限，难以满足弱势群体安全检测的需求。
method: 采用知识蒸馏框架，使轻量化YOLOv8-S学生模型学习YOLOv8-L教师模型的特征，并结合训练后量化技术进行评估。
result: "学生模型在INT8量化下表现稳健，精度比直接训练提升14.5%，且误报率较量化失效的教师模型降低了44%。"
conclusion: 知识蒸馏是实现在计算受限的边缘设备上部署高精度、高可靠性弱势群体检测系统的关键技术。
---

## 摘要
在边缘硬件上部署用于弱势路面使用者（VRU）安全的高精度目标检测，需要平衡模型容量与计算约束。大型模型虽然精度高，但在边缘部署所需的 INT8 量化下表现不佳，而小型模型则会牺牲检测性能。本文提出了一种知识蒸馏（KD）框架，训练紧凑的 YOLOv8-S 学生模型（11.2M 参数）来模仿 YOLOv8-L 教师模型（43.7M 参数），在实现 3.9 倍压缩的同时保留了量化鲁棒性。我们在全规模 BDD100K 数据集（7万张训练图像）上进行了评估，并采用了训练后量化（PTQ）至 INT8。教师模型在 INT8 下性能发生灾难性下降（mAP 下降 23%），而经 KD 训练的学生模型则保持了精度（mAP 仅下降 5.6%）。分析表明，知识蒸馏传递的是精度校准而非原始检测能力：在 INT8 下，KD 学生模型的精度达到 0.748，而直接训练的模型仅为 0.653，在等效召回率下提升了 14.5%，且相比性能崩溃的教师模型，虚警率降低了 44%。在 INT8 量化下，KD 学生模型的精度（0.748）甚至超过了体积大 3.9 倍的教师模型的 FP32 精度（0.718）。这些发现确立了知识蒸馏是在边缘硬件上部署高精度、安全关键型 VRU 检测的必要条件。

## Abstract
Deploying accurate object detection for Vulnerable Road User (VRU) safety on edge hardware requires balancing model capacity against computational constraints. Large models achieve high accuracy but fail under INT8 quantization required for edge deployment, while small models sacrifice detection performance. This paper presents a knowledge distillation (KD) framework that trains a compact YOLOv8-S student (11.2M parameters) to mimic a YOLOv8-L teacher (43.7M parameters), achieving 3.9x compression while preserving quantization robustness. We evaluate on full-scale BDD100K (70K training images) with Post-Training Quantization to INT8. The teacher suffers catastrophic degradation under INT8 (-23% mAP), while the KD student retains accuracy (-5.6% mAP). Analysis reveals that KD transfers precision calibration rather than raw detection capacity: the KD student achieves 0.748 precision versus 0.653 for direct training at INT8, a 14.5% gain at equivalent recall, reducing false alarms by 44% versus the collapsed teacher. At INT8, the KD student exceeds the teacher's FP32 precision (0.748 vs. 0.718) in a model 3.9x smaller. These findings establish knowledge distillation as a requirement for deploying accurate, safety-critical VRU detection on edge hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于自动驾驶中**弱势路面使用者（VRU，如行人、骑行者、摩托车手）**的检测安全。
*   **核心矛盾**：在边缘硬件（如 NVIDIA Jetson）上部署深度学习模型时，必须在模型容量与计算约束之间取得平衡。
*   **技术痛点**：
    1.  **大模型量化失效**：大型模型（如 YOLOv8-L）虽然精度高，但在边缘部署所需的 INT8 量化下，由于权重分布较宽，精度会发生灾难性下降。
    2.  **小模型精度不足**：小型模型（如 YOLOv8-S）虽然运行快，但在处理样本较少的 VRU 类别时表现不佳。
*   **研究目标**：探索知识蒸馏（KD）是否能让小型模型在获得 3.9 倍压缩的同时，继承大模型的精度校准能力并增强 INT8 量化的鲁棒性。

### 2. 方法论
论文提出了一个结合**任务减弱（Task-dampened）**策略的知识蒸馏框架：
*   **核心思想**：通过让紧凑的学生模型（YOLOv8-S）模仿大型教师模型（YOLOv8-L）的软输出（Soft Labels）和中间特征，使其学习到类别间的相似性和更优的置信度校准。
*   **关键技术细节**：
    *   **损失函数**：$L_{total} = \alpha L_{task} + \beta L_{logit} + \gamma L_{feat}$。
    *   **Logit 蒸馏**：使用带温度系数（T=10）的 KL 散度，使输出分布平滑化，捕捉一热编码（One-hot）标签缺失的类别相关性。
    *   **特征蒸馏**：通过 L2 距离对齐学生与教师骨干网络的中间特征图。
    *   **任务减弱策略**：将任务损失权重 $\alpha$ 设为 0.5（通常为 1.0），降低地面真值（GT）的监督强度，迫使学生模型更多地吸收教师模型的“软知识”。
*   **量化流程**：采用 TensorRT 的训练后量化（PTQ）技术，将 FP32 模型转换为 INT8。

### 3. 实验设计
*   **数据集**：使用 **BDD100K**，这是目前最大的驾驶场景数据集之一。包含 70K 训练集、10K 验证集。
*   **Benchmark（基准）**：
    *   **教师模型**：直接训练的 YOLOv8-L（43.7M 参数）。
    *   **直接学生模型**：直接在 GT 上训练的 YOLOv8-S（11.2M 参数）。
    *   **KD 学生模型**：通过教师引导训练的 YOLOv8-S。
*   **对比维度**：FP32 与 INT8 精度对比、VRU 特定类别（行人、骑行者、摩托车手）的 mAP、虚警率（FAR）、推理吞吐量（FPS）。

### 4. 资源与算力
*   **训练算力**：使用云端 GPU 基础设施，型号为 **NVIDIA RTX PRO 6000 Blackwell (96GB VRAM)**。
*   **训练时长**：
    *   教师模型：7 小时。
    *   直接学生模型：2.7 小时。
    *   KD 学生模型：4.9 小时（由于需要教师模型前向传播，开销增加）。
*   **推理测试**：在 **NVIDIA RTX 5070 (8GB VRAM)** 上进行，以模拟接近边缘端的部署环境。

### 5. 实验数量与充分性
*   **实验规模**：在 7 万张图像的全规模数据集上验证，而非小样本实验，具有较高的统计说服力。
*   **消融实验**：针对任务损失权重 $\alpha$ 做了专门的对比实验（$\alpha=0.5$ vs $\alpha=1.0$），证明了“任务减弱”是精度校准转移的关键。
*   **客观性**：实验对比了相同架构的学生模型在不同训练策略下的表现，控制变量严格，评估指标涵盖了安全关键的“虚警率”。

### 6. 主要结论与发现
*   **KD 增强量化鲁棒性**：INT8 量化后，教师模型 mAP 下降 23.4%（崩溃），而 KD 学生模型仅下降 5.6%。
*   **精度校准转移**：KD 转移的是“精度（Precision）”而非“召回率（Recall）”。在 INT8 下，KD 学生比直接训练的学生精度提升 14.5%，**虚警率降低了 44%**。
*   **性能超越**：在 INT8 量化下，KD 学生模型的精度（0.748）甚至超过了体积大 3.9 倍的教师模型在 FP32 下的精度（0.718）。
*   **实时性**：KD 学生模型在 RTX 5070 上达到 464 FPS，完全满足实时性要求。

### 7. 优点
*   **实战导向**：直接针对边缘 AI 部署中的“量化精度崩塌”这一核心痛点。
*   **深入的机制分析**：论文不仅展示了结果，还通过分析发现 KD 的本质作用在于“置信度校准”，并指出了任务损失权重对校准转移的重要性。
*   **安全视角**：引入虚警率（FAR）分析，这对于自动驾驶中建立用户信任至关重要。

### 8. 不足与局限
*   **架构局限性**：实验仅基于 YOLOv8 系列，未验证其他架构（如 Transformer-based 检测器）是否存在同样的校准转移效应。
*   **硬件偏差**：虽然在 RTX 5070 上测试，但仍非真正的嵌入式低功耗芯片（如 Jetson Orin），缺乏功耗和热指标分析。
*   **数据集单一**：仅在 BDD100K 上验证，未在 KITTI 或 nuScenes 等其他主流数据集上进行交叉验证。
*   **量化方法限制**：仅使用了 PTQ（训练后量化），未探讨量化感知训练（QAT）与 KD 结合可能带来的进一步提升。

（完）

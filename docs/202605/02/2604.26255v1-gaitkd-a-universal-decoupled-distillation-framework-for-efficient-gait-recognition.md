---
title: "GaitKD: A Universal Decoupled Distillation Framework for Efficient Gait Recognition"
title_zh: GaitKD：一种用于高效步态识别的通用解耦蒸馏框架
authors: "Yuqi Li, Qian Zhou, Huiran Duan, Jingjie Wang, Shunli Zhang, Chuanguang Yang, Guoying Zhao, Yingli Tian"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26255v1"
tags: ["query:cv"]
score: 9.5
evidence: 用于高效步态识别的解耦蒸馏框架，包含决策级和边界级蒸馏
tldr: 步态识别模型通常计算开销巨大，难以实际部署。本文提出GaitKD，一种通用的解耦蒸馏框架，旨在将高性能教师模型的知识迁移到轻量级学生模型中。该框架将知识迁移分为决策级（逻辑值蒸馏）和边界级（激活边界目标）两个互补部分，解决了传统蒸馏在处理步态模型局部结构化特征时的局限性。实验证明，GaitKD在多个基准测试中显著提升了轻量级模型的性能，且不增加推理成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26255v1/fig-001.webp\", \"caption\": \"Fig. 2. Computational profiles of different gait recognition models. Backbone FLOPs versus rank-1 accuracy on Gait3D, with bubble size indicating parameter count.\", \"page\": 6, \"index\": 1, \"width\": 516, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.26255v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of GaitKD for heterogeneous gait distillation. Given an input gait sequence, both a frozen teacher and an efficient student extract partstructured gait representations. To enable distillation across heterogeneous models, their part-wise outputs are aligned in a shared part-wise space through a gait part alignment module. On top of this shared space, GaitKD performs two complementary knowledge transfers: decision-level distillation, which aligns part-wise class distributions to transfer decision relations, and boundary-level distillation, which preserves teacher-induced discriminative boundaries in the embedding space instead of directly regressing feature values. The two objectives are optimized jointly with the student base task loss during training. At inference time, only the student is used, with no additional overhead. For clarity, the base task supervision (e.g., identification and metric learning losses) is omitted from the figure.\", \"page\": 2, \"index\": 2, \"width\": 1014, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.26255v1/fig-003.webp\", \"caption\": \"TABLE V MULTI-TEACHER DISTILLATION RESULTS ACROSS THREE BENCHMARKS (STUDENT: GAITBASE). ∆BASE IS COMPUTED AGAINST THE BASELINE; ∆BEST IS COMPUTED AGAINST THE BEST SINGLE-TEACHER RESULT.\", \"page\": 8, \"index\": 3, \"width\": 516, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2604.26255v1/fig-004.webp\", \"caption\": \"TABLE VII ABLATION ON DISTILLATION OBJECTIVES (TEACHER: DEEPGAITV2, STUDENT: GAITBASE).\", \"page\": 9, \"index\": 4, \"width\": 494, \"height\": 273}]"
motivation: 针对高性能步态识别模型计算量大且传统知识蒸馏难以有效处理其局部结构化特征的问题，寻求一种高效的轻量化方案。
method: 提出解耦蒸馏框架，通过部分校准的逻辑值蒸馏传递类别间决策关系，并利用激活边界目标而非直接特征回归来保持嵌入空间的划分。
result: 在多个步态识别基准测试和异构教师-学生配置下，该方法均取得了优于强基准模型的性能提升。
conclusion: GaitKD证明了决策级与边界级蒸馏的互补性，为构建高效且高性能的步态识别系统提供了一种通用的蒸馏范式。
---

## 摘要
步态识别是一种极具吸引力的生物特征识别方式，适用于远距离和非接触式身份识别，但高性能的步态模型通常依赖于深层且计算昂贵的架构，难以在实际中部署。知识蒸馏（KD）提供了一种将知识从强大的教师模型迁移到高效学生模型的自然方法；然而，标准 KD 对于局部结构化（part-structured）的步态模型往往效果较差，因为这类模型的监督信息由局部层面的分类 Logit 和局部层面的检索嵌入共同构成。在本文中，我们提出了 GaitKD，这是一个将步态知识迁移解耦为两个互补组件的蒸馏框架：决策级蒸馏和边界级蒸馏。具体而言，GaitKD 通过局部校准的 Logit 蒸馏来对齐教师和学生模型，以迁移类间决策关系，同时通过激活边界目标而非直接的特征回归，来保留教师模型引导的嵌入空间划分。凭借简单的对齐局部设计，GaitKD 支持异构的教师-学生步态模型，且不引入额外的推理成本。在多个步态识别基准测试和教师-学生配置下的实验结果表明，该方法相比强步态基准模型有持续的提升。我们的研究表明，这两个迁移组件是互补的，且保持边界的蒸馏比直接特征回归提供了更稳定的性能。源代码可在 https://github.com/liyiersan/GaitKD/ 获取。

## Abstract
Gait recognition is an attractive biometric modality for long-range and contact-free identification, but high-performing gait models often rely on deep and computationally expensive architectures that are difficult to deploy in practice. Knowledge distillation (KD) offers a natural way to transfer knowledge from a powerful teacher to an efficient student; however, standard KD is often less effective for part-structured gait models, where supervision is formed from both part-wise classification logits and part-wise retrieval embeddings. In this paper, we propose GaitKD, a distillation framework that decouples gait knowledge transfer into two complementary components: decision-level distillation and boundary-level distillation. Specifically, GaitKD aligns the teacher and student through part-calibrated logit distillation to transfer inter-class decision relations, while preserving the teacher-induced partitioning of the embedding space through an activation-boundary objective instead of direct feature regression. With a simple aligned part-wise design, GaitKD supports heterogeneous teacher-student gait models without introducing additional inference cost. Experimental results across multiple gait recognition benchmarks and teacher-student configurations show consistent improvements over strong gait baselines. Our study demonstrates that the two transfer components are complementary, and boundary-preserving distillation provides more stable performance than direct feature regression. Source code is available at https://github.com/liyiersan/GaitKD/

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GaitKD** 的通用解耦蒸馏框架，旨在提升轻量级步态识别模型的性能。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：高性能步态识别模型通常依赖深层架构（如 3D CNN、Transformer）或复杂的时空建模，导致计算开销巨大，难以在边缘设备或实际场景中部署。
*   **核心问题**：传统的知识蒸馏（KD）在步态识别上效果有限。这是因为现代步态模型多采用“局部结构化（Part-structured）”设计，同时输出局部层面的分类 Logit（用于身份识别）和局部层面的检索嵌入（用于特征比对）。
*   **挑战**：直接进行特征回归（MSE）或仅进行 Logit 蒸馏无法充分捕获教师模型的复杂知识，尤其是在教师和学生模型架构异构（如 Transformer 蒸馏给 CNN）时，过强的约束反而会限制学生模型的学习。

### 2. 方法论：核心思想与关键技术
GaitKD 的核心思想是将步态知识迁移解耦为**决策级（Decision-level）**和**边界级（Boundary-level）**两个互补部分：
*   **共享局部对齐空间**：为了处理异构模型，框架首先将教师和学生的输出对齐到相同的局部粒度（取两者局部数量的最小值 $P = \min(P_s, P_t)$）。
*   **决策级蒸馏（Logit Transfer）**：
    *   通过温度缩放的 KL 散度对齐局部类分布。
    *   引入“局部校准（Part-calibrated）”机制，确保每个局部对蒸馏信号的贡献均衡。
    *   支持解耦 Logit 蒸馏（TCKD/NCKD），分别处理目标类和非目标类的关系。
*   **边界级蒸馏（Activation-Boundary Transfer）**：
    *   **核心逻辑**：不要求学生模型精确复制教师的特征数值，而是要求学生模型保留教师在嵌入空间中形成的“决策边界”。
    *   **技术细节**：利用教师特征的符号（正负）作为边界指示器，通过带有边际（Margin）的 ReLU 损失函数，惩罚学生模型在边界错误一侧的激活值。
*   **多教师支持**：支持从多个异构教师模型中集成知识，通过置信度加权融合 Logit，并通过多数投票或最大置信度聚合嵌入边界。

### 3. 实验设计
*   **数据集**：
    *   **Gait3D**：大规模野外步态基准，包含复杂的视角和背景变化。
    *   **CCPG**：专注于换装场景的步态识别。
    *   **SUSTech1K**：室外 3D LiDAR 步态基准，涵盖遮挡、光照等挑战。
*   **Benchmark 设置**：
    *   **学生模型**：GaitBase（轻量级基准）。
    *   **教师模型**：DeepGaitV2、SwinGait、SkeletonGait++ 等高性能模型。
*   **对比方法**：对比了 Naive KD、MSE（特征回归）、KALSR、KAPS 等多种蒸馏变体。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **推理成本**：论文强调 GaitKD 在推理阶段**零额外开销**，因为教师模型在训练后被丢弃，学生模型保持原有的轻量级架构。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流数据集上进行了验证。
    *   涵盖了单教师蒸馏、多教师蒸馏、同构模型蒸馏（DeepGaitV2 不同深度间）等多种配置。
    *   进行了详细的消融实验（Logit vs. Boundary、不同 Loss 函数对比）。
*   **充分性与公平性**：实验设计较为充分，通过“Gap-Closing”分析量化了性能提升的比例。所有对比均在相同的 OpenGait 框架下进行，确保了实验的客观性和公平性。

### 6. 主要结论与发现
*   **互补性**：决策级和边界级蒸馏是互补的，两者结合效果最佳。
*   **边界优于回归**：在异构蒸馏中，保持激活边界（Boundary-preserving）比直接进行特征数值回归（MSE）更稳定且效果更好。
*   **多教师红利**：集成多个异构教师的知识能进一步提升轻量级模型的鲁棒性，GaitKD 的解耦设计比简单的损失平均（Mean-Teacher）更有效。
*   **性能提升**：在 Gait3D 上，通过多教师蒸馏，GaitBase 的 Rank-1 准确率从 61.5% 提升至 65.8%。

### 7. 优点（亮点）
*   **通用性强**：不限制教师和学生模型的具体架构，适用于 CNN、Transformer 等多种组合。
*   **设计简洁**：直接作用于模型现有的输出层（Logits 和 Embeddings），无需修改骨干网络或引入复杂的中间层对齐模块。
*   **部署友好**：在显著提升精度的同时，完全不增加推理时的计算负担。

### 8. 不足与局限
*   **超参数敏感性**：引入了温度 $T$、边界边际 $m$ 以及多个损失权重，这些超参数在不同数据集上可能需要精细调优。
*   **输入模态限制**：虽然在 LiDAR 数据集上做了测试，但大部分实验仍集中在剪影（Silhouette）输入上，对于原始视频或多模态融合的蒸馏探讨相对较少。
*   **算力细节缺失**：缺乏训练资源消耗的量化数据，不便于开发者评估训练成本。

（完）

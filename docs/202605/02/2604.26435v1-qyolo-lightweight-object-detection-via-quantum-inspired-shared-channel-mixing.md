---
title: "QYOLO: Lightweight Object Detection via Quantum Inspired Shared Channel Mixing"
title_zh: QYOLO：通过量子启发式共享通道混合实现的轻量级目标检测
authors: "Garvit Kumar Mittal, Sahil Tomar, Sandeep Kumar"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26435v1"
tags: ["query:cv"]
score: 10.0
evidence: 量子启发式通道混合用于目标检测中的真实架构压缩
tldr: 本研究针对单阶段目标检测器深层骨干网络参数量过大的问题，提出了QYOLO框架。该框架引入受量子启发的QMixBlock，通过正弦混合机制和跨阶段共享参数，替换了YOLOv8中最深的两层C2f模块。在VisDrone2019数据集上的实验表明，QYOLO在显著减少参数量和计算量的同时，保持了极高的检测精度，为实时视觉感知提供了一种高效的轻量化方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26435v1/fig-001.webp\", \"caption\": \"TABLE IV ABLATION STUDY OF DESIGN VARIANTS OF QMIXBLOCK (YOLOV8N)\", \"page\": 11, \"index\": 1, \"width\": 1056, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2604.26435v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed QYOLO architecture. The QMixBlock replaces C2f modules at backbone layers P4/16 and P5/32. The shared quantum-inspired mixer performs sinusoidal channel recalibration using global context aggregation, latent projection, and periodic mixing.\", \"page\": 3, \"index\": 2, \"width\": 1076, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2604.26435v1/fig-003.webp\", \"caption\": \"Fig. 2. Conceptual Comparison of Parameter Sharing Strategies in Feature Pyramid Networks\", \"page\": 4, \"index\": 3, \"width\": 1076, \"height\": 542}, {\"url\": \"assets/figures/arxiv/2604.26435v1/fig-004.webp\", \"caption\": \"TABLE V COMPARISON WITH ALTERNATIVE COMPRESSION STRATEGIES OF YOLOV8N\", \"page\": 12, \"index\": 4, \"width\": 1072, \"height\": 189}]"
motivation: 现有目标检测模型深层骨干网络的C2f模块因通道数增加导致参数量呈二次方增长，产生了巨大的计算负担。
method: 通过引入量子启发的QMixBlock，利用正弦混合机制和跨阶段共享的可学习参数替代深层骨干模块，实现高效的通道重校准。
result: "QYOLOv8n在VisDrone2019上减少了20.2%的参数和12.3%的计算量，且精度损失仅为0.4个百分点。"
conclusion: 量子启发的共享通道混合机制能有效压缩骨干网络规模，在维持检测性能的同时显著提升模型的轻量化程度。
---

## 摘要
目标检测架构的快速发展使单阶段检测器成为实时视觉感知的核心方案。这些模型的主要计算开销源于深层主干网络阶段，其中高步幅水平下的 C2f 瓶颈模块由于参数量随通道宽度呈二次方增长，占据了不成比例的参数份额。本研究提出了 QYOLO，这是一种量子启发式的通道混合框架，通过将 P4/16（512 通道）和 P5/32（1024 通道）处最深层的两个主干 C2f 模块替换为紧凑的 QMixBlock，实现了真正的架构压缩。所提出的模块通过正弦混合机制执行全局通道重新校准，并在两个主干阶段共享可学习参数，从而在不需要独立的分阶段参数集的情况下强制执行一致的通道重要性。颈部和检测头保持完全经典且未作改动。在 VisDrone2019 基准测试上的评估表明，QYOLOv8n 的参数量减少了 20.2%（从 3.01M 降至 2.40M），GFLOPs 减少了 12.3%，而 mAP@50 仅下降了 0.4 个百分点。QYOLOv8s 实现了 21.8% 的参数减少，性能下降仅为 0.1 个百分点。当结合知识蒸馏时，可以在不牺牲压缩率的情况下恢复完整的精度。扩展的主干加颈部变体实现了 38% 至 41% 的减少，但代价是更大的精度损失，这促使了最终仅针对主干网的设计。

## Abstract
The rapid advancement of object detection architectures has positioned single stage detectors as the dominant solution for real-time visual perception. A primary source of computational overhead in these models lies in the deep backbone stages, where C2f bottleneck modules at high stride levels accumulate a disproportionate share of parameters due to quadratic scaling with channel width. This work introduces QYOLO, a quantum-inspired channel mixing framework that achieves genuine architectural compression by replacing the two deepest backbone C2f modules at P4/16 (512 channels) and P5/32 (1024 channels) with a compact QMixBlock. The proposed block performs global channel recalibration through a sinusoidal mixing mechanism with shared learnable parameters across both backbone stages, enforcing consistent channel importance without requiring independent per-stage parameter sets. The neck and detection head remain fully classical and unchanged. Evaluation on the VisDrone2019 benchmark demonstrates that QYOLOv8n achieves a 20.2% reduction in parameter count (3.01M to 2.40M) and 12.3% GFLOPs reduction with only 0.4 pp mAP@50 degradation. QYOLOv8s achieves 21.8% reduction with 0.1 pp degradation. When combined with knowledge distillation, full accuracy parity is recovered at no cost to compression. An expanded backbone plus neck variant achieved 38 to 41% reduction at the cost of greater accuracy degradation, motivating the backbone-only final design.

---

## 论文详细总结（自动生成）

这是一份关于论文《QYOLO: Lightweight Object Detection via Quantum Inspired Shared Channel Mixing》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对实时目标检测模型（如 YOLOv8）在边缘设备部署时的**计算冗余问题**展开研究。作者发现，单阶段检测器的深层骨干网络（Backbone）中，C2f 瓶颈模块的参数量随通道数呈二次方增长。在 P4（512通道）和 P5（1024通道）阶段，这些模块占据了极高的参数比例，且传统的 3×3 局部卷积在处理高层语义特征时效率较低。

**研究动机**：利用量子计算中的周期性变换思想，设计一种轻量级的架构替代方案，在不依赖复杂剪枝技术的前提下，实现真正的“架构级”参数压缩。

### 2. 论文提出的方法论
核心思想是引入**量子启发式通道混合模块（QMixBlock）**，替换 YOLOv8 骨干网络中最深的两层 C2f 模块。

*   **核心技术细节**：
    *   **全局上下文聚合**：通过全局平均池化（GAP）将空间维度压缩，提取通道描述符。
    *   **流形投影**：使用线性压缩矩阵将通道描述符投影到低维潜在空间（缩减率 R=4）。
    *   **量子启发式混合（核心）**：借鉴量子电路中的酉旋转（Unitary Rotation）和参数纠缠概念，使用**正弦函数（Sinusoidal）**进行非线性变换。公式为：$H = \sin(z \cdot w + \Theta)$，其中 $w$ 是可学习权重，$\Theta$ 是可学习相位偏移。
    *   **共享参数机制**：在 P4 和 P5 两个阶段共享同一个 `SharedQuantumMixer` 参数集。这种设计基于一种假设：不同深度的语义特征在通道重要性逻辑上具有一致性。
    *   **特征重校准**：通过 Sigmoid 门控生成权重，并与原始特征图进行元素级相乘。

### 3. 实验设计
*   **数据集**：主要使用 **VisDrone2019-DET** 航空影像数据集。该数据集以小目标多、密度大、遮挡频繁著称，对模型的表征能力要求极高。
*   **基准模型（Benchmark）**：以 YOLOv8n（Nano）和 YOLOv8s（Small）作为原始基准。
*   **对比方法**：
    *   **非结构化剪枝（Unstructured Pruning）**：20% 稀疏度设置。
    *   **知识蒸馏（KD）**：将原始 YOLOv8n 作为教师模型指导 QYOLO。
    *   **设计变体**：包括 QYOLO-v0（同时替换骨干和颈部模块的激进版本）。

### 4. 资源与算力
论文明确记录了实验环境：
*   **GPU**：NVIDIA RTX 6000 Ada (49 GB VRAM)。
*   **CPU**：Intel Xeon W9-3595X。
*   **内存**：512 GB RAM。
*   **训练时长**：
    *   YOLOv8n 基准：4.44 小时。
    *   QYOLOv8n：3.46 小时（由于架构简化，训练速度提升约 22%）。
    *   所有模型均训练 300 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 Nano 和 Small 两种模型尺度，验证了方法的可扩展性。
*   **消融实验**：针对 QMixBlock 的内部组件（如相位偏移 $\Theta$ 的必要性、频率因子 $\alpha$ 的影响、是否加入空间卷积等）进行了详细对比。
*   **充分性评价**：实验设计较为客观，采用了相同的超参数和训练环境进行公平对比。通过对比剪枝和蒸馏方法，证明了 QYOLO 在“原生压缩”上的优势。不过，实验主要集中在 VisDrone 单一数据集上，缺乏在 COCO 等通用大规模数据集上的验证。

### 6. 主要结论与发现
*   **高效压缩**：QYOLOv8n 减少了 **20.2%** 的参数量和 **12.3%** 的 GFLOPs，mAP@50 仅下降 0.4%。QYOLOv8s 减少了 **21.8%** 的参数，精度几乎无损（下降 0.1%）。
*   **架构洞察**：仅替换骨干网络深层模块是最佳平衡点。若将 QMixBlock 扩展至颈部（Neck），虽然参数能减少 40% 以上，但会严重破坏空间融合逻辑，导致精度大幅下降。
*   **量子优势**：正弦激活函数（周期性变换）比传统的 ReLU/SiLU 能更高效地编码高频细节，这在处理 VisDrone 中的微小目标时表现优异。

### 7. 优点（亮点）
*   **真正的架构压缩**：不同于剪枝（Pruning）产生的稀疏矩阵需要特殊硬件支持，QYOLO 产生的是紧凑的稠密模型，直接兼容 ONNX、TensorRT 等标准部署工具。
*   **参数共享创新**：跨阶段共享量子混合器参数，不仅减少了存储开销，还起到了隐式正则化的作用。
*   **数学解释性**：将量子旋转操作与神经网络的通道注意力机制相结合，为轻量化设计提供了新的理论视角。

### 8. 不足与局限
*   **空间信息损失**：由于 QMixBlock 依赖全局平均池化，在处理极度依赖精细空间边界的类别（如行人、三轮车）时，性能下降比大尺寸物体（如巴士、卡车）更明显。
*   **泛化性验证不足**：目前仅在无人机视角数据集（VisDrone）上表现良好，尚未证明在常规视角（如 COCO、Pascal VOC）或其他视觉任务（如分割）中的通用性。
*   **优化难度**：论文提到更激进的压缩版本（v0）收敛更慢且需要更多 Epoch，说明量子启发式模块可能对初始化和学习率策略较为敏感。

（完）

---
title: On The Application of Linear Attention in Multimodal Transformers
title_zh: 论线性注意力在多模态 Transformer 中的应用
authors: "Armin Gerami, Seyedehanita Madani, Ramani Duraiswami"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10064v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于高效多模态Transformer的线性注意力机制
tldr: 本研究探讨了线性注意力（LA）在多模态Transformer中的应用，旨在解决标准Softmax注意力随序列长度呈平方级增长的计算瓶颈。通过在LAION-400M数据集上训练不同规模的ViT模型，研究证明了线性注意力能将计算复杂度降至线性，同时保持与标准模型相当的性能并遵循相同的缩放定律，为大规模多模态任务提供了高效可扩展的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10064v1/fig-001.webp\", \"caption\": \"Figure 4. Training loss scaling with model size for ViTs on LAION-400M. Our adjusted LA follows a trend similar to standard attention.\", \"page\": 4, \"index\": 1, \"width\": 485, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.10064v1/fig-002.webp\", \"caption\": \"Figure 3. Training metrics for ViT-S/16, ViT-B/16, and ViT-L/16 models trained on LAION-400M using four NVIDIA A5500 GPUs. Linear attention with our adjustments achieves performance comparable to standard attention.\", \"page\": 4, \"index\": 2, \"width\": 1021, \"height\": 683}]"
motivation: 针对多模态Transformer中标准注意力机制计算复杂度过高、难以扩展到长序列的问题，寻求更高效的替代方案。
method: 在ViT-S/B/L等不同规模的架构中引入线性注意力机制，并在LAION-400M大规模数据集上进行训练与验证。
result: 线性注意力显著降低了计算开销，且在ImageNet-21K零样本准确率上表现出与标准注意力相似的性能和缩放特性。
conclusion: 线性注意力是构建下一代高效、可扩展多模态Transformer模型的一种稳健且极具潜力的解决方案。
---

## 摘要
多模态 Transformer 是最先进视觉-语言模型的核心架构，但其注意力的二次复杂度仍是限制可扩展性的关键障碍。在本研究中，我们探讨了线性注意力（Linear Attention, LA）作为多模态框架内高效替代方案的可行性。通过集成线性注意力，我们将相对于序列长度的计算开销从二次降低为线性，同时保持了具有竞争力的性能。我们在 LAION-400M 数据集上训练的 ViT-S/16、ViT-B/16 和 ViT-L/16 架构上评估了我们的方法，验证重点在于 ImageNet-21K 的零样本准确率。我们的系统性评估表明，线性注意力不仅显著节省了计算资源，而且遵循与标准 Softmax 注意力相同的缩放法则。这些发现使线性注意力成为下一代多模态 Transformer 的稳健且可扩展的解决方案，适用于处理日益庞大且复杂的数据集。

## Abstract
Multimodal Transformers serve as the backbone for state-of-the-art vision-language models, yet their quadratic attention complexity remains a critical barrier to scalability. In this work, we investigate the viability of Linear Attention (LA) as a high-efficiency alternative within multimodal frameworks. By integrating LA, we reduce the computational overhead from quadratic to linear relative to sequence length while preserving competitive performance. We evaluate our approach across ViT-S/16, ViT-B/16, and ViT-L/16 architectures trained on the LAION-400M dataset, with validation focused on ImageNet-21K zero-shot accuracy. Our systematic evaluation demonstrates that Linear Attention not only yields significant computational savings but also adheres to the same scaling laws as standard softmax attention. These findings position Linear Attention as a robust, scalable solution for next-generation multimodal Transformers tasked with processing increasingly large and complex datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《On The Application of Linear Attention in Multimodal Transformers》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态 Transformer（如 CLIP、PaLI 等）已成为视觉-语言任务的核心架构。然而，其核心组件——**标准 Softmax 注意力机制**的计算复杂度随序列长度呈**二次方增长**（$O(N^2)$）。在处理包含大量视觉 Token 和长文本的多模态输入时，这一特性成为了严重的计算瓶颈，限制了模型处理高分辨率图像和长上下文的能力。
本文的研究动机在于验证**线性注意力（Linear Attention, LA）**在多模态学习中的可行性，探讨其是否能在大幅降低计算开销的同时，保持与标准 Transformer 相当的性能和可扩展性。

### 2. 方法论：核心思想与关键技术细节
论文的核心思想是利用线性注意力将复杂度降低至 $O(N)$。

*   **线性化原理**：通过将 Softmax 核函数替换为可分解的特征映射 $\phi(\cdot)$，利用矩阵乘法的结合律，先计算键（Key）和值（Value）的乘积，从而避免生成 $N \times N$ 的注意力矩阵。
*   **改进的仿射核（Affine Kernel）**：作者采用 $f(x) = 1 + x$ 作为核函数。
*   **关键改进——移除分母归一化**：
    *   **问题**：传统的线性注意力公式包含一个分母项用于权重归一化，但在长序列中，这会导致注意力分数过度平滑（Over-smoothing），使得模型难以捕捉关键的 Token 对，导致收敛极慢。
    *   **对策**：作者提出对查询（Query）和键（Key）进行预归一化（L2 Normalization），并**移除线性注意力公式中的分母**。
    *   **公式调整**：将输出计算简化为 $o_{ij} = \sum (1 + q_i \cdot k_n) v_{nj} / 2$。这种修改增强了注意力分数的对比度，显著提升了模型的表达能力和收敛速度。

### 3. 实验设计
*   **数据集**：使用大规模多模态数据集 **LAION-400M** 进行预训练。
*   **Benchmark**：主要评估指标为 **ImageNet-21K 零样本（Zero-shot）分类准确率**。
*   **对比方法**：
    *   标准 Softmax 注意力（Standard Attention）。
    *   常规线性注意力（Regular Linear Attention，带分母项）。
    *   本文改进的线性注意力（Proposed Linear Attention）。
*   **模型规模**：涵盖了 ViT-S/16（22M）、ViT-B/16（86M）和 ViT-L/16（304M）三种不同参数规模的架构。

### 4. 资源与算力
*   **硬件环境**：
    *   训练阶段：使用了 **4 台 NVIDIA A5500 GPU**。
    *   推理/时间测试：使用了 **NVIDIA H200 GPU**。
*   **训练细节**：
    *   框架：基于 OpenCLIP。
    *   Batch Size：ViT-S 为 64，ViT-B 为 16，ViT-L 为 4。
    *   训练时长：论文展示了长达 400 小时（针对 ViT-L）的训练曲线。

### 5. 实验数量与充分性
*   **实验组数**：作者针对三种模型规模（S/B/L）分别进行了对比实验，并进行了时间缩放测试和缩放定律（Scaling Law）分析。
*   **充分性评价**：实验设计较为系统。通过对比不同规模的模型，验证了方法在不同参数量下的稳定性；通过时间复杂度曲线验证了实际加速效果。
*   **客观性**：作者不仅展示了最终性能，还诚实地指出了线性注意力在收敛速度上（达到相同精度所需的 Epoch 数）略慢于标准注意力。

### 6. 主要结论与发现
*   **性能对等**：改进后的线性注意力在多模态任务中可以达到与标准 Softmax 注意力几乎相同的终端性能。
*   **高效缩放**：线性注意力在序列长度达到 $10^3$ 时优势开始显现，在 $4 \times 10^6$ 长度下，计算时间比标准注意力低约 3 个数量级。
*   **遵循缩放定律**：实验证明线性注意力遵循与标准模型相似的幂律缩放（Power-law scaling），即随着模型参数增加，损失函数以预测的速率下降。
*   **改进必要性**：移除分母的改进对于线性注意力在多模态场景下的成功至关重要，解决了过度平滑导致的收敛难问题。

### 7. 优点
*   **实用性强**：为长序列多模态处理提供了一个切实可行的低成本方案。
*   **理论与实践结合**：不仅有公式推导，还通过可视化（Heatmap）直观解释了为什么传统线性注意力在长序列中表现不佳。
*   **验证全面**：在 4 亿级的大规模数据集上进行验证，证明了该方法在工业级负载下的鲁棒性。

### 8. 不足与局限
*   **收敛效率**：虽然最终精度达标，但线性注意力达到相同精度所需的训练轮数（Epochs）多于标准注意力，这意味着**训练阶段的迭代成本**可能并未完全抵消计算复杂度的降低。
*   **任务覆盖面**：验证主要集中在零样本分类上，未广泛测试如视觉问答（VQA）、图像描述（Captioning）等更复杂的生成式多模态任务。
*   **超参数敏感性**：移除分母后的稳定性可能依赖于严格的 Q/K 预归一化，论文未深入讨论不同归一化策略的影响。

（完）

---
title: Weight Group-wise Post-Training Quantization for Medical Foundation Model
title_zh: 面向医疗基础模型的权重分组训练后量化
authors: "Yineng Chen, Peng Huang, Aozhong Zhang, Hui Guo, Penghang Yin, Shu Hu, Shao Lin, Xin Li, Tzu-Jen Kao, Balakrishnan Prabhakaran, MingChing Chang, Xin Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07674v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于医疗基础模型压缩的训练后量化
tldr: 针对医疗基础模型因架构庞大导致终端部署困难的问题，本文提出了一种名为Permutation-COMQ的训练后量化算法。该方法通过简单的点积和舍入操作取代了复杂的反向传播，消除了超参数调节的需求。同时，引入权重感知重排策略，在保持通道结构的前提下缓解了通道缩放带来的精度损失。实验证明，该方法在2、4、8位量化下均表现优异，显著提升了医疗模型的推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07674v1/fig-001.webp\", \"caption\": \"Figure 2. Distribution of magnitude of Simulated Weight.\", \"page\": 6, \"index\": 1, \"width\": 496, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.07674v1/fig-002.webp\", \"caption\": \"Figure 3. Relative quantization error for COMQ and permutationCOMQ under different bit-widths\", \"page\": 6, \"index\": 2, \"width\": 496, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.07674v1/fig-003.webp\", \"caption\": \"Figure 1. Conceptual illustration of COMQ and Permutation-COMQ Top: COMQ has quantization scale factors dominated by outliers due to heterogeneous weight magnitudes within each quantization unit, leading to coarse quantization of small weights. Bottom: PermutationCOMQ reorders weights by magnitude to group similar values, reducing intra-unit magnitude heterogeneity resulting in finer quantization resolution. The quantized weights are then mapped back to the original order via inverse permutation.\", \"page\": 3, \"index\": 3, \"width\": 882, \"height\": 745}]"
motivation: 医疗基础模型巨大的计算复杂度和参数量限制了其在终端医疗设备上的实时推理与应用。
method: 提出Permutation-COMQ算法，利用权重重排策略和无需反向传播的点积舍入操作实现高效的训练后量化。
result: 实验结果显示，该方法在2位、4位和8位量化配置下均达到了当前最优的精度表现。
conclusion: Permutation-COMQ在简化量化流程的同时有效解决了精度退化问题，为医疗基础模型的轻量化部署提供了有力支持。
---

## 摘要
基础模型在医学图像分析中取得了显著成果。然而，其庞大的网络架构和高计算复杂度显著影响了推理速度，限制了其在终端医疗设备上的应用。量化作为一种将模型压缩为低比特版本的技术，是应对这一挑战的解决方案。在本文中，我们提出了一种名为 Permutation-COMQ 的训练后量化算法。该算法通过使用简单的点积和舍入操作，消除了对反向传播的需求，从而免去了超参数调节并简化了流程。此外，我们引入了一种权重感知策略，通过对每层内的权重进行重排序，在保持通道结构的同时，解决了量化过程中由逐通道缩放引起的精度下降问题。实验表明，我们的方法在 2 比特、4 比特和 8 比特量化中均取得了最佳结果。

## Abstract
Foundation models have achieved remarkable results in medical image analysis. However, its large network architecture and high computational complexity significantly impact inference speed, limiting its application on terminal medical devices. Quantization, a technique that compresses models into low-bit versions, is a solution to this challenge. In this paper, we propose a post-training quantization algorithm, Permutation-COMQ. It eliminates the need for backpropagation by using simple dot products and rounding operations, thereby removing hyperparameter tuning and simplifying the process. Additionally, we introduce a weight-aware strategy that reorders the weight within each layer to address the accuracy degradation induced by channel-wise scaling during quantization, while preserving channel structure. Experiments demonstrate that our method achieves the best results in 2-bit, 4-bit, and 8-bit quantization.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Permutation-COMQ** 的训练后量化（PTQ）算法，专门用于压缩医疗基础模型（如 MedSAM），以实现在资源受限的医疗终端设备上的高效部署。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：医疗基础模型（如 MedSAM）虽然在图像分割上表现卓越，但其庞大的参数量和计算需求导致推理延迟高，难以部署在手术导航、实时病灶检测等临床终端设备上。
*   **核心挑战**：医疗图像具有低对比度、高分辨率和组织间高度相似的特点。传统的量化方法在极低比特（如 2-bit 或 4-bit）下会导致严重的精度退化，尤其是当权重矩阵中存在离群值（Outliers）时，量化缩放因子会被极端值主导，导致大部分小数值权重的分辨率丧失。

### 2. 方法论
*   **核心思想**：在不使用反向传播的情况下，通过“权重重排”优化权重分布，并结合“坐标下降法”最小化量化误差。
*   **关键技术细节**：
    *   **权重感知重排 (Permutation)**：在量化前，对每一层的权重矩阵按数值大小进行升序排列。这一步将数值相近的权重聚集在一起，减少了量化单元内部的异质性，防止离群值干扰缩放因子的计算。
    *   **坐标下降优化 (Coordinate-wise Minimization)**：将复杂的量化目标函数分解为一系列一元二次函数的最小化问题。通过迭代更新比特码（Bit-code）和缩放因子（Scale Factor），每个子问题都有闭式解（Closed-form solution）。
    *   **逆重排 (Reverse Permutation)**：量化完成后，利用逆矩阵将权重恢复到原始位置，从而在不改变模型拓扑结构的前提下完成压缩。
    *   **算法流程**：初始化缩放因子 -> 权重重排 -> 迭代更新比特码 -> 更新缩放因子 -> 逆重排恢复结构。

### 3. 实验设计
*   **数据集**：使用 **AbdomenCT1K** 数据集，包含来自 12 个医疗中心的 1000 多例 CT 扫描，目标任务是肝、肾、脾、胰腺的分割。
*   **Benchmark 模型**：以 **MedSAM** (基于 ViT-B 架构) 作为基准模型。
*   **对比方法**：
    *   **RTN (Round-to-Nearest)**：最基础的四舍五入量化。
    *   **COMQ**：原始的坐标下降量化（不含重排策略）。
*   **评估指标**：Dice 相似系数 (DSC) 和 归一化表面距离 (NSD)。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A100-SXM4-80GB** GPU 上进行。
*   **软件框架**：使用 PyTorch 实现。
*   **算力说明**：论文未明确给出具体的量化耗时，但强调该方法**无需反向传播**且**无需超参数调节**，这意味着其计算成本远低于需要微调的量化感知训练（QAT）方法。

### 5. 实验数量与充分性
*   **实验组数**：
    *   针对 2-bit、4-bit、8-bit 三种精度进行了对比实验。
    *   进行了**消融实验**，对比了“逐通道权重感知”与“逐层量化”的效果。
    *   提供了模拟数据实验，通过合成权重矩阵验证了重排策略对处理离群值的有效性。
*   **充分性评价**：实验设计较为合理，涵盖了从高比特到极低比特的性能演变，且使用了大规模医疗数据集。通过可视化预测掩码直观展示了量化效果，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：Permutation-COMQ 在所有比特设置下均优于 RTN 和 COMQ。
*   **极低比特优势**：在 2-bit 量化下，该方法 DSC 达到 86.9%，而 COMQ 仅为 71.8%，RTN 更是跌至 29.8%，证明了重排策略在极低比特下的关键作用。
*   **接近无损压缩**：在 4-bit 量化下，该方法的 DSC (93.43%) 已经非常接近全精度 FP32 模型 (93.51%)，实现了近乎无损的 8 倍压缩。

### 7. 优点
*   **算法简洁高效**：无需计算 Hessian 矩阵逆，无需反向传播，仅靠点积和舍入操作即可完成，易于工程实现。
*   **解决离群值问题**：通过简单的排序操作显著提升了量化分辨率，解决了 PTQ 中的一个核心痛点。
*   **医疗适用性强**：针对医疗图像的高精度需求，在大幅压缩参数的同时保持了极高的分割准确度。

### 8. 不足与局限
*   **模型泛化验证不足**：实验主要基于 ViT 架构的 MedSAM，尚未在基于 CNN 或 Mamba 等其他架构的医疗模型上验证普适性。
*   **缺乏激活量化讨论**：论文重点在于权重量化（W2/W4/W8），未深入探讨激活值量化（Activation Quantization），而激活量化通常是实现全整数推理的关键。
*   **端到端延迟缺失**：虽然目标是终端部署，但论文未提供在实际嵌入式医疗设备（如移动端芯片）上的推理速度实测数据。

（完）

---
title: On The Application of Linear Attention in Multimodal Transformers
title_zh: 论线性注意力在多模态 Transformer 中的应用
authors: "Armin Gerami, Seyedehanita Madani, Ramani Duraiswami"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10064v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于高效多模态视觉Transformer的线性注意力机制
tldr: 本研究探讨了线性注意力（LA）在多模态Transformer中的应用，旨在解决标准Softmax注意力随序列长度呈平方级增长的计算瓶颈。通过在LAION-400M数据集上训练不同规模的ViT模型，研究证明了线性注意力能将计算复杂度降至线性，同时保持与标准模型相当的性能并遵循相同的缩放定律，为大规模多模态任务提供了高效可扩展的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10064v1/fig-001.webp\", \"caption\": \"Figure 4. Training loss scaling with model size for ViTs on LAION-400M. Our adjusted LA follows a trend similar to standard attention.\", \"page\": 4, \"index\": 1, \"width\": 485, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.10064v1/fig-002.webp\", \"caption\": \"Figure 3. Training metrics for ViT-S/16, ViT-B/16, and ViT-L/16 models trained on LAION-400M using four NVIDIA A5500 GPUs. Linear attention with our adjustments achieves performance comparable to standard attention.\", \"page\": 4, \"index\": 2, \"width\": 1021, \"height\": 683}]"
motivation: 针对多模态Transformer中标准注意力机制计算复杂度过高、难以扩展到长序列的问题，寻求更高效的替代方案。
method: 在ViT-S/B/L等不同规模的架构中引入线性注意力机制，并在LAION-400M大规模数据集上进行训练与验证。
result: 线性注意力显著降低了计算开销，且在ImageNet-21K零样本准确率上表现出与标准注意力相似的性能和缩放特性。
conclusion: 线性注意力是构建下一代高效、可扩展多模态Transformer模型的一种稳健且极具潜力的解决方案。
---

## 摘要
多模态 Transformer 是最先进视觉-语言模型的核心架构，但其二次方级别的注意力复杂度仍是限制其可扩展性的关键障碍。在本研究中，我们探讨了线性注意力（Linear Attention, LA）作为多模态框架内高效替代方案的可行性。通过集成线性注意力，我们将相对于序列长度的计算开销从二次方降低到了线性，同时保持了具有竞争力的性能。我们在 LAION-400M 数据集上训练的 ViT-S/16、ViT-B/16 和 ViT-L/16 架构上评估了我们的方法，并重点验证了 ImageNet-21K 的零样本准确率。我们的系统性评估表明，线性注意力不仅显著节省了计算资源，而且遵循与标准 softmax 注意力相同的缩放法则（scaling laws）。这些发现使线性注意力成为处理日益庞大且复杂数据集的下一代多模态 Transformer 的一种稳健且可扩展的解决方案。

## Abstract
Multimodal Transformers serve as the backbone for state-of-the-art vision-language models, yet their quadratic attention complexity remains a critical barrier to scalability. In this work, we investigate the viability of Linear Attention (LA) as a high-efficiency alternative within multimodal frameworks. By integrating LA, we reduce the computational overhead from quadratic to linear relative to sequence length while preserving competitive performance. We evaluate our approach across ViT-S/16, ViT-B/16, and ViT-L/16 architectures trained on the LAION-400M dataset, with validation focused on ImageNet-21K zero-shot accuracy. Our systematic evaluation demonstrates that Linear Attention not only yields significant computational savings but also adheres to the same scaling laws as standard softmax attention. These findings position Linear Attention as a robust, scalable solution for next-generation multimodal Transformers tasked with processing increasingly large and complex datasets.

---

## 论文详细总结（自动生成）

这篇论文对线性注意力（Linear Attention, LA）在多模态 Transformer 中的应用进行了深入研究，旨在解决标准注意力机制在处理长序列时的计算瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：多模态 Transformer（如 CLIP、PaLI）是当前视觉-语言模型的核心，但其标准 Softmax 注意力机制的计算复杂度随序列长度 $N$ 呈平方级增长（$O(N^2)$）。在处理包含大量视觉和文本 Token 的多模态输入时，这一特性严重限制了模型的可扩展性和处理长序列的能力。
*   **核心目标**：验证线性注意力（复杂度为 $O(N)$）是否可以作为多模态框架中 Softmax 注意力的高效替代方案，并探究其是否遵循与标准模型相同的缩放定律（Scaling Laws）。

### 2. 方法论
*   **核心思想**：利用矩阵乘法的结合律，将注意力计算顺序从 $(QK^T)V$ 改为 $Q(K^TV)$，从而将复杂度从 $O(N^2D)$ 降低到 $O(ND^2)$。
*   **关键技术细节**：
    *   **仿射核函数**：采用 $f(x) = 1 + x$ 作为核函数。
    *   **归一化处理**：为了防止仿射核产生负值（当 $q_i \cdot k_n < -1$ 时），对 Query ($Q$) 和 Key ($K$) 进行 L2 归一化，确保注意力分数为非负。
    *   **公式改进（去分母化）**：作者发现标准线性注意力公式中的分母会导致“过度平滑”（Over-smoothing）现象，即随着序列增长，注意力分数趋于均匀，导致模型难以捕捉关键特征。
    *   **改进方案**：作者提出移除分母，直接使用改进后的线性组合形式。实验证明，这一改动能显著增强模型的表达能力，改善梯度流动，加快收敛速度。

### 3. 实验设计
*   **模型架构**：采用了三种规模的 Vision Transformer：ViT-S/16 (22M)、ViT-B/16 (86M) 和 ViT-L/16 (304M)。
*   **数据集**：使用大规模多模态数据集 **LAION-400M** 进行预训练。
*   **Benchmark**：主要评估指标为 **ImageNet-21K 的零样本（Zero-shot）分类准确率**。
*   **对比方法**：
    1.  标准 Softmax 注意力（使用 FlashAttention-2 加速）。
    2.  常规线性注意力（带分母的原始公式）。
    3.  作者改进后的线性注意力（不带分母）。

### 4. 资源与算力
*   **推理/时间测试**：在 **NVIDIA H200 GPU** 上进行，对比了不同序列长度下的前向和后向传播耗时。
*   **模型训练**：使用了 **4 台 NVIDIA A5500 GPU**。
*   **训练细节**：基于 OpenCLIP 框架。文中提到的全局 Batch Size 分别为 64 (S)、16 (B) 和 4 (L)。*注：对于 LAION-400M 这样的大规模数据集，这些 Batch Size 显得非常小，可能是受限于单机多卡的显存或特定实验设置。*

### 5. 实验数量与充分性
*   **实验组数**：涵盖了三个模型量级（S/B/L），每个量级都对比了标准注意力与线性注意力的训练曲线。
*   **充分性评价**：
    *   **优点**：实验跨越了从 22M 到 304M 的参数规模，并进行了长达 30 个 Epoch 的训练，提供了完整的训练损失和验证准确率曲线。
    *   **局限**：主要集中在图像分类任务上，缺乏对多模态检索、视觉问答（VQA）等其他常见多模态任务的广泛验证。

### 6. 主要结论与发现
*   **计算效率**：线性注意力在序列长度达到 $10^3$ 时优势开始显现；在 $4 \times 10^6$ 长度下，计算时间比标准注意力快约 1000 倍。
*   **性能对齐**：改进后的线性注意力在最终性能上能达到与标准 Softmax 注意力相当的水平。
*   **缩放定律**：线性注意力遵循与标准注意力相似的幂律缩放（Power-law scaling），即模型性能随参数量增加而稳定提升，证明了其在大规模模型中的潜力。
*   **收敛特性**：虽然线性注意力最终性能达标，但其收敛速度略慢于标准注意力，需要更多 Epoch 才能达到相同的准确率。

### 7. 优点
*   **理论与实践结合**：不仅应用了线性注意力，还针对多模态场景下的“过度平滑”问题提出了有效的公式改进。
*   **高效性证明**：通过 H200 上的实测数据直观展示了线性复杂度在处理超长序列时的巨大优势。
*   **可扩展性验证**：通过缩放定律分析，证明了该方法不是“小模型上的技巧”，而是可以扩展到大模型的稳健方案。

### 8. 不足与局限
*   **收敛效率**：线性注意力需要更长的训练时间（更多 Epoch）来弥补其核函数表达力相对较弱的缺陷。
*   **任务覆盖面**：验证任务较为单一（仅限 ImageNet 零样本分类），未展示在更复杂多模态交互任务中的表现。
*   **训练超参数**：实验中使用的 Batch Size 较小，可能未完全发挥出大规模分布式训练下线性注意力的吞吐量优势。

（完）

---
title: Geometric Layer-wise Approximation Rates for Deep Networks
title_zh: 深度网络的几何级逐层逼近率
authors: "Shijun Zhang, Zuowei Shen, Yuesheng Xu"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20219v1"
tags: ["query:cv"]
score: 9.0
evidence: 深度网络逐层逼近率和深度解释的数学框架
tldr: 深度学习中深度的作用通常仅在最终输出中体现，而中间层的作用尚不明确。本文提出了一种具有固定宽度和混合激活函数的神经网络架构，证明了每一层中间输出都能对目标函数进行逼近。通过多级深度学习机制，每一层都在更精细的几何尺度上修正残差，使得逼近误差随深度呈几何级数下降。该研究为深度神经网络的层级细化能力提供了量化的理论支撑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20219v1/fig-001.webp\", \"caption\": \"Figure 4: Interior cubes Qℓ,β and transition regions Ωℓ for N = 2 at the levels ℓ = 0, 1, 2.\", \"page\": 14, \"index\": 1, \"width\": 758, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.20219v1/fig-002.webp\", \"caption\": \"Figure 7: Conceptual overview of the multigrade construction, showing how the encoder and decoder are combined (here illustrated for ℓ = 2 and N = 2).\", \"page\": 18, \"index\": 2, \"width\": 692, \"height\": 225}]"
motivation: 旨在解决标准神经网络逼近理论仅关注最终输出，而忽略了中间层在逐步逼近目标函数中具体作用的问题。
method: 设计了一种固定宽度为 2dN+d+2 的混合激活架构，使每一层中间读出值都是对目标函数的逼近，并利用多级细化机制处理残差。
result: 证明了第 l 层的逼近误差受限于几何尺度 N^-l 下的连续模，对于 Lipschitz 函数，误差随深度呈几何速率下降。
conclusion: 深度在网络中起到了渐进式精炼的作用，支持在不重新设计前序网络的情况下实现对目标函数的自适应细化逼近。
---

## 摘要
深度被广泛认为是深度神经网络成功的核心因素，而标准的神经网络逼近理论通常仅为最终输出提供保证，在很大程度上未阐明中间层的作用。我们通过开发一个定量框架来填补这一空白，在该框架中，深度具有精确的尺度依赖性解释。具体而言，我们设计了一种单一共享的混合激活架构，其具有固定的宽度 $2dN+d+2$ 和任意指定的有限深度，使得每个中间读取值 $Φ_\ell$ 本身就是目标函数 $f$ 的逼近器。对于 $p\in [1,\infty)$ 的 $f\in L^p([0,1]^d)$，对于所有 $\ell$，$Φ_\ell$ 的逼近误差受几何尺度 $N^{-\ell}$ 下 $L^p$ 连续模的 $(2d+1)$ 倍控制。如果 $f$ 是 $1$-Lipschitz 的，则该估计简化为几何速率 $(2d+1)N^{-\ell}$。我们的网络设计灵感源自多级深度学习（multigrade deep learning），其中深度充当渐进式细化机制：每一次新的修正都针对更细尺度上的残差信息，而早期的修正项仍作为后续读取值的一部分，从而产生一种支持自适应细化且无需重新设计前序网络的嵌套架构。

## Abstract
Depth is widely viewed as a central contributor to the success of deep neural networks, whereas standard neural network approximation theory typically provides guarantees only for the final output and leaves the role of intermediate layers largely unclear. We address this gap by developing a quantitative framework in which depth admits a precise scale-dependent interpretation. Specifically, we design a single shared mixed-activation architecture of fixed width $2dN+d+2$ and any prescribed finite depth such that each intermediate readout $Φ_\ell$ is itself an approximant to the target function $f$. For $f\in L^p([0,1]^d)$ with $p\in [1,\infty)$, the approximation error of $Φ_\ell$ is controlled by $(2d+1)$ times the $L^p$ modulus of continuity at the geometric scale $N^{-\ell}$ for all $\ell$. The estimate reduces to the geometric rate $(2d+1)N^{-\ell}$ if $f$ is $1$-Lipschitz. Our network design is inspired by multigrade deep learning, where depth serves as a progressive refinement mechanism: each new correction targets residual information at a finer scale while the earlier correction terms remain part of the later readouts, yielding a nested architecture that supports adaptive refinement without redesigning the preceding network.

---

## 论文详细总结（自动生成）

这是一份关于论文《Geometric Layer-wise Approximation Rates for Deep Networks》（深度网络的几何级逐层逼近率）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的神经网络逼近理论（如通用逼近定理）通常将网络视为一个整体，仅关注最终输出的误差界限，而忽略了**中间层（Intermediate Layers）**的具体贡献。这导致深度在理论上缺乏透明度，且在实践中增加层数往往需要重新设计和训练整个架构。
*   **研究动机**：作者旨在为深度提供一种“尺度依赖”的定量解释。他们希望证明，深度不仅是增加模型复杂度的手段，更是一种**渐进式细化（Progressive Refinement）**机制，使得每一层都能在越来越精细的几何尺度上捕捉目标函数的特征。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：借鉴“多级深度学习”（Multigrade Deep Learning, MGDL）的概念，将网络构建为一个嵌套架构。每一层 $\ell$ 产生的输出 $\Phi_\ell$ 都是对目标函数 $f$ 的一个独立逼近，且 $\Phi_\ell = \Phi_{\ell-1} + \Gamma_\ell$，即新层只负责修正前一层的残差。
*   **关键技术细节**：
    *   **混合激活函数（Mixed Activations）**：设计了一种特殊的激活层 $\varrho$，包含两个 Sine 通道和多个 ReLU 通道。ReLU 用于几何定位（划分空间），Sine 用于数值实现（拟合函数值）。
    *   **固定宽度架构**：证明了在宽度固定为 $2dN + d + 2$（$d$ 为维度，$N$ 为缩放因子）的情况下，仅通过增加深度即可实现任意精度的逼近。
    *   **编码器-解码器机制**：
        *   **编码器**：利用 ReLU 网络将输入空间划分为 $N^{d\ell}$ 个小立方体单元，并为每个单元分配唯一索引。
        *   **解码器**：利用 Sine 函数的超强表达能力（基于 Kronecker 逼近定理），通过极少量的参数拟合所有单元格上的函数平均值。
*   **逼近流程**：输入 $\to$ 几何定位（ReLU） $\to$ 残差计算 $\to$ 单元值拟合（Sine） $\to$ 逐层累加修正。

### 3. 实验设计
*   **研究性质**：本文是一篇**纯数学理论证明论文**，不包含基于特定数据集（如 ImageNet 或 CIFAR）的数值实验。
*   **论证场景**：作者在数学上定义了目标函数属于 $L^p([0, 1]^d)$ 空间，并针对 Lipschitz 连续和 Hölder 连续等不同正则化条件的函数进行了推导。
*   **对比基准**：其对比对象是经典的神经网络逼近理论（如 Yarotsky 等人的 ReLU 网络逼近率）。本文的优势在于提供了“逐层”的误差保证，而不仅仅是“终层”保证。

### 4. 资源与算力
*   **算力说明**：由于是纯理论推导，文中**未提及**任何 GPU 型号、数量或训练时长。
*   **复杂度分析**：作者提供了参数复杂度的理论估计，指出达到 $\epsilon$ 精度所需的参数量随 $\epsilon^{-1}$ 呈对数增长，随维度 $d$ 呈多项式增长，这在理论上证明了该架构可以有效缓解“维度灾难”。

### 5. 实验数量与充分性
*   **理论严谨性**：虽然没有数值实验，但论文通过严密的数学证明（包括归纳法、测度论分析和构造性证明）确保了结论的可靠性。
*   **覆盖范围**：证明涵盖了从 $L^1$ 到 $L^\infty$ 的多种范数情况，并扩展到了任意矩形区域（Rectangular Boxes），论证过程详尽且具有普适性。

### 6. 主要结论与发现
*   **逐层逼近率**：第 $\ell$ 层的读取值 $\Phi_\ell$ 的误差受限于几何尺度 $N^{-\ell}$ 下的 $L^p$ 连续模：$\|f - \Phi_\ell\|_{L^p} \leq (2d+1) \omega_{f,p}(N^{-\ell})$。
*   **几何级收敛**：对于 1-Lipschitz 函数，误差随深度 $\ell$ 呈几何级数下降，即 $O(N^{-\ell})$。
*   **深度的角色**：深度被量化为“分辨率参数”。增加一层相当于将逼近的分辨率从 $N^{-(\ell-1)}$ 提升到 $N^{-\ell}$。
*   **自适应细化**：该架构支持在不改变已训练层参数的情况下，通过直接添加新层来提升精度。

### 7. 优点
*   **理论创新**：首次为深度神经网络提供了显式的、逐层递进的量化逼近保证。
*   **架构高效**：固定宽度的设计比不断增加宽度的模型更符合现代深度学习的硬件优化趋势。
*   **构造性强**：不同于非构造性的存在性证明，本文给出了具体的网络权重构造方案。

### 8. 不足与局限
*   **缺乏实证支持**：纯理论模型在实际随机梯度下降（SGD）训练中是否能收敛到构造解尚不明确。
*   **数值稳定性风险**：解码器依赖于 Sine 函数的高频特性，这在计算机有限浮点精度下可能会遇到严重的数值稳定性问题。
*   **优化与泛化缺失**：论文仅讨论了逼近误差（表达能力），未涉及优化误差（训练难度）和泛化误差（测试表现）。

（完）

---
title: "Beyond ZOH: Advanced Discretization Strategies for Vision Mamba"
title_zh: 超越 ZOH：Vision Mamba 的高级离散化策略
authors: "Fady Ibrahim, Guangjun Liu, Guanghui Wang"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20606v1"
tags: ["query:cv"]
score: 9.0
evidence: Vision Mamba效率的数学基础和离散化策略
tldr: 本研究针对Vision Mamba中零阶保持（ZOH）离散化导致的信号保真度不足问题，系统对比了ZOH、双线性变换（BIL）、多项式插值（POL）等六种离散化方案。通过在图像分类、分割和检测任务上的评估，发现POL和HOH精度最高但开销大，而BIL在性能与效率间取得了最佳平衡，为SSM架构的离散化选择提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20606v1/fig-001.webp\", \"caption\": \"Table 1: Discretization methods results on image classification tasks.\", \"page\": 10, \"index\": 1, \"width\": 716, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2604.20606v1/fig-002.webp\", \"caption\": \"Table 3: Fastest method per batch size based on latency and throughput.\", \"page\": 13, \"index\": 2, \"width\": 570, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2604.20606v1/fig-003.webp\", \"caption\": \"Fig. 1: The architecture of Vision Mamba model.\", \"page\": 5, \"index\": 3, \"width\": 930, \"height\": 230}]"
motivation: 现有的Vision Mamba模型多采用零阶保持离散化，其输入信号恒定的假设限制了模型在复杂视觉任务中的精度。
method: 在Vision Mamba框架中实现并对比了ZOH、FOH、BIL、POL、HOH和RK4六种不同的离散化策略。
result: 实验结果显示POL和HOH在精度上提升最大，而BIL以较低的额外开销实现了显著的性能改进。
conclusion: 本文阐明了离散化对SSM模型的重要性，并建议将双线性变换作为高性能视觉SSM模型的默认离散化方案。
---

## 摘要
Vision Mamba 作为一种状态空间模型 (SSM)，采用了零阶保持器 (ZOH) 离散化，该方法假设输入信号在采样时刻之间保持不变。这一假设降低了动态视觉环境中的时间保真度，并限制了现代基于 SSM 的视觉模型所能达到的准确度。在本文中，我们对 Vision Mamba 框架内实例化的六种离散化方案进行了系统且受控的比较：ZOH、一阶保持器 (FOH)、双线性/Tustin 变换 (BIL)、多项式插值 (POL)、高阶保持器 (HOH) 以及四阶龙格-库塔法 (RK4)。我们在标准视觉基准数据集上评估了每种方法，以量化其在图像分类、语义分割和目标检测中的影响。我们的结果表明，POL 和 HOH 以较高的训练时间计算成本换取了最大的准确度提升。相比之下，BIL 在仅增加适度额外开销的情况下，相比 ZOH 提供了持续的改进，在精度和效率之间提供了最有利的权衡。这些发现阐明了离散化在基于 SSM 的视觉架构中的关键作用，并为将 BIL 作为最先进 SSM 模型的默认离散化基准提供了基于经验的依据。

## Abstract
Vision Mamba, as a state space model (SSM), employs a zero-order hold (ZOH) discretization, which assumes that input signals remain constant between sampling instants. This assumption degrades temporal fidelity in dynamic visual environments and constrains the attainable accuracy of modern SSM-based vision models. In this paper, we present a systematic and controlled comparison of six discretization schemes instantiated within the Vision Mamba framework: ZOH, first-order hold (FOH), bilinear/Tustin transform (BIL), polynomial interpolation (POL), higher-order hold (HOH), and the fourth-order Runge-Kutta method (RK4). We evaluate each method on standard visual benchmarks to quantify its influence in image classification, semantic segmentation, and object detection. Our results demonstrate that POL and HOH yield the largest gains in accuracy at the cost of higher training-time computation. In contrast, the BIL provides consistent improvements over ZOH with modest additional overhead, offering the most favorable trade-off between precision and efficiency. These findings elucidate the pivotal role of discretization in SSM-based vision architectures and furnish empirically grounded justification for adopting BIL as the default discretization baseline for state-of-the-art SSM models.

---

## 论文详细总结（自动生成）

### 论文总结：超越 ZOH——Vision Mamba 的高级离散化策略

#### 1. 论文的核心问题与整体含义
本论文探讨了 **Vision Mamba (ViM)** 等视觉状态空间模型（SSM）中的一个关键但常被忽视的环节：**连续系统到离散系统的转换（离散化策略）**。
*   **研究动机**：现有的视觉 SSM 模型（如 ViM, VMamba）普遍沿用经典控制理论中的**零阶保持（ZOH）**方法。ZOH 假设输入信号在采样间隔内保持恒定，这虽然简化了计算，但忽略了视觉信号（如图像边缘、纹理梯度）的连续空间变化，导致离散化滞后和截断误差的累积，限制了模型捕捉复杂空间依赖的能力。
*   **核心目标**：系统性地评估并引入更高级的数值积分和插值方法，以提升视觉 SSM 在动态和复杂视觉环境中的建模精度。

#### 2. 论文提出的方法论
论文在统一的硬件感知框架下，对比了六种离散化方案，旨在将连续状态方程 $\frac{dh(t)}{dt} = Ah(t) + Bx(t)$ 转化为离散形式：
*   **Zero-Order Hold (ZOH)**：基准方法，假设采样间信号为常数。
*   **First-Order Hold (FOH)**：线性插值法，假设信号在采样点间线性变化，提供更平滑的过渡。
*   **Bilinear/Tustin Transform (BIL)**：核心推荐方法。利用**梯形积分规则**近似连续信号区域，能更好地保持高频特征（如边缘和细节）并确保系统稳定性。
*   **Polynomial Interpolation (POL)**：利用多项式曲线拟合采样点。由于 ViM 采用双向扫描，POL 可以利用“未来”像素信息进行非因果插值，从而最小化全局离散化误差。
*   **Higher-Order Hold (HOH)**：FOH 的推广，使用更高阶的多项式来近似复杂的时空变化。
*   **Runge-Kutta 4 (RK4)**：经典的四阶数值积分法，通过四个斜率的加权平均来逼近状态演化，理论精度最高。

**技术细节**：为了确保对比的公平性，作者开发了一个**统一的硬件感知 CUDA 内核**。该内核将离散化、状态更新和输出投影融合在一起，确保所有方法具有相同的内存访问模式和并行扩展性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：ImageNet-1K（大规模）和 CIFAR100（小规模）。
    *   **语义分割**：ADE20K（基于 UperNet 框架）。
    *   **目标检测**：MS COCO 2017（基于 Cascade Mask R-CNN 框架）。
*   **Benchmark**：以原始的 **Vision Mamba (ViM-Ti)** 为基准架构。
*   **对比实验**：在保持模型参数、训练策略（如 AdamW 优化器、数据增强）完全一致的前提下，仅替换离散化模块。

#### 4. 资源与算力
*   **硬件配置**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练时长**：
    *   在 ImageNet-1K 上，不同方法的训练耗时差异显著。ZOH 和 FOH 约需 **22.5 天**；BIL 约需 **22.7 天**；而计算最复杂的 RK4 则需 **28.2 天**。
    *   CIFAR100 上的训练时长在 1.5 小时左右。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础分类到下游高分辨率视觉任务（分割、检测）的全方位评估。
*   **统计显著性**：为了排除随机性干扰，作者针对 10 个不同的随机种子进行了实验，并使用了**单侧配对置换检验（Permutation Test）**。
*   **公平性**：实验设计非常严谨，通过统一 CUDA 内核消除了实现方式带来的偏差，确保了性能提升确实源于数学原理而非工程优化。

#### 6. 主要结论与发现
*   **精度排名**：**POL 和 HOH** 在所有任务中均取得了最高的准确率提升（如 ImageNet-1K 上 POL 比 ZOH 高出 1.64%）。
*   **效率权衡**：虽然 POL/HOH 精度高，但训练收敛较慢且计算开销大。
*   **最佳平衡点**：**BIL（双线性变换）** 被证明是综合表现最好的方案。它在 ImageNet 上提升了 0.83% 的 Top-1 准确率，在 ADE20K 上提升了 2.9 mIoU，且推理延迟极低（在 Batch Size 1 时甚至优于 ZOH），是精度与效率的最优折中。
*   **RK4 的局限**：尽管 RK4 理论精度高，但在标准训练机制下容易出现梯度不稳定，导致实际增益有限。

#### 7. 优点
*   **理论深度**：将控制理论中的数值分析方法成功引入深度学习架构设计，填补了 SSM 离散化研究的空白。
*   **硬件友好**：不仅关注数学精度，还通过自定义 CUDA 内核保证了方法在现代 GPU 上的执行效率。
*   **实证扎实**：通过多任务验证和统计学检验，证明了结论的鲁棒性。

#### 8. 不足与局限
*   **架构覆盖**：研究主要基于 ViM 架构，虽然理论上适用于所有 SSM（如 VMamba, VideoMamba），但未在这些变体上进行大规模交叉验证。
*   **超参数敏感性**：高级离散化方法（如 RK4, HOH）可能需要更精细的超参数调优（如学习率、权重衰减）才能发挥全部潜力，论文中统一使用默认配置可能限制了这些高阶方法的表现。
*   **应用限制**：对于极小规模的模型或实时性要求极高的边缘设备，高级离散化带来的微小延迟增加仍需权衡。

（完）

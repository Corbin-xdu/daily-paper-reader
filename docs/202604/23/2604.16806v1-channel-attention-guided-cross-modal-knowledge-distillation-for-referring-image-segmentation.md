---
title: Channel Attention-Guided Cross-Modal Knowledge Distillation for Referring Image Segmentation
title_zh: 通道注意力引导的跨模态知识蒸馏用于指代图像分割
authors: Chen Yang
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16806v1"
tags: ["query:cv"]
score: 9.0
evidence: 针对资源受限视觉任务的跨模态知识蒸馏
tldr: 针对指代图像分割（RIS）中大型模型难以部署在资源受限设备的问题，本文提出一种通道注意力引导的跨模态知识蒸馏方法。该方法通过将教师网络学习到的视觉与语言间的高阶细粒度关联以及通道语义成分关联迁移至学生网络，在不增加推理参数的前提下，显著提升了轻量级学生模型的性能，并有效缓解了学习偏差的传递。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的指代图像分割模型参数量巨大，导致其在计算资源有限的场景中部署受限。
method: 提出通道注意力引导的跨模态知识蒸馏，迁移视觉与语言的高阶细粒度关联及通道语义成分关联。
result: 在两个公开数据集上的实验证明，该方法在不增加推理负担的前提下显著提升了学生模型的分割性能。
conclusion: 该方法通过有效的跨模态知识迁移，在提升轻量级模型性能的同时保留了其独立学习能力并减轻了偏差传递。
---

## 摘要
指代图像分割（RIS）要求根据语言描述准确分割图像中的目标区域，是一项融合视觉和语言的跨模态任务。现有的 RIS 方法通常采用大规模视觉和语言编码模型来提高性能，但其庞大的参数量严重限制了在计算资源有限场景下的部署。为解决这一问题，本文提出了一种通道注意力引导的跨模态知识蒸馏方法，该方法将教师网络学习到的视觉与语言之间的高阶细粒度相关性，以及各通道所代表的语义成分之间的相关性，迁移至学生网络。与传统的像素级关系蒸馏相比，该方法不仅使学生网络能够学习教师网络的知识，还保留了其部分独立学习能力，从而缓解了学习偏差的迁移。在两个公开数据集上的实验结果表明，所提出的蒸馏方法在推理过程中不引入额外参数，并能显著提升学生模型的性能。

## Abstract
Referring image segmentation (RIS) requires accurate segmentation of target regions in images according to language descriptions, which is a cross-modal task integrating vision and language. Existing RIS methods typically employ large-scale vision and language encoding models to improve performance, but their enormous parameter size severely restricts deployment in scenarios with limited computing resources. To solve this problem, this paper proposes a channel attention-guided cross-modal knowledge distillation method, which transfers the high-order fine-grained correlations between vision and language learned by the teacher network, as well as the correlations between semantic components represented by each channel, to the student network. Compared with the traditional pixel-wise relational distillation, this method not only enables the student to learn the knowledge of the teacher, but also retains part of its independent learning ability, alleviating the transfer of learning bias. Experimental results on two public datasets show that the proposed distillation method does not introduce additional parameters during inference and can achieve significant performance improvement for the student model.

---

## 论文详细总结（自动生成）

以下是对论文《Channel Attention-Guided Cross-Modal Knowledge Distillation for Referring Image Segmentation》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：指代图像分割（Referring Image Segmentation, RIS）任务要求模型根据自然语言描述精确分割图像目标。虽然现有基于大型预训练模型（如 Swin-base, BERT-base）的方法性能优异，但其**庞大的参数量和计算开销**使得模型难以部署在边缘计算平台或移动终端等资源受限的场景中。
*   **研究动机**：为了在保持轻量化部署的同时不显著牺牲分割精度，本文探索将**知识蒸馏（Knowledge Distillation）**引入 RIS 任务，旨在将大型教师网络的跨模态对齐能力迁移给轻量级学生网络。

### 2. 论文提出的方法论
该方法由教师网络、学生网络和跨模态知识蒸馏机制三部分组成：
*   **核心思想**：不仅让学生网络模仿教师的输出，更重要的是让其学习教师网络中**视觉与语言之间的细粒度关联**以及**通道间的语义成分相关性**。
*   **关键技术细节**：
    1.  **跨模态注意力模块 (VLCAM)**：采用 Query-Key-Value 机制，将视觉特征作为 Query，文本特征作为 Key 和 Value，计算视觉-语言相关性矩阵 $A$，实现多尺度特征的融合。
    2.  **视觉-语言关系蒸馏 (VL Relational Distillation)**：通过 MSE 损失函数，强制学生网络的相关性矩阵 $A_s$ 模仿教师网络的相关性矩阵 $A_t$，引导学生学习跨模态对齐空间。
    3.  **通道注意力关系蒸馏 (Channel Attention Relational Distillation)**：在解码器端计算特征图的通道间相关性矩阵 $A_c$。这种“软约束”机制旨在让学生学习教师对语义成分的判别能力，而非死板地模仿像素细节，从而保留学生的独立学习能力并缓解学习偏差。
*   **总损失函数**：由分割损失（交叉熵）、跨模态关联损失和通道关联损失加权组成。

### 3. 实验设计
*   **数据集**：使用了两个主流的 RIS 基准数据集：
    *   **RefCOCO**：包含丰富的指代描述。
    *   **RefCOCO+**：禁用了位置词（如“左边”、“顶部”），更依赖于外观属性描述，难度更高。
*   **Benchmark 与对比对象**：
    *   **教师模型**：ResNet-101 + 12层 BERT。
    *   **学生模型（Baseline）**：ResNet-18 + 4层 BERT。
    *   **对比实验**：主要对比了学生模型在“有无蒸馏”情况下的性能提升，并参考了教师模型的性能上限。
*   **评价指标**：采用平均交并比（mIoU）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节**：使用 AdamW 优化器，初始学习率为 2e-5，在 20 个 epoch 后衰减至十分之一。
*   **推理开销**：论文强调蒸馏过程仅发生在训练阶段，**推理时不会引入任何额外参数或计算负担**。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个数据集的多个子集（val, testA, testB）上进行了测试。
*   **充分性评价**：
    *   **基础验证充分**：实验清晰地展示了蒸馏对轻量级模型的增益（mIoU 提升 3%~5%）。
    *   **消融实验缺失**：文中虽然提到了超参数 $\lambda_1$ 和 $\lambda_2$，但未展示不同蒸馏组件（VL 蒸馏 vs 通道蒸馏）各自贡献的消融实验数据。
    *   **对比范围有限**：主要进行了自对比，缺乏与其他先进蒸馏算法（如针对检测或分割的通用蒸馏方法）的横向对比。

### 6. 论文的主要结论与发现
*   **性能提升**：通过所提出的蒸馏方法，轻量级学生模型（ResNet-18）在 RefCOCO 和 RefCOCO+ 上的表现显著优于未蒸馏的版本。
*   **跨模态迁移有效性**：证明了模仿教师网络的高阶相关性矩阵（而非仅仅是特征图本身）是提升跨模态任务性能的有效途径。
*   **部署可行性**：该方法为在边缘设备上实现高精度的指代图像分割提供了一种可行的技术方案。

### 7. 优点
*   **关注高阶关联**：避开了简单的像素级模仿，通过通道关联和跨模态关联进行蒸馏，更符合多模态理解的本质。
*   **推理零成本**：在不改变轻量级模型推理结构的前提下，通过训练策略提升了模型上限。
*   **缓解偏差**：通道注意力蒸馏的设计考虑到了学生模型的独立学习能力，有助于减轻教师模型固有偏差的传递。

### 8. 不足与局限
*   **实验对比不够广泛**：未与近年来其他的轻量化 RIS 方案或最新的弱监督/零样本方法进行参数量与性能的综合对比。
*   **模型架构单一**：实验主要基于 ResNet 系列，未验证该蒸馏框架在更现代的轻量化架构（如 MobileNetV3, Tiny-ViT）上的泛化性。
*   **超参数敏感性未知**：缺乏关于平衡系数 $\lambda$ 的敏感性分析，读者难以判断该方法在不同任务下的调参难度。

（完）

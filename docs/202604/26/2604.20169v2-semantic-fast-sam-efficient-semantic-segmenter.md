---
title: "Semantic-Fast-SAM: Efficient Semantic Segmenter"
title_zh: Semantic-Fast-SAM：高效语义分割器
authors: Byunghyun Kim
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20169v2"
tags: ["query:cv"]
score: 9.0
evidence: 基于CNN的高效重新实现，旨在实现实时性能并减少内存占用
tldr: 本研究提出 Semantic-Fast-SAM (SFS)，旨在解决原始 SAM 模型在语义分割任务中计算开销大、难以实时应用的问题。SFS 将基于 CNN 的高效 FastSAM 与语义标注流水线相结合，通过集成语义分割一切（SSA）策略为掩码分配类别。该模型在保持高精度的同时，显著降低了内存占用，推理速度提升约 20 倍，并支持基于 CLIP 的开放词汇分割，为机器人等实时场景提供了实用的基础分割方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20169v2/fig-001.webp\", \"caption\": \"TABLE III SEMANTIC SEGMENTATION PERFORMANCE (MIOU). BEST WITHIN EACH SUPERVISION REGIME IS BOLDED; HIGHER IS BETTER.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-002.webp\", \"caption\": \"TABLE IV ABLATION RESULTS ON OPEN-VOCABULARY FUSION AND MASK COUNT. BEST WITHIN EACH ABLATION BLOCK IS BOLDED.\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-003.webp\", \"caption\": \"TABLE II GPU MEMORY USAGE FOR DIFFERENT METHODS. BEST WITHIN EACH COMPARABLE BLOCK IS BOLDED; LOWER IS BETTER.\", \"page\": 3, \"index\": 3, \"width\": 529, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-004.webp\", \"caption\": \"Fig. 2. Overall architecture of Semantic-Fast-SAM.\", \"page\": 2, \"index\": 4, \"width\": 529, \"height\": 254}]"
motivation: 原始基于 Transformer 的 SAM 模型及其语义标注扩展计算成本极高，无法满足实时语义分割的需求。
method: 结合 FastSAM 的快速掩码生成能力与 SSA 标注策略，并集成 CLIP 语义头以支持开放词汇识别。
result: 在 Cityscapes 和 ADE20K 榜单上精度与 SAM 相当，但推理速度比 SSA 快 20 倍。
conclusion: SFS 成功实现了高效且实时的“分割一切”语义分割框架，拓宽了基础分割模型在实际工程中的应用前景。
---

## 摘要
我们提出了 Semantic-Fast-SAM (SFS)，这是一个将 Fast Segment Anything 模型与语义标注流水线相结合的语义分割框架，在不牺牲准确性的情况下实现了实时性能。FastSAM 是 Segment Anything Model (SAM) 的一种基于 CNN 的高效重新实现，其运行速度远快于原始的基于 Transformer 的 SAM。基于 FastSAM 的快速掩码生成能力，我们集成了一种语义分割一切 (SSA) 标注策略，为每个掩码分配具有实际意义的类别。由此产生的 SFS 模型能够生成高质量的语义分割图，而其计算成本和内存占用仅为原始基于 SAM 方法的一小部分。在 Cityscapes 和 ADE20K 基准测试上的实验表明，SFS 的准确率与之前的基于 SAM 的方法相当（Cityscapes 上的 mIoU 约为 70.33，ADE20K 上约为 48.01），同时在闭集设置下的推理速度比 SSA 快约 20 倍。我们还展示了 SFS 通过利用基于 CLIP 的语义头，能够有效处理开放词汇分割，在广泛类别标注方面优于近期的开放词汇模型。这项工作实现了具有“分割一切”能力的实用实时语义分割，扩展了基础分割模型在机器人场景中的适用性。实现代码可在 https://github.com/KBH00/Semantic-Fast-SAM 获取。

## Abstract
We propose Semantic-Fast-SAM (SFS), a semantic segmentation framework that combines the Fast Segment Anything model with a semantic labeling pipeline to achieve real-time performance without sacrificing accuracy. FastSAM is an efficient CNN-based re-implementation of the Segment Anything Model (SAM) that runs much faster than the original transformer-based SAM. Building upon FastSAM's rapid mask generation, we integrate a Semantic-Segment-Anything (SSA) labeling strategy to assign meaningful categories to each mask. The resulting SFS model produces high-quality semantic segmentation maps at a fraction of the computational cost and memory footprint of the original SAM-based approach. Experiments on Cityscapes and ADE20K benchmarks demonstrate that SFS matches the accuracy of prior SAM-based methods (mIoU ~ 70.33 on Cityscapes and 48.01 on ADE20K) while achieving approximately 20x faster inference than SSA in the closed-set setting. We also show that SFS effectively handles open-vocabulary segmentation by leveraging CLIP-based semantic heads, outperforming recent open-vocabulary models on broad class labeling. This work enables practical real-time semantic segmentation with the "segment-anything" capability, broadening the applicability of foundation segmentation models in robotics scenarios. The implementation is available at https://github.com/KBH00/Semantic-Fast-SAM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic-Fast-SAM: Efficient Semantic Segmenter》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：原始的 Segment Anything Model (SAM) 虽然具备强大的零样本分割能力，但存在两个致命缺陷：一是**缺乏语义标签**（仅生成无类别的掩码），二是**计算开销巨大**（基于 ViT-H 架构，难以实现实时推理）。
*   **研究动机**：现有的改进方案如 Semantic-Segment-Anything (SSA) 虽然通过外挂分类器解决了标签问题，但由于底层仍依赖 SAM，推理速度极慢（单张图常需数秒）。
*   **整体含义**：本研究旨在开发一种**实时、高效且具备“分割一切”能力**的语义分割框架。通过结合轻量级的 FastSAM 和多分支语义标注流水线，在保持高精度的同时，大幅降低计算成本，使其能够应用于机器人和自动驾驶等对实时性要求极高的场景。

### 2. 论文提出的方法论
SFS 采用两阶段架构，将掩码生成与语义标注解耦：
*   **第一阶段：快速掩码生成（Module A）**
    *   使用 **FastSAM** 作为骨干。它基于 YOLOv8-seg 架构，利用 CNN 的高效性代替 Transformer。
    *   通过单次前向传播生成全图的类无关掩码（Class-agnostic masks），避免了 SAM 繁琐的迭代提示过程。
*   **第二阶段：多分支语义标注（Module B）**
    *   **闭集语义分支（Closed-Set Branch）**：利用预训练的 OneFormer（基于 ConvNeXt-L）在 COCO 和 ADE20K 数据集上生成粗略语义图，通过对掩码区域进行“投票”来确定已知类别。
    *   **开放词汇分支（Open-Vocabulary Branch）**：针对未知物体，利用 **BLIP** 生成图像区域描述，提取名词短语作为候选标签，再通过 **CLIP** 计算图像嵌入与文本嵌入的余弦相似度进行重排序。
    *   **融合与决策**：设计了启发式融合策略。如果闭集预测置信度高且与开放词汇建议不冲突，则保留闭集标签；否则，优先采用 CLIP 相似度高的开放词汇标签。

### 3. 实验设计
*   **数据集**：
    *   **Cityscapes**：用于评估城市场景下的闭集语义分割性能。
    *   **ADE20K**：用于评估复杂场景下的通用语义分割性能。
*   **Benchmark（基准）**：
    *   **零样本/基础模型类**：SSA (Semantic-SAM)。
    *   **全监督类**：OneFormer, Mask2Former, SegFormer-B5。
    *   **开放词汇类**：CLIPSeg, GroupViT, MaskCLIP。
*   **对比维度**：推理延迟（Latency）、GPU 显存占用（Memory Usage）、平均交并比（mIoU）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3090 GPU (24 GB)** 的工作站上完成。
*   **训练时长**：论文明确指出 SFS 是一个**推理时流水线（Inference-time pipeline）**，它直接组合了预训练的 FastSAM、OneFormer、BLIP 和 CLIP，**不需要额外的反向传播或模型微调**。因此，文中未涉及训练时长，重点在于推理效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集（Cityscapes, ADE20K）上进行了完整的精度对比。
    *   进行了**消融实验**，分别验证了“仅闭集”与“全模型（含开放词汇）”的性能差异，以及掩码数量（Top-100, 50, 25）对速度和精度的影响。
    *   进行了跨模型的定性与定量对比（与 5-6 种不同类型的模型对比）。
*   **充分性评价**：实验设计较为客观，涵盖了精度、速度、显存三个关键工程指标。虽然实验组数不算海量，但针对其实时性和高效性的核心主张提供了充分的证据支持。

### 6. 论文的主要结论与发现
*   **速度飞跃**：在闭集模式下，SFS 的推理速度（0.08s）比 SSA（1.65s）快了约 **20 倍**。
*   **显存优化**：SFS 的峰值显存占用约为 **4.5 GB**，而基于 SAM 的 SSA 需要约 19 GB，降低了约 75%，使其能在 8-12GB 显存的消费级显卡上运行。
*   **精度保持**：在 Cityscapes (70.33 mIoU) 和 ADE20K (48.01 mIoU) 上，SFS 的表现与昂贵的 SSA 几乎持平，证明了 FastSAM 替代 SAM 的可行性。
*   **开放词汇能力**：SFS 在处理未知类别时优于 GroupViT 等模型，得益于其更精细的掩码粒度。

### 7. 优点
*   **极高的工程实用性**：通过模块化组合现有最强模型，实现了性能与速度的平衡，无需重新训练。
*   **灵活的可扩展性**：用户可以根据算力预算，通过调整掩码数量或开关开放词汇分支来平衡性能。
*   **显存友好**：大幅降低了基础模型的使用门槛，有利于在边缘设备部署。

### 8. 不足与局限
*   **开放词汇模式下的瓶颈**：虽然闭集模式极快，但在开启开放词汇模式时，由于需要对每个掩码运行 BLIP 和 CLIP，推理时间会飙升至 10s 以上，**此时仍无法达到实时**。
*   **掩码精度风险**：FastSAM 在处理极小物体时，掩码精度偶尔略逊于原始 SAM，这导致了微小的 mIoU 下降。
*   **依赖性强**：SFS 的表现高度依赖于预训练的 OneFormer 和 CLIP 的质量，若这些组件在特定领域表现不佳，SFS 也会受限。

（完）

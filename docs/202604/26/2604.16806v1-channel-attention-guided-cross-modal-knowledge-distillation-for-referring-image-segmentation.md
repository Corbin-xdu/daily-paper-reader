---
title: Channel Attention-Guided Cross-Modal Knowledge Distillation for Referring Image Segmentation
title_zh: 通道注意力引导的跨模态知识蒸馏用于指代图像分割
authors: Chen Yang
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16806v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于指代图像分割的跨模态知识蒸馏，以减少参数规模
tldr: 针对指代图像分割（RIS）模型参数量大、难以在受限资源下部署的问题，本文提出一种通道注意力引导的跨模态知识蒸馏方法。该方法通过将教师网络学习到的视觉-语言高阶细粒度关联及通道间的语义成分关联传递给学生网络，在不增加推理参数的前提下显著提升了轻量级模型的性能，并有效缓解了学习偏差的传递。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的指代图像分割模型依赖大规模编码器导致计算资源消耗过高，限制了其在实际场景中的部署。
method: 提出一种通道注意力引导的蒸馏机制，将教师网络中的跨模态细粒度关联和通道语义相关性迁移至学生网络。
result: 在两个公开数据集上的实验表明，该方法在不增加推理负担的情况下，显著提升了学生模型的分割精度。
conclusion: 该蒸馏方法能有效平衡知识迁移与独立学习能力，是提升轻量化指代图像分割模型性能的有效方案。
---

## 摘要
指代图像分割（RIS）要求根据语言描述准确分割图像中的目标区域，是一项融合视觉和语言的跨模态任务。现有的 RIS 方法通常采用大规模视觉和语言编码模型来提高性能，但其庞大的参数量严重限制了在计算资源有限场景下的部署。为解决这一问题，本文提出了一种通道注意力引导的跨模态知识蒸馏方法，该方法将教师网络学习到的视觉与语言之间的高阶细粒度相关性，以及各通道所代表的语义成分之间的相关性，迁移至学生网络。与传统的像素级关系蒸馏相比，该方法不仅使学生网络能够学习教师网络的知识，还保留了其部分独立学习能力，从而缓解了学习偏差的迁移。在两个公开数据集上的实验结果表明，所提出的蒸馏方法在推理过程中不引入额外参数，并能显著提升学生模型的性能。

## Abstract
Referring image segmentation (RIS) requires accurate segmentation of target regions in images according to language descriptions, which is a cross-modal task integrating vision and language. Existing RIS methods typically employ large-scale vision and language encoding models to improve performance, but their enormous parameter size severely restricts deployment in scenarios with limited computing resources. To solve this problem, this paper proposes a channel attention-guided cross-modal knowledge distillation method, which transfers the high-order fine-grained correlations between vision and language learned by the teacher network, as well as the correlations between semantic components represented by each channel, to the student network. Compared with the traditional pixel-wise relational distillation, this method not only enables the student to learn the knowledge of the teacher, but also retains part of its independent learning ability, alleviating the transfer of learning bias. Experimental results on two public datasets show that the proposed distillation method does not introduce additional parameters during inference and can achieve significant performance improvement for the student model.

---

## 论文详细总结（自动生成）

这是一份关于论文《Channel Attention-Guided Cross-Modal Knowledge Distillation for Referring Image Segmentation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：指代图像分割（Referring Image Segmentation, RIS）任务要求模型根据自然语言描述精确分割图像目标。现有高性能模型普遍依赖大规模预训练编码器（如 Swin-base, BERT-base），导致**参数量巨大、计算开销高**，难以在边缘计算平台或移动端等资源受限场景部署。
*   **研究动机**：为了在保持轻量化（低参数量）的同时提升分割精度，本文试图通过**知识蒸馏（Knowledge Distillation）**技术，将大型教师网络的跨模态对齐能力和高阶语义特征迁移到轻量级学生网络中。

### 2. 方法论
论文提出了一种通道注意力引导的跨模态知识蒸馏框架，核心包含以下三个部分：
*   **整体架构**：
    *   **教师网络**：采用 ResNet-101 和 12 层 BERT 作为编码器。
    *   **学生网络**：采用 ResNet-18 或 EfficientNet-B0 以及前 4 层 BERT 作为编码器。
    *   两者均使用特征金字塔网络（FPN）作为解码器。
*   **视觉-语言跨模态注意力模块（VLCAM）**：
    *   将视觉特征作为 Query，文本特征作为 Key 和 Value，计算视觉-语言关联矩阵 $A$。通过残差连接将融合后的特征整合回视觉分支，实现细粒度的跨模态交互。
*   **双重蒸馏策略**：
    1.  **视觉-语言关系蒸馏（VL Relational Distillation）**：利用 MSE 损失函数，引导学生网络模仿教师网络生成的视觉-语言关联矩阵 $A$，从而学习教师的跨模态对齐逻辑。
    2.  **通道注意力关系蒸馏（Channel Attention Relational Distillation）**：在解码器输出端，计算特征图的通道间相关性矩阵 $A_c$。该方法不直接约束像素值（避免过度限制学生的自主学习能力），而是通过约束通道间的语义成分相关性，提升学生网络的语义判别能力。

### 3. 实验设计
*   **数据集**：使用了两个主流的 RIS 基准数据集：
    *   **RefCOCO**：包含丰富的空间和属性描述。
    *   **RefCOCO+**：禁用了方位词（如“左”、“右”），更依赖于物体外观描述，难度更高。
*   **Benchmark 与对比**：
    *   以 **mIoU（平均交并比）** 为核心评价指标。
    *   对比了教师模型（ResNet-101 基准）、学生模型（ResNet-18 基准）以及应用了蒸馏技术后的学生模型。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节**：使用 AdamW 优化器，初始学习率为 2e-5，在第 20 个 epoch 后衰减。Batch size 设置为 16。
*   **训练时长**：文中**未明确说明**具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个数据集的多个子集（val, testA, testB）上进行了测试，提供了完整的性能对比表格。
*   **充分性评价**：
    *   **客观性**：通过对比“无蒸馏”与“有蒸馏”的学生模型，直观展示了方法的有效性。
    *   **局限性**：实验主要集中在验证蒸馏框架的增益，**缺乏消融实验**来分别验证“视觉-语言关系蒸馏”和“通道关系蒸馏”各自的贡献比例。此外，对比的外部 SOTA 方法较少，主要侧重于自身框架内的性能提升。

### 6. 主要结论与发现
*   **性能提升**：在不增加推理阶段任何额外参数和计算量的前提下，该蒸馏方法使轻量级学生模型（ResNet-18）的 mIoU 提升了 **3% 至 5%**。
*   **有效性**：证明了通过模仿教师网络的高阶关联矩阵，轻量化模型可以获得接近大型模型的跨模态对齐能力。
*   **部署价值**：该方法为在边缘设备上实现高精度的指代图像分割提供了一种可行的轻量化方案。

### 7. 优点
*   **非侵入式推理**：蒸馏过程仅发生在训练阶段，推理时学生模型保持原有的轻量结构，完全不增加部署负担。
*   **软约束机制**：引入通道关系蒸馏而非像素级硬拷贝，既传递了语义知识，又保留了学生模型的独立学习能力，缓解了学习偏差的传递。
*   **跨模态对齐引导**：针对 RIS 任务的本质（视觉与语言的关联），设计了专门的关联矩阵蒸馏，抓住了任务的核心痛点。

### 8. 不足与局限
*   **实验覆盖面**：未在更大型的数据集（如 RefCOCOg）上进行验证。
*   **对比深度**：缺乏与其他经典蒸馏方法（如传统的特征图蒸馏或逻辑值蒸馏）的横向对比。
*   **超参数敏感性**：文中设置平衡系数 $\lambda_1, \lambda_2$ 均为 0.5，但未讨论这些超参数对不同规模学生模型的敏感程度。
*   **应用限制**：虽然减少了参数量，但对于极端低功耗设备，ResNet-18 可能依然存在计算压力，未探讨更小规模（如 MobileNet 系列）的极限性能。

（完）

---
title: "FlashFPS: Efficient Farthest Point Sampling for Large-Scale Point Clouds via Pruning and Caching"
title_zh: FlashFPS：通过剪枝与缓存实现大规模点云的高效最远点采样
authors: "Yuzhe Fu, Hancheng Ye, Cong Guo, Junyao Zhang, Qinsi Wang, Yueqian Lin, Changchun Zhou, Hai, Li, Yiran Chen"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17720v1"
tags: ["query:cv"]
score: 9.0
evidence: 计算机视觉中点云处理的高效采样与剪枝
tldr: 针对点云神经网络中最远点采样（FPS）在大规模场景下推理延迟高的问题，本文提出了 FlashFPS 框架。该框架通过系统分析识别出 FPS 中的计算冗余，引入 FPS-Prune 进行候选点和迭代剪枝以减少不必要计算，并利用 FPS-Cache 实现跨层采样结果的复用。FlashFPS 是一种硬件无关的即插即用方案，在保持采样质量的同时显著提升了推理速度，为高效点云处理提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17720v1/fig-001.webp\", \"caption\": \"Figure 1: (Left) Comparison between the original FPS-based (CUDA-optimized) and FlashFPS-based PNN architecture. (Right) FlashFPS well maintains geometric features and achieves an average 5.16x inference speedup for PNNs.\", \"page\": 1, \"index\": 1, \"width\": 482, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.17720v1/fig-002.webp\", \"caption\": \"Figure 6: FlashFPS: a plug-and-play acceleration framework that integrates FPS-Prune (in-layer pruning) and FPS-Cache (cross-layer reuse) to reduce redundancy and enable efficient FPS processing in PNNs.\", \"page\": 3, \"index\": 2, \"width\": 998, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2604.17720v1/fig-003.webp\", \"caption\": \"Table 1: Evaluation of FPS and FlashFPS for semantic segmentation on S3DIS and ScanNet. FlashFPS-p integrates FPSPrune and FPS-Cache, where 𝑝 denotes the pruning ratio.\", \"page\": 5, \"index\": 3, \"width\": 494, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2604.17720v1/fig-004.webp\", \"caption\": \"Figure 7: GPU inference speedup of FlashFPS on PointNeXtL and PointVector-L for segmenting S3DIS and ScanNet datasets, compared with FPS-CUDA (baseline) and QuickFPS.\", \"page\": 5, \"index\": 4, \"width\": 493, \"height\": 317}]"
motivation: FPS 在处理大规模点云时存在严重的计算冗余和推理延迟，成为点云神经网络性能提升的主要瓶颈。
method: 提出 FlashFPS 框架，通过 FPS-Prune 剪枝冗余计算并利用 FPS-Cache 缓存跨层采样结果以实现复用。
result: 在 GPU 上实现了 5.16 倍的加速，在 PNN 加速器上实现了 2.69 倍的加速，且精度损失极小。
conclusion: FlashFPS 是一种高效、可扩展且即插即用的加速方案，显著提升了大规模点云处理的推理效率。
---

## 摘要
基于点的神经网络（PNNs）已成为点云处理的关键方法。然而，这些模型中的核心操作——最远点采样（FPS），往往会引入显著的推理延迟，尤其是在大规模处理中。尽管现有的 CUDA 和硬件级优化已经存在，但由于 PNNs 中多个网络层的详尽计算，FPS 仍然是一个主要的瓶颈，阻碍了其可扩展性。通过系统性分析，我们识别出 FPS 中的三种实质性冗余，包括不必要的全云计算、冗余的后期迭代，以及使得后续 FPS 计算可以避免的可预测层间输出。为了解决这些问题，我们提出了 FlashFPS，这是一个硬件无关、即插即用的 FPS 加速框架，由 FPS-Prune 和 FPS-Cache 组成。FPS-Prune 引入了候选剪枝和迭代剪枝，在保持采样质量的同时减少了 FPS 中的冗余计算；而 FPS-Cache 通过缓存与重用消除了层间冗余。通过集成到现有的 CUDA 库和最先进的 PNN 加速器中，FlashFPS 在 GPU 上相比标准 CUDA 基准实现了 5.16 倍的加速，在 PNN 加速器上实现了 2.69 倍的加速，且精度损失微乎其微，从而实现了高效且可扩展的 PNN 推理。代码已发布于 https://github.com/Yuzhe-Fu/FlashFPS。

## Abstract
Point-based Neural Networks (PNNs) have become a key approach for point cloud processing. However, a core operation in these models, Farthest Point Sampling (FPS), often introduces significant inference latency, especially for large-scale processing. Despite existing CUDA- and hardware-level optimizations, FPS remains a major bottleneck due to exhaustive computations across multiple network layers in PNNs, which hinders scalability. Through systematic analysis, we identify three substantial redundancies in FPS, including unnecessary full-cloud computations, redundant late-stage iterations, and predictable inter-layer outputs that make later FPS computations avoidable. To address these, we propose \textbf{\textit{FlashFPS}}, a hardware-agnostic, plug-and-play framework for FPS acceleration, composed of \textit{FPS-Prune} and \textit{FPS-Cache}. \textit{FPS-Prune} introduces candidate pruning and iteration pruning to reduce redundant computations in FPS while preserving sampling quality, and \textit{FPS-Cache} eliminates layer-wise redundancy via cache-and-reuse. Integrated into existing CUDA libraries and state-of-the-art PNN accelerators, \textit{FlashFPS} achieves 5.16$\times$ speedup over the standard CUDA baseline on GPU and 2.69$\times$ on PNN accelerators, with negligible accuracy loss, enabling efficient and scalable PNN inference. Codes are released at https://github.com/Yuzhe-Fu/FlashFPS.

---

## 论文详细总结（自动生成）

这是一份关于论文《FlashFPS: Efficient Farthest Point Sampling for Large-Scale Point Clouds via Pruning and Caching》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心瓶颈**：在点云神经网络（PNNs）中，最远点采样（FPS）是保证空间覆盖率的标准操作。然而，随着点云规模从 1K 增长到 300K（如自动驾驶 LiDAR 数据），FPS 的计算复杂度呈二次方增长。
*   **性能鸿沟**：实验分析显示，在大规模点云处理中，FPS 占用了超过 90% 的总推理时间。
*   **研究动机**：现有的 CUDA 优化或硬件加速器往往忽略了 FPS 算法本身的内在冗余。本文旨在通过识别并消除这些算法和架构层面的冗余，实现一种硬件无关、即插即用的加速方案。

### 2. 方法论：核心思想与关键技术
FlashFPS 框架由两个核心组件组成，分别针对层内和层间的冗余：

*   **FPS-Prune（层内剪枝）**：
    *   **候选剪枝（Candidate Pruning）**：基于点云的置换不变性，先对原始点云进行均匀预采样（保留 $1-p$ 的点），减少参与距离计算的候选点基数。
    *   **迭代剪枝（Iteration Pruning）**：识别出 FPS 的早期迭代决定全局结构，而后期迭代仅细化局部细节。因此，仅运行前 $(1-p)M$ 次 FPS 迭代，剩余名额通过极快的随机采样补齐。
*   **FPS-Cache（层间缓存与复用）**：
    *   **核心发现（前缀属性）**：论文从数学上证明了在初始种子点固定的情况下，FPS 具有“前缀等价性”。即深层网络所需的采样点集实际上是第一层采样点集的子集（前缀）。
    *   **技术细节**：只需在网络第一层执行一次完整的（或经剪枝的）FPS，并将有序结果存入缓存。后续所有层直接根据需要从缓存中提取前 $M_i$ 个点，完全消除了重复计算。

### 3. 实验设计
*   **数据集与场景**：
    *   **语义分割**：S3DIS（室内大场景，点数达 289K）、ScanNet（室内场景，点数达 297K）。
    *   **大语言模型（LLM）**：Objaverse（用于评估 PointLLM 的 3D 推理能力）。
*   **Benchmark 与对比方法**：
    *   **软件基准**：标准 CUDA 优化版 FPS（来自 OpenPoints 库）、QuickFPS（基于 kd-tree 的优化方案）。
    *   **硬件基准**：最先进的 PNN 加速器 PointAcc 和 Crescent。
    *   **模型架构**：PointNeXt-L、PointVector-L、PointLLM。

### 4. 资源与算力
*   **GPU 平台**：主要实验在 NVIDIA TITAN RTX (24 GB) 上完成；PointLLM 实验在 NVIDIA A100 上完成。
*   **CPU 平台**：48 核 Intel Xeon CPU (3.0 GHz)。
*   **硬件模拟**：针对 PNN 加速器的评估使用了周期精确（Cycle-accurate）模拟器。
*   **训练时长**：文中强调该方法是**免训练（Training-free）**且即插即用的，因此不需要重新训练模型，直接在推理阶段应用。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 13K 到 300K 不同规模的点云输入，验证了算法的可扩展性。
*   **消融实验**：详细拆解了候选剪枝（CP）、迭代剪枝（IP）和跨层复用（FC）各自对速度和精度的贡献。
*   **充分性评价**：实验设计非常充分。不仅在通用 GPU 上进行了测试，还跨平台验证了专用硬件加速器的增益；同时涵盖了传统感知任务（分割）和新兴的生成式任务（PointLLM），证明了方案的普适性。

### 6. 主要结论与发现
*   **加速效果显著**：在 GPU 上相比标准 CUDA 实现平均加速 **5.16×**，相比 QuickFPS 加速 **1.96×**；在专用加速器上平均获得 **2.69×** 的额外加速。
*   **精度保持良好**：在 75% 的高剪枝率下，mIoU 精度损失通常控制在 **0.8% 以内**；在 25% 剪枝率下，精度损失几乎为零（<0.1%）。
*   **内存开销极低**：FPS-Cache 引入的额外内存占用仅为总模型内存的 **0.1%** 左右（约 3.3 MB），完全可以忽略不计。

### 7. 优点
*   **硬件无关性**：不依赖特定的硬件指令，在现有 GPU 和定制芯片上均能直接获益。
*   **理论支撑**：对 FPS 的前缀属性进行了形式化证明，确保了层间复用的数学正确性。
*   **即插即用**：无需修改网络架构，无需重新训练，极大降低了部署成本。
*   **解决痛点**：精准捕捉到了大规模点云处理中 FPS 占据 90% 耗时的痛点。

### 8. 不足与局限
*   **极端场景风险**：虽然在标准数据集上表现良好，但在某些对局部极细微几何特征高度敏感的任务中，迭代剪枝（用随机采样替代后期 FPS）可能会引入不可预知的偏差。
*   **超参数依赖**：剪枝率 $p$ 需要根据具体任务在速度和精度之间进行权衡，虽然论文给出了推荐值，但不同应用场景可能需要微调。
*   **架构限制**：FPS-Cache 的收益主要源于分层下采样架构，对于非分层或采样逻辑特殊的网络结构，其收益会受限。

（完）

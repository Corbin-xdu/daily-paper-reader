---
title: Silicon Aware Neural Networks
title_zh: 硅感知神经网络
authors: "Sebastian Fieldhouse, Kea-Tiong Tang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19334v1"
tags: ["query:cv"]
score: 9.0
evidence: 通过最小化每个神经元的预期面积来优化电路面积和功耗
tldr: "本研究提出了一种将可微分逻辑门网络（DLGN）直接映射到定制硅芯片的方法。通过将训练好的模型转换为门级网表，并引入一种基于标准单元面积优化的新型损失函数，实现了对电路面积和功耗的协同优化。在SkyWater 130nm工艺下的仿真结果显示，该方法在保持MNIST 97%准确率的同时，实现了极高的推理速度和低功耗，为高效硬件神经网络设计提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19334v1/fig-001.webp\", \"caption\": \"TABLE III: Performance Analysis\", \"page\": 4, \"index\": 1, \"width\": 943, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2604.19334v1/fig-002.webp\", \"caption\": \"Fig. 3: DLGN laid out in SkyWater 130nm as a hard macro\", \"page\": 4, \"index\": 2, \"width\": 420, \"height\": 648}, {\"url\": \"assets/figures/arxiv/2604.19334v1/fig-003.webp\", \"caption\": \"Fig. 2: Training dynamics across five values of δ. Models are initialized with random unique connections, 6 LogicLayers each with 64,000 neurons (a) Training accuracy and (b) average cell area measured every 1,000 iterations over 200,000 training steps.\", \"page\": 3, \"index\": 3, \"width\": 468, \"height\": 662}, {\"url\": \"assets/figures/arxiv/2604.19334v1/fig-004.webp\", \"caption\": \"Fig. 1: Concept of DLGN for image classification, from [2]\", \"page\": 2, \"index\": 4, \"width\": 506, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.19334v1/fig-005.webp\", \"caption\": \"TABLE I: Mapping of logic gate functions to SkyWater 130 nm Cadence standard cells\", \"page\": 2, \"index\": 5, \"width\": 524, \"height\": 431}]"
motivation: 旨在解决如何将可微分逻辑门网络高效地映射到定制数字CMOS芯片，并优化其硬件资源占用。
method: 提出将DLGN模型一对一转换为门级网表的方法，并设计了基于标准单元面积的损失函数来优化电路规模。
result: "在SkyWater 130nm工艺下，该电路实现了每秒4180万次MNIST分类，准确率达97%，功耗仅为83.88毫瓦。"
conclusion: 该研究证明了逻辑门神经网络在定制硅芯片上实现超高性能、低功耗推理的可行性与优越性。
---

## 摘要
最近的机器学习文献表明，深度学习可以训练由离散逻辑门函数组成的神经网络，从而在 CPU、GPU 和 FPGA 平台上以极高的速度执行简单的图像分类任务。由于这些可微分逻辑门网络（DLGN）由离散逻辑门构成，它们天然适用于定制硅片的实现。在这项工作中，我们提出了一种通过将训练好的模型转换为门级网表，将 DLGN 一一映射到数字 CMOS 标准单元库的方法。我们还提出了一种新型损失函数，使 DLGN 能够根据目标标准单元库中标准单元的面积，通过最小化每个神经元的预期面积，来优化所得电路的面积并间接降低功耗。最后，我们还首次展示了 DLGN 作为硅电路的仿真实现，使用 Cadence 标准单元库在 SkyWater 130nm 工艺中将 DLGN 布局为定制硬宏，并进行了布局后功耗分析。我们发现，我们的定制宏能够以 97% 的准确率在 MNIST 数据集上每秒执行 4180 万次分类，功耗仅为 83.88 mW。

## Abstract
Recent work in the machine learning literature has demonstrated that deep learning can train neural networks made of discrete logic gate functions to perform simple image classification tasks at very high speeds on CPU, GPU and FPGA platforms. By virtue of being formed by discrete logic gates, these Differentiable Logic Gate Networks (DLGNs) lend themselves naturally to implementation in custom silicon - in this work we present a method to map DLGNs in a one-to-one fashion to a digital CMOS standard cell library by converting the trained model to a gate-level netlist. We also propose a novel loss function whereby the DLGN can optimize the area, and indirectly power consumption, of the resulting circuit by minimizing the expected area per neuron based on the area of the standard cells in the target standard cell library. Finally, we also show for the first time an implementation of a DLGN as a silicon circuit in simulation, performing layout of a DLGN in the SkyWater 130nm process as a custom hard macro using a Cadence standard cell library and performing post-layout power analysis. We find that our custom macro can perform classification on MNIST with 97% accuracy 41.8 million times a second at a power consumption of 83.88 mW.

---

## 论文详细总结（自动生成）

这是一份关于论文《Silicon Aware Neural Networks》的深度结构化总结：

### 1. 研究动机与背景
在数字神经网络加速器设计中，存在一个根本性的矛盾：深度学习依赖于**连续可微函数**以实现梯度下降收敛，而数字电路在底层运行的是**离散逻辑运算**。传统的解决方法（如量化、激活函数简化）往往是折中方案。
*   **核心问题**：如何将基于离散逻辑门的神经网络（DLGN）直接且高效地映射到定制硅芯片（ASIC）上，并利用硬件特性在训练阶段就对电路面积和功耗进行优化？
*   **整体含义**：该研究旨在打破算法与硬件实现的壁垒，实现一种“硅感知”的训练模式，使神经网络在学习分类任务的同时，也在学习如何减小自身的物理电路尺寸。

### 2. 方法论
论文提出了一套从算法训练到硬件布局布线的完整流程：
*   **核心思想**：利用可微分逻辑门网络（DLGN）的特性，将逻辑门的选择概率化，并引入硬件面积作为正则化项。
*   **关键技术细节**：
    *   **逻辑门松弛**：将16种可能的2输入逻辑门（如AND, OR, XOR等）松弛为连续函数，并为每个节点分配一个16维的可训练权重向量 $z$，通过 Softmax 函数计算每种逻辑门的概率分布。
    *   **面积感知损失函数**：定义 $L_{area}$ 为所有神经元预期面积的平均值。公式为：$E[area_n] = \sum p_{n,i} \cdot A_i$，其中 $A_i$ 是目标标准单元库中对应逻辑门的物理面积。
    *   **总损失函数**：$L_{total} = L_{CE} + \delta \cdot L_{area}$。通过调节超参数 $\delta$，在分类准确率和电路面积之间取得平衡。
    *   **硬件映射**：训练完成后，取概率最大的逻辑门进行离散化，直接生成门级网表（Netlist），映射到 SkyWater 130nm CMOS 标准单元库。

### 3. 实验设计
*   **数据集**：MNIST（手写数字识别）和 CIFAR-10（物体分类）。
*   **Benchmark（基准）**：
    *   **内部对比**：对比了带有面积损失函数和不带面积损失函数的 DLGN 模型。
    *   **外部对比**：将该工作与现有的 FPGA 实现（如 FINN）、Tsetlin Machine 加速器以及 28nm/40nm 工艺下的混合信号 CNN 加速器进行了性能对比（见论文 Table III）。
*   **流程验证**：使用 Cadence Innovus 进行物理布局布线（Layout），生成硬宏（Hard Macro），并使用 Xcelium 进行门级仿真验证。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练阶段使用的具体 GPU 型号、数量或训练时长。
*   **硬件实现参数**：实验采用了 SkyWater 130nm 开源 PDK 和 Cadence 标准单元库。最终设计的 MNIST 宏电路包含 18 层，每层 4000 个神经元。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了针对超参数 $\delta$ 的消融实验（取值从 $10^{-5}$ 到 $0.1$），观察其对准确率和单元面积的影响。
    *   在两个主流数据集（MNIST, CIFAR-10）上验证了面积优化效果。
*   **充分性评价**：实验设计较为客观，涵盖了从算法收敛性分析到物理后端实现的完整链路。但由于 130nm 工艺布线资源的限制，作者被迫缩小了模型规模（从 6.4万神经元减至 4000），这在一定程度上限制了对超大规模网络在先进工艺下表现的探讨。

### 6. 主要结论与发现
*   **面积优化显著**：引入面积损失函数后，MNIST 模型的平均神经元面积从 $9.38 \mu m^2$ 降至 $6.11 \mu m^2$（下降约 35%），而准确率仅轻微下降（98.04% -> 97.66%）。
*   **极高性能**：在 130nm 工艺下，MNIST 分类吞吐量达到 **4180 万次/秒**，延迟仅为 **23.9 ns**。
*   **能效表现**：单次推理功耗为 83.88 mW，能量消耗为 2.0 nJ。若等效缩放到 16nm 工艺，单次推理能量估计仅为 **69 pJ**，延迟缩短至 **4.2 ns**，优于多数现有的 CNN 加速器。

### 7. 优点
*   **软硬协同创新**：首次将标准单元库的物理面积数据直接反馈到神经网络的损失函数中，实现了真正的“硅感知”训练。
*   **端到端流程**：展示了从 Python 训练到 GDSII 布局生成的完整自动化潜力，证明了 DLGN 在定制硅片上的可行性。
*   **超低延迟**：由于是纯组合逻辑实现（或极浅流水线），其推理速度远超传统的基于存储器和乘法累加单元（MAC）的架构。

### 8. 不足与局限
*   **布线挑战**：逻辑门网络具有高度随机的连接性，在老旧工艺（如 1P5M 的 130nm）中面临严重的布线拥塞问题，限制了网络深度和宽度。
*   **任务复杂度限制**：目前仅在 MNIST 和 CIFAR-10 等简单数据集上验证，对于更复杂的视觉任务（如 ImageNet），逻辑门网络的表达能力和可扩展性仍有待证明。
*   **仿真局限**：目前所有结果均基于布局后仿真（Post-layout Simulation），尚未进行实际流片（Tape-out）和实测芯片数据验证。

（完）

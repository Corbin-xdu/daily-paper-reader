---
title: "Bridging the Training-Deployment Gap: Gated Encoding and Multi-Scale Refinement for Efficient Quantization-Aware Image Enhancement"
title_zh: 弥合训练与部署的差距：面向高效量化感知图像增强的门控编码与多尺度细化
authors: "Dat To-Thanh, Nghia Nguyen-Trong, Hoang Vo, Hieu Bui-Minh, Tinh-Anh Nguyen-Nhu"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21743v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于高效移动图像增强的量化感知训练
tldr: 针对移动端图像增强模型在量化部署时出现的性能下降问题，本文提出了一种高效的图像增强方案。该方案采用分层网络架构，结合门控编码块和多尺度细化技术以保留细节特征，并引入量化感知训练（QAT）来模拟低精度环境。研究成功弥合了训练与部署间的差距，在保持低计算开销的同时实现了高质量的视觉输出。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21743v1/fig-001.webp\", \"caption\": \"Figure 2. Quantization-Aware Training (QAT) and inference pipelines. (Top) QAT simulates low-precision effects via FakeQuant nodes during the forward pass while maintaining FP32 compute. To bypass non-differentiable quantization steps, the backward pass uses the Straight-Through Estimator (STE) to route gradients and update FP32 weights, minimizing the loss (Ltotal) against the ground truth (GT). (Bottom) Post-training, the model is converted to a purely INT8 representation for efficient integer-only inference.\", \"page\": 6, \"index\": 1, \"width\": 932, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.21743v1/fig-002.webp\", \"caption\": \"Figure 1. The proposed hybrid architecture for RGB image enhancement. The model features a three-scale hierarchical structure comprising Gated Encoder Blocks with multichannel skip connections, Multi-Scale Refinement modules applied at scales S/2, S/4, and S/8, and a Multi-branch Feature Fusion strategy in the decoder to aggregate multi-level semantic features for high-fidelity restoration.\", \"page\": 4, \"index\": 2, \"width\": 932, \"height\": 547}]"
motivation: 解决深度学习图像增强模型在移动端部署时，因低精度量化导致的性能退化及处理速度与质量难以兼得的问题。
method: 提出一种包含门控编码块和多尺度细化机制的分层架构，并结合量化感知训练（QAT）来优化模型。
result: 实验证明该方法在标准移动设备上实现了高保真视觉效果与低计算开销的平衡。
conclusion: 通过结合高效架构设计与量化感知训练，可以有效提升移动端图像增强模型的实际部署表现。
---

## 摘要
移动设备的图像增强模型通常难以在高质量输出与移动硬件所需的快速处理速度之间取得平衡。虽然近期的深度学习模型可以将低质量的移动照片增强为高质量图像，但在转换为低精度格式以供手机实际使用时，其性能往往会下降。为了解决这种训练与部署不匹配的问题，我们提出了一种专门为移动部署设计的高效图像增强模型。我们的方法采用了一种包含门控编码器块和多尺度细化的分层网络架构，以保留细粒度的视觉特征。此外，我们引入了量化感知训练（QAT），以模拟训练过程中的低精度表示效应。这使得网络能够进行自适应，并防止了标准训练后量化（PTQ）中常见的质量下降。实验结果表明，该方法在产生高保真视觉输出的同时，保持了标准移动设备实际使用所需的低计算开销。代码将在 https://github.com/GenAI4E/QATIE.git 上发布。

## Abstract
Image enhancement models for mobile devices often struggle to balance high output quality with the fast processing speeds required by mobile hardware. While recent deep learning models can enhance low-quality mobile photos into high-quality images, their performance is often degraded when converted to lower-precision formats for actual use on mobile phones. To address this training-deployment mismatch, we propose an efficient image enhancement model designed specifically for mobile deployment. Our approach uses a hierarchical network architecture with gated encoder blocks and multiscale refinement to preserve fine-grained visual features. Moreover, we incorporate Quantization-Aware Training (QAT) to simulate the effects of low-precision representation during the training process. This allows the network to adapt and prevents the typical drop in quality seen with standard post-training quantization (PTQ). Experimental results demonstrate that the proposed method produces high-fidelity visual output while maintaining the low computational overhead needed for practical use on standard mobile devices. The code will be available at https://github.com/GenAI4E/QATIE.git.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bridging the Training-Deployment Gap: Gated Encoding and Multi-Scale Refinement for Efficient Quantization-Aware Image Enhancement》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**移动端图像增强模型在“训练”与“部署”之间的性能差距**。
*   **背景**：虽然深度学习在图像增强（Deep ISP）领域取得了巨大进步，但移动设备受限于硬件，通常需要将模型从全精度（FP32/FP16）量化为低精度（如 INT8）以实现实时处理。
*   **核心问题**：传统的训练后量化（PTQ）在图像增强任务中会导致严重的视觉伪影（如色偏、条带、纹理丢失）。这是因为 Deep ISP 模型的激活分布通常是长尾、非对称且对输入高度敏感的，微小的量化误差会被放大为明显的感知质量下降。

### 2. 方法论：核心思想与关键技术
论文提出了一种结合高效架构设计与**量化感知训练（QAT）**的方案：
*   **分层网络架构**：采用三尺度（S, S/2, S/4）的分层结构，平衡全局上下文和局部细节。
*   **门控编码块（Gated Encoder Block）**：
    *   使用双分支卷积（$F_a, F_b$）并通过 $tanh$ 激活。
    *   通过哈达玛积（Hadamard product）进行特征融合，起到轻量级空间-通道注意力机制的作用。
    *   采用多通道跳跃连接，将原始特征和门控特征同时传递给解码器，保留细粒度细节。
*   **多尺度细化（Multi-Scale Refinement）**：在编码器、瓶颈层和解码器融合后均加入基于 UNet 风格的残差卷积块，逐步增强特征辨别力。
*   **量化感知训练（QAT）**：
    *   在训练的前向传播中引入 **FakeQuant（伪量化）** 节点，模拟 8-bit 量化的截断和舍入误差。
    *   在反向传播中使用 **STE（直通估计器）** 绕过不可导的量化操作，使模型在训练阶段就学会补偿量化损失。
*   **复合损失函数**：结合了归一化 PSNR 损失、余弦相似度损失（保证结构一致性）和离群值感知损失（稳定学习过程）。

### 3. 实验设计
*   **数据集**：主要使用 **DPED 数据集**，包含由 iPhone 3GS、Sony 和 Blackberry 手机拍摄并与 Canon 70D DSLR 对齐的配对图像。
*   **Benchmark**：参与了 **Mobile AI 2026 RGB 图像增强挑战赛**。
*   **对比方法**：对比了 DPED 基准模型、PPCN 以及不同的量化策略（PTQ vs. QAT）。
*   **评价指标**：PSNR、SSIM、MOS（平均主观意见分）以及在真实手机（Snapdragon 8 Gen 2）上的推理延迟。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA RTX A6000 GPU** 进行训练。
*   **训练配置**：采用 PyTorch Lightning 框架，bfloat16 精度训练，Adam 优化器，Cosine 退火调度。
*   **时长**：在提出的损失函数配置下，训练约需 **1.67 小时**（50 个 epoch，约 62k 次迭代）。
*   **部署测试**：在移动端使用 TFLite 格式，通过 GPU Delegate 和 QNN HTP（DSP/NPU）进行基准测试。

### 5. 实验数量与充分性
论文进行了较为充分的消融实验和对比实验：
*   **架构消融**：分别验证了全局残差连接、融合细化块、分辨率细化块对性能和延迟的影响。
*   **通道数消融**：测试了通道宽度 $c \in \{16, 24, 32, 64\}$ 的权衡，最终选定 $c=32$ 作为性能与速度的最佳平衡点。
*   **损失函数消融**：对比了本文损失与包含 MSSSIM 的损失在训练时间和质量上的差异。
*   **量化对比**：详细对比了 FP32、INT8-PTQ 和 INT8-QAT 的表现。
*   **客观性**：实验涵盖了从理论指标（PSNR/SSIM）到实际硬件延迟（ms）再到主观视觉对比，评估维度全面。

### 6. 主要结论与发现
*   **QAT 的必要性**：直接对 FP32 模型进行 PTQ 量化会导致严重的色偏（如天空变粉）和噪声，而 QAT 能显著修复这些问题，使 INT8 模型的 PSNR 提升了 0.474 dB，SSIM 提升了 0.111。
*   **硬件加速显著**：在移动端 QNN HTP（专用加速器）上，INT8 模型比 FP16 模型快约 **3.6 倍**（延迟从 151ms 降至 41.8ms）。
*   **架构有效性**：门控编码和多尺度细化在保持低参数量（约 0.915M）的同时，成功达到了挑战赛的精度门槛。

### 7. 优点
*   **部署导向**：不只是追求理论高分，而是针对移动端 8-bit 整数运算环境进行优化，具有极强的实用价值。
*   **效率平衡**：通过通道消融和模块消融，清晰地展示了如何在有限的算力预算下最大化图像质量。
*   **训练高效**：相比于使用 MSSSIM 的方案，本文提出的损失函数显著缩短了训练时间（从 8.58 小时降至 1.67 小时）。

### 8. 不足与局限
*   **数据集单一**：实验主要集中在 DPED 数据集上，该数据集设备较老（如 iPhone 3GS），对现代高端智能手机计算摄影（RAW 域处理）的泛化能力尚待验证。
*   **量化方案固定**：主要讨论了对称 8-bit 量化，未探讨更低比特（如 4-bit）或混合精度量化对图像增强的影响。
*   **应用限制**：模型设计高度依赖于特定的移动端推理框架（TFLite），在其他嵌入式平台上的表现未提及。

（完）

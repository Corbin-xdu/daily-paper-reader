---
title: "EfficientPENet: Real-Time Depth Completion from Sparse LiDAR via Lightweight Multi-Modal Fusion"
title_zh: EfficientPENet：基于轻量级多模态融合的稀疏LiDAR实时深度补全
authors: "Johny J. Lopez, Md Meftahul Ferdaus, Mahdi Abdelguerfi, Anton Netchaev, Steven Sloan, Ken Pathak, Kendall N. Niles"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18790v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于实时嵌入式部署的轻量级多模态融合和现代化骨干网络
tldr: 针对稀疏LiDAR深度补全在嵌入式硬件上难以实时运行的问题，本文提出EfficientPENet。该网络采用轻量化双分支架构，结合现代化的ConvNeXt骨干网络与稀疏不变卷积，并通过卷积空间传播网络（CSPN）进行精细化处理。通过后期融合与多尺度深度监督，该方法在保持高精度的同时显著降低了参数量和推理延迟，为边缘计算平台提供了高效的实时深度补全方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18790v1/fig-001.webp\", \"caption\": \"Fig. 4: Normalized multi-metric comparison. Each axis is scaled to [0, 1]; outermost ring indicates best. EfficientPENet dominates on efficiency axes.\", \"page\": 7, \"index\": 1, \"width\": 529, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2604.18790v1/fig-002.webp\", \"caption\": \"Fig. 2: Accuracy–efficiency trade-off. Bubble size is proportional to parameter count. EfficientPENet achieves the lowest RMSE at the highest throughput.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2604.18790v1/fig-003.webp\", \"caption\": \"Fig. 3: Model complexity comparison. EfficientPENet reduces parameter count by 3.6× relative to PENet and computational cost by 3.3× relative to BP-Net.\", \"page\": 7, \"index\": 3, \"width\": 529, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.18790v1/fig-004.webp\", \"caption\": \"Fig. 6: Ablation study showing incremental RMSE reduction from each architectural component on the KITTI validation set.\", \"page\": 9, \"index\": 4, \"width\": 529, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2604.18790v1/fig-005.webp\", \"caption\": \"Fig. 1: Overview of the EfficientPENet architecture. The RGB branch (left) uses a pretrained ConvNeXt encoder; the depth branch (right) uses sparsity-invariant convolutions. Both branches predict depth and confidence maps, which are fused via confidence weighting and refined by CSPN.\", \"page\": 5, \"index\": 5, \"width\": 1076, \"height\": 592}]"
motivation: 现有的深度补全方法虽然精度高，但由于模型过于沉重，难以在资源受限的嵌入式硬件上实现实时部署。
method: 采用基于ConvNeXt的轻量化双分支架构，结合稀疏不变卷积、CSPN细化模块以及位置感知测试时增强技术。
result: 在KITTI基准测试中，该模型以36.24M参数量实现了48.76 FPS的运行速度，且RMSE误差仅为631.94 mm。
conclusion: EfficientPENet在精度与效率之间取得了极佳平衡，是适用于NVIDIA Jetson等边缘平台的实用实时深度补全方案。
---

## 摘要
从稀疏LiDAR测量值和相应的RGB图像进行深度补全是机器人系统中精确3D感知的先决条件。现有方法在标准基准测试中取得了高精度，但依赖于沉重的主干架构，这阻碍了在嵌入式硬件上的实时部署。我们提出了EfficientPENet，这是一种双分支深度补全网络，它用现代化的ConvNeXt主干替换了传统的ResNet编码器，为深度流引入了稀疏不变卷积，并通过卷积空间传播网络（CSPN）细化预测。RGB分支利用了经过ImageNet预训练的ConvNeXt块，具有层归一化（Layer Normalization）、7x7深度可分离卷积和随机深度正则化。来自两个分支的特征通过后期融合进行合并，并通过多尺度深度监督策略进行解码。我们还引入了一种位置感知测试时增强方案，该方案在水平翻转期间校正坐标张量，从而在推理时实现一致的误差降低。在KITTI深度补全基准测试中，EfficientPENet实现了631.94 mm的RMSE，具有36.24M个参数，延迟为20.51 ms，运行速度为48.76 FPS。与BP-Net相比，这代表了参数量减少了3.7倍，速度提升了23倍，同时保持了具有竞争力的精度。这些结果证明EfficientPENet是NVIDIA Jetson等资源受限边缘平台上实时深度补全的实用解决方案。

## Abstract
Depth completion from sparse LiDAR measurements and corresponding RGB images is a prerequisite for accurate 3D perception in robotic systems. Existing methods achieve high accuracy on standard benchmarks but rely on heavy backbone architectures that preclude real-time deployment on embedded hardware. We present EfficientPENet, a two-branch depth completion network that replaces the conventional ResNet encoder with a modernized ConvNeXt backbone, introduces sparsity-invariant convolutions for the depth stream, and refines predictions through a Convolutional Spatial Propagation Network (CSPN). The RGB branch leverages ImageNet-pretrained ConvNeXt blocks with Layer Normalization, 7x7 depthwise convolutions, and stochastic depth regularization. Features from both branches are merged via late fusion and decoded through a multi-scale deep supervision strategy. We further introduce a position-aware test-time augmentation scheme that corrects coordinate tensors during horizontal flipping, yielding consistent error reduction at inference. On the KITTI depth completion benchmark, EfficientPENet achieves an RMSE of 631.94 mm with 36.24M parameters and a latency of 20.51 ms, operating at 48.76 FPS. This represents a 3.7 times reduction in parameters and a 23 times speedup relative to BP-Net, while maintaining competitive accuracy. These results establish EfficientPENet as a practical solution for real-time depth completion on resource-constrained edge platforms such as the NVIDIA Jetson.

---

## 论文详细总结（自动生成）

这是一份关于论文《EfficientPENet: Real-Time Depth Completion from Sparse LiDAR via Lightweight Multi-Modal Fusion》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何在**资源受限的嵌入式边缘平台**（如 NVIDIA Jetson）上实现**实时且高精度**的稀疏 LiDAR 深度补全。
*   **背景**：地下基础设施（如下水道、管道）的机器人巡检需要精确的 3D 几何信息，但高精度 LiDAR 体积大且昂贵，小型机器人通常使用低成本、产生稀疏点云（有效像素通常不足 5%）的轻量级 LiDAR。
*   **动机**：现有的深度补全模型（如 PENet, BP-Net, CompletionFormer）虽然在 KITTI 基准上精度很高，但往往依赖于沉重的骨干网络（ResNet-101/152 或 Transformer），导致参数量巨大（>90M）且推理延迟高（>350ms），无法满足机器人实时导航和在线检测的需求。

### 2. 方法论：核心思想与关键技术
EfficientPENet 采用了一种**双分支编码器-解码器架构**，核心思想是通过现代化卷积设计在降低计算负担的同时提升特征表达能力。
*   **ConvNeXt RGB 编码器**：取代传统的 ResNet-34。引入了 7×7 深度可分离卷积（增大感受野）、层归一化（Layer Normalization）和 GELU 激活函数。通过 ImageNet 预训练权重初始化，并采用随机深度正则化防止过拟合。
*   **稀疏不变卷积（Sparsity-Invariant Convolutions）**：在深度编码分支中，使用二进制掩码（Mask）对卷积输出进行归一化，防止无效的零值像素污染特征图。
*   **后期融合与多尺度监督**：RGB 和深度分支在瓶颈层进行后期融合（Late Fusion）。解码器在 1/8、1/4、1/2 和全分辨率尺度上输出预测，并分别施加监督损失，加速收敛。
*   **CSPN 几何细化**：利用卷积空间传播网络（CSPN）学习亲和力矩阵，通过 6 次迭代传播修正物体边界处的模糊现象。
*   **位置感知测试时增强（TTA）**：针对输入中包含显式坐标编码（Position Encoding）的特点，在水平翻转推理时同步修正坐标张量，从而在不重新训练的情况下降低 RMSE。

### 3. 实验设计
*   **数据集**：主要使用 **KITTI 深度补全基准数据集**（包含 80,000+ 训练对）。
*   **Benchmark**：以 KITTI 官方验证集为基准，对比指标包括 RMSE（首要指标）、MAE、iRMSE 和 iMAE。
*   **对比方法**：对比了 PENet（基准模型）、BP-Net（高性能模型）和 DMD3C（基于蒸馏的模型）等主流算法。
*   **应用场景扩展**：论文还讨论了在地下管道巡检场景下的领域自适应（Domain Adaptation），使用了混合真实-合成的下水道数据集进行微调。

### 4. 资源与算力
*   **训练环境**：使用了 PyTorch 分布式数据并行（DDP）框架，在多 GPU 环境下训练。
*   **推理平台**：重点测试了在 **NVIDIA Jetson AGX** 嵌入式平台上的表现。
*   **具体数值**：模型参数量为 **36.24M**，计算量为 **220.46 GFLOPs**。在 Jetson 平台上实现了 **20.51 ms** 的延迟，帧率高达 **48.76 FPS**。
*   **未明确说明**：文中未明确给出具体的训练总时长和所使用的训练级 GPU（如 A100/V100）的具体数量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 KITTI 验证集上进行了完整的定量对比。
    *   提供了 5 组不同场景（骑行者、城市、住宅区等）的定性视觉对比。
    *   **消融实验**：针对 ConvNeXt 骨干网、多尺度监督、位置感知 TTA 三个核心组件分别做了消融分析，验证了各部分的增益。
*   **充分性评价**：实验设计较为充分，不仅关注精度（RMSE），还深入分析了参数量、FLOPs 和实际推理延迟。对比实验涵盖了近年来具有代表性的重型和轻型模型，评价标准客观、公平。

### 6. 主要结论与发现
*   **性能领先**：EfficientPENet 在 KITTI 上取得了 **631.94 mm 的 RMSE**，优于所有对比的重型模型。
*   **效率极高**：相比 BP-Net，参数量减少了 **3.7 倍**，推理速度提升了 **23 倍**。
*   **误差特性**：发现该模型在 RMSE（减少灾难性离群值）上表现极佳，但在 MAE（平均误差）上略逊于重型模型，这表明轻量化模型在远场精度上有所牺牲，但对近场导航影响较小。
*   **位置编码的重要性**：证明了在 TTA 过程中修正坐标张量对保持几何一致性至关重要。

### 7. 优点（亮点）
*   **架构现代化**：成功将 ConvNeXt 的设计理念引入深度补全领域，证明了纯卷积架构在边缘设备上的优越性。
*   **端到端实用性**：不仅追求指标，还考虑了 TensorRT 部署时的内存访问效率（如 NHWC 格式）。
*   **位置感知 TTA**：这是一个很巧妙的改进，解决了带坐标编码的模型在数据增强时的逻辑矛盾。

### 8. 不足与局限
*   **远场精度牺牲**：MAE 指标较高，说明在 40 米以外的远距离区域，由于编码器容量减小，平均预测精度不如重型模型。
*   **领域迁移验证尚不完整**：虽然提到了地下管道的自适应，但文中缺乏在真实地下场景下的定量评估数据（仅有定性描述和初步集成结果）。
*   **细化模块固定**：使用的 CSPN 仍是固定核大小，未采用更先进但计算量更大的动态传播网络（DySPN）。

（完）

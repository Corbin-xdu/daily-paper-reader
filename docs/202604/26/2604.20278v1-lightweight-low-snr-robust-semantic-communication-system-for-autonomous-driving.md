---
title: Lightweight Low-SNR-Robust Semantic Communication System for Autonomous Driving
title_zh: 面向自动驾驶的轻量级低信噪比鲁棒语义通信系统
authors: "Ruixing Ren, Minjie Wei, Junhui Zhao"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20278v1"
tags: ["query:cv"]
score: 9.0
evidence: 基于BN层缩放因子的结构化剪枝用于自动驾驶图像传输
tldr: 针对自动驾驶协作感知中资源受限及低信噪比环境下通信鲁棒性差的问题，本文提出一种轻量化且鲁棒的深度联合信源信道编码（JSCC）语义通信系统。通过结构化剪枝大幅降低模型复杂度，并结合量化与M-QAM调制实现与现有数字系统的兼容。实验证明，该系统在减少一半以上参数的同时，在低信噪比下表现优于传统方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20278v1/fig-001.webp\", \"caption\": \"Fig. 1: Block Diagram of V2V Semantic Communication System for Autonomous Driving\", \"page\": 2, \"index\": 1, \"width\": 505, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.20278v1/fig-002.webp\", \"caption\": \"TABLE I: The parameter settings of the model. Conv: Convolutional Layer. Trans ConV: Transposed convolutional layer.\", \"page\": 5, \"index\": 2, \"width\": 446, \"height\": 279}]"
motivation: 解决自动驾驶协作感知中车载资源有限、无线信道衰落及传统通信方案在低信噪比下性能骤降的问题。
method: 采用基于BN层缩放因子和L1正则化的结构化剪枝，并设计了适配JSCC的均匀量化与M-QAM调制方案。
result: "在Cityscapes数据集上，剪枝后的模型在移除超50%参数后仍保持高性能，且在低信噪比条件下显著优于传统通信方法。"
conclusion: 该轻量化语义通信系统在保证图像重建质量的同时，提升了低信噪比下的鲁棒性与数字系统兼容性，适用于资源受限的自动驾驶场景。
---

## 摘要
自动驾驶中车对车（V2V）协同感知的图像传输面临着车载终端资源有限、时变无线信道衰落以及低信噪比（SNR）下鲁棒性差等挑战。传统的源信道分离编码方案存在“悬崖效应”，而现有的语义通信模型则受限于参数量大和数字兼容性弱的问题。本文提出了一种轻量级、低信噪比鲁棒的深度联合源信道编码（JSCC）语义通信系统。首先，基于批归一化（BN）层缩放因子和L1正则化实现了结构化剪枝，在保证图像重建质量的同时显著降低了模型复杂度。其次，设计了适配JSCC特性的均匀量化和M-QAM调制方案，并采用训练-部署分离策略解决不可微量化问题，实现了与现有数字通信系统的兼容。在Cityscapes数据集上的仿真结果表明，即使在移除超过一半参数的情况下，剪枝后的模型仍能保持与原模型相当的性能和鲁棒性。值得注意的是，所提方案在低信噪比条件下相比传统通信方法表现出显著优势。

## Abstract
Image transmission for vehicle-to-vehicle collaborative perception in autonomous driving faces challenges including limited on-board terminal resources, time-varying wireless channel fading, and poor robustness under low signal-to-noise (SNR) ratio. Traditional separate source-channel coding schemes suffer from the cliff effect, while existing semantic communication models are limited by large parameter sizes and weak digital compatibility. This paper proposes a lightweight, low-SNR-robust deep joint source-channel coding (JSCC) semantic communication system. First, structured pruning is implemented based on batch normalization layer scaling factors and L1 regularization, which significantly reduces model complexity while ensuring image reconstruction quality. Second, a uniform quantization and M-QAM modulation scheme adapted to JSCC features is designed, and a training-deployment separation strategy is adopted to address the non-differentiable quantization problem, enabling compatibility with existing digital communication systems. Simulation results on the Cityscapes dataset show that the pruned model maintains comparable performance and robustness to the original one, even with over half of its parameters removed. Notably, the proposed scheme exhibits significant advantages over conventional communication methods under low SNR conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Lightweight Low-SNR-Robust Semantic Communication System for Autonomous Driving》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：自动驾驶已从单车智能转向多车协同感知（V2V）。这种模式需要车辆间实时共享视觉信息，以突破遮挡和感知盲区。
*   **核心挑战**：
    1.  **资源受限**：车载终端的计算和存储资源有限，难以运行大型深度学习模型。
    2.  **信道恶劣**：无线信道存在时变衰落和低信噪比（SNR）干扰。
    3.  **传统方案缺陷**：基于信源-信道分离编码（如BPG+LDPC）的传统方案在低SNR下会产生“悬崖效应”（性能骤降），且语义通信模型通常参数量巨大且与现有数字通信系统不兼容。
*   **研究目的**：开发一种既能大幅压缩模型体积，又能在低信噪比下保持高鲁棒性，且兼容现有数字调制（QAM）的语义通信系统。

### 2. 论文提出的方法论
该系统基于深度联合信源信道编码（JSCC）架构，核心创新点包括：
*   **基于结构化剪枝的轻量化设计**：
    *   **核心思想**：利用批归一化（BN）层的缩放因子（$\eta$）来评估通道重要性。
    *   **技术细节**：在损失函数中引入L1正则化项，强制不重要的通道缩放因子趋于零。
    *   **流程**：先进行稀疏训练，随后根据全局剪枝率 $\gamma$ 移除 $\eta$ 值较小的通道及其对应的卷积核，最后通过微调（Fine-tuning）恢复模型性能。
*   **低SNR鲁棒的量化调制方案**：
    *   **核心思想**：设计适配JSCC特征的均匀量化与M-QAM调制。
    *   **训练-部署分离策略**：为解决量化操作不可微导致的梯度回传问题，训练阶段采用模拟传输（在特征图中加入等效噪声），推理阶段则应用实际的量化和M-QAM映射。
    *   **鲁棒性原理**：由于解码器在训练时已适应了噪声干扰，因此对量化误差和解调错误具有天然的容忍度。

### 3. 实验设计
*   **数据集**：使用自动驾驶领域权威的 **Cityscapes** 街景数据集（分辨率调整为 512×512）。
*   **基准模型（Benchmark）**：
    *   **传统方案**：BPG（信源编码）+ LDPC（信道编码，码率3/4）+ QAM（调制）。
    *   **对比变体**：未剪枝的JSCC模型、不同剪枝率（0.2-0.9）的模型、模拟传输（Analog）方案。
*   **评价指标**：峰值信噪比（PSNR）和结构相似性（SSIM）。
*   **实验场景**：模拟慢瑞利衰落信道（Slow Rayleigh Fading Channel），覆盖不同SNR范围（0dB至25dB）。

### 4. 资源与算力
*   **算力说明**：论文提到实验在服务器端进行训练，剪枝后的轻量化模型部署在车载端推理。
*   **具体参数**：使用了 Adam 优化器，Batch Size 为 32，初始学习率为 $1 \times 10^{-4}$。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 RTX 3090/A100）、GPU 数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 5 种不同的剪枝率（0, 0.2, 0.5, 0.7, 0.9）。
    *   对比了 3 种调制阶数（4QAM, 64QAM, 256QAM）。
    *   在全 SNR 范围内进行了性能曲线对比。
*   **充分性评价**：实验设计较为充分，涵盖了从模型压缩比到通信鲁棒性的多维度验证。通过消融实验对比了“结构化剪枝”与“直接降低带宽压缩比”的效果，证明了剪枝策略的优越性。实验结果基于测试集平均值，具有较好的客观性。

### 6. 论文的主要结论与发现
*   **剪枝高效性**：在移除 50% 的参数（$\gamma=0.5$）后，模型的 PSNR 和 SSIM 几乎没有损失；即使剪枝 70%，性能依然稳定。
*   **抗悬崖效应**：在低 SNR（<14dB）条件下，所提方案的性能远超传统 BPG+LDPC 方案，后者在低 SNR 下图像完全无法重建。
*   **数字兼容性**：采用 256QAM 调制的数字 JSCC 方案性能非常接近理想的模拟传输方案，且在极低 SNR 下通过硬决策抑制了部分模拟噪声，重建图像更清晰。
*   **轻量化优势**：在相同参数量下，结构化剪枝模型比简单减小网络层宽度的模型表现更好。

### 7. 优点（亮点）
*   **实用性强**：通过训练-部署分离策略，解决了语义通信难以进入现有数字通信协议的痛点。
*   **双重优化**：同时解决了计算资源（通过剪枝）和通信资源（通过JSCC）的受限问题。
*   **鲁棒性突出**：在自动驾驶急需的低信噪比环境下，表现出了极强的图像恢复能力。

### 8. 不足与局限
*   **硬件细节缺失**：未提供具体的推理延迟（Latency）数据，对于自动驾驶这一实时性要求极高的场景，仅有 MACs 和参数量是不够的。
*   **任务单一性**：实验主要关注图像重建（Pixel-level），未进一步验证剪枝对下游感知任务（如目标检测、语义分割）的具体精度影响。
*   **信道模型简化**：主要在瑞利衰落信道下测试，未考虑更复杂的城市多径效应或高速移动下的多普勒频移影响。

（完）

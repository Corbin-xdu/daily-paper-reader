---
title: Efficient Logic Gate Networks for Video Copy Detection
title_zh: 用于视频拷贝检测的高效逻辑门网络
authors: Katarzyna Fojcik
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21694v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于高吞吐视频复制检测的高效逻辑门网络
tldr: 针对大规模视频版权检测中深度学习模型计算成本高、描述符大的问题，本文提出了一种基于可微分逻辑门网络（LGN）的检测框架。该方法通过将传统浮点运算替换为紧凑的逻辑运算，结合帧缩减和二进制预处理，实现了可离散化为纯布尔电路的模型。实验证明，该方法在保持高精度的同时，显著降低了存储需求并极大提升了推理速度，为高效视频检索提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21694v1/fig-001.webp\", \"caption\": \"Fig. 1: Example of video frame with applied transformations.\", \"page\": 2, \"index\": 1, \"width\": 681, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.21694v1/fig-002.webp\", \"caption\": \"Table 6: Comparison with other existing models. The metric “sps” stands for samples per second, measuring the inference performance of the models. The results marked with an asterisk (*) indicate the mean results across all tested partitions, not for the entire dataset.\", \"page\": 13, \"index\": 2, \"width\": 673, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2604.21694v1/fig-003.webp\", \"caption\": \"Table 5: Performance metrics for Top32 and L models across different frame sizes and LGN layer widths.\", \"page\": 12, \"index\": 3, \"width\": 556, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2604.21694v1/fig-004.webp\", \"caption\": \"Fig. 2: End-to-end workflow of the proposed framework, showing each stage from raw video preprocessing to model-based similarity computation.\", \"page\": 5, \"index\": 4, \"width\": 725, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.21694v1/fig-005.webp\", \"caption\": \"Fig. 3: Overview of LILogic Net training strategies a), b) used in this study and inference pipeline c): all logic gates and connections are fully binarized.\", \"page\": 5, \"index\": 5, \"width\": 704, \"height\": 297}]"
motivation: 传统的深度学习视频版权检测模型计算开销大且描述符占用空间多，难以满足超大规模实时处理的需求。
method: 提出一种基于可微分逻辑门网络（LGN）的框架，通过学习逻辑操作和互连关系，将模型转化为极速推理的纯布尔电路。
result: 该模型在多个数据集上达到了与现有模型相当或更优的准确率，同时描述符体积缩小了几个数量级，推理速度超过每秒1.1万个样本。
conclusion: 逻辑门网络是实现可扩展、资源高效型视频版权检测的一种极具潜力的替代方案。
---

## 摘要
视频拷贝检测要求在超大规模运行的同时，能够在各种视觉失真下进行鲁棒的相似性评估。尽管深度神经网络取得了强大的性能，但其计算成本和描述符大小限制了在高吞吐量系统中的实际部署。在这项工作中，我们提出了一种基于可微逻辑门网络（LGNs）的视频拷贝检测框架，该框架用紧凑的、基于逻辑的表示取代了传统的浮点特征提取器。我们的方法结合了激进的帧小型化、二进制预处理以及一个可训练的 LGN 嵌入模型，该模型能够同时学习逻辑运算和互连关系。训练完成后，该模型可以被离散化为纯布尔电路，从而实现极快且内存高效的推理。我们在多个数据集折数和难度级别上，系统地评估了不同的相似性策略、二值化方案和 LGN 架构。实验结果表明，与之前的模型相比，基于 LGN 的模型在实现具有竞争力或更优的准确率和排名性能的同时，生成的描述符缩小了几个数量级，且推理速度超过每秒 1.1 万个样本。这些研究结果表明，基于逻辑的模型为可扩展且资源高效的视频拷贝检测提供了一种极具前景的替代方案。

## Abstract
Video copy detection requires robust similarity estimation under diverse visual distortions while operating at very large scale. Although deep neural networks achieve strong performance, their computational cost and descriptor size limit practical deployment in high-throughput systems. In this work, we propose a video copy detection framework based on differentiable Logic Gate Networks (LGNs), which replace conventional floating-point feature extractors with compact, logic-based representations. Our approach combines aggressive frame miniaturization, binary preprocessing, and a trainable LGN embedding model that learns both logical operations and interconnections. After training, the model can be discretized into a purely Boolean circuit, enabling extremely fast and memory-efficient inference. We systematically evaluate different similarity strategies, binarization schemes, and LGN architectures across multiple dataset folds and difficulty levels. Experimental results demonstrate that LGN-based models achieve competitive or superior accuracy and ranking performance compared to prior models, while producing descriptors several orders of magnitude smaller and delivering inference speeds exceeding 11k samples per second. These findings indicate that logic-based models offer a promising alternative for scalable and resource-efficient video copy detection.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Logic Gate Networks for Video Copy Detection》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着在线视频平台的爆发式增长，视频拷贝检测（VCD）面临严峻的可扩展性挑战。现有的深度学习模型（如 CNN、Transformer）虽然准确率高，但计算开销巨大、内存带宽需求高且特征描述符体积大，难以在超大规模高吞吐量系统或资源受限的边缘设备中部署。
*   **核心问题**：如何在保持对各类视觉失真（如压缩、旋转、模糊）鲁棒性的同时，极大地降低视频特征提取的计算成本和存储需求？

### 2. 方法论
该论文提出了一种基于**可微逻辑门网络（LGN）**的视频拷贝检测框架，核心流程如下：
*   **激进的预处理**：
    *   **帧小型化**：将视频帧大幅下采样至 8×8 或 32×32 像素。
    *   **二值化**：对 RGB 通道使用多个固定阈值进行二值化，将图像转换为高维二进制向量。
*   **LILogic Net 架构**：
    *   **可微松弛（Differentiable Relaxation）**：在训练阶段，将离散的逻辑门（AND, OR, XOR 等）松弛为连续的概率分布，使模型能够通过梯度下降学习逻辑操作和神经元间的互连关系（Connectome）。
    *   **互连策略**：对比了“全连接学习（Dense）”和“Top-K 稀疏连接”两种方案，以平衡表达能力与计算成本。
*   **相似性计算**：研究了三种策略，最终确定 **Frame-Pair Max Similarity**（帧对最大池化）效果最好，即计算两段视频帧与帧之间的余弦相似度矩阵，并进行全局最大池化。
*   **离散化部署**：训练完成后，将模型“坍缩”为纯布尔电路。推理时完全不涉及浮点运算，仅通过基础逻辑位运算完成。

### 3. 实验设计
*   **数据集**：使用了一个基于 500 个电影预告片的合成数据集，包含 3000 对近重复视频和 3000 对非相似视频。
*   **失真场景**：涵盖了压缩、高斯模糊、多重噪声、调整大小、像素丢失和旋转等 12 种增强类型。
*   **Benchmark 与对比方法**：
    *   **对比模型**：包括经典的 ViSiL (ResNet/I3D 变体)、3D-CSL (自监督 3D 模型) 以及轻量级的 2ConvSN。
    *   **评估指标**：准确率 (Acc)、召回率 (Recall)、精确率 (Prec)、F1 分数以及微平均精度 (μAP)。

### 4. 资源与算力
*   **硬件环境**：推理性能测试在 **NVIDIA GeForce RTX 2080 Ti** GPU 上完成。
*   **训练细节**：使用二元交叉熵损失函数，学习率为 0.1，Batch Size 为 128，最高训练 300 个 Epoch。
*   **性能表现**：推理速度惊人，超过 **11,000 samples/s**（不含数据加载），比传统深度模型快几个数量级。

### 5. 实验数量与充分性
*   **实验规模**：采用了 12 折交叉验证（Cross-validation），确保每种失真类型都在验证集和测试集中出现，结果取 5 次试验的平均值。
*   **消融实验**：
    *   对比了不同帧分辨率（8x8 vs 32x32）。
    *   对比了不同二值化阈值数量（4 vs 7）。
    *   对比了“有无 LGN 模型”的效果（证明了逻辑学习层相比单纯预处理有显著提升）。
*   **充分性评价**：实验设计较为严谨，通过多折验证降低了特定失真带来的偏差。但由于数据集规模（500个原始视频）相对较小，其在数百万级超大规模真实场景下的泛化能力仍有待进一步验证。

### 6. 主要结论与发现
*   **性能卓越**：LGN 模型在准确率和 F1 分数上达到了与 SOTA 深度学习模型相当甚至更优的水平（F1 > 0.98）。
*   **极致压缩**：描述符大小仅为 **0.25 - 0.5 KB**，远小于 ViSiL 的 2025 KB 或 3D-CSL 的 9000 KB。
*   **分辨率敏感性**：发现 **8×8 的极低分辨率** 效果反而优于 32×32，这表明对于拷贝检测，全局结构信息比易受噪声干扰的细节信息更重要。
*   **逻辑优势**：证明了逻辑门网络可以从时间序列数据（视频）中提取有效的结构化特征。

### 7. 优点
*   **效率巅峰**：通过纯布尔运算实现了极高的推理吞吐量，非常适合高并发的版权监测系统。
*   **硬件友好**：模型可直接映射到 FPGA 或具有自定义逻辑指令的 RISC-V 处理器上，功耗极低。
*   **创新性**：这是首次将可微逻辑门网络应用于视频分析领域，打破了“视频处理必须依赖浮点 CNN”的固有思维。

### 8. 不足与局限
*   **数据集局限性**：实验主要基于合成失真的电影预告片，场景相对单一。真实世界中的视频拷贝（如短视频二次创作、复杂剪辑）可能包含更复杂的时空变换。
*   **时间建模较弱**：目前采用的是单层 LGN 配合帧间池化，对长程复杂动作或语义关联的建模能力可能不如 3D-CNN 或 Transformer。
*   **预处理依赖**：性能高度依赖于前端的二值化阈值选择和帧采样策略，若预处理阶段丢失过多信息，逻辑层难以补救。

（完）

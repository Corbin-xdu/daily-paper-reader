---
title: "SVD-Prune: Training-Free Token Pruning For Efficient Vision-Language Models"
title_zh: SVD-Prune：面向高效视觉语言模型的免训练 Token 剪枝方法
authors: "Yvon Apedo, Martyna Poreba, Michal Szczepanski, Samia Bouchafa"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11530v1"
tags: ["query:cv"]
score: 9.0
evidence: 基于SVD的视觉语言模型免训练Token剪枝
tldr: 视觉语言模型（VLM）因处理大量视觉标记而面临巨大的计算和内存压力，而现有剪枝方法常受限于位置偏差和信息分散。本文提出 SVD-Prune，一种无需训练、即插即用的视觉标记剪枝方法。该方法通过奇异值分解（SVD）计算统计杠杆得分，筛选出对全局方差贡献最大的关键标记。实验表明，在极端剪枝比例下，该方法显著优于现有技术，在仅保留16或32个标记时仍能保持强劲性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11530v1/fig-001.webp\", \"caption\": \"Fig. 3. Overview of SVD-Prune. The method performs outside-encoder vision token pruning by applying a global SVD decomposition to vision encoder outputs, estimating token importance via leverage scores, and selecting a compact subset of informative vision tokens before multimodal decoding.\", \"page\": 3, \"index\": 1, \"width\": 890, \"height\": 557}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-002.webp\", \"caption\": \"Fig. 1. Layer-wise variation of attention weights assigned to different types of tokens (including instruction, vision, and response) in LMMs. “A→B” means the attention weights from A to B.\", \"page\": 2, \"index\": 2, \"width\": 523, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-003.webp\", \"caption\": \"Fig. 2. Illustration of the bias induced by the use of the average attention scores across vision tokens as a pruning metric. In graph at the top, averaging attention over all tokens favors earlier tokens, leading to pruning later tokens more frequently. In graph at the bottom, averaging only over attending tokens reverses the bias, leading to earlier tokens being pruned more often.\", \"page\": 2, \"index\": 3, \"width\": 535, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-004.webp\", \"caption\": \"Table 1. Comparison on GQA and TextVQA under varying vision token budgets using LLaVA-1.5-7B. All methods are evaluated without retraining.\", \"page\": 4, \"index\": 4, \"width\": 550, \"height\": 761}]"
motivation: 现有的视觉标记剪枝方法依赖局部启发式规则，容易产生位置偏差并导致视觉细节在处理过程中丢失。
method: 提出基于奇异值分解的 SVD-Prune，利用统计杠杆得分识别并保留对全局特征矩阵方差贡献最大的核心标记。
result: 在极低标记预算（如16或32个标记）的严苛条件下，SVD-Prune 的性能表现持续优于之前的剪枝方法。
conclusion: SVD-Prune 为提升视觉语言模型的推理效率提供了一种高效、无需训练且具有鲁棒性的即插即用解决方案。
---

## 摘要
视觉语言模型（VLM）通过联合处理视觉和文本信息，彻底改变了多模态学习。然而，由于处理长序列视觉 Token 对计算和内存的高需求，它们面临着重大挑战。许多现有方法依赖于局部启发式方法，例如注意力分数或 Token 范数。然而，这些标准存在位置偏差和信息分散的问题，限制了它们在高剪枝率下保留核心内容的能力，并导致在视觉细节丰富的图像上性能下降。为了解决这些问题，我们提出了 SVD-Prune，一种基于奇异值分解（SVD）的免训练、即插即用的 Token 剪枝方法。它对视觉 Token 特征矩阵进行分解，并利用统计杠杆得分选择前 K 个 Token，从而确保仅保留对主要全局方差贡献最大的 Token。实验表明，在极端的视觉 Token 预算下，SVD-Prune 始终优于先前的剪枝方法，即使在仅剩 32 和 16 个视觉 Token 的情况下也能保持强大的性能。

## Abstract
Vision-Language Models (VLM) have revolutionized multimodal learning by jointly processing visual and textual information. Yet, they face significant challenges due to the high computational and memory demands of processing long sequences of vision tokens. Many existing methods rely on local heuristics, such as attention scores or token norms. However, these criteria suffer from positional bias and information dispersion, limiting their ability to preserve essential content at high pruning ratios and leading to performance degradation on visually detailed images. To address these issues, we propose SVD-Prune, a trainingfree, plug-and-play token pruning method based on Singular Value Decomposition. It decomposes the vision token feature matrix and selects the top-K tokens using statistical leverage scores, ensuring only tokens contributing most to the dominant global variance are preserved. Experiments show that SVD-Prune consistently outperforms prior pruning methods under extreme vision token budgets, maintaining strong performance even with 32 and 16 vision tokens.

---

## 论文详细总结（自动生成）

这是一份关于论文《SVD-Prune: Training-Free Token Pruning For Efficient Vision-Language Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **背景**：视觉语言模型（VLM）在处理图像时会生成大量视觉 Token（如 LLaVA-1.5 默认生成 576 个），这导致了巨大的计算开销和内存占用，尤其是在 LLM 解码阶段。
*   **核心问题**：现有的 Token 剪枝方法多依赖于“注意力分数”或“Token 范数”等局部启发式规则。
*   **痛点**：
    1.  **位置偏差**：由于 LLM 的因果掩码（Causal Masking），靠后的 Token 往往注意力得分较低，导致剪枝决策受位置而非语义驱动。
    2.  **信息分散**：局部指标难以捕捉全局视觉结构，在极端剪枝比例下会导致关键细节丢失。
*   **目标**：开发一种无需训练、能克服位置偏差、且在极低 Token 预算下仍能保持性能的剪枝方案。

### 2. 方法论：SVD-Prune
核心思想是利用**奇异值分解（SVD）**从全局角度识别对图像特征方差贡献最大的 Token。
*   **步骤一：全局模式提取**：将视觉编码器输出的特征矩阵 $F$ 进行 SVD 分解，得到左奇异向量 $U$、奇异值矩阵 $\Sigma$ 和右奇异向量 $V$。
*   **步骤二：主方差截断**：计算累积解释方差比例，选择最小的秩 $k$，使得前 $k$ 个奇异值能保留大部分（如 70%-95%）的方差信息。
*   **步骤三：杠杆得分（Leverage Scores）计算**：计算每个 Token 在前 $k$ 个主方向上的投影平方和。得分越高，代表该 Token 对全局特征的代表性越强。
*   **步骤四：Token 选择与重排**：根据杠杆得分保留前 $m$ 个 Token，并将其恢复到原始的空间顺序，以保持位置编码的连贯性。

### 3. 实验设计
*   **基准模型**：LLaVA-1.5-7B。
*   **数据集**：
    *   **GQA**：评估组合视觉推理能力。
    *   **TextVQA**：评估对图像中文字的识别与理解能力。
*   **对比方法**：
    *   **编码器端剪枝**：ToMe, HIRED, VisionZip, TRIM, DivPrune。
    *   **解码器端剪枝**：FastV, PyramidDrop, SparseVLM。
*   **剪枝梯度**：测试了从 192、128、64 到极端的 32 和 16 个 Token 预算。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA A40 GPU (48GB VRAM)**，配合 Intel Core i7-14700 CPU。
*   **训练时长**：该方法为**免训练（Training-free）**，因此无需训练时长。
*   **推理开销**：文中提供了 FLOPs 分析，显示将 Token 从 576 减至 16 可减少约 **84.8%** 的总计算量。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准上进行了多梯度（5 种不同 Token 数量）的对比实验。
*   **充分性**：实验涵盖了从轻微剪枝到极端压缩（仅保留 2.7% 的 Token）的场景，对比了近年来（2023-2025年）几乎所有主流的剪枝基准方法。
*   **客观性**：所有方法均在不进行微调的公平条件下对比，实验结果具有较强的说服力，尤其是在极低预算下的性能曲线非常清晰。

### 6. 主要结论与发现
*   **极端鲁棒性**：在仅保留 16 或 32 个 Token 时，SVD-Prune 的性能显著优于其他方法。例如在 GQA 上，保留 32 个 Token 时仅比全量 Token 下降约 8.38 点，而其他方法往往出现崩溃。
*   **效率提升**：视觉 Token 是推理成本的主要驱动力。通过 SVD-Prune 减少 Token 数量，可以线性降低 LLM 解码器的计算负担。
*   **多模态推理特性**：研究发现多模态推理实际上是“文本驱动”的，视觉 Token 存在极大的冗余，这为大幅度剪枝提供了理论支撑。

### 7. 优点
*   **即插即用**：无需对模型进行任何重新训练或架构修改。
*   **克服偏差**：通过 SVD 的全局统计特性，有效解决了基于注意力机制剪枝带来的位置偏差问题。
*   **理论严谨**：利用矩阵分解中的杠杆得分进行采样，比简单的范数或相似度度量更具数学解释性。
*   **极端压缩能力**：在极低资源场景（如边缘设备）下表现优异。

### 8. 不足与局限
*   **计算开销偏移**：虽然减少了 LLM 的计算量，但 SVD 分解本身在 CPU/GPU 上会产生额外的计算开销（尽管对于 576x1024 规模的矩阵来说，SVD 相对较快）。
*   **超参数敏感性**：方差保留比例 $\epsilon$ 需要根据不同任务进行微调以达到最优平衡。
*   **任务覆盖面**：实验主要集中在视觉问答（VQA）类任务，尚未在长视频理解或更复杂的视觉定位（Grounding）任务中验证其有效性。

（完）

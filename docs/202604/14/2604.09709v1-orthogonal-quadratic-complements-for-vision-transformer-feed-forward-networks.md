---
title: Orthogonal Quadratic Complements for Vision Transformer Feed-Forward Networks
title_zh: 视觉 Transformer 前馈网络的正交二次补
authors: Wang Zixian
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.09709v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于高效视觉Transformer前馈网络的低秩二次辅助分支
tldr: 针对视觉Transformer（ViT）中双线性前馈网络存在的冗余问题，本文提出正交二次互补（OQC）机制。该方法通过构建低秩二次辅助分支，并将其显式投影到主分支的正交补空间，确保辅助特征仅捕捉主分支未涵盖的信息。研究涵盖了高效的低秩实现及动态门控扩展版本。实验表明，OQC在CIFAR-100和TinyImageNet上显著提升了模型精度，并优化了表示几何结构和类别分离度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09709v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture evolution. Left: The host FFN (e.g., standard MLP or AFBO) produces a dominant main branch. Middle: OQC adds a low-rank quadratic auxiliary feature and explicitly projects it onto the orthogonal complement of the main branch. Right: The gated OQC family maintains the orthogonal complement but dynamically or statically modulates its injection to the host branch.\", \"page\": 4, \"index\": 1, \"width\": 963, \"height\": 446}]"
motivation: 现有的双线性前馈网络往往混淆了二阶交互增强与相对于主分支的冗余增加，导致信息捕获效率不高。
method: 提出将低秩二次辅助分支显式投影到主分支正交补空间的OQC机制，并探索了低秩实现与门控扩展变体。
result: 在CIFAR-100和TinyImageNet上，OQC及其变体显著优于基准模型，其中动态门控版本在TinyImageNet上提升了1.43个百分点。
conclusion: 通过消除辅助分支与主分支的信息重叠，OQC有效增强了ViT的表示能力和类分离度，在多个数据集上展现出一致的泛化性。
---

## 摘要
近期用于视觉 Transformer 的双线性前馈替代方案可以显著提高准确率，但它们往往混淆了两种效应：更强的二阶交互以及相对于主分支增加的冗余。我们研究了一种互补的设计原则，其中辅助二次特征仅贡献主导隐藏表示尚未捕获的信息。为此，我们提出了正交二次补（OQC），它构建了一个低秩二次辅助分支，并在注入前将其显式投影到主分支的正交补空间上。我们进一步研究了一种高效的低秩实现（OQC-LR）和门控扩展（OQC-static 和 OQC-dynamic）。在参数匹配的 Deep-ViT 和 CIFAR-100 实验方案下（采用固定的倒数第二层残差读取），完整的 OQC 将 AFBO 基准从 64.25 +/- 0.22 提升至 65.59 +/- 0.22，而 OQC-LR 达到了 65.52 +/- 0.25，且具有显著更好的速度-准确率权衡。在 TinyImageNet 上，门控扩展 OQC-dynamic 达到了 51.88 +/- 0.32，比基准（50.45 +/- 0.21）提高了 1.43 个百分点，并优于所有非门控变体。机制分析表明，投影后辅助分支与主分支之间的重叠接近于零，同时表示几何结构和类别分离度得到改善。包括非门控和门控变体在内的整个系列在两个数据集上都表现出一致的泛化性。

## Abstract
Recent bilinear feed-forward replacements for vision transformers can substantially improve accuracy, but they often conflate two effects: stronger second-order interactions and increased redundancy relative to the main branch. We study a complementary design principle in which auxiliary quadratic features contribute only information not already captured by the dominant hidden representation. To this end, we propose Orthogonal Quadratic Complements (OQC), which construct a low-rank quadratic auxiliary branch and explicitly project it onto the orthogonal complement of the main branch before injection. We further study an efficient low-rank realization (OQC-LR) and gated extensions (OQC-static and OQC-dynamic).   Under a parameter-matched Deep-ViT and CIFAR-100 protocol with a fixed penultimate residual readout, full OQC improves an AFBO baseline from 64.25 +/- 0.22 to 65.59 +/- 0.22, while OQC-LR reaches 65.52 +/- 0.25 with a substantially better speed-accuracy tradeoff. On TinyImageNet, the gated extension OQC-dynamic achieves 51.88 +/- 0.32, improving the baseline (50.45 +/- 0.21) by 1.43 points and outperforming all ungated variants. Mechanism analyses show near-zero post-projection auxiliary-main overlap together with improved representation geometry and class separation. The full family, including both ungated and gated variants, generalizes consistently across both datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《Orthogonal Quadratic Complements for Vision Transformer Feed-Forward Networks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于视觉 Transformer（ViT）中前馈网络（FFN）的设计优化。虽然近年来引入双线性算子（如 AFBO）来捕捉高阶特征交互能提升精度，但作者发现这些方法存在**表示冗余**的问题：新增的二阶辅助分支往往会坍缩并模仿主分支的表示，导致计算资源浪费且信息增量有限。
**核心问题：** 如何确保辅助的二次项分支只提供主分支尚未捕获的、非冗余的互补信息？

### 2. 方法论：正交二次补（OQC）
核心思想是从希尔伯特空间的角度出发，将主分支视为一个主导子空间，通过显式的**正交投影**，强制辅助分支仅在主分支的正交补空间内运行。

*   **核心流程：**
    1.  **构建原始二次特征：** 通过两个低秩投影 $u(x)$ 和 $v(x)$ 的哈达玛积（Hadamard product）生成原始二次特征 $q(x)$。
    2.  **主分支映射：** 将主分支的隐藏表示 $b(x)$ 投影到相同的低秩辅助空间，得到 $m(x)$。
    3.  **正交化投影：** 利用公式 $q_{\perp}(x) = q(x) - \text{proj}_{m(x)}q(x)$，减去 $q(x)$ 在 $m(x)$ 方向上的分量，确保结果与主分支正交。
    4.  **注入与混合：** 将处理后的正交补特征 $q_{\perp}(x)$ 重新映射回隐藏维度，并与主分支相加。
*   **变体设计：**
    *   **OQC-LR：** 在低秩空间完成正交化后再提升维度，以优化计算效率。
    *   **OQC-static/dynamic：** 引入静态标量门控或基于输入的动态空间门控，对正交补特征进行自适应调制。

### 3. 实验设计
*   **数据集：** CIFAR-100 和 TinyImageNet。
*   **基准模型（Backbone）：** Deep-ViT（深度为 8，宽度为 256）。
*   **对比方法（Benchmark）：** 
    *   **AFBO + PR：** 异步分解双线性算子（AFBO）配合倒数第二层残差读取（PR）。
    *   **标准 MLP：** 用于验证方法的通用性。
*   **评估指标：** Top-1 准确率、参数量（Params）、推理速度（Images/s）、有效秩（Effective Rank）、类别分离度（Separation Score）。

### 4. 资源与算力
论文**未明确说明**具体的硬件型号（如 GPU 类型）、显存占用或总训练时长。文中仅提供了推理吞吐量（Images/s）作为效率指标，并提到使用了 AdamW 优化器和余弦衰减学习率策略。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了两个主流中小型数据集的完整训练，并进行了 3 个随机种子的重复实验以确保统计显著性。
*   **消融实验：** 进行了辅助秩（Rank）的灵敏度分析、不同门控设计的对比、以及“有无正交化”的对比实验。
*   **公平性：** 实验采用了“参数匹配”协议，并固定了 PR 读取机制，确保性能提升确实源于 FFN 结构的改进而非其他超参数或读取技巧。
*   **充分性评价：** 实验设计较为严谨，通过机制分析（如余弦重叠度测量）直接证明了正交化的物理意义，客观性较强。

### 6. 主要结论与发现
*   **性能提升：** 在 CIFAR-100 上，OQC 将 AFBO 基准提升了 1.34%；在 TinyImageNet 上，OQC-dynamic 提升了 1.43%。
*   **冗余消除：** 投影后辅助分支与主分支的余弦重叠度从 ~0.14 降至 $10^{-8}$ 级别，证明了正交化的有效性。
*   **几何与分离度：** 全量 OQC 更有利于保持表示的几何丰富度（有效秩更高），而动态门控版本则能显著提升类别分离度。
*   **通用性：** OQC 不仅能增强复杂的 AFBO，也能让简单的 MLP 获得显著增益（+3.48%）。

### 7. 优点（亮点）
*   **理论严谨：** 引入希尔伯特空间正交补的概念，为神经网络分支设计提供了清晰的数学解释。
*   **效率平衡：** 提出的 OQC-LR 变体在几乎不损失精度的情况下，大幅提升了推理速度，具有实际应用价值。
*   **机制透明：** 通过可视化和统计量（如重叠度、有效秩）深入剖析了模型为何有效，而非单纯的“黑盒”调参。

### 8. 不足与局限
*   **数据集规模：** 实验主要集中在 CIFAR-100 和 TinyImageNet 等较小规模的数据集上，缺乏在 ImageNet-1K 等大规模数据集上的验证。
*   **边际递减：** 实验表明，如果主分支本身已经具备极强的二次交互建模能力，OQC 带来的增益会相对减小。
*   **动态门控波动：** OQC-dynamic 虽然在 TinyImageNet 上表现最好，但其标准差（±0.32）高于其他变体，显示出一定的训练不稳定性。

（完）

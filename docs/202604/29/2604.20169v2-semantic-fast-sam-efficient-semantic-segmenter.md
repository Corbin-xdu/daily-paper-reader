---
title: "Semantic-Fast-SAM: Efficient Semantic Segmenter"
title_zh: Semantic-Fast-SAM：高效语义分割器
authors: Byunghyun Kim
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20169v2"
tags: ["query:cv"]
score: 9.0
evidence: 基于CNN的高效重新实现，旨在实现实时性能并减少内存占用
tldr: 本研究提出Semantic-Fast-SAM (SFS)，旨在解决SAM模型推理速度慢的问题。SFS结合了基于CNN的高效FastSAM模型与语义标注流水线，通过集成语义分割一切（SSA）策略为掩码分配类别。该模型在保持高精度的同时，显著降低了计算成本和内存占用，在Cityscapes和ADE20K基准上实现了与原方法相当的精度，且推理速度提升约20倍，支持实时及开放词汇分割任务。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20169v2/fig-001.webp\", \"caption\": \"TABLE III SEMANTIC SEGMENTATION PERFORMANCE (MIOU). BEST WITHIN EACH SUPERVISION REGIME IS BOLDED; HIGHER IS BETTER.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-002.webp\", \"caption\": \"TABLE IV ABLATION RESULTS ON OPEN-VOCABULARY FUSION AND MASK COUNT. BEST WITHIN EACH ABLATION BLOCK IS BOLDED.\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-003.webp\", \"caption\": \"TABLE II GPU MEMORY USAGE FOR DIFFERENT METHODS. BEST WITHIN EACH COMPARABLE BLOCK IS BOLDED; LOWER IS BETTER.\", \"page\": 3, \"index\": 3, \"width\": 529, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-004.webp\", \"caption\": \"Fig. 2. Overall architecture of Semantic-Fast-SAM.\", \"page\": 2, \"index\": 4, \"width\": 529, \"height\": 254}]"
motivation: 针对原始SAM模型因基于Transformer架构导致推理速度慢、难以满足实时语义分割需求的问题。
method: 将高效的CNN架构FastSAM与语义标注策略相结合，并利用CLIP头部实现开放词汇的类别分配。
result: 在Cityscapes和ADE20K上达到领先精度，且在闭集设置下推理速度比SSA快约20倍。
conclusion: SFS实现了兼具高效性与通用性的实时语义分割，为机器人等对性能敏感的场景提供了实用的基础分割模型。
---

## 摘要
我们提出了 Semantic-Fast-SAM (SFS)，这是一个将 Fast Segment Anything 模型与语义标注流水线相结合的语义分割框架，旨在不牺牲准确性的情况下实现实时性能。FastSAM 是 Segment Anything Model (SAM) 的一种基于 CNN 的高效重新实现，其运行速度远快于原始的基于 Transformer 的 SAM。在 FastSAM 快速掩码生成的基础上，我们集成了 Semantic-Segment-Anything (SSA) 标注策略，为每个掩码分配具有实际意义的类别。由此产生的 SFS 模型能够生成高质量的语义分割图，而其计算成本和内存占用仅为原始基于 SAM 方法的一小部分。在 Cityscapes 和 ADE20K 基准测试上的实验表明，SFS 的准确率与之前的基于 SAM 的方法相当（Cityscapes 上的 mIoU 约为 70.33，ADE20K 上为 48.01），同时在闭集设置下的推理速度比 SSA 快约 20 倍。我们还展示了 SFS 通过利用基于 CLIP 的语义头，能够有效处理开放词汇分割，在广泛类别标注方面优于近期的开放词汇模型。这项工作实现了具有“分割一切”能力的实用实时语义分割，扩展了基础分割模型在机器人场景中的适用性。代码实现可在 https://github.com/KBH00/Semantic-Fast-SAM 获取。

## Abstract
We propose Semantic-Fast-SAM (SFS), a semantic segmentation framework that combines the Fast Segment Anything model with a semantic labeling pipeline to achieve real-time performance without sacrificing accuracy. FastSAM is an efficient CNN-based re-implementation of the Segment Anything Model (SAM) that runs much faster than the original transformer-based SAM. Building upon FastSAM's rapid mask generation, we integrate a Semantic-Segment-Anything (SSA) labeling strategy to assign meaningful categories to each mask. The resulting SFS model produces high-quality semantic segmentation maps at a fraction of the computational cost and memory footprint of the original SAM-based approach. Experiments on Cityscapes and ADE20K benchmarks demonstrate that SFS matches the accuracy of prior SAM-based methods (mIoU ~ 70.33 on Cityscapes and 48.01 on ADE20K) while achieving approximately 20x faster inference than SSA in the closed-set setting. We also show that SFS effectively handles open-vocabulary segmentation by leveraging CLIP-based semantic heads, outperforming recent open-vocabulary models on broad class labeling. This work enables practical real-time semantic segmentation with the "segment-anything" capability, broadening the applicability of foundation segmentation models in robotics scenarios. The implementation is available at https://github.com/KBH00/Semantic-Fast-SAM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic-Fast-SAM: Efficient Semantic Segmenter》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文旨在解决基础分割模型 **SAM (Segment Anything Model)** 在实际应用中的两大痛点：
*   **计算开销巨大**：原始 SAM 基于重型 Transformer 架构（如 ViT-H），推理速度慢（单张图通常超过 1 秒），难以满足自动驾驶、机器人等实时场景需求。
*   **缺乏语义信息**：SAM 仅能生成“类无关”的掩码（Mask），无法直接告诉用户物体是什么类别。
虽然已有研究（如 SSA）尝试为 SAM 添加语义头，但由于底层仍依赖 SAM，整体效率依然低下。因此，作者提出了 **Semantic-Fast-SAM (SFS)**，旨在打造一个既能“分割一切”又能“实时标注”的高效系统。

### 2. 方法论：核心思想与技术细节
SFS 采用两阶段架构，将高效的掩码生成与多分支语义标注相结合：
*   **阶段 1：快速掩码生成（Fast Mask Generation）**
    *   使用 **FastSAM** 替代原始 SAM。FastSAM 基于 YOLOv8-seg 架构，利用 CNN 的高效性在单次前向传播中生成全图掩码，速度比 SAM 快约 50 倍。
*   **阶段 2：语义标注模块（Semantic Labeling Module）**
    *   **闭集分支（Closed-Set Branch）**：利用在 COCO 或 ADE20K 上预训练的高性能分割器（如 OneFormer），通过对 FastSAM 生成的掩码区域进行“投票”来确定已知类别。
    *   **开放词汇分支（Open-Vocabulary Branch）**：针对未知物体，利用 **BLIP** 生成图像描述并提取名词短语，再通过 **CLIP** 计算图像特征与文本候选标签的余弦相似度，实现零样本（Zero-shot）分类。
    *   **融合决策**：综合两个分支的结果。如果闭集分支置信度高则采用闭集标签；否则采用 CLIP 推荐的开放词汇标签。

### 3. 实验设计
*   **数据集**：主要在 **Cityscapes**（城市街道场景）和 **ADE20K**（室内外通用场景）上进行验证。
*   **Benchmark（基准）**：
    *   **精度对比**：与 SSA (Semantic-SAM)、OneFormer、Mask2Former、SegFormer 等模型对比 mIoU。
    *   **性能对比**：对比推理延迟（Latency）和显存占用（GPU Memory）。
    *   **开放词汇对比**：与 CLIPSeg、GroupViT、MaskCLIP 等专门的开放词汇模型对比。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3090 GPU (24 GB)** 的工作站上完成。
*   **训练时长**：论文明确指出 SFS 是一个**推理时（Inference-time）流水线**，直接组合了预训练组件，**不需要额外的反向传播或模型微调**。因此，不存在训练时长的概念，这体现了该方法极高的部署效率。

### 5. 实验数量与充分性
作者进行了多维度的实验：
*   **精度与速度实验**：在两个主流数据集上验证了闭集和开集模式。
*   **消融实验**：分析了“仅闭集”与“全模型”的性能差异，以及掩码数量（Top-100, 50, 25）对精度和速度的权衡影响。
*   **充分性评价**：实验设计较为客观，直接对比了 SFS 与其原型 SSA 的性能增益。虽然实验主要集中在两个数据集上，但涵盖了从实时性到开放词汇能力的全面评估，足以支撑其核心论点。

### 6. 主要结论与发现
*   **速度飞跃**：在闭集设置下，SFS 的推理速度（0.08s）比基于 SAM 的 SSA（1.65s）快了约 **20 倍**。
*   **精度持平**：SFS 在 Cityscapes (70.33 mIoU) 和 ADE20K (48.01 mIoU) 上的表现与 SSA 极其接近，证明更换轻量化骨干网络对精度的损失微乎其微。
*   **显存友好**：峰值显存从 SSA 的 19GB 降至 **4.5GB**，使得该模型可以在 8-12GB 显存的消费级显卡或嵌入式设备上运行。

### 7. 优点
*   **极高的实用性**：通过组合现有最优模型（FastSAM + OneFormer + CLIP），无需重新训练即可实现高性能系统。
*   **灵活的权衡**：用户可以根据算力预算调整掩码数量或关闭开放词汇分支，以获得更高的帧率。
*   **零样本能力**：继承了 CLIP 的优势，能够识别训练集之外的物体。

### 8. 不足与局限
*   **开放词汇模式的瓶颈**：虽然闭集模式极快，但在开启 BLIP/CLIP 的开放词汇模式下，推理时间仍需约 10 秒，虽然比 SSA 快，但仍未达到真正的实时。
*   **小物体精度**：FastSAM 在处理极小物体时的掩码精度偶尔略逊于原始 SAM。
*   **依赖性风险**：系统的最终效果高度依赖于预训练语义头（如 OneFormer）的质量，若语义头预测错误，掩码投票机制也无法纠正。

（完）

---
title: Device-Conditioned Neural Architecture Search for Efficient Robotic Manipulation
title_zh: 面向高效机器人操控的设备感知神经架构搜索
authors: "Yiming Wu, Huan Wang, Zhenghao Chen, Ge Yuan, Dong Xu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10170v1"
tags: ["query:cv"]
score: 9.0
evidence: 针对高效机器人操作的硬件约束架构搜索
tldr: 针对机器人视觉运动策略在异构硬件上部署效率低、适配耗时的问题，本文提出DC-QFA框架。通过构建包含多种架构和混合精度的超网，并结合硬件感知正则化与多步在线蒸馏，实现了无需重复训练的快速跨平台部署，在保持任务成功率的同时显著提升了推理速度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10170v1/fig-001.webp\", \"caption\": \"Fig. 2: Experimental setups in simulation and the real world. (a) Push-T. (b) LIBERO. (c) CALVIN. (d) Real robot tasks, including object handling (egg) and tool use (brush).\", \"page\": 7, \"index\": 1, \"width\": 684, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2604.10170v1/fig-002.webp\", \"caption\": \"Table 3: Results of MDT-V [25] on the CALVIN [21] and LIBERO [19] datasets, including performance metrics across various quantization methods and bit-widths. The table presents the model size, accuracy, and average performance (average length on CALVIN and success rate on LIBERO) for each method, highlighting the effectiveness of our DC-QFA when compared to other quantization methods.\", \"page\": 10, \"index\": 2, \"width\": 683, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.10170v1/fig-003.webp\", \"caption\": \"Table 1: Performance of OpenVLA-OFT [16] on the LIBERO [19] benchmark. We compare FP16 and quantized variants at FP8/W8A8 and W4A4 using SmoothQuant [32], AWQ [18], SQIL [24], and BatchQuant [3]. Metrics are success rates (%) on four subsets; Avg is the mean (higher is better). DC-QFA (ours) is highlighted and matches or surpasses SQIL at the same bit-width.\", \"page\": 9, \"index\": 3, \"width\": 534, \"height\": 316}]"
motivation: 现有的机器人策略优化方法通常针对特定设备且缺乏通用性，导致在不同硬件间迁移时面临巨大的计算和时间成本。
method: 提出一种设备感知的全量化超网，利用硬件查找表指导架构搜索，并引入多步在线蒸馏以减少低比特量化下的闭环执行误差。
result: 在多种主流策略模型上实现了2-3倍的推理加速，且在边缘设备和云端平台上均保持了极高的任务成功率。
conclusion: DC-QFA为复杂机器人策略在多样化硬件上的高效部署提供了一种通用且低成本的解决方案，并验证了低比特量化在接触密集型任务中的稳定性。
---

## 摘要
视觉运动策略日益增长的复杂性，给在异构机器人硬件约束下的部署带来了重大挑战。然而，大多数现有的机器人操控模型高效方法都是针对特定设备和模型的，缺乏泛化性，且在适配过程中需要耗时的逐设备优化。在这项工作中，我们提出了一个名为“设备感知全量化”（Device-Conditioned Quantization-For-All, DC-QFA）的统一框架，通过设备感知的量化感知训练和硬件约束的架构搜索来分摊部署成本。具体而言，我们引入了一个单一的超网络（supernet），其设计空间涵盖了丰富的网络架构和混合精度位宽。该网络在逐设备查找表的指导下，利用延迟和内存感知的正则化进行优化。借助该超网络，对于每个目标平台，我们可以执行一次性的轻量级搜索来选择最优子网络，而无需任何逐设备重新优化，从而实现了在异构硬件上更具泛化性的部署，并显著缩短了部署时间。为了提高低精度下的长时程稳定性，我们进一步引入了多步同策略蒸馏，以减轻闭环执行过程中的误差累积。在 DiffusionPolicy-T、MDT-V 和 OpenVLA-OFT 等三个代表性策略骨干网络上的广泛实验表明，我们的 DC-QFA 在边缘设备、消费级 GPU 和云平台上实现了 2-3 倍的加速，且任务成功率的性能下降可以忽略不计。在配备力/扭矩传感器的 Inovo 机器人上进行的真实世界评估进一步证实，即使在严重的量化情况下，我们的低比特 DC-QFA 策略仍能保持稳定且具有丰富接触的操控。

## Abstract
The growing complexity of visuomotor policies poses significant challenges for deployment with heterogeneous robotic hardware constraints. However, most existing model-efficient approaches for robotic manipulation are device- and model-specific, lack generalizability, and require time-consuming per-device optimization during the adaptation process. In this work, we propose a unified framework named \textbf{D}evice-\textbf{C}onditioned \textbf{Q}uantization-\textbf{F}or-\textbf{A}ll (DC-QFA) which amortizes deployment effort with the device-conditioned quantization-aware training and hardware-constrained architecture search. Specifically, we introduce a single supernet that spans a rich design space over network architectures and mixed-precision bit-widths. It is optimized with latency- and memory-aware regularization, guided by per-device lookup tables. With this supernet, for each target platform, we can perform a once-for-all lightweight search to select an optimal subnet without any per-device re-optimization, which enables more generalizable deployment across heterogeneous hardware, and substantially reduces deployment time. To improve long-horizon stability under low precision, we further introduce multi-step on-policy distillation to mitigate error accumulation during closed-loop execution. Extensive experiments on three representative policy backbones, such as DiffusionPolicy-T, MDT-V, and OpenVLA-OFT, demonstrate that our DC-QFA achieves $2\text{-}3\times$ acceleration on edge devices, consumer-grade GPUs, and cloud platforms, with negligible performance drop in task success. Real-world evaluations on an Inovo robot equipped with a force/torque sensor further validates that our low-bit DC-QFA policies maintain stable, contact-rich manipulation even under severe quantization.

---

## 论文详细总结（自动生成）

这是一份关于论文《Device-Conditioned Neural Architecture Search for Efficient Robotic Manipulation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 Transformer 架构在视觉运动策略（Visuomotor Policies）中的广泛应用，机器人操控能力显著提升，但其巨大的计算量和内存需求使得在资源受限的边缘设备（如嵌入式 GPU/NPU）上部署变得异常困难。
*   **现有挑战**：目前的模型压缩方法（如剪枝、量化）通常是“设备特定”的，即针对每种硬件都需要重新训练或微调，耗时耗力且缺乏通用性。
*   **核心目标**：提出一种“一次训练，到处部署”的框架，使单一模型能够快速适配具有不同延迟和内存约束的异构机器人硬件，同时解决低比特量化在长时程任务中容易产生的误差累积问题。

### 2. 方法论：核心思想与关键技术
论文提出了 **DC-QFA（Device-Conditioned Quantization-For-All）** 框架，主要包含三个阶段：

*   **设备感知的超网训练（Stage I）**：
    *   **设计空间**：构建一个包含网络深度（Block Selection）、宽度（MLP/Attention Head Ratio）以及量化位宽（INT4/8, FP4/8/16）的超网。
    *   **硬件感知正则化**：在训练损失函数中引入基于硬件查找表（LUT）的延迟和内存约束项。通过拉格朗日松弛法，使超网在训练过程中学习在不同资源预算下的最优表现。
*   **多步在线蒸馏（Multi-step On-policy Distillation, OPD）**：
    *   针对量化导致的闭环执行不稳定，引入 OPD。让量化后的“学生”策略进行多步 Rollout 产生轨迹，由全精度“教师”策略在每个状态点提供监督。
    *   **公式逻辑**：通过最小化学生与教师在多步轨迹上的动作分布差异（如 MSE），并随训练进程逐渐扩大 Rollout 步数，从而抑制误差累积。
*   **硬件感知子网搜索（Stage II & III）**：
    *   使用 **NSGA-II 多目标搜索算法**，在无需重新训练的情况下，从超网中快速筛选出满足目标设备（如 Jetson Orin）约束的最优子网配置。

### 3. 实验设计
*   **仿真基准（Benchmarks）**：
    *   **Push-T**：平面推箱子任务，评估任务成功率。
    *   **CALVIN**：长时程语言条件操控任务，评估平均完成步骤数。
    *   **LIBERO**：包含 130 个任务的多任务基准，评估泛化能力。
*   **策略骨干（Backbones）**：对比了 DiffusionPolicy-T、MDT-V 和 OpenVLA-OFT 三种主流架构。
*   **对比方法**：
    *   **PTQ（后量化）**：SmoothQuant, AWQ。
    *   **QAT（量化感知训练）**：SQIL。
    *   **QFA（全量化搜索）**：BatchQuant。
*   **真实世界验证**：在 Inovo 6-DOF 机器人臂上，针对扫豆子、抓鸡蛋、插辣椒等接触密集型任务进行 20 次重复实验。

### 4. 资源与算力
论文明确提到了部分训练时长：
*   **DiffusionPolicy-T**：在超网训练阶段约需 **85 GPU 小时**。
*   **MDT-V**：约需 **156 GPU 小时**。
*   **部署端硬件**：主要使用 **NVIDIA Jetson Orin NX** 边缘计算模块，利用 TensorRT 进行推理加速。
*   *注：未明确说明训练所使用的具体 GPU 型号（如 A100 或 H100）及数量。*

### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了从轻量级扩散策略到大规模视觉语言动作模型（VLA），跨越了仿真与现实环境。
*   **充分性**：
    *   **多维度对比**：不仅对比了成功率，还给出了 Pareto 前沿曲线（性能 vs 参数量）和混合精度分析。
    *   **消融实验**：专门验证了 OPD（在线蒸馏）对长时程稳定性的贡献，证明了其在 W4A4 等极低比特下的必要性。
*   **客观性**：通过在不同硬件（边缘端 vs 云端）上实测延迟和内存，验证了硬件感知搜索的真实有效性，而非仅停留在理论 FLOPs 计算。

### 6. 主要结论与发现
*   **显著加速**：在 Jetson Orin NX 上实现了 **2-3 倍的推理加速**，并将内存占用减少了 **2-4 倍**。
*   **性能保持**：在 W8A8 甚至 W4A4 量化下，DC-QFA 的任务成功率与全精度（FP16）模型相比下降极小（通常在 1-2% 以内），显著优于传统的 PTQ 方法。
*   **长时程稳定性**：多步在线蒸馏（OPD）能有效修复量化带来的轨迹漂移，使模型在 CALVIN 等复杂任务中保持连贯动作。

### 7. 优点（亮点）
*   **部署效率高**：解决了机器人领域“一种硬件一套模型”的痛点，通过超网搜索实现了分钟级的跨平台适配。
*   **软硬结合**：将真实的硬件延迟/内存数据（LUT）直接闭环到训练目标中，搜索出的子网在目标硬件上具有真实的帕累托最优性。
*   **鲁棒性强**：在真实世界接触密集型任务（如抓取易碎的鸡蛋）中表现出极高的稳定性，证明了低比特量化在机器人领域的实用性。

### 8. 不足与局限
*   **训练成本**：虽然部署阶段很快，但超网的训练（Stage I）需要较长的 GPU 时间和大量的专家演示数据。
*   **编译器依赖**：性能高度依赖于底层的推理引擎（如 TensorRT），对于不支持特定混合精度的硬件，加速效果可能受限。
*   **任务复杂度限制**：虽然在 LIBERO 等基准上表现良好，但对于需要极高精细力控或极长推理链条的超复杂任务，极低比特（如 4-bit）是否依然稳健仍需进一步验证。

（完）

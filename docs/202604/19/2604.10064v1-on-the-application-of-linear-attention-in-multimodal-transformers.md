---
title: On The Application of Linear Attention in Multimodal Transformers
title_zh: 论线性注意力在多模态 Transformer 中的应用
authors: "Armin Gerami, Seyedehanita Madani, Ramani Duraiswami"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10064v1"
tags: ["query:cv"]
score: 9.0
evidence: 线性注意力作为ViT架构的高效替代方案，旨在降低复杂度
tldr: 本研究针对多模态Transformer中Softmax注意力机制的平方级复杂度瓶颈，探索了线性注意力（LA）作为高效替代方案的可行性。通过在LAION-400M数据集上对ViT-S/B/L等多种规模架构进行实验，证明了线性注意力在显著降低计算开销的同时，能保持与标准注意力相当的性能，并遵循相同的缩放法则，为大规模多模态模型提供了高效的扩展路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10064v1/fig-001.webp\", \"caption\": \"Figure 4. Training loss scaling with model size for ViTs on LAION-400M. Our adjusted LA follows a trend similar to standard attention.\", \"page\": 4, \"index\": 1, \"width\": 485, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.10064v1/fig-002.webp\", \"caption\": \"Figure 3. Training metrics for ViT-S/16, ViT-B/16, and ViT-L/16 models trained on LAION-400M using four NVIDIA A5500 GPUs. Linear attention with our adjustments achieves performance comparable to standard attention.\", \"page\": 4, \"index\": 2, \"width\": 1021, \"height\": 683}]"
motivation: 多模态Transformer的平方级计算复杂度限制了其在处理大规模数据和长序列时的扩展能力。
method: 在ViT系列架构中集成线性注意力机制，并在LAION-400M数据集上进行大规模训练与零样本性能评估。
result: 线性注意力成功将计算复杂度降至线性，且在ImageNet-21K零样本准确率上表现出与标准Softmax注意力一致的缩放规律。
conclusion: 线性注意力是多模态Transformer中一种稳健且高效的可扩展解决方案，适用于处理日益增长的复杂多模态数据集。
---

## 摘要
多模态 Transformer 是最先进视觉语言模型的核心架构，但其注意力的二次复杂度仍是制约可扩展性的关键障碍。在本研究中，我们探讨了线性注意力（Linear Attention, LA）作为多模态框架内高效替代方案的可行性。通过集成线性注意力，我们将相对于序列长度的计算开销从二次降低到线性，同时保持了具有竞争力的性能。我们在 LAION-400M 数据集上训练的 ViT-S/16、ViT-B/16 和 ViT-L/16 架构上评估了我们的方法，验证重点在于 ImageNet-21K 的零样本准确率。我们的系统性评估表明，线性注意力不仅显著节省了计算资源，而且遵循与标准 Softmax 注意力相同的缩放法则（scaling laws）。这些发现使线性注意力成为处理日益庞大且复杂数据集的下一代多模态 Transformer 的一种稳健且可扩展的解决方案。

## Abstract
Multimodal Transformers serve as the backbone for state-of-the-art vision-language models, yet their quadratic attention complexity remains a critical barrier to scalability. In this work, we investigate the viability of Linear Attention (LA) as a high-efficiency alternative within multimodal frameworks. By integrating LA, we reduce the computational overhead from quadratic to linear relative to sequence length while preserving competitive performance. We evaluate our approach across ViT-S/16, ViT-B/16, and ViT-L/16 architectures trained on the LAION-400M dataset, with validation focused on ImageNet-21K zero-shot accuracy. Our systematic evaluation demonstrates that Linear Attention not only yields significant computational savings but also adheres to the same scaling laws as standard softmax attention. These findings position Linear Attention as a robust, scalable solution for next-generation multimodal Transformers tasked with processing increasingly large and complex datasets.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了线性注意力（Linear Attention, LA）在多模态 Transformer 架构中的应用，旨在解决标准注意力机制在处理长序列时的计算瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：多模态 Transformer（如 CLIP）在视觉语言任务中表现卓越，但其核心的 Softmax 注意力机制具有**二次方计算复杂度** $O(N^2)$。随着图像分辨率提高或文本序列增长，计算开销和内存占用呈爆炸式增长，限制了模型的可扩展性。
*   **核心目标**：验证线性注意力（复杂度为 $O(N)$）是否能在多模态框架中替代标准注意力，并在保持性能的同时显著提升效率。

### 2. 方法论
*   **核心思想**：利用矩阵乘法的结合律，将注意力计算顺序从 $(QK^T)V$ 改为 $Q(K^TV)$。
*   **关键技术细节**：
    *   **仿射核函数**：采用 $f(x) = 1 + x$ 作为核函数，避免了 Softmax 的指数运算。
    *   **归一化改进**：作者发现标准线性注意力在序列变长时会出现“过度平滑（Over-smoothing）”问题，导致模型难以区分相关特征。
    *   **公式优化**：作者提出**移除分母归一化项**，并对 $Q$ 和 $K$ 进行预归一化（L2 Norm）。改进后的公式确保了注意力分数在 $[0, 1]$ 之间，增强了模型的表达能力和梯度流动。

### 3. 实验设计
*   **数据集**：使用大规模多模态数据集 **LAION-400M** 进行预训练。
*   **Benchmark**：以 **ImageNet-21K** 的零样本（Zero-shot）分类准确率为主要评估指标。
*   **对比方法**：
    *   标准 Softmax 注意力（Standard Attention）。
    *   常规线性注意力（Regular Linear Attention）。
    *   作者改进后的线性注意力（Proposed Linear Attention）。
*   **模型架构**：涵盖了 ViT-S/16、ViT-B/16 和 ViT-L/16 三种不同规模的 Vision Transformer。

### 4. 资源与算力
*   **训练设备**：使用 **4 块 NVIDIA A5500 GPU** 进行模型训练。
*   **推理/效率测试**：在 **NVIDIA H200 GPU** 上测量前向和后向传播的时间。
*   **训练时长**：根据实验曲线，ViT-L 模型的训练墙钟时间接近 400 小时，ViT-B 约 50 小时。

### 5. 实验数量与充分性
*   **实验规模**：针对三种不同参数量（22M, 86M, 304M）的模型进行了完整的预训练对比。
*   **充分性评价**：实验设计较为充分。作者不仅对比了最终精度，还分析了**运行时间随序列长度的变化趋势**，并拟合了**缩放法则（Scaling Laws）**曲线。通过不同规模模型的对比，证明了该方法在不同参数量级下的稳健性。

### 6. 主要结论与发现
*   **性能等效性**：改进后的线性注意力在最终收敛精度上与标准注意力基本持平。
*   **效率优势**：在序列长度达到 $10^3$ 级别时，线性注意力的速度优势开始显现；在极长序列（$4 \times 10^6$）下，耗时比标准注意力低约 3 个数量级。
*   **缩放法则**：线性注意力遵循与标准注意力相似的幂律缩放规律（$L(N) \approx aN^{-\alpha}$），这意味着它具有良好的大模型扩展潜力。
*   **收敛特性**：虽然线性注意力最终能达到相似精度，但其**收敛速度较慢**，需要更多的训练轮数（Epochs）来弥补核函数表达能力的细微差距。

### 7. 优点
*   **显著降本增效**：成功将多模态模型的复杂度降至线性，为处理超高分辨率图像和长文本提供了可能。
*   **理论与实践结合**：不仅提出了公式改进，还通过大规模数据集验证了其在工业级场景下的可行性。
*   **发现关键瓶颈**：指出了标准线性注意力在多模态任务中的平滑问题，并给出了简洁有效的解决方案。

### 8. 不足与局限
*   **训练效率权衡**：虽然单步迭代快，但达到相同精度所需的总 Epoch 数更多，这在一定程度上抵消了部分算力节省。
*   **任务覆盖面**：实验主要集中在 CLIP 风格的零样本分类上，未广泛测试在视觉问答（VQA）、图像描述（Captioning）等生成式多模态任务上的表现。
*   **超参数敏感性**：移除分母后的稳定性可能依赖于特定的预归一化策略，在其他架构上的通用性有待进一步验证。

（完）

---
title: "Bridging the Training-Deployment Gap: Gated Encoding and Multi-Scale Refinement for Efficient Quantization-Aware Image Enhancement"
authors: "Dat To-Thanh, Nghia Nguyen-Trong, Hoang Vo, Hieu Bui-Minh, Tinh-Anh Nguyen-Nhu"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21743v1"
tags: ["query:cv"]
score: 9.5
evidence: 用于移动设备高效图像增强的量化感知训练
tldr: 本研究针对移动端图像增强模型在部署时因精度降低导致性能下降的问题，提出了一种高效的量化感知图像增强模型。该模型采用带有门控编码块和多尺度细化的分层网络架构，并结合量化感知训练（QAT）来模拟低精度表示，从而在保持低计算开销的同时，实现了高质量的图像增强效果，有效弥合了训练与部署之间的差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21743v1/fig-001.webp\", \"caption\": \"Figure 2. Quantization-Aware Training (QAT) and inference pipelines. (Top) QAT simulates low-precision effects via FakeQuant nodes during the forward pass while maintaining FP32 compute. To bypass non-differentiable quantization steps, the backward pass uses the Straight-Through Estimator (STE) to route gradients and update FP32 weights, minimizing the loss (Ltotal) against the ground truth (GT). (Bottom) Post-training, the model is converted to a purely INT8 representation for efficient integer-only inference.\", \"page\": 6, \"index\": 1, \"width\": 932, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.21743v1/fig-002.webp\", \"caption\": \"Figure 1. The proposed hybrid architecture for RGB image enhancement. The model features a three-scale hierarchical structure comprising Gated Encoder Blocks with multichannel skip connections, Multi-Scale Refinement modules applied at scales S/2, S/4, and S/8, and a Multi-branch Feature Fusion strategy in the decoder to aggregate multi-level semantic features for high-fidelity restoration.\", \"page\": 4, \"index\": 2, \"width\": 932, \"height\": 547}]"
motivation: 移动端图像增强模型在转换为低精度格式进行实际部署时，往往会出现性能大幅下降的训练与部署失配问题。
method: 提出一种包含门控编码块和多尺度细化的分层架构，并引入量化感知训练以在训练阶段模拟低精度环境。
result: 实验表明该方法在标准移动设备上实现了高保真视觉输出，同时保持了极低的计算开销。
conclusion: 该研究通过结合高效架构与量化训练，成功解决了移动端图像增强模型在训练与部署之间的性能失配问题。
---

## Abstract
Image enhancement models for mobile devices often struggle to balance high output quality with the fast processing speeds required by mobile hardware. While recent deep learning models can enhance low-quality mobile photos into high-quality images, their performance is often degraded when converted to lower-precision formats for actual use on mobile phones. To address this training-deployment mismatch, we propose an efficient image enhancement model designed specifically for mobile deployment. Our approach uses a hierarchical network architecture with gated encoder blocks and multiscale refinement to preserve fine-grained visual features. Moreover, we incorporate Quantization-Aware Training (QAT) to simulate the effects of low-precision representation during the training process. This allows the network to adapt and prevents the typical drop in quality seen with standard post-training quantization (PTQ). Experimental results demonstrate that the proposed method produces high-fidelity visual output while maintaining the low computational overhead needed for practical use on standard mobile devices. The code will be available at https://github.com/GenAI4E/QATIE.git.

---

## 论文详细总结（自动生成）

这篇论文针对移动端图像增强任务中“训练与部署性能不一致”的核心痛点，提出了一种结合高效架构与量化感知训练（QAT）的解决方案。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：深度学习图像增强模型在高性能服务器上训练时通常使用全精度（FP32），但在移动端部署时为了追求速度，必须转换为低精度（如 INT8）。这种**训练-部署失配（Training-Deployment Mismatch）**会导致严重的性能下降，表现为颜色偏移、伪影和纹理丢失。
*   **研究背景**：移动设备硬件（传感器、光学器件）限制了成像质量。现有的深度 ISP 模型虽然能提升画质，但往往忽略了移动端推理框架（如 TensorFlow Lite）下的量化敏感性，尤其是图像增强任务对激活值的长尾分布非常敏感。

### 2. 论文提出的方法论
该方法由**高效网络架构**和**量化优化策略**两部分组成：
*   **核心架构（三尺度分层结构）**：
    *   **门控编码块（Gated Encoder Block）**：采用双分支卷积结构，通过 `tanh` 激活后进行 Hadamard 积（元素级乘法）操作。这种门控机制充当轻量级空间-通道注意力，过滤冗余信息。
    *   **多尺度细化（Multi-Scale Refinement）**：在 S/2、S/4 和 S/8 尺度上应用 UNet 风格的残差卷积块，平衡全局光照调整与局部纹理恢复。
    *   **多分支特征融合**：解码器融合了高层语义、细化后的编码特征以及原始跳跃连接特征，通过 1x1 卷积降维后进行重建。
*   **量化感知训练（QAT）**：
    *   在训练前向传播中引入 **FakeQuant（伪量化）** 节点，模拟 INT8 的截断和舍入误差。
    *   在反向传播中使用 **STE（Straight-Through Estimator，直通估计器）** 绕过不可导的舍入操作，使 FP32 权重能够感知量化损失并进行补偿。
*   **损失函数**：组合了 PSNR 损失（像素保真度）、余弦相似度损失（结构对齐）和离群值感知损失（Outlier-Aware Loss，稳定学习过程）。

### 3. 实验设计
*   **数据集**：使用 **DPED 数据集**，包含由 iPhone 3GS 拍摄的低质图像与 Canon 70D DSLR 拍摄的高质图像组成的配对数据（约 16 万个 100x100 训练补丁）。
*   **Benchmark**：参加了 **Mobile AI 2026 RGB 图像增强挑战赛**。
*   **对比方法**：包括 DPED 基准模型、PPCN 以及不同量化配置下的消融模型。
*   **评估指标**：PSNR、SSIM、MOS（平均主观得分）以及在移动设备上的推理延迟（Latency）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX A6000 GPU** 进行训练。
*   **训练细节**：采用 PyTorch Lightning 框架，Adam 优化器，Cosine Annealing 学习率调度。
*   **时长**：论文提到其提出的损失函数组合在 **1.67 小时**内即可完成训练（相比之下，使用 MSSSIM 损失的变体需要 8.58 小时）。
*   **部署测试**：在搭载 **Snapdragon 8 Gen 2** 的智能手机上，使用 AI Benchmark 应用测试 TFLite 格式模型的延迟。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了较为全面的消融实验，包括：
    1.  **模块消融**：验证了残差连接、融合细化块和分辨率细化块的作用。
    2.  **通道数消融**：测试了通道宽度 $c \in \{16, 24, 32, 64\}$ 对性能与延迟的权衡。
    3.  **损失函数消融**：对比了不同损失组合对训练时间和精度的影响。
    4.  **量化策略对比**：对比了 FP32、INT8 PTQ（后量化）和 INT8 QAT 的表现。
*   **充分性评价**：实验设计逻辑严密，涵盖了从架构设计到硬件部署的全流程验证。通过在真实手机硬件（GPU 和 NPU/HTP）上测试延迟，增强了结果的客观性和说服力。

### 6. 主要结论与发现
*   **QAT 的必要性**：直接对 FP32 模型进行 INT8 量化（PTQ）会导致严重的颜色偏移（如天空变粉），而 QAT 能显著恢复画质，PSNR 提升 0.474dB，SSIM 提升 0.111。
*   **硬件加速效果**：在专用加速器（Qualcomm Hexagon NPU/HTP）上，INT8 模型比 FP16 基准快 **3.6 倍**，延迟从 151ms 降至 **41.8ms**。
*   **架构效率**：通道数 $c=32$ 被确定为最佳平衡点，在满足挑战赛 PSNR 阈值（>22dB）的同时，参数量仅为 $c=64$ 版本的 25%。

### 7. 优点
*   **部署导向**：不只是追求理论指标，而是针对移动端 INT8 推理的底层特性进行优化。
*   **高效门控机制**：提出的门控编码块在不显著增加计算量的情况下，有效提升了特征表达能力。
*   **训练效率高**：优化的损失函数组合显著缩短了收敛时间，利于快速迭代。

### 8. 不足与局限
*   **数据集单一**：主要在 DPED 数据集上验证，该数据集设备较老（iPhone 3GS），未在更现代的移动传感器数据上广泛测试。
*   **泛化性未知**：论文未讨论该架构在其他底层视觉任务（如去噪、去模糊或超分辨率）中的通用性。
*   **延迟瓶颈**：虽然在 NPU 上表现优异，但在普通移动 GPU 上的延迟（319ms）对于实时视频增强可能仍显不足。

（完）

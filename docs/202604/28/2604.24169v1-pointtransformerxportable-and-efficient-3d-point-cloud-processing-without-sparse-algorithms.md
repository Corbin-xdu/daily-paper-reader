---
title: "PointTransformerX:Portable and Efficient 3D Point Cloud Processing without Sparse Algorithms"
title_zh: PointTransformerX：无需稀疏算法的可移植且高效的 3D 点云处理
authors: "Laurenz Reichardt, Nikolas Ebert, Oliver Wasenmüller"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24169v1"
tags: ["query:cv"]
score: 9.0
evidence: 无需自定义CUDA算子的高效3D视觉Transformer骨干网络
tldr: 针对3D点云感知过度依赖自定义CUDA算子导致的可移植性差问题，本文提出PointTransformerX (PTX)。这是一个完全基于PyTorch原生的视觉Transformer骨干网络，通过引入3D-GS-RoPE旋转位置编码和线性投影补丁嵌入，彻底摆脱了对稀疏卷积和邻域构建的依赖。PTX在保持高精度的同时，显著降低了参数量和内存占用，并实现了跨平台的高效运行。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24169v1/fig-001.webp\", \"caption\": \"Table 3. Inference scaling the attention window size after training with a fixed window size consistently improves results. Colors reflect the improvement relative to the baseline setting in each column, which trains and evaluates with the same window size.\", \"page\": 6, \"index\": 1, \"width\": 485, \"height\": 647}, {\"url\": \"assets/figures/arxiv/2604.24169v1/fig-002.webp\", \"caption\": \"Table 6. Comparison of point cloud segmentation backbones across hardware platforms. Peak memory and runtimes are averaged over the ScanNet evaluation split [7]. CPU refers to Intel Core i5-10400F. mIoU is reported with test-time augmentation. PTX uses BF16 on all hardware, except FP8 for applicable linear layers on the NVIDIA H100. For fair comparison, all applicable networks are upgraded to FlashAttention-3 [38] on the H100.\", \"page\": 8, \"index\": 2, \"width\": 920, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2604.24169v1/fig-003.webp\", \"caption\": \"Table 4. Impact of our contributions on PointTransformerX’s accuracy, parameter count, average runtime and average peak allocated memory for ScanNet [7] in comparison to the baseline PointTransformer V3 [47] on an NVIDIA H100.\", \"page\": 7, \"index\": 3, \"width\": 990, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2604.24169v1/fig-004.webp\", \"caption\": \"Table 5. Ablation of the FFN design, varying the expansion ratio r ∈ {1, 2, 4}, activation function (GeLU, SwiGLU [39], ReLU2 [51]), and the use of DenseNet (DN) [18] skip connections. ReLU2 improves performance consistently, while DN skip connections yield mixed results at added complexity.\", \"page\": 7, \"index\": 4, \"width\": 372, \"height\": 366}]"
motivation: 现有的3D点云处理方法高度依赖自定义CUDA算子，限制了其在非NVIDIA硬件和嵌入式设备上的可移植性与效率。
method: 提出一种无需邻域构建的3D-GS-RoPE旋转位置编码，并采用线性投影替代稀疏卷积，实现完全原生的PyTorch架构。
result: "在ScanNet上达到PointTransformer V3精度的98.7%，参数量减少79.2%，速度提升1.6倍，且仅需253MB内存。"
conclusion: PTX为3D点云感知提供了一个高效、轻量且具备极强跨平台通用性的基础模型。
---

## 摘要
3D 点云感知目前仍高度依赖于用于空间操作的自定义 CUDA 算子，这限制了其在非 NVIDIA 硬件、AMD 硬件以及嵌入式设备上的可移植性和效率。我们提出了 PointTransformerX (PTX)，这是一个完全基于 PyTorch 原生的 3D 点云视觉 Transformer 骨干网络，在移除所有自定义 CUDA 算子和外部库的同时，保持了极具竞争力的准确率。PTX 引入了 3D-GS-RoPE，这是一种旋转位置嵌入，能够在无需构建邻域的情况下直接在自注意力机制中编码 3D 空间关系，并进一步使用线性投影取代了稀疏卷积 Patch 嵌入。PTX 探索了推理时的注意力窗口缩放技术，旨在不经过重新训练的情况下提升准确率。凭借重新设计的前馈网络，PTX 在 ScanNet 数据集上达到了 PointTransformer V3 98.7% 的准确率，参数量减少了 79.2%，运行速度提升了 1.6 倍，且仅需 253 MB 内存。PTX 可在 NVIDIA GPU、AMD GPU (ROCm) 和 CPU 上原生运行，为点云感知提供了一个高效且可移植的基础。

## Abstract
3D point cloud perception remains tightly coupled to custom CUDA operators for spatial operations, limiting portability and efficiency on non-NVIDIA, AMD, and embedded hardware. We introduce PointTransformerX (PTX), a fully PyTorch-native vision transformer backbone for 3D point clouds, removing all custom CUDA operators and external libraries while retaining competitive accuracy. PTX introduces 3D-GS-RoPE, a rotary positional embedding that encodes 3D spatial relationships directly in self-attention without neighborhood construction, and further replaces sparse convolutional patch embedding with a linear projection. PTX explores inference-time scaling of attention windows to improve accuracy without retraining. With a redesigned feed-forward network, PTX achieves 98.7\% of PointTransformer V3's accuracy on ScanNet with 79.2\% fewer parameters and executing 1.6\times faster while requiring just 253 MB memory. PTX runs natively on NVIDIA GPUs, AMD GPUs (ROCm), and CPUs, providing an efficient and portable foundation for point cloud perception.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PointTransformerX (PTX)** 的新型 3D 点云处理骨干网络。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 点云感知模型（如 PTv3、MinkUNet）高度依赖**自定义 CUDA 算子**（如稀疏卷积、kNN 搜索、半径搜索）。
*   **研究背景**：这种依赖导致了严重的“供应商锁定”问题，使得模型难以移植到非 NVIDIA 硬件（如 AMD GPU、Intel CPU）或嵌入式设备（如 Jetson）。此外，自定义算子往往不支持现代深度学习框架的优化特性（如 BF16/FP8 精度、JIT 编译、内核融合）。
*   **整体含义**：PTX 旨在构建一个**完全基于 PyTorch 原生算子**的 Transformer 架构，在彻底摆脱稀疏算法和自定义 CUDA 库的同时，保持与最先进模型（SOTA）相当的精度和更高的效率。

### 2. 论文提出的方法论
PTX 基于序列化架构（如 PTv3），但通过以下核心技术实现了“去稀疏化”：
*   **3D-GS-RoPE（3D Gram-Schmidt 旋转位置编码）**：
    *   **核心思想**：在自注意力机制中直接编码 3D 空间关系，无需构建邻域。
    *   **技术细节**：改进了传统的 Axial-RoPE（轴向旋转位置编码）。它为每个注意力头引入了一个**可学习的正交坐标旋转矩阵**。
    *   **算法流程**：利用 Gram-Schmidt 正交化过程约束旋转矩阵，确保坐标变换是刚性的（保持距离和角度）。每个头学习不同的旋转视角，从而捕捉跨轴的空间相关性，解决了 Axial-RoPE 缺乏对角线敏感性的问题。
*   **线性投影补丁嵌入（Linear Patch Embedding）**：用简单的线性层取代了 PTv3 中占用大量内存和参数的稀疏卷积层。
*   **推理时窗口缩放（Inference-time Window Scaling）**：借鉴大语言模型（LLM）的经验，PTX 发现使用 RoPE 后，可以在推理阶段直接扩大注意力窗口（如从训练时的 512 扩展到 2048），无需重新训练即可提升精度。
*   **高效前馈网络（Efficient FFN）**：通过消融实验，采用 **ReLU² 激活函数**并将扩展比从 4 降低到 2，在减少参数的同时缓解了点云数据上的过拟合。

### 3. 实验设计
*   **数据集**：主要在 **ScanNet**（室内场景语义分割）上进行评估。
*   **Benchmark（基准）**：以 **PointTransformer V3 (PTv3)** 为主要对标对象。
*   **对比方法**：涵盖了点云处理的三大流派：
    *   **Transformer 类**：PTv1, PTv2, StratifiedTransformer, Swin3D, LitePT, OctFormer。
    *   **卷积类**：MinkUNet, SpUNet, OACNN。
    *   **混合类**：PTv3。
*   **跨平台测试**：在 NVIDIA H100、AMD MI300X、Jetson Orin AGX 和 Intel i5 CPU 上分别测试了运行时间和内存占用。

### 4. 资源与算力
*   **训练设备**：单张 **NVIDIA H100** GPU。
*   **训练配置**：Batch size 为 12，采用 AdamW 优化器，学习率为 0.006。
*   **精度支持**：PTX 全程使用 **BF16** 精度，部分线性层在 H100 上使用了 **FP8**。
*   **训练时长**：文中未明确给出总训练小时数，但提到其参数量仅为 PTv3 的 20.8%，暗示了更短的训练周期。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，包括：
    *   不同 RoPE 变体（1D, Axial, Mixed, 3D-GS-RoPE）的对比。
    *   嵌入层（卷积 vs 线性 vs mini-PointNet）的对比。
    *   推理窗口大小从 64 到 65536 的网格搜索。
    *   FFN 设计中 30 种不同激活函数与扩展比的组合。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了精度，还详细对比了不同硬件平台上的**峰值内存**和**端到端延迟**，这在同类论文中较为少见，充分证明了其“可移植性”的主张。

### 6. 主要结论与发现
*   **精度与效率平衡**：PTX 达到了 PTv3 98.7% 的精度，但参数量减少了 **79.2%**（仅 9.6M），推理速度提升了 **1.6 倍**。
*   **极低内存占用**：在 H100 上平均峰值内存仅为 **253 MB**，远低于其他 Transformer 模型。
*   **原生跨平台**：PTX 是唯一一个无需修改代码即可在 AMD GPU 和 CPU 上高效运行的高精度模型，且支持导出为 **ONNX** 格式。
*   **位置编码的重要性**：证明了 3D-GS-RoPE 可以有效替代稀疏卷积提供的归纳偏置。

### 7. 优点
*   **高度可移植性**：彻底解决了 3D 点云模型对特定硬件和复杂第三方库（如 `spconv`, `MinkowskiEngine`）的依赖。
*   **架构简洁**：回归纯粹的 Transformer 设计，利用原生 PyTorch 优化（如 FlashAttention-3）获得性能红利。
*   **推理灵活性**：通过窗口缩放技术，允许用户根据硬件算力动态调整推理时的感受野。

### 8. 不足与局限
*   **数据集覆盖有限**：实验主要集中在 ScanNet 室内场景，缺乏在大型室外自动驾驶数据集（如 SemanticKITTI 或 Waymo）上的验证。
*   **预处理瓶颈**：虽然模型计算部分很快，但点云的**序列化（Sorting）和池化（Pooling）**操作在 CPU/GPU 同步上仍存在瓶颈，这限制了整体吞吐量的进一步提升。
*   **精度微降**：尽管非常接近，但在绝对精度上仍略低于拥有重型稀疏卷积组件的 PTv3。

（完）

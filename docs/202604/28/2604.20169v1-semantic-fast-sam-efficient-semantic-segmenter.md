---
title: "Semantic-Fast-SAM: Efficient Semantic Segmenter"
title_zh: Semantic-Fast-SAM：高效语义分割器
authors: Byunghyun Kim
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20169v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于实时分割的高效CNN重新实现
tldr: 本研究提出Semantic-Fast-SAM (SFS)，旨在解决SAM模型在语义分割任务中计算成本高、推理速度慢的问题。SFS结合了高效的CNN架构FastSAM与语义标注策略，在保持高精度的同时实现了实时性能。实验表明，SFS在Cityscapes和ADE20K数据集上达到了与现有方法相当的精度，但推理速度提升了约20倍，并支持开放词汇分割，为机器人等实时应用场景提供了可能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20169v1/fig-001.webp\", \"caption\": \"TABLE III SEMANTIC SEGMENTATION PERFORMANCE (MIOU). BEST WITHIN EACH SUPERVISION REGIME IS BOLDED; HIGHER IS BETTER.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.20169v1/fig-002.webp\", \"caption\": \"TABLE IV ABLATION RESULTS ON OPEN-VOCABULARY FUSION AND MASK COUNT. BEST WITHIN EACH ABLATION BLOCK IS BOLDED.\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.20169v1/fig-003.webp\", \"caption\": \"TABLE II GPU MEMORY USAGE FOR DIFFERENT METHODS. BEST WITHIN EACH COMPARABLE BLOCK IS BOLDED; LOWER IS BETTER.\", \"page\": 3, \"index\": 3, \"width\": 529, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.20169v1/fig-004.webp\", \"caption\": \"Fig. 2. Overall architecture of Semantic-Fast-SAM.\", \"page\": 2, \"index\": 4, \"width\": 529, \"height\": 254}]"
motivation: 针对原始SAM模型及其语义标注扩展在实时语义分割任务中计算开销巨大且推理缓慢的局限性。
method: 将基于CNN的高效FastSAM模型与语义标注流水线相结合，并利用CLIP头实现开放词汇的类别分配。
result: 在Cityscapes和ADE20K上保持了高精度，且在闭集设置下的推理速度比SSA快约20倍。
conclusion: SFS成功实现了兼具“分割一切”能力与实时性的语义分割，显著提升了基础分割模型在实际场景中的适用性。
---

## 摘要
我们提出了 Semantic-Fast-SAM (SFS)，这是一个将 Fast Segment Anything 模型与语义标注流水线相结合的语义分割框架，旨在不牺牲准确性的情况下实现实时性能。FastSAM 是 Segment Anything Model (SAM) 的一种基于 CNN 的高效重新实现，其运行速度远快于原始的基于 Transformer 的 SAM。基于 FastSAM 的快速掩码生成能力，我们集成了语义分割一切 (SSA) 标注策略，为每个掩码分配有意义的类别。由此产生的 SFS 模型能够以原始基于 SAM 方法的一小部分计算成本和内存占用，生成高质量的语义分割图。在 Cityscapes 和 ADE20K 基准测试上的实验表明，SFS 的准确率与之前的基于 SAM 的方法相当（Cityscapes 上的 mIoU 约为 70.33，ADE20K 上为 48.01），同时在闭集设置下的推理速度比 SSA 快约 20 倍。我们还展示了 SFS 通过利用基于 CLIP 的语义头，能够有效处理开放词汇分割，在广泛类别标注方面优于近期的开放词汇模型。这项工作实现了具有“分割一切”能力的实用实时语义分割，扩展了基础分割模型在机器人场景中的适用性。实现代码可在 https://github.com/KBH00/Semantic-Fast-SAM 获取。

## Abstract
We propose Semantic-Fast-SAM (SFS), a semantic segmentation framework that combines the Fast Segment Anything model with a semantic labeling pipeline to achieve real-time performance without sacrificing accuracy. FastSAM is an efficient CNN-based re-implementation of the Segment Anything Model (SAM) that runs much faster than the original transformer-based SAM. Building upon FastSAM's rapid mask generation, we integrate a Semantic-Segment-Anything (SSA) labeling strategy to assign meaningful categories to each mask. The resulting SFS model produces high-quality semantic segmentation maps at a fraction of the computational cost and memory footprint of the original SAM-based approach. Experiments on Cityscapes and ADE20K benchmarks demonstrate that SFS matches the accuracy of prior SAM-based methods (mIoU ~ 70.33 on Cityscapes and 48.01 on ADE20K) while achieving approximately 20x faster inference than SSA in the closed-set setting. We also show that SFS effectively handles open-vocabulary segmentation by leveraging CLIP-based semantic heads, outperforming recent open-vocabulary models on broad class labeling. This work enables practical real-time semantic segmentation with the "segment-anything" capability, broadening the applicability of foundation segmentation models in robotics scenarios. The implementation is available at https://github.com/KBH00/Semantic-Fast-SAM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic-Fast-SAM: Efficient Semantic Segmenter》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：原始的 Segment Anything Model (SAM) 虽然具备强大的零样本分割能力，但存在两个主要缺陷：一是**缺乏语义标签**（仅生成无类别掩码）；二是**计算开销巨大**（基于 ViT-H 架构，难以实现实时推理）。
*   **研究背景**：现有的改进方案如 Semantic-Segment-Anything (SSA) 虽然通过外接分类器解决了标签问题，但由于底层仍依赖 SAM，推理速度极慢（通常 >1s/张）。
*   **研究目标**：开发一种既能“分割一切”，又能“实时标注”，且对硬件资源友好的高效语义分割框架。

### 2. 论文提出的方法论：Semantic-Fast-SAM (SFS)
SFS 采用两阶段架构，将高效的掩码生成与多分支语义标注相结合：
*   **阶段 A：快速掩码生成**
    *   采用 **FastSAM** 作为核心引擎。它基于 YOLOv8-seg 架构，利用 CNN 的高效性代替 Transformer，通过单次前向传播生成全图的类无关掩码（Class-agnostic masks）。
*   **阶段 B：多分支语义标注流水线**
    *   **闭集分支（Closed-Set Branch）**：使用预训练的 OneFormer 模型（在 COCO/ADE20K 上训练），对全图生成初步语义图。通过对 FastSAM 掩码区域内的像素进行“投票”来确定已知类别。
    *   **开放词汇分支（Open-Vocabulary Branch）**：针对未知物体，利用 **BLIP** 生成图像区域描述，提取名词短语作为候选标签；随后利用 **CLIP** 计算图像特征与文本特征的余弦相似度，进行重排序和筛选。
    *   **决策融合**：通过启发式规则融合两个分支的结果。如果闭集预测置信度高则保留，否则采用 CLIP 推荐的开放词汇标签。

### 3. 实验设计
*   **数据集**：
    *   **Cityscapes**：用于评估城市场景下的闭集分割性能。
    *   **ADE20K**：用于评估复杂室内外场景下的通用分割性能。
*   **Benchmark（基准）**：
    *   **精度对比**：与 SSA (SAM + 标注)、OneFormer、Mask2Former、SegFormer 等模型对比 mIoU。
    *   **效率对比**：对比推理延迟（Latency）和 GPU 显存占用。
    *   **开放词汇对比**：与 CLIPSeg、GroupViT、MaskCLIP 等专门的开放词汇模型对比。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3090 GPU (24 GB)** 的工作站上完成。
*   **训练时长**：论文明确指出 SFS 是一个**推理时流水线（Inference-time pipeline）**，直接组合了预训练组件，**无需重新训练或微调**，因此没有训练时长数据。
*   **推理开销**：闭集模式下推理时间约为 0.08s，显存占用约 4.5GB。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集（Cityscapes, ADE20K）上进行了完整的精度测试。
    *   进行了**消融实验**，分别验证了“开放词汇融合”对精度的提升，以及“掩码数量预算”对速度与精度平衡的影响。
*   **充分性评价**：实验设计较为客观，直接对比了 SFS 与其“慢速版本”SSA 的性能差异。虽然实验组数不算极多，但涵盖了速度、内存、精度和开放词汇能力四个核心维度，足以支撑其“高效”这一核心论点。

### 6. 论文的主要结论与发现
*   **速度飞跃**：在闭集设置下，SFS 的推理速度比基于 SAM 的 SSA 快约 **20 倍**（0.08s vs 1.65s）。
*   **精度保持**：尽管使用了更轻量级的 FastSAM，但 SFS 在 Cityscapes (70.33 mIoU) 和 ADE20K (48.01 mIoU) 上的表现与原始 SAM 方案非常接近，精度损失微乎其微。
*   **资源友好**：显存占用从 SSA 的 19GB 降至 4.5GB，使得该模型可以在 8-12GB 显存的消费级显卡或嵌入式设备上运行。

### 7. 优点（亮点）
*   **实用性极强**：解决了基础模型落地难的痛点，将“分割一切”的能力推向了实时应用水平。
*   **即插即用**：无需昂贵的重新训练过程，通过组合现有最强组件（FastSAM + CLIP + BLIP）实现了 1+1>2 的效果。
*   **灵活性**：用户可以根据算力需求，通过调整掩码预算或关闭开放词汇分支来灵活切换性能模式。

### 8. 不足与局限
*   **开放词汇模式下的瓶颈**：虽然闭集模式极快，但在开启开放词汇模式时，由于 BLIP 和 CLIP 的逐掩码处理，推理时间会大幅增加（升至 10.24s），尚未实现真正的全功能实时。
*   **小物体精度**：FastSAM 在处理极小物体时的掩码精度略逊于原始 SAM，这在某些高精度要求的工业场景下可能是个限制。
*   **架构冗余**：目前各组件（FastSAM、OneFormer、CLIP）的特征提取器是独立的，存在大量的重复计算，未来仍有融合优化的空间。

（完）

---
title: Deployment-Aligned Low-Precision Neural Architecture Search for Spaceborne Edge AI
title_zh: 面向星载边缘人工智能的部署对齐低精度神经架构搜索
authors: "Parampuneet Kaur Thind, Vaibhav Katturu, Giacomo Zema, Roberto Del Prete"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24492v1"
tags: ["query:cv"]
score: 9.5
evidence: 针对边缘加速器的低精度训练硬件感知神经架构搜索
tldr: 针对星载边缘AI中硬件感知神经架构搜索（NAS）与低精度部署不匹配导致的精度下降问题，本文提出了一种部署对齐的低精度NAS框架。该方法在搜索过程中直接引入FP16数值约束，使候选架构在微调和评估阶段即适应低精度环境。在Intel Myriad X VPU上的船舶分割实验表明，该方法在不增加模型复杂度的前提下，成功修复了约三分之二的部署精度损失，显著提升了边缘AI模型的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24492v1/fig-001.webp\", \"caption\": \"Figure 1. The diagram illustrates the proposed NAS framework in which each candidate architecture generated by the genetic algorithm is trained and evaluated under two deployment-aligned numerical regimes. First, an FP32 baseline model is trained on GPU (A1) and directly exported to FP16 OpenVINO intermediate representation, enabling post-training precision conversion (PTQ-style deployment) and subsequent on-device evaluation (A2). In parallel, the same architecture is fine-tuned using low-precision-aware training (B1), initialized from the FP32 weights and incorporating fake-FP16 activation rounding and weight parametrization with straight-through estimators, before being exported to FP16 OpenVINO IR and evaluated on the target hardware (B2). Both FP32-trained and FP16-aware fine-tuned models are therefore deployed and benchmarked on the target device, allowing a direct comparison between PTQ-based deployment and deployment-aligned low-precision training. The NAS fitness L is computed from on-device performance metrics, while the paired evaluation paths enable quantitative and qualitative assessment of accuracy degradation due to post-training conversion and the corresponding recovery achieved through low-precision-aware fine-tuning, as reported in the results.\", \"page\": 4, \"index\": 1, \"width\": 1036, \"height\": 573}, {\"url\": \"assets/figures/arxiv/2604.24492v1/fig-002.webp\", \"caption\": \"Figure 2. Quantitative comparison between post-training precision conversion and low-precision-aware fine-tuning within the hardwareaware NAS framework. Top row: PTQ-based optimization. Bottom row: low-precision-aware fine-tuning. From left to right: IoU–FPS trade-offs comparing GPU and Myriad VPU execution, IoU versus FPS evolution across generations, fitness trends over generations, and maximum IoU and FPS progression. Low-precision-aware training consistently reduces deployment-induced accuracy loss and improves convergence stability on the target device.\", \"page\": 7, \"index\": 2, \"width\": 1036, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2604.24492v1/fig-003.webp\", \"caption\": \"Figure 3. Qualitative comparison of vessel segmentation under different numerical conditions. From left to right: input image, ground truth mask, GPU FP32 prediction, Myriad X VPU prediction after post-training precision conversion, and Myriad X VPU prediction after deployment-aligned FP16 training. False negatives (missed vessel pixels) are highlighted in red, while false positives (spurious detections) are highlighted in blue. Post-training precision conversion introduces fragmented contours, internal holes, and missed detections, particularly for elongated or low-contrast vessels. In contrast, deployment-aligned low-precision training preserves vessel connectivity and morphology more reliably under on-device execution.\", \"page\": 8, \"index\": 3, \"width\": 1036, \"height\": 1138}]"
motivation: 传统的硬件感知NAS通常在全精度下搜索，导致模型在部署到低精度边缘硬件时会出现显著的性能退化。
method: 将部署对齐的低精度训练直接集成到NAS的微调和评估环节，使架构在搜索阶段就暴露于FP16数值约束下。
result: 在星载船舶分割任务中，该方法将设备端mIoU从0.78提升至0.826，在不增加参数量的情况下弥补了大部分精度损失。
conclusion: 在NAS过程中引入与部署一致的数值约束，能显著增强模型在资源受限边缘设备上的性能对齐性和数值鲁棒性。
---

## 摘要
在边缘加速器上设计满足严格延迟和精度约束的深度网络，日益依赖于硬件感知优化，包括由设备级指标引导的神经架构搜索（NAS）。然而，大多数硬件感知 NAS 流水线仍是在全精度假设下优化架构，并仅在搜索后应用低精度适配，这导致了优化时的行为与低精度硬件上的部署执行之间存在不匹配，从而可能显著降低精度。我们通过将部署对齐的低精度训练直接集成到硬件感知 NAS 中来解决这一局限性。候选架构在微调和评估过程中处于 FP16 数值约束下，从而在不修改搜索空间或进化策略的情况下，实现了架构效率和数值鲁棒性的联合优化。我们针对 Intel Movidius Myriad X 视觉处理单元（VPU），在星载海事监测的船只分割任务上评估了所提出的框架。虽然训练后精度转换将设备端性能从 0.85 mIoU 降低到 0.78 mIoU，但部署对齐的低精度训练在相同架构（95,791 个参数）下实现了 0.826 mIoU 的设备端性能，在不增加模型复杂性的情况下恢复了约三分之二由部署引起的精度差距。这些结果表明，将部署一致的数值约束纳入硬件感知 NAS，显著提高了资源受限边缘人工智能（AI）在优化与部署之间的鲁棒性和对齐性。

## Abstract
Designing deep networks that meet strict latency and accuracy constraints on edge accelerators increasingly relies on hardware-aware optimization, including neural architecture search (NAS) guided by device-level metrics. Yet most hardware-aware NAS pipelines still optimize architectures under full-precision assumptions and apply low-precision adaptation only after the search, leading to a mismatch between optimization-time behavior and deployment-time execution on low-precision hardware that can substantially degrade accuracy. We address this limitation by integrating deployment-aligned low-precision training directly into hardware-aware NAS. Candidate architectures are exposed to FP16 numerical constraints during fine-tuning and evaluation, enabling joint optimization of architectural efficiency and numerical robustness without modifying the search space or evolutionary strategy. We evaluate the proposed framework on vessel segmentation for spaceborne maritime monitoring, targeting the Intel Movidius Myriad X Visual Processing Unit (VPU). While post-training precision conversion reduces on-device performance from 0.85 to 0.78 mIoU, deployment-aligned low-precision training achieves 0.826 mIoU on-device for the same architecture (95,791 parameters), recovering approximately two-thirds of deployment-induced accuracy gap without increasing model complexity. These results demonstrate that incorporating deployment-consistent numerical constraints into hardware-aware NAS substantially improves robustness and alignment between optimization and deployment for resource-constrained edge Artificial Intelligence (AI).

---

## 论文详细总结（自动生成）

这是一份关于论文《Deployment-Aligned Low-Precision Neural Architecture Search for Spaceborne Edge AI》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：在卫星等星载边缘设备上部署深度学习模型，面临极端的功耗、内存和计算资源限制。硬件感知神经架构搜索（Hardware-aware NAS）已成为自动设计高效架构的主流方法。
*   **核心问题**：现有的硬件感知 NAS 存在“优化与部署脱节”的问题。搜索过程通常在 GPU 上以全精度（FP32）进行，而实际部署在边缘加速器（如 VPU）时会转换为低精度（如 FP16）。这种数值精度的不一致会导致模型在部署后出现显著的精度退化（Accuracy Drop），使得搜索阶段选出的“最优”架构在实际运行中并非最优。

### 2. 方法论：部署对齐的低精度 NAS
*   **核心思想**：将部署时的低精度数值约束直接引入 NAS 的评估循环中。不再将精度转换视为搜索后的被动步骤，而是在搜索过程中对候选架构进行“部署对齐”的微调。
*   **关键技术细节**：
    *   **两阶段训练**：每个候选架构先进行 10 个 Epoch 的 FP32 预训练，随后进行 10 个 Epoch 的 **FP16 感知微调（FP16-aware fine-tuning）**。
    *   **FP16 投影算子**：在微调的前向传播中注入投影算子 $P(\cdot)$，模拟 FP16 的尾数精度和舍入行为。
    *   **直通估计器（STE）**：在反向传播中使用 STE 解决舍入函数不可导的问题，确保梯度更新稳定。
    *   **激活裁剪（Activation Clipping）**：使用固定的对称边界（$|x| \leq 12$）限制激活值范围，以匹配 FP16 的动态范围并减少溢出风险。
    *   **硬件在环评估**：将微调后的模型导出为 OpenVINO FP16 格式，直接在 Intel Myriad X VPU 硬件上测量吞吐量（FPS）和精度（mIoU），并以此计算适应度函数。

### 3. 实验设计
*   **数据集与场景**：使用 **HRSC2016** 数据集，专注于高分辨率光学卫星图像中的**船舶分割**任务。
*   **目标硬件**：Intel Movidius Myriad X VPU（典型的低功耗星载边缘加速器）。
*   **对比基准（Benchmark）**：
    *   **PTQ Baseline**：传统的硬件感知 NAS，在 FP32 下搜索，部署前进行训练后量化/转换。
    *   **Deployment-Aligned NAS**：本文提出的方法，在搜索循环中包含低精度感知训练。
*   **搜索空间**：包含 6 个可学习块的单路径网络，涵盖 Conv、MBConv、CSPConv、DenseNetBlock 等多种算子。

### 4. 资源与算力
*   **硬件设备**：训练和搜索在配备 **AMD Radeon™ GPU** 的工作站上完成；推理评估在 **Intel Movidius Myriad X VPU** 上完成。
*   **训练规模**：进化算法设置种群大小为 16，演化 10 代。每个候选架构总计训练 20 个 Epoch。
*   **算力说明**：论文未明确给出总的 GPU 小时数，但考虑到种群规模和代数较小（总计评估约 160 个架构），该搜索过程相对高效。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了 PTQ 模式和部署对齐模式下的进化全过程，包括 IoU-FPS 权衡曲线、适应度进化曲线以及定性的分割效果对比。
*   **充分性评价**：实验设计较为客观，通过在同一硬件、同一搜索空间下进行对比，清晰地展示了数值对齐带来的增益。但实验仅限于单一数据集（HRSC2016）和单一硬件平台（Myriad X），在跨任务和跨硬件的泛化性验证上略显不足。

### 6. 主要结论与发现
*   **精度恢复**：对于同一架构，PTQ 转换使 mIoU 从 0.85 降至 0.78，而部署对齐训练可将其提升至 **0.826**，恢复了约 **2/3** 的精度损失。
*   **鲁棒性增强**：低精度感知训练改变了架构选择的动态，使搜索过程倾向于选择那些不仅结构高效、而且对数值舍入更具鲁棒性的架构。
*   **定性改善**：在实际图像中，新方法显著减少了船只边缘的破碎感、内部空洞和漏检现象。

### 7. 优点与亮点
*   **端到端对齐**：首次在星载 AI 领域强调了 NAS 搜索阶段与部署阶段“数值精度”对齐的重要性。
*   **无需修改搜索空间**：该方法作为一种评估策略的改进，可以无缝集成到现有的各种进化或强化学习 NAS 框架中。
*   **硬件真实性**：采用真实的设备在环（Device-in-the-loop）评估，而非模拟器，结果对工程实践具有高度指导意义。

### 8. 不足与局限
*   **计算开销增加**：由于每个候选架构都需要额外的低精度微调，搜索时间相比传统 FP32 NAS 会翻倍。
*   **精度覆盖有限**：目前仅针对 FP16 进行了优化，未探讨更低比特（如 INT8 或混合精度）的情况，而后者在最新一代边缘芯片中更为常见。
*   **超参数敏感性**：激活裁剪的阈值（12）是经验设定的，对于不同分布的数据集可能需要重新调整。

（完）

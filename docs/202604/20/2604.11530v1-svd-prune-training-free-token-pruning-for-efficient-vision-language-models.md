---
title: "SVD-Prune: Training-Free Token Pruning For Efficient Vision-Language Models"
title_zh: SVD-Prune：面向高效视觉语言模型的无需训练的 Token 剪枝
authors: "Yvon Apedo, Martyna Poreba, Michal Szczepanski, Samia Bouchafa"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11530v1"
tags: ["query:cv"]
score: 9.0
evidence: 基于SVD的免训练Token剪枝，用于高效视觉语言模型
tldr: 视觉语言模型在处理长视觉序列时面临巨大的计算挑战，现有剪枝方法因局部启发式策略常导致高剪枝率下的性能下降。本文提出SVD-Prune，一种无需训练、即插即用的Token剪枝方法。该方法利用奇异值分解计算统计杠杆得分，筛选出对全局方差贡献最大的关键Token。实验证明，在极低Token预算下，该方法依然能保持优异性能，显著提升了推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11530v1/fig-001.webp\", \"caption\": \"Fig. 3. Overview of SVD-Prune. The method performs outside-encoder vision token pruning by applying a global SVD decomposition to vision encoder outputs, estimating token importance via leverage scores, and selecting a compact subset of informative vision tokens before multimodal decoding.\", \"page\": 3, \"index\": 1, \"width\": 890, \"height\": 557}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-002.webp\", \"caption\": \"Fig. 1. Layer-wise variation of attention weights assigned to different types of tokens (including instruction, vision, and response) in LMMs. “A→B” means the attention weights from A to B.\", \"page\": 2, \"index\": 2, \"width\": 523, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-003.webp\", \"caption\": \"Fig. 2. Illustration of the bias induced by the use of the average attention scores across vision tokens as a pruning metric. In graph at the top, averaging attention over all tokens favors earlier tokens, leading to pruning later tokens more frequently. In graph at the bottom, averaging only over attending tokens reverses the bias, leading to earlier tokens being pruned more often.\", \"page\": 2, \"index\": 3, \"width\": 535, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-004.webp\", \"caption\": \"Table 1. Comparison on GQA and TextVQA under varying vision token budgets using LLaVA-1.5-7B. All methods are evaluated without retraining.\", \"page\": 4, \"index\": 4, \"width\": 550, \"height\": 761}]"
motivation: 现有的视觉Token剪枝方法存在位置偏差和信息分散问题，导致在处理视觉细节丰富的图像及高剪枝率时性能大幅下降。
method: 提出基于奇异值分解（SVD）的剪枝策略，通过计算视觉Token特征矩阵的统计杠杆得分来识别并保留最具全局代表性的Token。
result: 实验表明SVD-Prune在极端的Token预算（如仅保留16或32个Token）下，性能显著优于现有的剪枝方法。
conclusion: SVD-Prune作为一种高效、无需训练的即插即用方案，为提升视觉语言模型的推理效率提供了有力支持。
---

## 摘要
视觉语言模型 (VLM) 通过联合处理视觉和文本信息，彻底改变了多模态学习。然而，由于处理长序列视觉 Token 时对计算和内存的高需求，它们面临着重大挑战。许多现有方法依赖于局部启发式策略，例如注意力分数或 Token 范数。然而，这些标准存在位置偏差和信息分散的问题，限制了它们在高剪枝率下保留核心内容的能力，并导致在视觉细节丰富的图像上性能下降。为了解决这些问题，我们提出了 SVD-Prune，这是一种基于奇异值分解 (Singular Value Decomposition) 的无需训练、即插即用的 Token 剪枝方法。它通过分解视觉 Token 特征矩阵，并利用统计杠杆得分 (statistical leverage scores) 选择前 K 个 Token，确保仅保留对主导全局方差贡献最大的 Token。实验表明，在极端的视觉 Token 预算下，SVD-Prune 始终优于先前的剪枝方法，即使在仅保留 32 或 16 个视觉 Token 的情况下也能保持强劲的性能。

## Abstract
Vision-Language Models (VLM) have revolutionized multimodal learning by jointly processing visual and textual information. Yet, they face significant challenges due to the high computational and memory demands of processing long sequences of vision tokens. Many existing methods rely on local heuristics, such as attention scores or token norms. However, these criteria suffer from positional bias and information dispersion, limiting their ability to preserve essential content at high pruning ratios and leading to performance degradation on visually detailed images. To address these issues, we propose SVD-Prune, a trainingfree, plug-and-play token pruning method based on Singular Value Decomposition. It decomposes the vision token feature matrix and selects the top-K tokens using statistical leverage scores, ensuring only tokens contributing most to the dominant global variance are preserved. Experiments show that SVD-Prune consistently outperforms prior pruning methods under extreme vision token budgets, maintaining strong performance even with 32 and 16 vision tokens.

---

## 论文详细总结（自动生成）

这是一份关于论文《SVD-Prune: Training-Free Token Pruning For Efficient Vision-Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLM）在处理图像时会生成大量的视觉 Token（如 LLaVA-1.5 默认生成 576 个），这导致了巨大的计算开销和内存占用，尤其是在资源受限的边缘设备上。
*   **现有挑战**：目前的 Token 剪枝方法多依赖局部启发式策略（如注意力分数、Token 范数或跨模态相似度）。
*   **研究动机**：作者发现现有的注意力机制存在**位置偏差**（由于因果掩码，靠后的 Token 注意力权重天然较低）和**注意力汇聚效应**，导致在高剪枝率下无法准确识别关键信息。
*   **核心目标**：提出一种无需训练、能捕捉全局结构且不受位置偏差影响的视觉 Token 剪枝方案。

### 2. 论文提出的方法论
作者提出了 **SVD-Prune**，一种基于奇异值分解（SVD）的即插即用剪枝方法，其核心流程分为四个阶段：
1.  **全局模式提取**：将视觉编码器输出的特征矩阵 $F$ 进行 SVD 分解，得到左奇异向量 $U$、奇异值矩阵 $\Sigma$ 和右奇异向量 $V^\top$。这能从全局视角捕捉 Token 间的方差结构。
2.  **主导方差截断**：计算累积解释方差比例，选择最小的 $k$ 个奇异值，使得保留的方差比例达到预设阈值 $\epsilon$（通常在 0.7 到 0.95 之间）。这能过滤掉噪声和冗余细节。
3.  **杠杆得分计算（Leverage Scores）**：利用前 $k$ 个左奇异向量计算每个 Token 的统计杠杆得分。得分越高，代表该 Token 对全局主导模式的贡献越大。
4.  **Token 选择与恢复**：根据得分保留前 $m$ 个 Token，并将其重新按原始空间顺序排列，以保持位置编码的连贯性，最后输入 LLM 解码器。

### 3. 实验设计
*   **基准模型**：LLaVA-1.5-7B。
*   **数据集**：
    *   **GQA**：评估组合视觉推理能力。
    *   **TextVQA**：评估对图像中文字的识别与理解能力。
*   **对比方法**：涵盖了编码器端剪枝（ToMe, HIRED, VisionZip, TRIM）和解码器端剪枝（FastV, PyramidDrop, SparseVLM）共 7 种主流方法。
*   **评估指标**：在不同 Token 预算（192, 128, 64, 32, 16）下的模型准确率，以及 FLOPs 减少比例。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA A40 GPU (48GB VRAM)**，配以 Intel Core i7-14700 CPU。
*   **训练时长**：该方法为 **Training-free（无需训练）**，因此不存在训练时长。
*   **推理开销**：文中提供了 FLOPs 分析，显示将 Token 从 576 减至 16 可减少约 **84.8%** 的总计算量。

### 5. 实验数量与充分性
*   **实验规模**：作者在两个核心 Benchmark 上进行了多梯度的对比实验，Token 预算跨度从 192 一直下探到极端的 16。
*   **充分性评价**：实验设计较为充分。通过与 7 种不同类型的先进方法对比，证明了 SVD-Prune 在各种压缩率下的稳健性。特别是在“极端低 Token 预算”（32 和 16 个 Token）这一以往研究较少的领域，提供了详实的数据支撑。
*   **客观性**：实验遵循了 LLaVA-1.5 的标准评测协议，对比基准公平。

### 6. 主要结论与发现
*   **极端压缩下的优越性**：在仅保留 32 或 16 个 Token 时，SVD-Prune 的性能显著超过所有对比方法。例如在 GQA 上，保留 32 个 Token 时仍能达到 53.52 的分数，而其他方法大多跌破 50。
*   **克服偏差**：SVD 杠杆得分能有效避开 LLM 注意力机制带来的位置偏差，更准确地定位图像中的核心语义区域。
*   **效率提升**：视觉 Token 是 VLM 推理成本的主要驱动力，通过 SVD-Prune 大幅削减 Token 数量，可以在损失较小精度的情况下实现数倍的推理加速。

### 7. 优点
*   **无需训练**：即插即用，不需要昂贵的微调或大规模数据集重新训练。
*   **全局视角**：相比局部启发式方法，SVD 能从矩阵分解的角度捕捉图像的全局显著性。
*   **鲁棒性强**：在 TextVQA 等对细节要求极高的任务中，即使在高剪枝率下也能保留关键的文字相关 Token。

### 8. 不足与局限
*   **模型覆盖面**：实验仅基于 LLaVA-1.5-7B 展开，未在更先进的架构（如基于 InternLM 或 Qwen 的 VLM）上验证泛化性。
*   **SVD 计算开销**：虽然减少了 LLM 的计算量，但 SVD 分解本身在 CPU/GPU 上的实时计算开销（Latency）未在文中详细量化，对于极小模型，SVD 可能会成为新的瓶颈。
*   **超参数敏感性**：方差保留比例 $\epsilon$ 需要手动调节，论文未提供针对不同场景自动调整该参数的策略。

（完）

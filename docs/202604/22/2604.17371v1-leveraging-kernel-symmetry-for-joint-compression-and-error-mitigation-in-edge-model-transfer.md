---
title: Leveraging Kernel Symmetry for Joint Compression and Error Mitigation in Edge Model Transfer
title_zh: 利用卷积核对称性实现边缘模型传输中的联合压缩与误差缓解
authors: "Anis Hamadouche, Mathini Sellathurai"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17371v1"
tags: ["query:cv"]
score: 9.0
evidence: 利用卷积核对称性进行神经网络压缩和高效传输。
tldr: 本研究针对边缘计算中神经网络传输的通信效率问题，提出了一种利用卷积核结构对称性的自由度（DoF）编解码框架。该方法仅传输对称群隐含的唯一系数，在接收端实现权重的确定性重建，并结合投影技术在量化和噪声信道下进行误差缓解。实验证明，该方案在大幅减少带宽的同时，其鲁棒性和准确率均优于剪枝算法，为受限链路下的模型交付提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17371v1/fig-001.webp\", \"caption\": \"Fig. 1: Orbits in a 5 × 5 convolution kernel under different symmetry groups. Each panel displays the orbit ID assigned to index (i, j) (darker/lighter cells denote different IDs). Entries with the same ID belong to the same orbit and are tied to a single degree of freedom. In (b), the center element is constrained by antisymmetry and marked with ×.\", \"page\": 6, \"index\": 1, \"width\": 367, \"height\": 531}]"
motivation: 旨在解决边缘模型传输过程中高带宽需求与信道噪声导致的性能下降之间的矛盾。
method: 提出基于自由度的编解码器，通过仅发送对称核的独立系数并在接收端执行对称性投影来压缩模型并抑制噪声。
result: 在MNIST和CIFAR-10数据集上，该方法在带宽大幅缩减的情况下，准确率显著高于易受噪声影响的剪枝基准。
conclusion: 证明了利用卷积核的结构化冗余（尤其是中心斜对称性）可以同时实现高效的模型压缩与可靠的错误缓解。
---

## 摘要
本文通过利用卷积核中的结构化对称约束，研究了通信高效的神经网络传输。我们提出了一种基于自由度（DoF）的编解码器，不再传输所有模型参数，而是仅发送由所选对称群隐含的唯一系数，从而使接收端能够确定性地重建完整的权重张量。该框架在多种对称模式、信噪比和位宽下，针对量化和噪声信道条件进行了评估。为了提高对传输损伤的鲁棒性，在接收端进一步应用了投影步骤，以强制与对称不变子空间保持一致，从而有效地对受损参数进行去噪。在 MNIST 和 CIFAR-10 数据集上使用 DeepCNN 架构的实验结果表明，与经常遭受灾难性性能下降的基于剪枝的基准方法相比，基于自由度的传输在实现大幅带宽缩减的同时，保持了显著更高的准确率。在测试的对称性中，中心斜对称（central-skew symmetry）始终提供最佳的准确率-压缩权衡，证实了可以利用结构化冗余在受限链路上实现可靠且高效的神经模型交付。

## Abstract
This paper investigates communication-efficient neural network transmission by exploiting structured symmetry constraints in convolutional kernels. Instead of transmitting all model parameters, we propose a degrees-of-freedom (DoF) based codec that sends only the unique coefficients implied by a chosen symmetry group, enabling deterministic reconstruction of the full weight tensor at the receiver. The proposed framework is evaluated under quantization and noisy channel conditions across multiple symmetry patterns, signal-to-noise ratios, and bit-widths. To improve robustness against transmission impairments, a projection step is further applied at the receiver to enforce consistency with the symmetry-invariant subspace, effectively denoising corrupted parameters. Experimental results on MNIST and CIFAR-10 using a DeepCNN architecture demonstrate that DoF-based transmission achieves substantial bandwidth reduction while preserving significantly higher accuracy than pruning-based baselines, which often suffer catastrophic degradation. Among the tested symmetries, \textit{central-skew symmetry} consistently provides the best accuracy-compression tradeoff, confirming that structured redundancy can be leveraged for reliable and efficient neural model delivery over constrained links.

---

## 论文详细总结（自动生成）

这是一份关于论文《Leveraging Kernel Symmetry for Joint Compression and Error Mitigation in Edge Model Transfer》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对**边缘计算**和**联邦学习**中的模型传输瓶颈问题展开研究。在无线网络环境下，将深度学习模型分发到边缘设备面临两大挑战：
*   **带宽限制**：模型参数量巨大，频繁传输会导致严重的通信延迟。
*   **信道噪声**：无线信道的不稳定性会导致传输比特错误，进而使接收到的模型性能大幅下降。

论文的核心动机是利用卷积神经网络（CNN）中卷积核的**结构化对称性**（如旋转、镜像等），将其作为一种归纳偏置，同时实现**模型压缩**（减少传输数据量）和**误差缓解**（通过对称性约束修复传输噪声）。

### 2. 方法论
论文提出了一种基于**自由度（DoF）的编解码框架**，核心步骤如下：

*   **轨道分解（Orbit Decomposition）**：根据选定的对称群（如旋转 90 度、水平镜像等），将卷积核的索引划分为多个不相交的“轨道”。同一轨道内的元素被约束为相等（偶对称）或互为相反数（斜对称）。
*   **DoF 编解码器**：
    *   **发送端**：不再传输完整的卷积核，而是仅提取每个轨道的代表值（即自由度系数），并进行量化处理。这直接按比例减少了传输比特数。
    *   **接收端**：根据预定义的对称规则，将接收到的系数重新填充到完整卷积核的对应位置。
*   **对称投影（Symmetry Projection）**：在训练阶段和接收端重建后，应用投影算子 $P_S$。该算子通过对轨道内受损的元素求平均值，强制权重回归到对称子空间。
*   **去噪原理**：由于无线信道引入的噪声在不同比特上是独立的，通过对轨道内多个位置进行平均化处理，可以有效降低噪声方差，实现隐式的纠错功能。

### 3. 实验设计
*   **数据集**：MNIST（基础验证）和 CIFAR-10（进阶验证）。
*   **模型架构**：DeepCNN（包含三个卷积层及全局池化层）。
*   **Benchmark（基准）**：
    *   **None**：全参数传输，不施加对称约束。
    *   **Pruning（剪枝）**：为了公平对比，研究者设计了与对称性 DoF 数量完全一致的幅度剪枝方案，并考虑了稀疏索引（COO 格式）带来的额外传输开销。
*   **对比维度**：评估了 10 种不同的对称性（如中心对称、旋转、Toeplitz 等）、不同的信噪比（SNR）以及位宽（8-bit 量化）。
*   **信道模型**：模拟 AWGN 信道，采用 BPSK 调制，包含 CRC32 校验和丢包处理。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了 MNIST 训练 8 个 epoch，CIFAR-10 训练 30 个 epoch，Batch Size 均为 128。实验结果是基于 10 次独立运行的平均值。

### 5. 实验数量与充分性
*   **实验规模**：研究者对 10 种对称模式在两个数据集上进行了详尽测试，每种模式都对比了对应的剪枝基准。
*   **充分性**：实验考虑了量化误差、信道比特错误、数据包校验失败导致的丢包等多种现实因素，并报告了端到端（E2E）延迟。
*   **公平性**：通过匹配剪枝后的参数量与对称性 DoF 数量，并计入剪枝索引的传输成本，确保了对比的客观性。

### 6. 主要结论与发现
*   **带宽优势**：基于 DoF 的传输在保持较高准确率的同时，可减少 **33.33% 至 66.67%** 的有效载荷。
*   **鲁棒性胜出**：在 SNR=10dB 的噪声环境下，对称性方案的准确率远高于剪枝方案。剪枝方案由于索引信息对错误极度敏感，往往会出现准确率断崖式下跌（如在 MNIST 上跌至 9.8%）。
*   **最佳对称模式**：**中心斜对称（Central-skew symmetry）**被证明是最佳的平衡点，它在大幅减小带宽（约 55.56%）的同时，在 MNIST 上甚至实现了比全参数传输更高的鲁棒准确率（+5.53 pp）。
*   **去噪效果**：接收端的对称投影能有效抑制量化噪声和信道干扰，证明了结构化冗余在可靠通信中的价值。

### 7. 优点
*   **创新视角**：将深度学习的“归纳偏置”（对称性）与通信领域的“信道编码/去噪”结合，提供了一种无需额外冗余比特的纠错思路。
*   **实用性强**：DoF 编解码过程简单，投影操作计算开销极低，非常适合资源受限的边缘设备。
*   **对比严谨**：在对比剪枝算法时，诚实地计入了索引开销，揭示了稀疏模型在实际无线传输中的脆弱性。

### 8. 不足与局限
*   **模型深度有限**：实验仅在小型 CNN 上进行，尚未在 ResNet、Transformer 等更深、更复杂的现代架构上验证其有效性。
*   **信道模型单一**：主要基于 AWGN 信道，未探讨多径衰落、多用户干扰等更复杂的移动通信场景。
*   **任务局限**：目前仅限于图像分类任务，对于目标检测、语义分割等对空间细节更敏感的任务，强加对称约束可能会带来更大的性能损失。

（完）

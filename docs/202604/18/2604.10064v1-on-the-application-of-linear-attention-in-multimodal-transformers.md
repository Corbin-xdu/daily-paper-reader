---
title: On The Application of Linear Attention in Multimodal Transformers
title_zh: 论线性注意力在多模态 Transformer 中的应用
authors: "Armin Gerami, Seyedehanita Madani, Ramani Duraiswami"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10064v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于高效多模态Transformer的线性注意力机制
tldr: "本研究探讨了线性注意力（Linear Attention, LA）在多模态Transformer中的应用，旨在解决传统Softmax注意力机制带来的平方级计算复杂度瓶颈。通过在LAION-400M数据集上训练不同规模的ViT模型，并进行ImageNet-21K零样本准确率验证，证明了LA能将计算开销降至线性，同时遵循与标准注意力相同的缩放法则，为大规模多模态任务提供了高效且可扩展的方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10064v1/fig-001.webp\", \"caption\": \"Figure 4. Training loss scaling with model size for ViTs on LAION-400M. Our adjusted LA follows a trend similar to standard attention.\", \"page\": 4, \"index\": 1, \"width\": 485, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.10064v1/fig-002.webp\", \"caption\": \"Figure 3. Training metrics for ViT-S/16, ViT-B/16, and ViT-L/16 models trained on LAION-400M using four NVIDIA A5500 GPUs. Linear attention with our adjustments achieves performance comparable to standard attention.\", \"page\": 4, \"index\": 2, \"width\": 1021, \"height\": 683}]"
motivation: 传统多模态Transformer的平方级注意力复杂度限制了其在处理大规模数据时的可扩展性。
method: 在多模态框架中引入线性注意力机制，将计算复杂度从序列长度的平方降低到线性。
result: 实验表明线性注意力在显著降低计算开销的同时，保持了与标准Softmax注意力一致的性能缩放规律。
conclusion: 线性注意力是处理大规模复杂多模态数据集的一种稳健且高效的替代方案。
---

## 摘要
多模态 Transformer 是最先进视觉语言模型的核心架构，但其注意力的二次复杂度仍是制约可扩展性的关键障碍。在本研究中，我们探讨了线性注意力（Linear Attention, LA）作为多模态框架内高效替代方案的可行性。通过集成线性注意力，我们将相对于序列长度的计算开销从二次降低到线性，同时保持了具有竞争力的性能。我们在 LAION-400M 数据集上训练的 ViT-S/16、ViT-B/16 和 ViT-L/16 架构上评估了我们的方法，验证重点在于 ImageNet-21K 的零样本准确率。我们的系统性评估表明，线性注意力不仅显著节省了计算资源，而且遵循与标准 Softmax 注意力相同的缩放法则（scaling laws）。这些发现使线性注意力成为处理日益庞大且复杂数据集的下一代多模态 Transformer 的一种稳健且可扩展的解决方案。

## Abstract
Multimodal Transformers serve as the backbone for state-of-the-art vision-language models, yet their quadratic attention complexity remains a critical barrier to scalability. In this work, we investigate the viability of Linear Attention (LA) as a high-efficiency alternative within multimodal frameworks. By integrating LA, we reduce the computational overhead from quadratic to linear relative to sequence length while preserving competitive performance. We evaluate our approach across ViT-S/16, ViT-B/16, and ViT-L/16 architectures trained on the LAION-400M dataset, with validation focused on ImageNet-21K zero-shot accuracy. Our systematic evaluation demonstrates that Linear Attention not only yields significant computational savings but also adheres to the same scaling laws as standard softmax attention. These findings position Linear Attention as a robust, scalable solution for next-generation multimodal Transformers tasked with processing increasingly large and complex datasets.

---

## 论文详细总结（自动生成）

这篇论文对线性注意力（Linear Attention, LA）在多模态 Transformer 架构中的应用进行了深入研究，旨在解决传统 Softmax 注意力在处理长序列时的计算瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态 Transformer（如 CLIP、PaLI 等）在处理视觉和文本跨模态信息时表现卓越，但其核心的 Softmax 注意力机制具有**二次方计算复杂度** $O(N^2)$。随着图像分辨率提高或序列长度增加，计算开销和内存占用变得不可持续。
*   **研究动机**：虽然线性注意力在纯语言或纯视觉任务中有所应用，但在**大规模多模态表示学习**中的有效性、收敛性以及是否遵循缩放法则（Scaling Laws）尚缺乏系统研究。

### 2. 方法论：改进的线性注意力
*   **核心思想**：利用矩阵乘法的结合律，将注意力计算顺序从 $(QK^T)V$ 改为 $Q(K^T V)$，从而将复杂度降为 $O(N)$。
*   **关键技术细节**：
    *   **仿射核函数**：采用 $f(x) = 1 + x$ 作为核函数。
    *   **解决负值问题**：为了防止核函数输出负值，对 Query ($q$) 和 Key ($k$) 进行 **L2 归一化**，确保其点积范围在 $[-1, 1]$，从而保证 $1 + q \cdot k \ge 0$。
    *   **解决过度平滑（Over-smoothing）**：作者发现传统线性注意力公式中的分母（归一化项）会导致注意力分数随序列长度增加而迅速衰减（趋于均匀），限制了模型的表达能力。
    *   **公式改进**：作者提议**移除分母归一化项**，并引入常数缩放。改进后的公式为：$o_{ij} = \sum_{n=1}^{i} (1 + q_i \cdot k_n) v_{nj} / 2$。这种修改增强了模型区分相关 Token 对的能力，改善了梯度流动。

### 3. 实验设计
*   **数据集**：使用大规模多模态数据集 **LAION-400M** 进行预训练。
*   **模型架构**：基于 ViT-S/16、ViT-B/16 和 ViT-L/16 三种规模的 Vision Transformer。
*   **Benchmark 与对比方法**：
    *   **基准**：标准 Softmax 注意力（使用 FlashAttention-2 加速）。
    *   **对比**：标准线性注意力（带分母项） vs. 作者改进的线性注意力。
    *   **评估指标**：ImageNet-21K 零样本（Zero-shot）分类准确率、训练损失、运行时间。

### 4. 资源与算力
*   **训练设备**：使用了 **4 台 NVIDIA A5500 GPU**。
*   **推理/时间测试设备**：使用 **NVIDIA H200 GPU** 进行单层算子耗时测试。
*   **训练细节**：针对不同规模模型设置了不同的全局 Batch Size（S: 64, B: 16, L: 4），训练时长通过 Wallclock Time（小时）在图中展示（ViT-L 训练耗时接近 400 小时）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 22M 到 304M 参数量的三个模型等级，验证了模型的可扩展性。
*   **效率测试**：测试了从 $10^3$ 到 $4 \times 10^6$ 不同序列长度下的耗时，展示了线性缩放的优势。
*   **充分性评价**：实验设计较为充分，不仅对比了最终性能，还详细记录了训练曲线和缩放法则拟合曲线。对比了“标准 LA”与“改进 LA”，证明了作者改进点的必要性。

### 6. 主要结论与发现
*   **性能对等**：改进后的线性注意力在最终收敛性能上与标准 Softmax 注意力相当。
*   **缩放法则一致**：线性注意力遵循与标准注意力相似的幂律缩放（Power-law scaling），这意味着增加模型参数能稳定提升 LA 模型的性能。
*   **极高的效率**：在序列长度达到 $4 \times 10^6$ 时，LA 的计算速度比标准注意力快约 1000 倍。
*   **收敛速度差异**：虽然最终性能接近，但 LA 需要**更多的训练轮数（Epochs）**才能达到与 Softmax 相同的精度，这是因为线性核在区分 Query-Key 对时的对比度不如指数核（Softmax）。

### 7. 优点
*   **理论与实践结合**：不仅提出了公式改进，还通过大规模预训练验证了其在多模态场景下的实用性。
*   **解决痛点**：针对线性注意力常见的“过度平滑”问题提出了简单有效的解决方案（移除分母）。
*   **前瞻性**：验证了 LA 在多模态领域的 Scaling Law，为未来超大规模长序列多模态模型提供了技术路径。

### 8. 不足与局限
*   **收敛效率**：LA 达到相同精度所需的训练时间/轮数更多，这在一定程度上抵消了其单步计算速度的优势。
*   **任务覆盖面**：主要关注零样本分类，未深入探讨在视觉问答（VQA）或图像描述（Captioning）等生成式多模态任务中的表现。
*   **超参数敏感性**：移除归一化分母后，模型在更深层架构中的数值稳定性是否具有普适性仍需更多验证。

（完）

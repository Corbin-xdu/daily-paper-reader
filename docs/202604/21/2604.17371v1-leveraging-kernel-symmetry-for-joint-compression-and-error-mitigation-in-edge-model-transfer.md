---
title: Leveraging Kernel Symmetry for Joint Compression and Error Mitigation in Edge Model Transfer
title_zh: 利用卷积核对称性实现边缘模型传输中的联合压缩与误差缓解
authors: "Anis Hamadouche, Mathini Sellathurai"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17371v1"
tags: ["query:cv"]
score: 9.0
evidence: 利用卷积核中的结构对称约束进行联合压缩
tldr: 本研究针对边缘模型传输中的通信效率问题，提出了一种利用卷积核结构对称性的压缩与错误缓解框架。通过仅传输对称群隐含的独立自由度系数，并在接收端进行确定性重构和投影去噪，该方法在大幅降低带宽需求的同时，显著提升了在量化和噪声信道下的鲁棒性。实验证明，该方法在MNIST和CIFAR-10任务上优于传统的剪枝基准，其中中心斜对称模式表现出最佳的性能权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17371v1/fig-001.webp\", \"caption\": \"Fig. 1: Orbits in a 5 × 5 convolution kernel under different symmetry groups. Each panel displays the orbit ID assigned to index (i, j) (darker/lighter cells denote different IDs). Entries with the same ID belong to the same orbit and are tied to a single degree of freedom. In (b), the center element is constrained by antisymmetry and marked with ×.\", \"page\": 6, \"index\": 1, \"width\": 367, \"height\": 531}]"
motivation: 旨在解决边缘计算场景下神经网络模型传输的高带宽消耗及信道噪声导致的性能下降问题。
method: 提出一种基于自由度的编解码器，仅传输卷积核的唯一系数，并利用对称不变子空间投影技术在接收端进行错误修复。
result: 实验结果显示，该方法在显著减少传输数据量的同时，其分类准确率远高于传统的剪枝压缩方案。
conclusion: 利用卷积核的结构化冗余（特别是中心斜对称性）是实现高效且可靠的神经网络模型远程交付的有效途径。
---

## 摘要
本文通过利用卷积核中的结构化对称约束，研究了通信高效的神经网络传输。我们提出了一种基于自由度（DoF）的编解码器，不再传输所有模型参数，而是仅发送由所选对称群隐含的唯一系数，从而使接收端能够确定性地重建完整的权重张量。该框架在多种对称模式、信噪比和位宽下，针对量化和噪声信道条件进行了评估。为了提高对传输损伤的鲁棒性，在接收端进一步应用了投影步骤，以强制与对称不变子空间保持一致，从而有效地对受损参数进行去噪。在 MNIST 和 CIFAR-10 数据集上使用 DeepCNN 架构的实验结果表明，基于自由度的传输实现了大幅带宽降低，同时保持了显著高于剪枝基准方法的准确率，而后者往往会遭受灾难性的性能下降。在测试的对称性中，中心斜对称（central-skew symmetry）始终提供最佳的准确率-压缩权衡，证实了可以利用结构化冗余在受限链路上实现可靠且高效的神经模型交付。

## Abstract
This paper investigates communication-efficient neural network transmission by exploiting structured symmetry constraints in convolutional kernels. Instead of transmitting all model parameters, we propose a degrees-of-freedom (DoF) based codec that sends only the unique coefficients implied by a chosen symmetry group, enabling deterministic reconstruction of the full weight tensor at the receiver. The proposed framework is evaluated under quantization and noisy channel conditions across multiple symmetry patterns, signal-to-noise ratios, and bit-widths. To improve robustness against transmission impairments, a projection step is further applied at the receiver to enforce consistency with the symmetry-invariant subspace, effectively denoising corrupted parameters. Experimental results on MNIST and CIFAR-10 using a DeepCNN architecture demonstrate that DoF-based transmission achieves substantial bandwidth reduction while preserving significantly higher accuracy than pruning-based baselines, which often suffer catastrophic degradation. Among the tested symmetries, \textit{central-skew symmetry} consistently provides the best accuracy-compression tradeoff, confirming that structured redundancy can be leveraged for reliable and efficient neural model delivery over constrained links.

---

## 论文详细总结（自动生成）

这是一份关于论文《Leveraging Kernel Symmetry for Joint Compression and Error Mitigation in Edge Model Transfer》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在边缘计算和联邦学习（FL）场景中，将训练好的神经网络模型通过无线链路分发到各节点面临两大挑战：
*   **带宽瓶颈**：即使是中等规模的卷积神经网络（CNN），其参数量也给带宽受限的无线信道带来了巨大的传输压力。
*   **信道噪声**：无线传输过程中的位错误（Bit Errors）会直接破坏模型参数，导致接收端模型推理准确率大幅下降。

本文的研究动机在于：利用卷积核内在的**结构对称性**（如旋转、镜像等），将其转化为通信优势，实现**联合模型压缩**与**传输误差缓解**。

### 2. 方法论：核心思想与关键技术
论文提出了一种基于自由度（DoF）的编解码框架，核心思想是“只传输独立参数，利用对称性重构与去噪”。

*   **对称投影（Symmetry Projection）**：
    *   定义对称群 $S$ 作用于卷积核索引，将其划分为多个轨道（Orbits）。
    *   在训练和推理阶段，通过投影算子 $P_S$ 强制卷积核符合对称约束。公式上，投影通过对同一轨道内的所有元素取平均值来实现。
    *   **去噪原理**：由于投影算子会对轨道内的独立信道扰动进行平均化处理，根据统计学原理，这能有效降低噪声方差，起到隐式纠错的作用。
*   **DoF 编解码器（DoF Codec）**：
    *   **发送端**：仅提取每个轨道的代表值（即自由度系数）进行量化和序列化，从而显著减少传输比特数。
    *   **接收端**：根据预定义的对称模式，将接收到的系数填充回完整张量。
*   **中心斜对称（Central-Skew Symmetry）**：
    *   特别设计了一种反对称构造，通过正负配对强制中心系数为零，进一步提高了对量化和位错误的鲁棒性。

### 3. 实验设计
*   **数据集**：MNIST（手写数字识别）和 CIFAR-10（物体分类）。
*   **模型架构**：DeepCNN（包含3层卷积层及全局池化分类头）。
*   **对比基准（Benchmark）**：
    *   **None**：全参数传输，不施加对称约束。
    *   **Pruned Equivalent**：量级剪枝（Magnitude Pruning）。为了公平竞争，剪枝后的非零参数量（NNZ）被设定为与对应对称模式的 DoF 数量完全一致，并采用 COO 格式传输（计入索引开销）。
*   **测试场景**：模拟 AWGN 信道，采用 BPSK 调制，设定 SNR = 10 dB，参数进行 8-bit 对称量化。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：文中提到 MNIST 训练 8 个 epoch，CIFAR-10 训练 30 个 epoch，Batch Size 均为 128，使用 AdamW 优化器。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 **10 种对称模式**（包括无对称、中心偶对称、中心斜对称、水平/垂直镜像、主/副对角线对称、90度旋转、径向对称及 Toeplitz 对称）。
*   **充分性与公平性**：
    *   每组实验均独立运行 **10 次**并报告均值与标准差（SE），确保了结果的统计显著性。
    *   通过引入“匹配 DoF 预算的剪枝”作为基准，考虑了稀疏索引的传输开销，这比单纯对比压缩率更符合实际无线通信场景，体现了实验的客观性。

### 6. 主要结论与发现
*   **带宽大幅节省**：基于 DoF 的传输在保持基准准确率的同时，可减少高达 **2/3（66.67%）** 的比特负载。
*   **鲁棒性优于剪枝**：在噪声信道下，剪枝方法常因索引错误或关键权重丢失导致准确率“灾难性崩溃”（如在某些任务中跌至 9.8%），而对称 DoF 传输表现极其稳健。
*   **最佳模式**：**中心斜对称（Central-Skew）** 被证明是最佳的权衡点。在 MNIST 上实现了 55.56% 的带宽缩减，同时准确率（98.8%）甚至高于全参数传输基准。
*   **隐式去噪**：接收端的对称投影能有效修复因信道干扰导致的对称性破缺，提升了端到端的可靠性。

### 7. 优点（亮点）
*   **双重功效**：将原本用于增强模型泛化能力的“对称性”巧妙转化为通信中的“压缩”与“纠错”工具。
*   **确定性重构**：与剪枝不同，对称性是预定义的结构，不需要传输额外的掩码或索引，消除了稀疏传输的元数据开销。
*   **低延迟**：实验表明，由于传输数据量减小且无需复杂的纠错算法，端到端（E2E）延迟显著降低。

### 8. 不足与局限
*   **模型复杂度较低**：实验仅在简单的 DeepCNN 上完成，尚未在 ResNet、Transformer 等现代大规模架构上验证其可扩展性。
*   **信道模型单一**：主要基于加性高斯白噪声（AWGN）信道，未考虑更复杂的衰落信道（Fading Channels）或突发错误（Burst Errors）环境。
*   **任务覆盖面**：目前仅限于图像分类任务，对于回归、检测或生成式任务的效果尚待观察。
*   **对称性限制**：强制施加对称约束可能会在某些非对称特征明显的任务中引入归纳偏置偏差，导致模型性能上限受限。

（完）

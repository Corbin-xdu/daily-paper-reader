---
title: "SVD-Prune: Training-Free Token Pruning For Efficient Vision-Language Models"
title_zh: SVD-Prune：面向高效视觉语言模型的免训练 Token 剪枝
authors: "Yvon Apedo, Martyna Poreba, Michal Szczepanski, Samia Bouchafa"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11530v1"
tags: ["query:cv"]
score: 9.0
evidence: 基于SVD的视觉语言模型免训练Token剪枝
tldr: 视觉语言模型（VLM）在处理长视觉序列时面临巨大的计算和内存挑战。现有剪枝方法依赖局部启发式策略，在高剪枝率下易受位置偏差影响。本文提出SVD-Prune，一种无需训练、即插即用的视觉Token剪枝方法。该方法利用奇异值分解（SVD）计算统计杠杆得分，筛选出对全局方差贡献最大的关键Token。实验证明，在极端Token预算下，SVD-Prune显著优于现有方法，有效提升了VLM的推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11530v1/fig-001.webp\", \"caption\": \"Fig. 3. Overview of SVD-Prune. The method performs outside-encoder vision token pruning by applying a global SVD decomposition to vision encoder outputs, estimating token importance via leverage scores, and selecting a compact subset of informative vision tokens before multimodal decoding.\", \"page\": 3, \"index\": 1, \"width\": 890, \"height\": 557}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-002.webp\", \"caption\": \"Fig. 1. Layer-wise variation of attention weights assigned to different types of tokens (including instruction, vision, and response) in LMMs. “A→B” means the attention weights from A to B.\", \"page\": 2, \"index\": 2, \"width\": 523, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-003.webp\", \"caption\": \"Fig. 2. Illustration of the bias induced by the use of the average attention scores across vision tokens as a pruning metric. In graph at the top, averaging attention over all tokens favors earlier tokens, leading to pruning later tokens more frequently. In graph at the bottom, averaging only over attending tokens reverses the bias, leading to earlier tokens being pruned more often.\", \"page\": 2, \"index\": 3, \"width\": 535, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2604.11530v1/fig-004.webp\", \"caption\": \"Table 1. Comparison on GQA and TextVQA under varying vision token budgets using LLaVA-1.5-7B. All methods are evaluated without retraining.\", \"page\": 4, \"index\": 4, \"width\": 550, \"height\": 761}]"
motivation: 针对现有视觉Token剪枝方法存在的局部启发式偏差和高剪枝率下性能大幅下降的问题。
method: 提出基于奇异值分解（SVD）的剪枝策略，通过统计杠杆得分识别并保留对全局特征方差贡献最大的Token。
result: 在仅保留16或32个视觉Token的极端情况下，SVD-Prune的性能依然优于之前的剪枝方法。
conclusion: SVD-Prune为视觉语言模型提供了一种高效、无需训练且能保持全局信息完整性的Token压缩方案。
---

## 摘要
视觉语言模型（VLM）通过联合处理视觉和文本信息，彻底改变了多模态学习。然而，由于处理长序列视觉 Token 对计算和内存的高需求，它们面临着重大挑战。许多现有方法依赖于局部启发式方法，如注意力分数或 Token 范数。然而，这些标准存在位置偏差和信息分散的问题，限制了它们在高剪枝率下保留核心内容的能力，并导致在视觉细节丰富的图像上性能下降。为了解决这些问题，我们提出了 SVD-Prune，这是一种基于奇异值分解（SVD）的免训练、即插即用的 Token 剪枝方法。它对视觉 Token 特征矩阵进行分解，并使用统计杠杆得分选择前 K 个 Token，确保仅保留对主导全局方差贡献最大的 Token。实验表明，在极端的视觉 Token 预算下，SVD-Prune 始终优于先前的剪枝方法，即使在仅保留 32 和 16 个视觉 Token 的情况下也能保持强大的性能。

## Abstract
Vision-Language Models (VLM) have revolutionized multimodal learning by jointly processing visual and textual information. Yet, they face significant challenges due to the high computational and memory demands of processing long sequences of vision tokens. Many existing methods rely on local heuristics, such as attention scores or token norms. However, these criteria suffer from positional bias and information dispersion, limiting their ability to preserve essential content at high pruning ratios and leading to performance degradation on visually detailed images. To address these issues, we propose SVD-Prune, a trainingfree, plug-and-play token pruning method based on Singular Value Decomposition. It decomposes the vision token feature matrix and selects the top-K tokens using statistical leverage scores, ensuring only tokens contributing most to the dominant global variance are preserved. Experiments show that SVD-Prune consistently outperforms prior pruning methods under extreme vision token budgets, maintaining strong performance even with 32 and 16 vision tokens.

---

## 论文详细总结（自动生成）

这是一份关于论文《SVD-Prune: Training-Free Token Pruning For Efficient Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLM）在处理图像时会生成大量的视觉 Token（如 LLaVA-1.5 默认生成 576 个），这导致了巨大的计算开销和内存占用，尤其是在资源受限的边缘设备上。
*   **现有方法的缺陷**：目前的 Token 剪枝技术多依赖于注意力分数、Token 范数或跨模态相似度等局部启发式指标。
*   **位置偏差问题**：论文指出，由于 LLM 解码器使用因果掩码（Causal Masking），注意力分数会受到 Token 位置的严重干扰（早期 Token 或末尾 Token 的分数被异常放大或缩小），导致剪枝决策并非基于语义重要性，而是位置伪影。
*   **研究目标**：提出一种不依赖注意力机制、全局化且无需训练的剪枝方案，以在极高剪枝率下保持模型性能。

### 2. 论文提出的方法论：SVD-Prune
该方法将视觉 Token 剪枝视为一个低秩子空间选择问题，核心步骤如下：
1.  **全局模式提取（SVD 分解）**：对视觉编码器输出的特征矩阵 $F \in \mathbb{R}^{T \times D}$ 进行奇异值分解（SVD），得到左奇异向量 $U$、奇异值矩阵 $\Sigma$ 和右奇异向量 $V$。
2.  **主导方差截断**：计算累积解释方差比例，选择最小的秩 $k$，使得前 $k$ 个奇异值能保留大部分（如 70%-95%）的全局方差。这能过滤掉噪声和冗余细节。
3.  **统计杠杆得分（Leverage Scores）计算**：利用前 $k$ 个左奇异向量计算每个 Token 的杠杆得分。得分越高，代表该 Token 对全局主导模式的贡献越大。
4.  **Token 选择与恢复**：根据得分排序并选择最小 Token 子集，随后将保留的 Token 恢复到原始空间顺序，以保持位置编码的连贯性。

### 3. 实验设计
*   **基准模型**：LLaVA-1.5-7B。
*   **数据集**：
    *   **GQA**：评估组合视觉推理能力。
    *   **TextVQA**：评估对图像中文字的识别与理解能力。
*   **对比方法**：涵盖了编码器端和解码器端的多种主流剪枝方法，包括 ToMe、FastV、HIRED、PyramidDrop、VisionZip、TRIM 和 SparseVLM。
*   **Token 预算**：测试了从 192、128、64 到极端的 32 和 16 个 Token 的表现（原始为 576 个）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA A40 GPU (48GB VRAM)**，配以 Intel Core i7-14700 CPU。
*   **训练时长**：该方法为 **免训练（Training-free）** 且 **即插即用**，因此不需要训练阶段。
*   **推理开销**：论文提供了 FLOPs 分析，显示在保留 16 个 Token 时，总计算量可从 3.45 TFLOPs 降至 0.52 TFLOPs，降幅达 **84.8%**。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个核心 Benchmark 上进行了多梯度（5 种 Token 预算）的对比实验。
*   **充分性评价**：实验设计较为充分，特别是在“极端低 Token 预算”（16/32 Token）下的对比非常详尽，这在以往的研究中较少涉及。
*   **客观性**：通过与 7 种不同类型的剪枝方法对比，证明了 SVD-Prune 在高压缩比下的稳健性。不过，消融实验部分相对简略，主要集中在性能对比上。

### 6. 主要结论与发现
*   **性能领先**：在所有 Token 预算下，SVD-Prune 均优于现有方法。特别是在仅保留 32 个 Token 时，GQA 得分仅下降 8.38 点，而其他方法下降更为剧烈。
*   **文本敏感性**：在 TextVQA 上，即使剪枝掉 97% 的视觉 Token（仅剩 16 个），模型依然能保持较高的准确率，说明 SVD 杠杆得分能有效捕捉到包含关键文本信息的 Token。
*   **效率提升**：视觉 Token 是 VLM 推理成本的主要驱动力，通过 SVD-Prune 大幅削减 Token 数量可显著降低延迟和计算成本。

### 7. 优点
*   **克服偏差**：通过 SVD 的全局视角，成功规避了因果掩码带来的位置偏差问题。
*   **免训练且通用**：不需要对模型进行任何微调，适用于任何基于 Transformer 架构的 VLM。
*   **极端压缩下的鲁棒性**：在极低 Token 预算（16-32）下表现出的性能稳定性远超基于注意力的启发式方法。

### 8. 不足与局限
*   **超参数依赖**：方法依赖于方差保留比例 $\epsilon$ 的选择，文中未详细讨论不同图像内容对该参数的敏感度。
*   **模型覆盖面**：实验主要集中在 LLaVA-1.5 架构上，尚未在更大规模（如 70B+）或其他变体（如 Qwen-VL, InternVL）上验证泛化性。
*   **计算开销抵消**：虽然减少了 LLM 的计算量，但 SVD 分解本身在 CPU/GPU 上的计算开销在文中未做极细致的端到端延迟（Latency）对比，仅提供了理论上的 FLOPs 减少。

（完）

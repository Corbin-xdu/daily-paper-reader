---
title: Channel Attention-Guided Cross-Modal Knowledge Distillation for Referring Image Segmentation
title_zh: 通道注意力引导的跨模态知识蒸馏用于指代图像分割
authors: Chen Yang
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16806v1"
tags: ["query:cv"]
score: 10.0
evidence: 针对资源受限指向性图像分割的跨模态知识蒸馏
tldr: 指称图像分割（RIS）结合视觉与语言，但高性能模型参数量巨大，难以在资源受限场景部署。本文提出一种通道注意力引导的跨模态知识蒸馏方法，通过将教师网络学习到的高阶细粒度跨模态关联及通道语义相关性迁移给学生网络。该方法在不增加推理参数的前提下，显著提升了轻量级学生模型的性能，并有效缓解了学习偏差的传递。
source: arxiv
selection_source: fresh_fetch
motivation: 现有RIS模型依赖大规模编码器导致参数量巨大，限制了其在计算资源受限场景下的实际应用。
method: 提出通道注意力引导的蒸馏策略，迁移视觉与语言的高阶细粒度关联以及各通道代表的语义成分相关性。
result: 在两个公开数据集上的实验证明，该方法在不引入额外推理参数的情况下，显著提升了学生模型的分割性能。
conclusion: 该蒸馏方法能有效平衡知识迁移与学生模型的独立学习能力，是优化轻量级跨模态分割模型的有效方案。
---

## 摘要
指代图像分割（RIS）要求根据语言描述准确分割图像中的目标区域，是一项融合视觉和语言的跨模态任务。现有的 RIS 方法通常采用大规模视觉和语言编码模型来提高性能，但其庞大的参数量严重限制了在计算资源有限场景下的部署。为解决这一问题，本文提出了一种通道注意力引导的跨模态知识蒸馏方法，该方法将教师网络学习到的视觉与语言之间的高阶细粒度相关性，以及各通道所代表的语义成分之间的相关性，迁移至学生网络。与传统的像素级关系蒸馏相比，该方法不仅使学生网络能够学习教师网络的知识，还保留了其部分独立学习能力，从而缓解了学习偏差的迁移。在两个公开数据集上的实验结果表明，所提出的蒸馏方法在推理过程中不引入额外参数，并能显著提升学生模型的性能。

## Abstract
Referring image segmentation (RIS) requires accurate segmentation of target regions in images according to language descriptions, which is a cross-modal task integrating vision and language. Existing RIS methods typically employ large-scale vision and language encoding models to improve performance, but their enormous parameter size severely restricts deployment in scenarios with limited computing resources. To solve this problem, this paper proposes a channel attention-guided cross-modal knowledge distillation method, which transfers the high-order fine-grained correlations between vision and language learned by the teacher network, as well as the correlations between semantic components represented by each channel, to the student network. Compared with the traditional pixel-wise relational distillation, this method not only enables the student to learn the knowledge of the teacher, but also retains part of its independent learning ability, alleviating the transfer of learning bias. Experimental results on two public datasets show that the proposed distillation method does not introduce additional parameters during inference and can achieve significant performance improvement for the student model.

---

## 论文详细总结（自动生成）

这篇论文针对指代图像分割（Referring Image Segmentation, RIS）任务中模型参数量过大、难以在资源受限设备部署的问题，提出了一种创新的跨模态知识蒸馏方案。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 RIS 模型为了追求高精度，普遍采用大规模预训练模型（如 Swin-base, BERT-base, SAM 等）作为编码器。这导致模型参数量巨大、计算开销高，严重限制了其在移动端或边缘计算平台上的实际应用。
*   **研究背景**：RIS 任务要求模型能够理解自然语言指令并精确分割出对应的视觉区域。传统的全监督方法或基于大模型的方法虽然效果好，但“重”模型难以普及。
*   **整体含义**：本文旨在通过**知识蒸馏（Knowledge Distillation）**技术，将大型“教师”模型中蕴含的跨模态对齐知识迁移到轻量级的“学生”模型中，从而在保持高性能的同时显著降低计算成本。

### 2. 方法论：核心思想与关键技术
论文提出了一种**通道注意力引导的跨模态知识蒸馏方法**，主要包含以下核心组件：

*   **整体架构**：
    *   **教师网络**：采用 ResNet-101 和 12 层 BERT。
    *   **学生网络**：采用轻量级的 ResNet-18（或 EfficientNet-B0）和 BERT 的前 4 层。
    *   两者均使用视觉-语言跨模态注意力模块（VLCAM）进行特征融合，并配合 FPN 解码器。
*   **视觉-语言关系蒸馏（VL Relational Distillation）**：
    *   **核心思想**：让学生模仿教师如何关联视觉像素和文本单词。
    *   **技术细节**：计算教师和学生网络中 VLCAM 模块产生的“视觉-语言相关性矩阵” $A$。通过 MSE 损失函数最小化两者之间的差异，引导学生学习教师的跨模态对齐逻辑。
*   **通道注意力关系蒸馏（Channel Attention Relational Distillation）**：
    *   **核心思想**：利用高层特征图中每个通道代表特定语义成分的特性，进行软约束迁移。
    *   **技术细节**：在解码器输出端，计算特征图的通道间相关性矩阵（Inter-channel Correlation Matrix）。这种方法避开了严苛的像素级对齐，允许学生模型保留一定的自主学习空间，从而缓解学习偏差的传递。
*   **总损失函数**：由分割损失（交叉熵）、跨模态对齐损失和通道关系损失加权组成。

### 3. 实验设计
*   **数据集**：使用了两个主流的 RIS 基准数据集：
    *   **RefCOCO**：包含丰富的空间位置描述。
    *   **RefCOCO+**：禁用了位置词（如“左”、“右”），更强调外观属性描述，难度更高。
*   **Benchmark 与对比**：
    *   基准线（Baseline）是未经蒸馏的轻量级模型（ResNet-18）。
    *   对比对象包括全监督的大规模教师模型（ResNet-101）。
*   **评价指标**：采用平均交并比（mIoU）。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节**：
    *   输入图像分辨率：384 × 384。
    *   优化器：AdamW，初始学习率 2e-5。
    *   训练周期：在第 20 个 epoch 后学习率衰减。
    *   Batch Size：16。
*   **说明**：文中未明确提及具体的总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个数据集的多个子集（val, testA, testB）上进行了测试。
*   **充分性评价**：
    *   **数据覆盖**：涵盖了位置敏感和属性敏感两种典型的 RIS 场景。
    *   **对比性**：清晰展示了“教师”、“原始学生”和“蒸馏后学生”的性能差异。
    *   **局限性**：实验组数相对较少，缺乏针对不同轻量级骨干网络（如 MobileNet）的广泛消融实验，也未展示推理速度（FPS）的具体提升数据。

### 6. 主要结论与发现
*   **性能提升**：通过蒸馏，轻量级学生模型（ResNet-18）在 mIoU 指标上提升了 **3%–5%**。
*   **零成本推理**：该方法仅在训练阶段引入额外计算，**推理阶段不增加任何参数或计算开销**。
*   **有效性**：证明了跨模态相关性和通道语义相关性的迁移对于 RIS 任务是有效的，能够使小模型获得接近大模型的语义理解能力。

### 7. 优点（亮点）
*   **思路新颖**：将知识蒸馏引入 RIS 领域，并巧妙地结合了跨模态注意力矩阵和通道相关性。
*   **平衡性好**：通道关系蒸馏作为一种“软约束”，既传递了知识，又避免了学生模型过度拟合教师模型的错误（缓解学习偏差）。
*   **实用性强**：直接针对工业界关注的“模型轻量化”和“边缘部署”问题，具有较高的应用价值。

### 8. 不足与局限
*   **实验对比范围有限**：未与现有的其他蒸馏算法（如针对检测或分割的通用蒸馏方法）进行横向对比。
*   **缺乏消融研究**：文中未详细拆解“视觉-语言关系蒸馏”和“通道关系蒸馏”各自对最终性能的贡献占比。
*   **指标单一**：虽然 mIoU 提升明显，但未提供模型压缩比、FLOPs 减少量等量化部署指标。
*   **时间偏差**：论文标注日期为 2026 年（可能为预印本或特定会议标注），部分引用的最新方法（如 2025 年的文献）可能尚未经过广泛的社区验证。

（完）

---
title: "Semantic-Fast-SAM: Efficient Semantic Segmenter"
title_zh: Semantic-Fast-SAM：高效语义分割器
authors: Byunghyun Kim
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20169v2"
tags: ["query:cv"]
score: 9.0
evidence: 面向资源受限语义分割任务的高效CNN重构实现
tldr: 本研究提出Semantic-Fast-SAM (SFS)，旨在解决SAM模型推理速度慢的问题。SFS结合了高效的CNN架构FastSAM与语义标注策略，在保持高精度的同时实现了实时语义分割。该模型在Cityscapes和ADE20K数据集上表现优异，推理速度比SSA快约20倍，且支持开放词汇分割，显著提升了基础分割模型在机器人等实时场景中的实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20169v2/fig-001.webp\", \"caption\": \"TABLE III SEMANTIC SEGMENTATION PERFORMANCE (MIOU). BEST WITHIN EACH SUPERVISION REGIME IS BOLDED; HIGHER IS BETTER.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-002.webp\", \"caption\": \"TABLE IV ABLATION RESULTS ON OPEN-VOCABULARY FUSION AND MASK COUNT. BEST WITHIN EACH ABLATION BLOCK IS BOLDED.\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-003.webp\", \"caption\": \"TABLE II GPU MEMORY USAGE FOR DIFFERENT METHODS. BEST WITHIN EACH COMPARABLE BLOCK IS BOLDED; LOWER IS BETTER.\", \"page\": 3, \"index\": 3, \"width\": 529, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-004.webp\", \"caption\": \"Fig. 2. Overall architecture of Semantic-Fast-SAM.\", \"page\": 2, \"index\": 4, \"width\": 529, \"height\": 254}]"
motivation: 针对原始SAM模型及其语义标注流程计算成本高、难以满足实时应用需求的问题，寻求一种高效的语义分割方案。
method: 将基于CNN的高效FastSAM掩码生成与语义标注策略相结合，并利用CLIP头实现开放词汇的类别分配。
result: 在Cityscapes和ADE20K上达到了与领先方法相当的精度，且推理速度提升了约20倍，同时在开放词汇任务中表现出色。
conclusion: SFS成功实现了兼具“分割一切”能力与实时性能的语义分割，为机器人等对计算资源敏感的领域提供了实用的基础模型。
---

## 摘要
我们提出了 Semantic-Fast-SAM (SFS)，这是一个将 Fast Segment Anything 模型与语义标注流水线相结合的语义分割框架，旨在不牺牲准确性的前提下实现实时性能。FastSAM 是 Segment Anything Model (SAM) 的一种高效 CNN 重构版本，其运行速度远快于原始的基于 Transformer 的 SAM。基于 FastSAM 的快速掩码生成能力，我们集成了一种语义分割一切 (SSA) 标注策略，为每个掩码分配具有实际意义的类别。由此产生的 SFS 模型能够生成高质量的语义分割图，而其计算成本和内存占用仅为原始基于 SAM 方法的一小部分。在 Cityscapes 和 ADE20K 基准测试上的实验表明，SFS 的准确率与之前的基于 SAM 的方法相当（Cityscapes 上的 mIoU 约为 70.33，ADE20K 上约为 48.01），同时在闭集设置下的推理速度比 SSA 快约 20 倍。我们还展示了 SFS 通过利用基于 CLIP 的语义头，能够有效处理开放词汇分割，在广泛类别标注方面优于近期的开放词汇模型。这项工作实现了具有“分割一切”能力的实用实时语义分割，扩展了基础分割模型在机器人场景中的适用性。实现代码可在 https://github.com/KBH00/Semantic-Fast-SAM 获取。

## Abstract
We propose Semantic-Fast-SAM (SFS), a semantic segmentation framework that combines the Fast Segment Anything model with a semantic labeling pipeline to achieve real-time performance without sacrificing accuracy. FastSAM is an efficient CNN-based re-implementation of the Segment Anything Model (SAM) that runs much faster than the original transformer-based SAM. Building upon FastSAM's rapid mask generation, we integrate a Semantic-Segment-Anything (SSA) labeling strategy to assign meaningful categories to each mask. The resulting SFS model produces high-quality semantic segmentation maps at a fraction of the computational cost and memory footprint of the original SAM-based approach. Experiments on Cityscapes and ADE20K benchmarks demonstrate that SFS matches the accuracy of prior SAM-based methods (mIoU ~ 70.33 on Cityscapes and 48.01 on ADE20K) while achieving approximately 20x faster inference than SSA in the closed-set setting. We also show that SFS effectively handles open-vocabulary segmentation by leveraging CLIP-based semantic heads, outperforming recent open-vocabulary models on broad class labeling. This work enables practical real-time semantic segmentation with the "segment-anything" capability, broadening the applicability of foundation segmentation models in robotics scenarios. The implementation is available at https://github.com/KBH00/Semantic-Fast-SAM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic-Fast-SAM: Efficient Semantic Segmenter》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于解决基础分割模型（如 SAM）在实际应用中的**计算效率与语义缺失**之间的矛盾。
*   **背景**：SAM 虽然具备强大的“分割一切”能力，但其基于 Transformer 的架构（ViT-H）推理极其缓慢，且仅能生成无类别的掩码（Mask）。
*   **动机**：现有的语义化方案（如 SSA）虽然能为 SAM 掩码添加标签，但因依赖原始 SAM 的密集采样，导致整体流水线无法满足实时性需求。
*   **目标**：提出 **Semantic-Fast-SAM (SFS)**，旨在结合 FastSAM 的高效掩码生成能力与 SSA 的语义标注策略，打造一个既能“分割一切”又能“实时标注”的高效语义分割器。

### 2. 方法论：核心思想与技术细节
SFS 采用两阶段推理架构，无需对模型进行重新训练或微调：
*   **阶段 1：快速掩码生成（FastSAM）**
    *   利用基于 YOLOv8-seg 的 CNN 架构（FastSAM）代替原始 SAM。它通过单次前向传播生成类无关掩码，速度比 SAM 快约 50 倍。
*   **阶段 2：多分支语义标注（SSA 策略）**
    *   **闭集分支（Closed-Set）**：使用预训练的 OneFormer（以 ConvNeXt-L 为骨干）在 COCO 和 ADE20K 标签空间内生成语义图。对每个 FastSAM 掩码区域进行投票，确定其闭集类别。
    *   **开集分支（Open-Vocabulary）**：针对新颖物体，利用 BLIP 生成图像区域描述，提取名词短语作为候选标签，再通过 CLIP 的图像-文本嵌入相似度进行重排序。
*   **融合与决策**：
    *   通过启发式规则合并两个分支的结果。如果闭集预测置信度高且未被开集建议反驳，则保留闭集标签；否则采用 CLIP 排名最高的开集标签。

### 3. 实验设计
*   **数据集**：Cityscapes（城市景观）、ADE20K（室内外通用场景）。
*   **Benchmark（基准）**：
    *   **精度基准**：mIoU（平均交并比）。
    *   **性能基准**：推理延迟（Inference Time）、显存占用（Peak GPU Memory）。
*   **对比方法**：
    *   **语义基础模型**：Semantic-Segment-Anything (SSA/Semantic-SAM)。
    *   **全监督模型**：OneFormer, Mask2Former, SegFormer-B5。
    *   **开集模型**：CLIPSeg, GroupViT, MaskCLIP。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3090 GPU (24 GB)** 的工作站上完成。
*   **训练时长**：文中明确指出 SFS 是一个**推理时（Inference-time）流水线**，直接组合了预训练组件，**不需要进行反向传播或模型微调**。
*   **运行效率**：闭集模式下推理时间约为 0.08s/张，显存占用约 4.5GB。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了零样本（Zero-shot）性能评估。
    *   进行了推理速度和显存占用的量化对比。
    *   **消融实验**：针对“开集融合策略”和“掩码保留数量（Top-K）”对精度与速度的影响进行了测试。
*   **充分性评价**：实验设计较为客观，直接对比了 SFS 与其原型 SSA 的性能差异。虽然数据集种类不算极多，但涵盖了自动驾驶和通用场景，足以证明其在实时性上的巨大提升。

### 6. 主要结论与发现
*   **速度飞跃**：在闭集设置下，SFS 的推理速度比基于 SAM 的 SSA 快约 **20 倍**（0.08s vs 1.65s）。
*   **精度持平**：SFS 在 Cityscapes (70.33 mIoU) 和 ADE20K (48.01 mIoU) 上的表现与 SSA 极其接近，证明更换高效骨干网对精度的损失微乎其微。
*   **资源友好**：显存占用从 SSA 的 19GB 降至 4.5GB，使得该模型可以在 8-12GB 显存的消费级显卡或嵌入式设备上运行。
*   **开集能力**：通过 CLIP 增强，SFS 在处理未知类别时优于传统的 GroupViT 等模型。

### 7. 优点
*   **极高的实用性**：将“分割一切”模型从实验室演示推向了机器人、自动驾驶等实时工业场景。
*   **模块化设计**：无需重新训练，可灵活更换更强的闭集分割器或更轻量的描述模型。
*   **平衡性佳**：在保持 FastSAM 速度优势的同时，通过多分支融合弥补了其缺乏语义信息的短板。

### 8. 不足与局限
*   **开集模式瓶颈**：虽然闭集模式极快，但在开启开集分支（BLIP+CLIP）时，推理时间会大幅增加至 10s 以上，这限制了其在全开集场景下的实时表现。
*   **掩码精度限制**：FastSAM 在处理极小物体时的掩码精度略逊于原始 SAM，这在 mIoU 的微小下降中有所体现。
*   **依赖性强**：系统性能高度依赖于预训练的 OneFormer 和 CLIP 的质量，若这些组件存在偏差，SFS 无法通过自身机制纠正。

（完）

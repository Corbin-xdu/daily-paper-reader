---
title: Selective Depthwise Separable Convolution for Lightweight Joint Source-Channel Coding in Wireless Image Transmission
title_zh: 无线图像传输中轻量级联合信源信道编码的选择性深度可分离卷积
authors: "Ming Ye, Kui Cai, Cunhua Pan, Zhen Mei, Wanting Yang, Chunguo Li"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22338v1"
tags: ["query:cv"]
score: 9.0
evidence: 通过选择性深度可分离卷积实现轻量化JSCC
tldr: 本研究针对无线图像传输中的联合源信道编码（JSCC）计算复杂度问题，提出了一种基于选择性深度可分离卷积（DSConv）的轻量级框架。通过系统地研究不同层位置和替换比例对模型性能的影响，发现中间层的卷积替换能有效平衡重建质量与计算开销。该方法在显著减少参数量的同时保持了较高的图像重建精度，为资源受限的边缘设备提供了灵活的部署方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22338v1/fig-001.webp\", \"caption\": \"Table II. Architectures of DSC-JSCC variants with different replacement positions at a 60% replacement ratio.\", \"page\": 3, \"index\": 1, \"width\": 485, \"height\": 288}]"
motivation: 旨在解决深度学习JSCC方案中标准卷积计算量大，且缺乏系统性DSConv替换策略指导的问题。
method: 提出一种可配置框架，通过在编码器和解码器的不同深度及比例下，选择性地将标准卷积替换为深度可分离卷积。
result: 实验证明中间层的替换策略在大幅降低模型参数量的同时，仅导致极小的性能下降。
conclusion: 该研究揭示了JSCC系统的层级冗余特性，为在边缘设备上实现高效、灵活的无线图像传输提供了理论依据和技术支持。
---

## 摘要
深度可分离卷积（DSConv）层已成功应用于基于深度学习（DL）的联合信源信道编码（JSCC）方案，以降低计算复杂度。然而，在用于无线图像传输的 JSCC 系统中，关于逐层和按比例使用 DSConv 层替换标准卷积（Conv）层的系统性研究在很大程度上仍未被探索。在本文中，我们提出了一个可配置的轻量级 JSCC 框架，该框架结合了选择性替换策略，能够在不同的层位置和替换比例下，灵活地用 DSConv 层替换标准 Conv 层。通过调整替换层的比例，我们实现了不同的模型压缩水平，并分析了它们对重建性能的影响。此外，我们研究了在固定替换比例下，编码器和解码器不同深度的替换如何影响重建质量。我们的结果表明，在中间层进行 Conv 到 DSConv 的替换可以实现良好的复杂度与性能折衷，揭示了基于 DL 的 JSCC 系统中存在的逐层冗余。大量实验进一步证明，所提出的框架在仅有轻微性能下降的情况下实现了大幅度的参数减少，为资源受限的边缘设备提供了灵活的复杂度与性能折衷。

## Abstract
Depthwise separable convolutional (DSConv) layers have been successfully applied to deep learning (DL)-based joint source-channel coding (JSCC) schemes to reduce computational complexity. However, a systematic investigation of the layerwise and ratio-wise replacement of standard convolutional (Conv) layers with DSConv layers in JSCC systems for wireless image transmission remains largely unexplored. In this letter, we propose a configurable lightweight JSCC framework that incorporates a selective replacement strategy, enabling flexible substitution of standard Conv layers with DSConv layers at various layer positions and replacement ratios. By adjusting the proportion of layers replaced, we achieve different model compression levels and analyze their impact on reconstruction performance. Furthermore, we investigate how replacements at different encoder and decoder depths influence reconstruction quality under a fixed replacement ratio. Our results show that Conv-to-DSConv replacement at intermediate layers achieves a favorable complexity-performance trade-off, revealing layer-wise redundancy in DL-based JSCC systems. Extensive experiments further demonstrate that the proposed framework achieves substantial parameter reduction with only slight performance degradation, enabling flexible complexity-performance trade-offs for resource-constrained edge devices.

---

## 论文详细总结（自动生成）

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在无线图像传输中，基于深度学习的联合信源信道编码（JSCC）模型虽然性能优异，但其高昂的计算复杂度和内存占用限制了在资源受限的边缘设备（如物联网传感器、移动终端）上的部署。
*   **研究动机**：尽管深度可分离卷积（DSConv）已被用于降低模型复杂度，但目前缺乏对 JSCC 系统中 Conv 到 DSConv 替换的**系统性研究**。具体而言，在哪些层替换、替换多少比例的卷积层能达到性能与复杂度的最优平衡，这一问题尚不明确。

### 2. 方法论
*   **核心思想**：提出一种名为 **DSC-JSCC** 的可配置轻量级框架，引入“选择性替换策略”，允许在编码器和解码器的不同位置、以不同比例灵活地将标准卷积（Conv/TConv）替换为深度可分离卷积（DSConv/DSTConv）。
*   **关键技术细节**：
    *   **DSConv 结构**：将标准卷积分解为深度卷积（Depthwise，负责空间特征提取）和逐点卷积（Pointwise，负责通道混合），显著减少参数量和计算量（FLOPs）。
    *   **选择性替换策略**：定义了不同替换比例（如 20% 到 100%）和不同替换位置（早期层 E1/D1、中间层 E2/D2、后期层 E3/D3）。
    *   **端到端优化**：系统由编码器 $f_\theta$、加性高斯白噪声（AWGN）信道和解码器 $g_\psi$ 组成，通过最小化均方误差（MSE）损失函数进行训练。
    *   **功率约束**：在编码器末端进行功率归一化，以满足无线传输的平均功率限制。

### 3. 实验设计
*   **数据集**：使用 **CelebA-HQ** 高清人脸数据集（24,000 张训练，2,000 张测试），图像裁剪为 $256 \times 256 \times 3$。
*   **基准模型（Benchmark）**：经典的 **Deep-JSCC** 模型（Bourtsoulatze 等人提出）。
*   **对比维度**：
    *   **替换比例实验**：对比 DSC-JSCC-20/40/60/80/100 在不同信噪比（SNR）下的表现。
    *   **替换位置实验**：在 60% 固定比例下，对比 E1D1、E2D2、E3D2 等不同位置组合的性能。
    *   **评价指标**：峰值信噪比（PSNR）衡量像素保真度，感知图像补丁相似度（LPIPS）衡量视觉感知质量。

### 4. 资源与算力
*   **软件环境**：Python 3.8.20, TensorFlow 2.6.0。
*   **训练配置**：使用 Adam 优化器，初始学习率 0.001，Batch Size 为 32，训练 20 个 Epoch。
*   **硬件说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多组系统性实验，包括 6 种不同替换比例的对比，以及 5 种不同替换位置的消融实验。
*   **充分性与公平性**：实验覆盖了从低信噪比（0dB）到高信噪比（20dB）的范围，并同时使用了客观指标（PSNR）和主观感知指标（LPIPS）。对比实验在相同的带宽压缩比（$\rho$）和训练参数下进行，保证了对比的公平性。

### 6. 主要结论与发现
*   **层级冗余性**：研究发现 JSCC 系统存在层级冗余。在**中间层**进行替换（如 E2D2 配置）能获得最佳的复杂度-性能折衷。
*   **性能表现**：DSC-JSCC-60 (E2D2) 在大幅减少参数（减少约 82%）和 FLOPs（减少约 75%）的情况下，性能仅有轻微下降。
*   **低 SNR 优势**：在低信噪比环境下，轻量级模型与全量模型的性能差距极小，说明在恶劣信道下轻量化设计的性价比极高。

### 7. 优点
*   **灵活性高**：提出的框架允许根据硬件资源限制，动态调整替换比例和位置。
*   **系统性强**：不仅是简单的模型压缩，还深入探讨了卷积层在 JSCC 编码/解码路径中不同位置的重要性。
*   **效率显著**：通过 DSConv 成功将参数量从 143.7K 压缩至最低 12.3K，极大地提升了部署可行性。

### 8. 不足与局限
*   **数据集单一**：实验仅基于 CelebA-HQ（人脸），未在更通用的数据集（如 ImageNet 或 Kodak）上验证泛化性。
*   **信道模型简单**：主要基于 AWGN 信道，虽然提到可扩展至瑞利衰落信道，但缺乏复杂的衰落信道实验数据。
*   **硬件实测缺失**：虽然计算了 FLOPs，但未在真实的边缘设备（如树莓派或移动端芯片）上测试实际推理延迟和功耗。

（完）

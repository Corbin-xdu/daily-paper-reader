---
title: "KAConvNet: Kolmogorov-Arnold Convolutional Networks for Vision Recognition"
title_zh: KAConvNet：用于视觉识别的 Kolmogorov-Arnold 卷积网络
authors: "Zhaoxiang Liu, Zhicheng Ma, Kaikai Zhao, Kai Wang, Shiguo Lian"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23320v1"
tags: ["query:cv"]
score: 9.0
evidence: 将Kolmogorov-Arnold表示定理应用于视觉卷积网络
tldr: 本研究针对现有卷积神经网络中MLP的局限性，结合Kolmogorov-Arnold表示定理，提出了一种新型的Kolmogorov-Arnold卷积层。通过将可学习的非线性激活函数与卷积操作深度融合，克服了以往研究中B样条曲线效率低且易过拟合的问题。基于此设计的KAConvNet架构在视觉识别任务中表现优异，不仅超越了现有的KAN与卷积结合的方法，还展现出与主流ViT和CNN相当的竞争力，为计算机视觉领域提供了更具解释性和高效的新模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23320v1/fig-001.webp\", \"caption\": \"Fig. 3. Illustrations of (a) KAConvLayer structure and (b) sketch of activation on different positions.\", \"page\": 6, \"index\": 1, \"width\": 654, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.23320v1/fig-002.webp\", \"caption\": \"Fig. 1. Comparison of some competitive methods on ImageNet-1K. Our KAConvNets achieve higher accuracy with fewer parameters.\", \"page\": 2, \"index\": 2, \"width\": 577, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2604.23320v1/fig-003.webp\", \"caption\": \"Fig. 2. The (a) training loss and (b) test accuracy after 300 epoch on the CIFAR-100. VGGlike CKAN and VGG-like KACN which adopt B-spline overfit compared to convolution and our method.\", \"page\": 3, \"index\": 3, \"width\": 646, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.23320v1/fig-004.webp\", \"caption\": \"Fig. A.6. Visual comparison of semantic segmentation performance on Cityscapes.\", \"page\": 23, \"index\": 4, \"width\": 684, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.23320v1/fig-005.webp\", \"caption\": \"Fig. 4. Architectural design of KAConvNet, which comprises Stem, Stages and Transitions as conventional CNNs.\", \"page\": 8, \"index\": 5, \"width\": 577, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2604.23320v1/fig-006.webp\", \"caption\": \"Fig. 5. Grad-CAM of ConvNeXt-T, ConvNet-L† and our KAConvNet-L.\", \"page\": 16, \"index\": 6, \"width\": 650, \"height\": 620}]"
motivation: 现有的KAN与卷积结合方法缺乏理论一致性，且使用的B样条曲线存在计算效率低和易过拟合的问题。
method: 提出一种将Kolmogorov-Arnold表示定理与卷积深度集成的KA卷积层，并构建了高效的KAConvNet网络架构。
result: KAConvNet在视觉任务中优于现有的KAN卷积结合方法，并达到了与主流ViT和CNN相媲美的性能。
conclusion: 该研究证明了Kolmogorov-Arnold表示定理在卷积神经网络中的有效性，为未来创新型CNN的设计提供了新思路。
---

## 摘要
卷积神经网络（CNN）一直是通用计算机视觉任务中的主导且有效的方法。最近，基于 Kolmogorov-Arnold 表示定理的 Kolmogorov-Arnold 神经网络（KAN）在深度学习中展现出了取代多层感知机（MLP）的潜力。KAN 在边上使用可学习的非线性激活函数，在节点上进行简单求和，与 MLP 相比，其参数更少且具有更强的可解释性。然而，将 Kolmogorov-Arnold 表示定理与计算机视觉任务中的卷积方法相结合的探索还很有限。现有的尝试仅仅是用权重替换了可学习的激活函数，这破坏了 KAN 的理论基础并限制了其潜在的有效性。此外，KAN 中使用的 B 样条曲线存在计算效率低和易于过拟合的问题。在本文中，我们提出了一种新型的 Kolmogorov-Arnold 卷积层，它将 Kolmogorov-Arnold 表示定理与卷积深度集成。该层提供了更强的方法可解释性，因为它基于已建立的数学定理，且其设计具有理论一致性。基于 Kolmogorov-Arnold 卷积层，我们设计了一种名为 KAConvNet 的高效网络架构，其性能优于现有的结合 KAN 与卷积的方法，并与主流的 ViT 和 CNN 相比具有竞争力的表现。我们相信，我们的工作为人工智能领域提供了宝贵的见解，并将启发 2020 年代更多创新型 CNN 的开发。代码已在 https://github.com/UnicomAI/KAConvNet 公开。

## Abstract
The Convolutional Neural Networks (CNNs) have been the dominant and effective approach for general computer vision tasks. Recently, Kolmogorov-Arnold neural networks (KANs), based on the Kolmogorov-Arnold representation theorem, have shown potential to replace Multi-Layer Perceptrons (MLPs) in deep learning. KANs, which use learnable nonlinear activations on edges and simple summation on nodes, offer fewer parameters and greater explainability compared to MLPs. However, there has been limited exploration of integrating the Kolmogorov-Arnold representation theorem with convolutional methods for computer vision tasks. Existing attempts have merely replaced learnable activation functions with weights, undermining KANs' theoretical foundation and limiting their potential effectiveness. Additionally, the B-spline curves used in KANs suffer from computational inefficiency and a tendency to overfit. In this paper, we propose a novel Kolmogorov-Arnold Convolutional Layer that deeply integrates the Kolmogorov-Arnold representation theorem with convolution. This layer provides stronger method interpretability because it is based on established mathematical theorems and its design has theoretical alignment. Building on the Kolmogorov-Arnold Convolutional Layer, we design an efficient network architecture called KAConvNet, which outperforms existing methods combining KAN and convolution, and achieves competitive performance compared to mainstream ViTs and CNNs. We believe that our work offers valuable insight into the field of artificial intelligence and will inspire the development of more innovative CNNs in the 2020s. The code is publicly available at https://github.com/UnicomAI/KAConvNet.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **KAConvNet** 的新型视觉骨干网络，旨在通过深度融合 Kolmogorov-Arnold 表示定理与卷积操作，克服现有 KAN-CNN 结合方案的理论缺陷与性能瓶颈。

以下是对该论文的详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：传统的 CNN 依赖于多层感知机（MLP）和固定激活函数。近期出现的 Kolmogorov-Arnold Networks (KANs) 通过在边上使用可学习的非线性激活函数，展现了比 MLP 更强的拟合能力和可解释性。
*   **核心问题**：
    1.  **理论对齐不足**：现有的 KAN 与卷积结合的尝试（如 CKAN, KACN）往往只是简单地将激活函数放在权重上，背离了 Kolmogorov-Arnold 表示定理的原始数学结构。
    2.  **计算效率与过拟合**：KAN 原始使用的 B-spline（B样条曲线）在视觉任务中计算极其缓慢，且极易导致严重的过拟合。
*   **目标**：设计一种既符合数学定理、又能在视觉任务中高效运行且不轻易过拟合的 KAN 卷积架构。

### 2. 方法论：核心思想与关键技术
*   **KAConvLayer（核心层设计）**：
    *   **数学对齐**：根据定理，将函数分解为内部函数（$\phi$）和外部函数（$\Phi$）。在卷积中，内部函数用于每个通道内的信息聚合，外部函数用于跨通道的信息交换。
    *   **GLinear 激活函数**：为了解决 B-spline 的过拟合和低效问题，提出了一种**分段线性可学习激活函数（Grid Linear, GLinear）**。它通过简单的线性插值实现，计算开销极低，且通过降低函数复杂度提升了泛化能力。
    *   **结构实现**：KA 卷积层结合了逐通道卷积（Depthwise-like）和全连接层（MLP），并在权重位置应用可学习激活函数，而非传统的神经元激活。
*   **KAConvNet 架构**：
    *   采用类似现代 CNN（如 ConvNeXt）的 4 阶段架构，包含 Stem（下采样）、Stages（堆叠 KAConvBlock）和 Transitions（阶段过渡）。
    *   引入了 SE Block（通道注意力）来增强空间聚合和通道混合。

### 3. 实验设计
*   **数据集**：ImageNet-1K（分类）、MS COCO（检测）、Cityscapes（语义分割）、CIFAR-100（消融实验）。
*   **Benchmark 与对比方法**：
    *   **轻量级 CNN**：MobileOne, GhostNet, ShuffleNetV2, RedNet。
    *   **Transformer/Mamba**：DeiT, EfficientFormer, VMamba, MambaVision。
    *   **KAN 相关方法**：KACN, CKAN, Fast-KAN, JacobiKAN。
*   **任务场景**：涵盖了从基础分类到下游密集预测（检测与分割）的全方位评估。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU (80GB)** 进行训练。
*   **训练细节**：ImageNet-1K 训练 300 个 epoch，使用 AdamW 优化器，初始学习率 2e-3，包含 RandAugment、Mixup 和 CutMix 等增强手段。
*   **推理测试**：在 **H800 GPU** 上测试了单层的推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：论文设计了三种规模的模型（Small, Base, Large），并在三个主流视觉任务上进行了验证。
*   **消融实验**：针对激活函数类型（PReLU, B-Spline, GLinear）、分段区间数量、KAConvLayer 的位置与数量、以及残差结构的影响做了详细对比。
*   **充分性评价**：实验设计较为全面，不仅对比了精度，还对比了参数量、FLOPs 和实际推理延迟（Latency），并利用 Grad-CAM 进行了可视化分析，客观地展示了模型在目标定位上的优势。

### 6. 主要结论与发现
*   **性能领先**：KAConvNet 在同等参数规模下，准确率显著超过了以往的 KAN-CNN 混合模型，并达到了与主流 ViT 和 CNN 相当的水平。
*   **克服过拟合**：证明了 GLinear 函数在视觉任务中比 B-spline 更稳健，且残差结构能有效缓解 KAN 层在深层可能导致的性能退化。
*   **高效性**：虽然比标准卷积慢，但其推理速度远快于现有的其他 KAN 卷积变体。
*   **可解释性**：Grad-CAM 显示 KAConvNet 能更完整、精确地覆盖目标物体（如鸟的喙和尾部）。

### 7. 优点
*   **理论严谨性**：相比前人工作，更忠实地还原了 Kolmogorov-Arnold 表示定理的数学形式。
*   **实用性改进**：用分段线性函数（GLinear）替代 B-spline 是一个非常务实的改进，解决了 KAN 落地视觉任务的核心痛点。
*   **通用性**：证明了 KAN 结构可以作为通用的视觉骨干网络，在下游任务中表现稳定。

### 8. 不足与局限
*   **计算效率瓶颈**：尽管比其他 KAN 方法快，但由于需要使用 `unfold` 操作和复杂的权重激活，其推理速度和计算效率仍低于高度优化的标准卷积（如标准 3x3 卷积）。
*   **感受野限制**：目前主要使用 3x3 小内核，在捕捉长程依赖方面略逊于大核卷积网络（如 RepLKNet）。
*   **硬件加速缺乏**：目前依赖 PyTorch 基础算子实现，缺乏针对分段激活函数的底层 CUDA 算子优化。

（完）

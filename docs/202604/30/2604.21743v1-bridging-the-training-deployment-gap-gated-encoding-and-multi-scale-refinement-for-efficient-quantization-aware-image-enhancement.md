---
title: "Bridging the Training-Deployment Gap: Gated Encoding and Multi-Scale Refinement for Efficient Quantization-Aware Image Enhancement"
title_zh: 弥合训练与部署之间的差距：用于高效量化感知图像增强的门控编码与多尺度细化
authors: "Dat To-Thanh, Nghia Nguyen-Trong, Hoang Vo, Hieu Bui-Minh, Tinh-Anh Nguyen-Nhu"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21743v1"
tags: ["query:cv"]
score: 9.0
evidence: 用于高效移动端图像增强的量化感知训练
tldr: 针对移动端图像增强模型在量化部署时出现的性能下降问题，本文提出了一种高效的图像增强模型。该模型采用分层网络架构，结合门控编码器块和多尺度细化技术以保留视觉细节。通过引入量化感知训练（QAT），模型在训练阶段模拟低精度表示，有效弥合了训练与部署之间的差距，在保持低计算开销的同时实现了高质量的图像增强效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21743v1/fig-001.webp\", \"caption\": \"Figure 2. Quantization-Aware Training (QAT) and inference pipelines. (Top) QAT simulates low-precision effects via FakeQuant nodes during the forward pass while maintaining FP32 compute. To bypass non-differentiable quantization steps, the backward pass uses the Straight-Through Estimator (STE) to route gradients and update FP32 weights, minimizing the loss (Ltotal) against the ground truth (GT). (Bottom) Post-training, the model is converted to a purely INT8 representation for efficient integer-only inference.\", \"page\": 6, \"index\": 1, \"width\": 932, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.21743v1/fig-002.webp\", \"caption\": \"Figure 1. The proposed hybrid architecture for RGB image enhancement. The model features a three-scale hierarchical structure comprising Gated Encoder Blocks with multichannel skip connections, Multi-Scale Refinement modules applied at scales S/2, S/4, and S/8, and a Multi-branch Feature Fusion strategy in the decoder to aggregate multi-level semantic features for high-fidelity restoration.\", \"page\": 4, \"index\": 2, \"width\": 932, \"height\": 547}]"
motivation: 解决深度学习图像增强模型在移动端进行低精度量化部署时，因训练与部署不匹配导致的性能显著下降问题。
method: 提出一种包含门控编码器和多尺度细化结构的分层网络，并结合量化感知训练（QAT）来模拟低精度环境。
result: 实验表明，该方法在标准移动设备上实现了高保真度的视觉输出，并保持了极低的计算开销。
conclusion: 该研究通过结构优化与量化训练相结合，为移动端高效且高质量的图像增强任务提供了一种切实可行的解决方案。
---

## 摘要
移动设备的图像增强模型通常难以在高质量输出与移动硬件所需的快速处理速度之间取得平衡。虽然最近的深度学习模型可以将低质量的移动照片增强为高质量图像，但在转换为用于手机实际使用的低精度格式时，其性能往往会下降。为了解决这种训练与部署不匹配的问题，我们提出了一种专门为移动部署设计的高效图像增强模型。我们的方法采用具有门控编码器块和多尺度细化的分层网络架构，以保留细粒度的视觉特征。此外，我们引入了量化感知训练（QAT），以模拟训练过程中的低精度表示效果。这使得网络能够自适应，并防止了标准训练后量化（PTQ）中常见的质量下降。实验结果表明，所提方法在产生高保真视觉输出的同时，保持了标准移动设备实际使用所需的低计算开销。代码将在 https://github.com/GenAI4E/QATIE.git 上发布。

## Abstract
Image enhancement models for mobile devices often struggle to balance high output quality with the fast processing speeds required by mobile hardware. While recent deep learning models can enhance low-quality mobile photos into high-quality images, their performance is often degraded when converted to lower-precision formats for actual use on mobile phones. To address this training-deployment mismatch, we propose an efficient image enhancement model designed specifically for mobile deployment. Our approach uses a hierarchical network architecture with gated encoder blocks and multiscale refinement to preserve fine-grained visual features. Moreover, we incorporate Quantization-Aware Training (QAT) to simulate the effects of low-precision representation during the training process. This allows the network to adapt and prevents the typical drop in quality seen with standard post-training quantization (PTQ). Experimental results demonstrate that the proposed method produces high-fidelity visual output while maintaining the low computational overhead needed for practical use on standard mobile devices. The code will be available at https://github.com/GenAI4E/QATIE.git.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bridging the Training-Deployment Gap: Gated Encoding and Multi-Scale Refinement for Efficient Quantization-Aware Image Enhancement》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文聚焦于**移动端图像增强模型在训练与部署之间的性能鸿沟**。
*   **背景**：虽然深度学习模型能显著提升手机照片质量，但移动端硬件（如手机芯片）通常需要将模型量化为低精度格式（如 INT8）以实现实时处理。
*   **核心问题**：传统的全精度（FP32）训练模型在直接进行训练后量化（PTQ）时，会出现严重的性能退化（如色彩偏移、伪影和纹理丢失）。这是因为图像增强任务对激活值的分布极其敏感，而标准量化无法处理其长尾、非对称的分布特性。

### 2. 方法论：核心思想与关键技术
论文提出了一种结合高效架构设计与量化感知训练（QAT）的框架：
*   **分层网络架构**：采用三尺度（S, S/2, S/4）的层次结构，平衡全局上下文与局部细节。
    *   **门控编码器块（Gated Encoder Block）**：利用双分支卷积和 `tanh` 激活函数，通过 Hadamard 积实现轻量级的空间-通道注意力机制，并使用多通道跳跃连接保留原始特征。
    *   **多尺度细化（Multi-Scale Refinement）**：在编码器、解码器及瓶颈层插入 UNet 风格的残差卷积块，增强特征的判别力。
    *   **多分支特征融合**：在解码器端整合高层语义、细化后的编码特征及原始跳跃特征。
*   **量化感知训练（QAT）**：
    *   在训练前向传播中引入 **Fake Quantization（伪量化）** 节点，模拟四舍五入和截断误差。
    *   在反向传播中使用 **直通估计器（STE）** 绕过不可导的量化操作，使模型在训练阶段就学习如何补偿精度损失。
*   **损失函数**：组合了 PSNR 损失（像素保真度）、余弦相似度损失（结构对齐）和离群值感知损失（稳定学习过程）。

### 3. 实验设计
*   **数据集**：主要使用 **DPED 数据集**（包含 iPhone 3GS 与 Canon 70D DSLR 的配对图像），涵盖超过 16 万个 100x100 的训练补丁。
*   **Benchmark 与对比方法**：
    *   参加了 **Mobile AI 2026 RGB 图像增强挑战赛**。
    *   对比了基准模型（DPED 原型）、PPCN 以及不同量化策略（PTQ vs. QAT）。
*   **评估指标**：PSNR、SSIM、MOS（平均主观意见分）以及在真实手机上的推理延迟。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A6000 GPU**。
*   **训练细节**：
    *   使用 PyTorch Lightning 框架。
    *   训练 50 个 epoch（约 6.2 万次迭代），有效 Batch Size 为 128。
    *   采用 `bfloat16` 精度加速训练，并使用梯度裁剪（-1.0 到 1.0）。
    *   QAT 阶段使用极低的学习率（0.00001）进行微调。
*   **部署测试**：在 **Snapdragon 8 Gen 2** 智能手机上，通过 TFLite 和 QNN HTP 插件测试 FullHD（1080P）图像的延迟。

### 5. 实验数量与充分性
论文进行了较为充分的消融实验和对比分析：
*   **架构消融**：分别验证了全局残差连接、融合细化块和分辨率细化块对性能和延迟的影响。
*   **通道数消融**：测试了通道宽度 $c \in \{16, 24, 32, 64\}$，最终确定 $c=32$ 为性能与速度的最佳平衡点。
*   **损失函数对比**：对比了所提损失组合与包含 MSSSIM 的损失变体，证明了所提方法在训练效率和 PSNR 上的优势。
*   **量化对比**：对比了 FP32、INT8-PTQ 和 INT8-QAT。
*   **客观性**：实验涵盖了客观指标（PSNR/SSIM）与主观评价（MOS），并在真实移动硬件上实测了延迟，具有较强的说服力。

### 6. 主要结论与发现
*   **QAT 的必要性**：在 8-bit 量化下，QAT 相比 PTQ 提升了 0.474 dB 的 PSNR 和 0.111 的 SSIM，有效消除了 PTQ 产生的“粉色天空”等色彩伪影。
*   **硬件加速效果**：通过 INT8 量化结合 Qualcomm HTP 加速，推理延迟从 FP16 的 151ms 降至 **41.8ms**，实现了约 **3.6 倍的加速**。
*   **竞赛表现**：该模型在 Mobile AI 2026 挑战赛中获得总分第二名。

### 7. 优点（亮点）
*   **部署导向**：不只关注理论精度，而是针对移动端 INT8 推理进行端到端优化，解决了实际落地中的精度崩塌问题。
*   **高效架构**：门控编码器和多尺度细化模块在保持轻量化的同时，有效地捕捉了图像增强所需的复杂特征。
*   **实用性强**：提供了明确的硬件加速数据，证明了在商用手机上处理 1080P 图像的可行性。

### 8. 不足与局限
*   **泛化性验证有限**：实验主要集中在 DPED 数据集上，对于其他光照条件或不同手机传感器的泛化能力探讨较少。
*   **量化方案单一**：目前仅探讨了对称 INT8 量化，未涉及更低比特（如 4-bit）或非对称量化对图像增强任务的影响。
*   **延迟瓶颈**：虽然在专用加速器（HTP）上很快，但在标准 GPU Delegate 上处理 FullHD 图像仍需 319ms，对于超高清（4K）实时处理仍有挑战。

（完）

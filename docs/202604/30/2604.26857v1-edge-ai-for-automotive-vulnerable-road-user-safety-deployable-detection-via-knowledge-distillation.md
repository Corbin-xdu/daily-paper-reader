---
title: "Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation"
title_zh: 面向汽车弱势路人安全的边缘人工智能：通过知识蒸馏实现可部署的检测
authors: "Akshay Karjol, Darrin M. Hanna"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26857v1"
tags: ["query:cv"]
score: 10.0
evidence: 通过知识蒸馏实现3.9倍压缩与INT8量化鲁棒性
tldr: "本研究针对车载边缘硬件上弱势道路使用者（VRU）检测的部署难题，提出了一种知识蒸馏（KD）框架。通过让轻量化的YOLOv8-S学生模型学习大型YOLOv8-L教师模型的特征，实现了3.9倍的模型压缩。实验表明，该方法显著提升了模型在INT8量化下的鲁棒性，不仅解决了大型模型量化后精度大幅下降的问题，还在保持高召回率的同时将误报率降低了44%，为安全关键型边缘AI部署提供了有效方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26857v1/fig-001.webp\", \"caption\": \"Fig. 3. Deployment comparison: KD INT8 achieves highest precision (0.75) and lowest FAR (0.25) among all deployable configurations.\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-002.webp\", \"caption\": \"Fig. 2. Quantization impact: FP32 (solid) vs INT8 (hatched). Teacher collapses (−23% mAP, −24% precision), students retain performance. KD gains +4% precision under INT8.\", \"page\": 4, \"index\": 2, \"width\": 512, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2604.26857v1/fig-003.webp\", \"caption\": \"Fig. 1. BDD100K class distribution showing severe imbalance. VRU classes (red) are significantly underrepresented compared to dominant classes such as cars.\", \"page\": 3, \"index\": 3, \"width\": 514, \"height\": 356}]"
motivation: 针对自动驾驶中VRU检测任务，大型模型在边缘端INT8量化时精度骤降，而小型模型原生精度不足，难以平衡性能与功耗。
method: 采用知识蒸馏框架，训练参数量仅为11.2M的YOLOv8-S学生模型去模仿43.7M的YOLOv8-L教师模型，并进行INT8后量化评估。
result: "KD学生模型在INT8量化下仅损失5.6%的mAP，其精度（0.748）甚至超过了教师模型的FP32版本，且比直接训练的量化模型误报率降低了44%。"
conclusion: 知识蒸馏是实现在资源受限的边缘硬件上部署高精度、高安全性VRU检测系统的关键技术要求。
---

## 摘要
在边缘硬件上部署用于弱势路人（VRU）安全的高精度目标检测，需要平衡模型容量与计算约束。大型模型虽然精度高，但在边缘部署所需的 INT8 量化下表现不佳，而小型模型则会牺牲检测性能。本文提出了一种知识蒸馏（KD）框架，训练紧凑的 YOLOv8-S 学生模型（1120万参数）来模仿 YOLOv8-L 教师模型（4370万参数），在实现 3.9 倍压缩的同时保持了量化鲁棒性。我们在全规模 BDD100K 数据集（7万张训练图像）上进行了评估，并采用了训练后量化（PTQ）至 INT8。教师模型在 INT8 下遭受了灾难性的性能下降（mAP 下降 23%），而 KD 学生模型则保持了精度（mAP 仅下降 5.6%）。分析表明，KD 传递的是精度校准而非原始检测能力：KD 学生模型在 INT8 下达到了 0.748 的精度，而直接训练的模型仅为 0.653，在等效召回率下提升了 14.5%，且相比崩溃的教师模型，虚警减少了 44%。在 INT8 下，KD 学生模型的精度超过了教师模型的 FP32 精度（0.748 对 0.718），且模型体积缩小了 3.9 倍。这些研究结果表明，知识蒸馏是在边缘硬件上部署高精度、安全关键型 VRU 检测的必要条件。

## Abstract
Deploying accurate object detection for Vulnerable Road User (VRU) safety on edge hardware requires balancing model capacity against computational constraints. Large models achieve high accuracy but fail under INT8 quantization required for edge deployment, while small models sacrifice detection performance. This paper presents a knowledge distillation (KD) framework that trains a compact YOLOv8-S student (11.2M parameters) to mimic a YOLOv8-L teacher (43.7M parameters), achieving 3.9x compression while preserving quantization robustness. We evaluate on full-scale BDD100K (70K training images) with Post-Training Quantization to INT8. The teacher suffers catastrophic degradation under INT8 (-23% mAP), while the KD student retains accuracy (-5.6% mAP). Analysis reveals that KD transfers precision calibration rather than raw detection capacity: the KD student achieves 0.748 precision versus 0.653 for direct training at INT8, a 14.5% gain at equivalent recall, reducing false alarms by 44% versus the collapsed teacher. At INT8, the KD student exceeds the teacher's FP32 precision (0.748 vs. 0.718) in a model 3.9x smaller. These findings establish knowledge distillation as a requirement for deploying accurate, safety-critical VRU detection on edge hardware.

---

## 论文详细总结（自动生成）

这是一份关于论文《Edge AI for Automotive Vulnerable Road User Safety: Deployable Detection via Knowledge Distillation》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在资源受限的汽车边缘硬件上，既保证弱势路人（VRU，如行人、骑行者）检测的高精度，又满足实时性和低功耗的部署要求？
*   **研究背景**：
    *   **量化困境**：边缘加速器通常需要将模型从 FP32 量化为 INT8 以提升速度，但大型模型（如 YOLOv8-L）在量化后往往出现精度“崩塌”；而小型模型（如 YOLOv8-S）虽然量化鲁棒性好，但初始检测精度不足。
    *   **安全敏感性**：在自动驾驶中，虚警（误报）会严重损害驾驶员的信任，因此高精度（Precision）校准对于安全至关重要。
*   **整体含义**：本文旨在证明**知识蒸馏（KD）**不仅能压缩模型，更是提升模型量化鲁棒性和精度校准、使其达到“可部署”状态的关键技术。

### 2. 论文提出的方法论
*   **核心思想**：采用“教师-学生”架构，利用一个预训练的大型教师模型（YOLOv8-L）引导紧凑的学生模型（YOLOv8-S）进行学习。
*   **关键技术细节**：
    *   **多损失函数融合**：总损失由任务损失（$L_{task}$）、逻辑蒸馏损失（$L_{logit}$）和特征蒸馏损失（$L_{feat}$）组成。
    *   **逻辑蒸馏与温度缩放**：使用 KL 散度衡量学生与教师输出概率分布的差异，并通过温度系数 $T=10$ 软化标签，从而传递类别间的相似性信息。
    *   **任务损失抑制（Task-Dampening）**：这是本文的核心发现。通过将任务损失权重 $\alpha$ 设为 0.5（而非传统的 1.0），强迫学生模型更多地学习教师的“软标签”，从而实现精度校准的转移。
*   **算法流程**：先训练教师模型并冻结，再在蒸馏框架下训练学生模型，最后对所有模型进行训练后量化（PTQ）至 INT8。

### 3. 实验设计
*   **数据集**：使用 **BDD100K**，这是目前最大的驾驶场景数据集之一，包含 7 万张训练图像和 1 万张验证图像。
*   **评估对象（Benchmark）**：
    1.  **Teacher (L)**：YOLOv8-L 直接训练。
    2.  **Direct Student (S)**：YOLOv8-S 直接训练（无蒸馏）。
    3.  **KD Student (S)**：YOLOv8-S 通过知识蒸馏训练。
*   **对比维度**：涵盖 FP32 与 INT8 精度对比、VRU 特定类别（行人、骑行者、摩托车手）的 mAP、虚警率（FAR）以及推理吞吐量（FPS）。

### 4. 资源与算力
*   **训练算力**：使用云端 GPU 基础设施，具体型号为 **NVIDIA RTX PRO 6000 Blackwell (96GB VRAM)**。
*   **训练时长**：
    *   教师模型：7 小时。
    *   直接学生模型：2.7 小时。
    *   KD 学生模型：4.9 小时（由于需要教师模型前向传播，开销增加）。
*   **推理测试算力**：在 **NVIDIA RTX 5070 (8GB VRAM)** 上进行，以模拟接近边缘端的部署环境。

### 5. 实验数量与充分性
*   **实验规模**：在全规模数据集（70K 图像）上运行，而非小样本测试，保证了结果的统计学意义。
*   **消融实验**：针对任务损失权重 $\alpha$ 做了专门的对比实验（$\alpha=0.5$ vs $\alpha=1.0$），成功分离并验证了“精度校准转移”的触发条件。
*   **客观性与公平性**：
    *   对比了相同架构（YOLOv8-S）在有无蒸馏下的表现，变量控制严格。
    *   使用了标准的 TensorRT PTQ 量化流程，符合工业界部署现状。
    *   **充分性评价**：实验设计较为充分，不仅关注 mAP，还深入分析了 Precision/Recall 的权衡，这在安全领域非常客观。

### 6. 论文的主要结论与发现
*   **KD 提升量化鲁棒性**：INT8 量化后，教师模型 mAP 暴跌 23.4%，而 KD 学生模型仅下降 5.6%。
*   **精度校准转移（核心发现）**：KD 传递的不是原始检测能力（召回率 Recall 几乎没变），而是**精度校准（Precision）**。KD 学生在 INT8 下的精度（0.748）甚至超过了教师在 FP32 下的精度（0.718）。
*   **虚警率大幅降低**：相比量化后崩溃的教师模型，KD 学生模型的虚警率（FAR）降低了 44%。
*   **高效部署**：KD 学生实现了 3.9 倍的参数压缩和 2.37 倍的推理加速，且在 INT8 下依然保持高性能。

### 7. 优点
*   **工业实用性极强**：直接针对边缘部署中的量化痛点，提供了可落地的技术路径。
*   **理论见解深刻**：揭示了 KD 在量化场景下不仅仅是压缩工具，更是“校准工具”，并指出了任务损失权重对校准转移的关键作用。
*   **安全导向**：评价指标紧扣自动驾驶安全需求（关注虚警率和 VRU 类别）。

### 8. 不足与局限
*   **架构单一性**：实验仅基于 YOLOv8 家族，未验证该结论在 Transformer-based 检测器或其他架构（如 SSD, Faster R-CNN）上的普适性。
*   **硬件覆盖有限**：虽然在 RTX 5070 上做了测试，但尚未在真正的嵌入式车载芯片（如 NVIDIA Jetson Orin 或车规级 SoC）上进行功耗和热管理分析。
*   **数据集局限**：仅使用了 BDD100K，未在 KITTI 或 nuScenes 等其他主流自动驾驶数据集上进行交叉验证，可能存在场景偏差。

（完）

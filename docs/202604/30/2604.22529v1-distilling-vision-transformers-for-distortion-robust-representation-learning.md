---
title: Distilling Vision Transformers for Distortion-Robust Representation Learning
title_zh: 蒸馏视觉 Transformer 以实现抗失真表征学习
authors: "Konstantinos Alexis, Giorgos Giannopoulos, Dimitrios Gunopulos"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22529v1"
tags: ["query:cv"]
score: 9.0
evidence: 蒸馏Vision Transformer以实现鲁棒表示学习
tldr: 针对自监督学习在处理失真数据时的局限性，本文提出一种基于预训练视觉Transformer（ViT）的非对称知识蒸馏框架。通过让教师模型处理清晰图像，学生模型处理失真图像，并进行多层级特征对齐（包括全局嵌入、补丁特征和注意力图），使学生模型在不直接接触清晰数据的情况下获得鲁棒的表征能力，在多项分类任务中表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22529v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of our multi-target distillation framework. A frozen teacher ViT processes clean images while a student ViT of identical architecture receives their distorted counterparts, being trained to align with the teacher at three complementary levels: global embeddings, patch-level features, and attention maps. In this way, the student learns to recover clean semantic representations directly from corrupted inputs, producing robust encoders that are effective for downstream tasks on distorted images.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.22529v1/fig-002.webp\", \"caption\": \"Fig. 4. Attention maps of our distilled student models and the supervised baselines on ImageNet-100 validation images, under different distortion types. For each example, we show the original image, the distorted input, and the attention maps predicted by the supervised baseline and the encoder trained by our method.\", \"page\": 13, \"index\": 2, \"width\": 710, \"height\": 258}]"
motivation: 现有的自监督学习在缺乏清晰观测数据或数据存在严重失真时，难以学习到高质量且鲁棒的视觉表征。
method: 提出一种非对称蒸馏框架，利用预训练ViT作为教师处理清晰图像，引导处理失真图像的学生模型进行多层级特征对齐。
result: 在多个数据集和多种失真类型下的图像分类实验中，该方法在相同监督强度下均显著优于现有的基准方法。
conclusion: 通过多层级知识蒸馏，预训练模型能有效帮助学生模型在仅有失真数据的情况下学习到接近清晰图像的鲁棒表征。
---

## 摘要
自监督学习在从干净数据中学习视觉表征方面取得了显著成功，但当干净的观测数据稀缺或完全不可用时，这仍然具有挑战性。在本文中，我们证明了可以利用预训练的视觉模型来学习抗失真的表征，这些表征随后可以有效地应用于处理失真观测数据的下游任务。具体而言，我们提出了一种非对称知识蒸馏框架，其中教师和学生模型都初始化自同一个预训练的视觉 Transformer（Vision Transformer），但接收每张图像的不同视图：教师处理干净图像，而学生则观察其失真版本。我们引入了多级蒸馏，对齐了全局嵌入、补丁级（patch-level）特征和注意力图，并证明了尽管学生模型从未直接访问干净数据，但它能够逼近干净图像的表征。我们在多个数据集和各种失真情况下的图像分类任务上评估了我们的方法，在相同的人力监督量下，其表现始终优于现有的替代方案。

## Abstract
Self-supervised learning has achieved remarkable success in learning visual representations from clean data, yet remains challenging when clean observations are sparse or not available at all. In this paper, we demonstrate that pretrained vision models can be leveraged to learn distortion-robust representations, which can then be effectively applied to downstream tasks operating on distorted observations. In particular, we propose an asymmetric knowledge distillation framework in which both teacher and student are initialized from the same pretrained Vision Transformer but receive different views of each image: the teacher processes clean images, while the student sees their distorted versions. We introduce multi-level distillation that aligns global embeddings, patch-level features, and attention maps and show that the student is able to approximate clean-image representations despite never directly accessing clean data. We evaluate our approach on image classification tasks across several datasets and under various distortions, consistently outperforming existing alternatives for the same amount of human supervision.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distilling Vision Transformers for Distortion-Robust Representation Learning》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机与背景）
在现实世界的应用（如医疗影像、遥感和自动驾驶）中，获取高质量、干净的图像数据往往成本高昂或不可行，模型通常必须直接处理带有噪声、模糊或像素缺失的失真观测值。现有的自监督学习模型虽然在干净数据上表现优异，但在面对严重失真时，其语义表征能力会大幅下降。
**核心问题：** 能否在仅观察到失真图像的情况下，利用预训练模型的先验知识，恢复出接近干净图像的语义特征空间？本文旨在通过知识蒸馏技术，训练出一种对严重失真具有鲁棒性的视觉编码器。

### 2. 方法论：核心思想与技术细节
论文提出了一种**非对称知识蒸馏框架**，其核心思想是将鲁棒性建模为“潜在空间中的表征恢复问题”。

*   **核心架构：** 采用教师-学生架构。教师模型和学生模型均初始化自相同的预训练 ViT（如 DINO-ViT）。
    *   **教师模型（Teacher）：** 保持冻结，输入为**干净图像**，作为语义基准。
    *   **学生模型（Student）：** 进行参数更新，输入为对应的**失真图像**。
*   **多层级蒸馏目标（Multi-Target Distillation）：**
    1.  **全局语义对齐（Global）：** 通过均方误差（MSE）对齐教师和学生的 `[CLS]` Token，确保全局类别语义的一致性。
    2.  **局部空间对齐（Local）：** 对齐所有位置的补丁（Patch）Token 嵌入，防止失真导致局部空间结构坍塌。
    3.  **注意力结构对齐（Attention）：** 提取最后层中 `[CLS]` 对所有 Patch 的注意力权重，通过 KL 散度对齐教师与学生的注意力图（使用温度参数 $\tau$ 平滑）。这引导学生模型像教师一样关注图像的语义关键区域。
*   **总损失函数：** $L = \lambda_{cls} L_{cls} + \lambda_{patch} L_{patch} + \lambda_{attn} L_{attn}$，其中 $\lambda_{attn}$ 权重较大（设为 50），以平衡量级差异。

### 3. 实验设计
*   **数据集：**
    *   **域内评估：** ImageNet-100（100类子集）。
    *   **迁移学习：** CIFAR-10/100、STL-10（自然图像）、RESISC45（遥感）、CAMELYON17（医疗组织病理学）。
*   **失真类型（Benchmark）：**
    *   随机像素遮蔽（Masking）：比例达 75% 和 90%。
    *   高斯噪声（Gaussian Noise）：标准差 $\sigma$ 为 0.3 和 0.5。
    *   高斯模糊（Gaussian Blur）：核大小达 37。
*   **对比方法：**
    1.  **Supervised Baseline：** 在失真图像上直接进行端到端监督微调。
    2.  **Contrastive Inversion (CI)：** 基于 CLIP-ResNet101 的对比学习恢复方法（Ravula et al., 2021）。

### 4. 资源与算力
*   **硬件：** 使用了 **2 张 NVIDIA RTX A6000 GPU**。
*   **训练细节：** 在 ImageNet-100 上预训练 25 个 epoch，Batch Size 为 128。
*   **优化器：** AdamW，峰值学习率 $10^{-5}$，采用余弦退火和混合精度训练（float16）。

### 5. 实验数量与充分性
论文进行了多维度的详尽实验，体现了高度的客观性与公平性：
*   **多任务覆盖：** 涵盖了域内分类、跨域迁移学习。
*   **消融实验：** 分别验证了全局、局部、注意力蒸馏组件的贡献，证明了三者结合效果最佳。
*   **压力测试：** 测试了模型在超出训练强度的失真水平下的泛化能力（Robustness under increasing distortions）。
*   **标签效率实验：** 测试了在仅有 0.5% 到 100% 标签情况下的微调表现。
*   **可视化分析：** 通过注意力图可视化，直观展示了学生模型在失真输入下依然能精准定位目标。
**评价：** 实验设计严谨，对比基准统一（相同的初始化和数据量），结论具有说服力。

### 6. 主要结论与发现
*   **表征恢复有效性：** 即使在 90% 像素缺失的情况下，学生模型也能通过蒸馏学习到接近干净图像的特征。
*   **性能领先：** 在所有失真类型下，该方法均显著优于监督基线；在迁移学习中，尤其是在遥感和医疗等领域，表现远超基于 CLIP 的方法。
*   **标签高效性：** 仅需 5%-10% 的标签进行微调，性能即可接近全量标签的监督模型，证明了预训练表征的强大语义组织能力。
*   **注意力图的鲁棒性：** 蒸馏后的模型在失真图像上的注意力分布与干净图像高度一致，而监督基线则会发生严重偏移。

### 7. 优点与亮点
*   **非对称设计的巧妙性：** 避开了像素级重建（Denoising）的复杂性，直接在语义空间进行特征恢复。
*   **多级对齐：** 引入注意力图蒸馏，不仅对齐了“结果”（特征），还对齐了“过程”（模型关注哪里）。
*   **无需干净数据训练：** 学生模型在训练过程中从未见过干净图像，这在实际应用中极具价值。
*   **跨域泛化强：** 在与 ImageNet 差异巨大的医疗和遥感数据集上表现稳健。

### 8. 不足与局限
*   **失真特异性：** 目前的方法需要针对每种特定的失真类型（如单独针对噪声或模糊）训练专门的编码器，缺乏一个能同时处理未知或混合失真的通用模型。
*   **合成失真偏差：** 实验主要基于合成的数学失真（高斯噪声等），在面对现实中更复杂的非线性失真（如极端天气、传感器故障）时的表现尚待验证。
*   **计算开销：** 虽然推理时只需学生模型，但训练阶段需要同时运行教师和学生两个 ViT-Base 模型，对显存有一定要求。

（完）

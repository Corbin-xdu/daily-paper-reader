---
title: "Semantic-Fast-SAM: Efficient Semantic Segmenter"
title_zh: Semantic-Fast-SAM：高效语义分割器
authors: Byunghyun Kim
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20169v2"
tags: ["query:cv"]
score: 9.0
evidence: 基于高效CNN的实时语义分割重新实现
tldr: 本研究提出Semantic-Fast-SAM (SFS)，旨在解决SAM模型推理速度慢的问题。SFS结合了高效的CNN架构FastSAM与语义标注策略，在保持高精度的同时实现了实时语义分割。该模型在Cityscapes和ADE20K上表现优异，推理速度比SSA快约20倍，且支持开放词汇分割，显著提升了基础分割模型在机器人等实时场景中的实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20169v2/fig-001.webp\", \"caption\": \"TABLE III SEMANTIC SEGMENTATION PERFORMANCE (MIOU). BEST WITHIN EACH SUPERVISION REGIME IS BOLDED; HIGHER IS BETTER.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-002.webp\", \"caption\": \"TABLE IV ABLATION RESULTS ON OPEN-VOCABULARY FUSION AND MASK COUNT. BEST WITHIN EACH ABLATION BLOCK IS BOLDED.\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-003.webp\", \"caption\": \"TABLE II GPU MEMORY USAGE FOR DIFFERENT METHODS. BEST WITHIN EACH COMPARABLE BLOCK IS BOLDED; LOWER IS BETTER.\", \"page\": 3, \"index\": 3, \"width\": 529, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-004.webp\", \"caption\": \"Fig. 2. Overall architecture of Semantic-Fast-SAM.\", \"page\": 2, \"index\": 4, \"width\": 529, \"height\": 254}]"
motivation: 针对原始SAM模型及其语义标注流程计算成本高、难以满足实时应用需求的问题，寻求一种高效的语义分割方案。
method: 将基于CNN的高效FastSAM掩码生成与语义标注策略相结合，并利用CLIP头实现开放词汇的类别分配。
result: 在Cityscapes和ADE20K上达到了与领先方法相当的精度，且推理速度提升了约20倍，同时在开放词汇任务中表现出色。
conclusion: SFS成功实现了兼具“分割一切”能力与实时性能的语义分割，为机器人等对计算资源敏感的领域提供了实用的基础模型。
---

## 摘要
我们提出了 Semantic-Fast-SAM (SFS)，这是一个将 Fast Segment Anything 模型与语义标注流水线相结合的语义分割框架，旨在不牺牲准确性的前提下实现实时性能。FastSAM 是 Segment Anything Model (SAM) 的一种基于 CNN 的高效重新实现，其运行速度远快于原始的基于 Transformer 的 SAM。基于 FastSAM 的快速掩码生成能力，我们集成了一种语义分割一切 (SSA) 标注策略，为每个掩码分配具有实际意义的类别。由此产生的 SFS 模型能够以原始基于 SAM 方法的一小部分计算成本和内存占用，生成高质量的语义分割图。在 Cityscapes 和 ADE20K 基准测试上的实验表明，SFS 的准确率与之前的基于 SAM 的方法相当（Cityscapes 上的 mIoU 约为 70.33，ADE20K 上约为 48.01），同时在闭集设置下的推理速度比 SSA 快约 20 倍。我们还展示了 SFS 通过利用基于 CLIP 的语义头，能够有效处理开放词汇分割，在广泛类别标注方面优于近期的开放词汇模型。这项工作实现了具有“分割一切”能力的实用实时语义分割，扩展了基础分割模型在机器人场景中的适用性。代码实现可在 https://github.com/KBH00/Semantic-Fast-SAM 获取。

## Abstract
We propose Semantic-Fast-SAM (SFS), a semantic segmentation framework that combines the Fast Segment Anything model with a semantic labeling pipeline to achieve real-time performance without sacrificing accuracy. FastSAM is an efficient CNN-based re-implementation of the Segment Anything Model (SAM) that runs much faster than the original transformer-based SAM. Building upon FastSAM's rapid mask generation, we integrate a Semantic-Segment-Anything (SSA) labeling strategy to assign meaningful categories to each mask. The resulting SFS model produces high-quality semantic segmentation maps at a fraction of the computational cost and memory footprint of the original SAM-based approach. Experiments on Cityscapes and ADE20K benchmarks demonstrate that SFS matches the accuracy of prior SAM-based methods (mIoU ~ 70.33 on Cityscapes and 48.01 on ADE20K) while achieving approximately 20x faster inference than SSA in the closed-set setting. We also show that SFS effectively handles open-vocabulary segmentation by leveraging CLIP-based semantic heads, outperforming recent open-vocabulary models on broad class labeling. This work enables practical real-time semantic segmentation with the "segment-anything" capability, broadening the applicability of foundation segmentation models in robotics scenarios. The implementation is available at https://github.com/KBH00/Semantic-Fast-SAM.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Semantic-Fast-SAM (SFS)** 的高效语义分割框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：原始的 Segment Anything Model (SAM) 虽然具备强大的零样本（zero-shot）分割能力，但存在两个主要缺陷：一是**缺乏语义标签**（仅生成无类别掩码）；二是**计算成本极高**（基于 Transformer 架构，难以实现实时推理）。
*   **研究动机**：现有的改进方案如 Semantic-Segment-Anything (SSA) 虽然解决了标签问题，但因依赖重型 SAM 骨干网络，推理速度极慢。作者旨在结合 **FastSAM**（基于 CNN 的高效 SAM 实现）与 **SSA** 的标注策略，打造一个既能“分割一切”又能实现**实时语义标注**的实用系统，特别针对机器人和自动驾驶等对延迟敏感的场景。

### 2. 论文提出的方法论
SFS 采用两阶段架构，将掩码生成与语义标注解耦：
*   **阶段 A：快速掩码生成（Fast Mask Generation）**
    *   使用 **FastSAM** 作为骨干。它基于 YOLOv8-seg 架构，利用 CNN 的 C2f 模块和 YOLACT 风格的实例分割分支，在单次前向传播中生成全图的类无关掩码（class-agnostic masks）。
*   **阶段 B：多分支语义标注（Semantic Labeling）**
    *   **闭集分支（Closed-Set Branch）**：利用预训练的分割模型（如在 COCO 或 ADE20K 上训练的 OneFormer）。通过对掩码区域进行投票，快速分配已知类别的标签。
    *   **开放词汇分支（Open-Vocabulary Branch）**：针对未知物体，提取掩码区域图像，利用 **BLIP** 生成文本描述，再通过 **CLIP** 计算图像嵌入与候选标签（名词短语）的余弦相似度，实现零样本分类。
*   **融合与决策**：综合两个分支的结果。若闭集分支置信度高则采用闭集标签；若开放词汇分支发现新类别且 CLIP 相似度达标，则更新标签。

### 3. 实验设计
*   **数据集**：
    *   **Cityscapes**：用于评估城市场景下的闭集语义分割性能。
    *   **ADE20K**：用于评估复杂室内外场景的通用分割能力。
*   **基准（Benchmark）与对比方法**：
    *   **效率对比**：对比了 SSA (基于 SAM ViT-H)、OneFormer、FastSAM。
    *   **精度对比**：对比了 SSA、OneFormer、Mask2Former、SegFormer 等全监督模型。
    *   **开放词汇对比**：对比了 CLIPSeg、GroupViT、MaskCLIP 等专门的开放词汇模型。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3090 GPU (24 GB)** 的工作站上完成。
*   **训练时长**：论文指出 SFS 是一个**推理时（Inference-time）流水线**，直接组合了预训练组件，**无需进行额外的模型微调或反向传播训练**。
*   **推理性能**：在闭集模式下，单幅图像处理仅需 **0.08 秒**（约 12.5 FPS）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个主流语义分割数据集的精度测试。
    *   进行了详细的消融实验，探讨了“开放词汇融合”和“掩码数量预算（Top-100/50/25）”对速度与精度的影响。
    *   提供了内存占用（Peak Memory）的对比分析。
*   **充分性评价**：实验设计较为客观，直接对比了 SFS 与其“慢速版本”SSA 的性能差异。虽然实验主要集中在零样本评估上，但通过与全监督模型的对比，证明了其在实际应用中的竞争力。

### 6. 论文的主要结论与发现
*   **速度飞跃**：在闭集设置下，SFS 的推理速度比基于 SAM 的 SSA 快了约 **20 倍**。
*   **精度保持**：尽管使用了更轻量级的 FastSAM，但在 Cityscapes (70.33 mIoU) 和 ADE20K (48.01 mIoU) 上的表现与重型 SAM 模型非常接近，精度损失微乎其微。
*   **内存友好**：峰值显存占用从 SSA 的 19GB 降至 **4.5GB**，使得该模型可以在 8-12GB 显存的消费级 GPU 或嵌入式设备上运行。

### 7. 优点
*   **极高的实用性**：解决了基础模型“好用但太慢”的痛点，真正实现了“分割一切”的实时化。
*   **模块化设计**：无需重新训练，可以灵活更换更先进的闭集分割器或轻量级语言模型。
*   **平衡性好**：在保持开放词汇能力的同时，通过闭集先验保证了常见类别的准确性。

### 8. 不足与局限
*   **开放词汇模式下的瓶颈**：虽然闭集模式极快，但在开启 BLIP/CLIP 的开放词汇模式后，推理时间增加到了 10.24 秒，虽然比 SSA 快，但仍未达到实时水平。
*   **小物体精度**：FastSAM 在处理极小物体时，掩码精度偶尔略逊于原始 SAM。
*   **依赖预训练模型**：性能高度依赖于所选取的 OneFormer 或 CLIP 模型的质量，存在模型偏差传递的风险。

（完）

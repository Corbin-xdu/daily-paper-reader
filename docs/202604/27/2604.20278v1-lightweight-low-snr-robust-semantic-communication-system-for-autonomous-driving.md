---
title: Lightweight Low-SNR-Robust Semantic Communication System for Autonomous Driving
title_zh: 面向自动驾驶的轻量化低信噪比鲁棒语义通信系统
authors: "Ruixing Ren, Minjie Wei, Junhui Zhao"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20278v1"
tags: ["query:cv"]
score: 9.5
evidence: 用于自动驾驶轻量级语义通信的结构化剪枝
tldr: 针对自动驾驶车联网中资源受限及低信噪比环境下通信鲁棒性差的问题，本文提出一种轻量化且鲁棒的深度联合源信道编码（JSCC）语义通信系统。通过结构化剪枝大幅降低模型复杂度，并结合均匀量化与M-QAM调制解决数字兼容性问题。实验表明，该系统在大幅减少参数量的同时，在低信噪比下表现优于传统方法，有效提升了自动驾驶协作感知的通信效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20278v1/fig-001.webp\", \"caption\": \"Fig. 1: Block Diagram of V2V Semantic Communication System for Autonomous Driving\", \"page\": 2, \"index\": 1, \"width\": 505, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.20278v1/fig-002.webp\", \"caption\": \"TABLE I: The parameter settings of the model. Conv: Convolutional Layer. Trans ConV: Transposed convolutional layer.\", \"page\": 5, \"index\": 2, \"width\": 446, \"height\": 279}]"
motivation: 解决自动驾驶协作感知中车载资源有限、无线信道衰落及低信噪比下传统通信方案鲁棒性不足的问题。
method: 采用基于BN层缩放因子和L1正则化的结构化剪枝，并设计了适配JSCC的量化调制方案及训练部署分离策略。
result: 实验证明剪枝后的模型在减少超过一半参数的情况下仍保持高性能，且在低信噪比条件下显著优于传统通信方法。
conclusion: 该系统成功实现了语义通信的轻量化与数字系统兼容性，为低信噪比环境下的自动驾驶提供了高效可靠的通信方案。
---

## 摘要
自动驾驶中车对车协同感知的图像传输面临着车载终端资源有限、时变无线信道衰落以及低信噪比（SNR）下鲁棒性差等挑战。传统的信源信道分离编码方案存在“悬崖效应”，而现有的语义通信模型受限于参数量大且数字兼容性弱。本文提出了一种轻量化、低信噪比鲁棒的深度联合信源信道编码（JSCC）语义通信系统。首先，基于批归一化层缩放因子和L1正则化实现了结构化剪枝，在保证图像重建质量的同时显著降低了模型复杂度。其次，设计了适配JSCC特征的均匀量化和M-QAM调制方案，并采用训练-部署分离策略解决不可微量化问题，实现了与现有数字通信系统的兼容。在Cityscapes数据集上的仿真结果表明，剪枝后的模型在移除超过一半参数的情况下，仍能保持与原模型相当的性能和鲁棒性。值得注意的是，所提方案在低信噪比条件下相比传统通信方法表现出显著优势。

## Abstract
Image transmission for vehicle-to-vehicle collaborative perception in autonomous driving faces challenges including limited on-board terminal resources, time-varying wireless channel fading, and poor robustness under low signal-to-noise (SNR) ratio. Traditional separate source-channel coding schemes suffer from the cliff effect, while existing semantic communication models are limited by large parameter sizes and weak digital compatibility. This paper proposes a lightweight, low-SNR-robust deep joint source-channel coding (JSCC) semantic communication system. First, structured pruning is implemented based on batch normalization layer scaling factors and L1 regularization, which significantly reduces model complexity while ensuring image reconstruction quality. Second, a uniform quantization and M-QAM modulation scheme adapted to JSCC features is designed, and a training-deployment separation strategy is adopted to address the non-differentiable quantization problem, enabling compatibility with existing digital communication systems. Simulation results on the Cityscapes dataset show that the pruned model maintains comparable performance and robustness to the original one, even with over half of its parameters removed. Notably, the proposed scheme exhibits significant advantages over conventional communication methods under low SNR conditions.

---

## 论文详细总结（自动生成）

以下是对论文《Lightweight Low-SNR-Robust Semantic Communication System for Autonomous Driving》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：自动驾驶中的车对车（V2V）协同感知需要实时传输高分辨率图像，但面临车载终端算力/存储受限、无线信道时变衰落以及低信噪比（SNR）环境等严峻挑战。
*   **核心痛点**：
    1.  **传统方案缺陷**：基于信源信道分离编码（如BPG+LDPC）的传统通信在低SNR下存在“悬崖效应”（性能骤降）。
    2.  **现有语义通信局限**：深度联合源信道编码（JSCC）模型通常参数量巨大，难以部署在资源受限的车载设备上；且大多采用模拟传输，与现有的数字通信系统不兼容。
*   **研究目标**：构建一个既能兼容数字系统、又具备轻量化特征且在低SNR下表现鲁棒的语义通信框架。

### 2. 方法论
*   **核心思想**：结合深度联合源信道编码（JSCC）、结构化模型剪枝以及适配数字系统的量化调制技术。
*   **关键技术细节**：
    *   **结构化剪枝（Structured Pruning）**：利用批归一化（BN）层的缩放因子 $\eta$ 作为通道重要性评分。在损失函数中引入 **L1 正则化项** $\lambda \sum |\eta_i|$，迫使不重要的通道缩放因子趋于零。剪枝后通过微调（Fine-tuning）恢复性能。
    *   **数字兼容性设计**：在编码器末端使用 Sigmoid 激活函数将特征限制在 $(0, 1)$ 区间，随后进行 **M级均匀量化** 并映射至 **M-QAM 星座点**。
    *   **训练-部署分离策略**：
        *   **训练阶段**：采用模拟传输（加性高斯白噪声），保证梯度可微，使模型学习到对噪声的内在鲁棒性。
        *   **部署阶段**：引入不可微的量化与解调模块。由于解码器已在训练中适应了噪声，它能有效处理量化误差和解调错误。
*   **算法流程**：先进行稀疏化训练，再根据剪枝率移除通道，最后进行微调，推理时执行“特征提取->量化->调制->信道->解调->重建”。

### 3. 实验设计
*   **数据集**：使用 **Cityscapes** 街景数据集（调整为 512×512 分辨率）。
*   **Benchmark（基准方案）**：
    *   传统分离方案：**BPG（图像压缩）+ LDPC（信道编码）+ QAM（调制）**。
    *   对比模型：未剪枝的 JSCC 模型、直接降低带宽压缩比（k/n）的压缩模型。
*   **对比维度**：不同剪枝率（$\gamma=0, 0.2, 0.5, 0.7, 0.9$）、不同 SNR（0-25 dB）、不同调制阶数（4/64/256-QAM）。

### 4. 资源与算力
*   **算力说明**：文中提到剪枝和训练在服务器端完成，推理在车载端进行。
*   **具体细节**：论文**未明确说明**具体的 GPU 型号、数量以及训练所需的总时长。

### 5. 实验数量与充分性
*   **实验规模**：做了多组对比实验，涵盖了从极低 SNR 到高 SNR 的全量程扫描，并对比了 PSNR（峰值信噪比）和 SSIM（结构相似性）两个核心指标。
*   **充分性评价**：实验设计较为**充分且客观**。通过设置相同的带宽压缩比（k/n=2/3）确保了与传统方案对比的公平性；同时通过消融实验对比了“结构化剪枝”与“直接降低压缩比”的效果，证明了剪枝策略的优越性。

### 6. 主要结论与发现
*   **轻量化成效**：在移除 **50% 以上参数**（$\gamma=0.5$）的情况下，模型的 PSNR 和 SSIM 几乎没有下降，证明了 JSCC 模型中存在大量冗余。
*   **低SNR鲁棒性**：在 SNR 低于 14.5 dB 时，所提方案远优于传统 BPG+LDPC 方案，后者在低 SNR 下完全失效（PSNR 趋近 0），而所提方案仍能重建清晰轮廓。
*   **数字兼容性**：采用 256-QAM 的数字版本性能非常接近理想的模拟传输版本，且由于数字硬决策的去噪作用，在某些低 SNR 点甚至表现更优。

### 7. 优点
*   **结构化剪枝的实用性**：相比非结构化剪枝，结构化剪枝直接减少通道数，无需特殊硬件即可在车载终端实现真实的内存节省和推理加速。
*   **解决不可微问题**：通过训练-部署分离策略，巧妙解决了深度学习模型与数字量化调制结合时的梯度回传难题。
*   **抗悬崖效应**：成功实现了在恶劣信道环境下的平滑性能退化，而非突然崩溃。

### 8. 不足与局限
*   **任务覆盖有限**：实验仅关注图像重建（像素级恢复），未验证该轻量化模型对自动驾驶下游任务（如目标检测、避障决策）的直接影响。
*   **环境泛化性**：仅在 Cityscapes 数据集上验证，未测试在雨、雪、雾等极端天气或高速动态场景下的表现。
*   **缺乏实测延迟**：虽然给出了 MACs（计算量）和参数量，但缺乏在真实车载嵌入式平台（如 NVIDIA Orin 或华为 MDC）上的端到端时延实测数据。

（完）

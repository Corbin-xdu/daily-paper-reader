---
title: Leveraging Kernel Symmetry for Joint Compression and Error Mitigation in Edge Model Transfer
title_zh: 利用卷积核对称性实现边缘模型传输中的联合压缩与误差缓解
authors: "Anis Hamadouche, Mathini Sellathurai"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17371v1"
tags: ["query:cv"]
score: 9.0
evidence: 利用卷积核对称性进行边缘模型传输的联合压缩
tldr: 本研究针对边缘模型传输中的通信效率问题，提出了一种利用卷积核结构对称性的压缩与误差缓解框架。通过仅传输对称群隐含的独立自由度系数，并在接收端利用投影步骤强制执行对称一致性，实现了确定性模型重建与去噪。实验表明，该方法在带宽大幅降低的情况下，其准确率显著优于剪枝基准，尤其在噪声环境下表现出更强的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17371v1/fig-001.webp\", \"caption\": \"Fig. 1: Orbits in a 5 × 5 convolution kernel under different symmetry groups. Each panel displays the orbit ID assigned to index (i, j) (darker/lighter cells denote different IDs). Entries with the same ID belong to the same orbit and are tied to a single degree of freedom. In (b), the center element is constrained by antisymmetry and marked with ×.\", \"page\": 6, \"index\": 1, \"width\": 367, \"height\": 531}]"
motivation: 旨在解决边缘计算场景下神经网络模型传输的高带宽需求以及信道噪声导致的性能下降问题。
method: 提出一种基于自由度的编解码器，仅传输唯一的对称系数，并结合接收端的对称子空间投影进行误差缓解。
result: 在MNIST和CIFAR-10数据集上，该方法在大幅减少带宽的同时，准确率显著高于传统的剪枝方案。
conclusion: 利用卷积核的结构冗余（特别是中心斜对称性）可以有效实现高效且可靠的神经网络模型远程交付。
---

## 摘要
本文通过利用卷积核中的结构化对称约束，研究了通信高效的神经网络传输。我们提出了一种基于自由度（DoF）的编解码器，不再传输所有模型参数，而是仅发送由选定对称群隐含的唯一系数，从而使接收端能够确定性地重建完整的权重张量。该框架在多种对称模式、信噪比和位宽下的量化及噪声信道条件下进行了评估。为了提高对传输损伤的鲁棒性，接收端进一步应用了投影步骤，以强制与对称不变子空间保持一致，从而有效地对受损参数进行去噪。在 MNIST 和 CIFAR-10 数据集上使用 DeepCNN 架构的实验结果表明，基于自由度的传输在实现大幅带宽缩减的同时，保持了显著高于基于剪枝的基准方法的准确率，而后者往往会遭受灾难性的性能下降。在测试的对称性中，中心斜对称（central-skew symmetry）始终提供最佳的准确率-压缩权衡，证实了可以利用结构化冗余在受限链路上实现可靠且高效的神经模型交付。

## Abstract
This paper investigates communication-efficient neural network transmission by exploiting structured symmetry constraints in convolutional kernels. Instead of transmitting all model parameters, we propose a degrees-of-freedom (DoF) based codec that sends only the unique coefficients implied by a chosen symmetry group, enabling deterministic reconstruction of the full weight tensor at the receiver. The proposed framework is evaluated under quantization and noisy channel conditions across multiple symmetry patterns, signal-to-noise ratios, and bit-widths. To improve robustness against transmission impairments, a projection step is further applied at the receiver to enforce consistency with the symmetry-invariant subspace, effectively denoising corrupted parameters. Experimental results on MNIST and CIFAR-10 using a DeepCNN architecture demonstrate that DoF-based transmission achieves substantial bandwidth reduction while preserving significantly higher accuracy than pruning-based baselines, which often suffer catastrophic degradation. Among the tested symmetries, \textit{central-skew symmetry} consistently provides the best accuracy-compression tradeoff, confirming that structured redundancy can be leveraged for reliable and efficient neural model delivery over constrained links.

---

## 论文详细总结（自动生成）

这篇论文由赫瑞-瓦特大学的研究人员撰写，探讨了在边缘计算和无线通信环境下，如何利用卷积神经网络（CNN）内核的对称性来优化模型传输。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在边缘智能和联邦学习（FL）场景中，在带宽受限且具有噪声的无线链路上分发深度学习模型是一个重大挑战。
*   **研究动机**：传统的模型压缩方法（如剪枝、量化）虽然能减少数据量，但在面对无线信道干扰（位错误）时往往表现脆弱。
*   **核心问题**：如何同时实现高效的模型压缩（减少传输比特数）和增强模型对传输误差的鲁棒性（纠错/去噪）。
*   **整体含义**：论文提出利用卷积核内在的结构化对称性（如旋转、镜像等），将其作为一种“归纳偏置”和“冗余来源”，在减少传输参数量的同时，利用对称约束在接收端对受损信号进行去噪。

### 2. 论文提出的方法论
核心思想是将卷积核的对称性转化为通信优势，通过**自由度（DoF）编解码器**和**对称投影**实现。

*   **轨道（Orbits）与自由度**：对称性将卷积核的元素划分为不同的“轨道”。同一轨道内的元素根据对称规则是相等或互为相反数的。因此，传输时只需发送每个轨道的代表值（DoF），从而实现压缩。
*   **DoF 编解码器**：
    *   **发送端**：提取独立自由度系数，进行量化并打包。
    *   **接收端**：根据预定义的对称模式重建完整卷积核。
*   **对称投影（Symmetry Projection）**：
    *   在训练阶段，通过投影算子 $P_S$ 强制内核满足对称性。
    *   在接收端，再次应用投影。其关键数学原理是：通过对轨道内多个受损样本求平均，可以降低信道噪声的方差（方差缩减比例与轨道大小成正比），起到隐式纠错的作用。
*   **中心斜对称（Central-skew Symmetry）**：论文特别设计了一种反对称构造，在实验中表现出最佳的性能平衡。

### 3. 实验设计
*   **数据集**：MNIST（手写数字识别）和 CIFAR-10（物体分类）。
*   **实验场景**：模拟 AWGN（加性高斯白噪声）信道，采用 BPSK 调制，设定信噪比 SNR = 10 dB，使用 8-bit 量化。
*   **Benchmark（基准）**：
    1.  **None**：无对称约束的完整模型传输。
    2.  **Pruned Equivalent（剪枝等效）**：使用基于幅度的剪枝，使保留的参数量与对称模型的 DoF 数量一致，并考虑了 COO 格式存储索引带来的额外开销。
*   **对比维度**：传输准确率（Acc Rx）、带宽缩减率、端到端延迟、有效载荷大小。

### 4. 资源与算力
*   **训练配置**：MNIST 训练 8 个 epoch，CIFAR-10 训练 30 个 epoch，batch size 均为 128。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。但从其使用的 DeepCNN 架构和数据集规模来看，计算需求属于中轻量级。

### 5. 实验数量与充分性
*   **实验规模**：测试了 10 种不同的对称模式（包括水平、垂直、对角线、旋转 90 度、径向、Toeplitz 等）。
*   **充分性**：每组实验均独立运行 10 次并取平均值（附带标准差），涵盖了从简单到复杂的两种数据集。
*   **客观性**：对比剪枝基准时，显式计算了剪枝索引（Index）占用的带宽，这在无线通信研究中是非常公平且客观的做法，因为索引在传输中同样会受损。

### 6. 主要结论与发现
*   **压缩效率**：基于 DoF 的传输在保持准确率的同时，最高可减少约 **66.67%** 的有效载荷。
*   **鲁棒性优势**：在 10dB SNR 下，对称模型（尤其是**中心斜对称**）的接收准确率远高于剪枝模型。剪枝模型在无线环境下经常出现“准确率崩溃”（如从 98% 掉到 9%），而对称模型表现稳定。
*   **最佳模式**：**中心斜对称（Central-skew）**被证明是最佳的权衡点，在 MNIST 上实现了 +5.53 pp 的准确率增益（相比 baseline），同时减少了 55.56% 的带宽。
*   **去噪效果**：接收端的对称投影能有效消除信道引入的位错误干扰。

### 7. 优点
*   **双效合一**：巧妙地将深度学习的结构化偏置（对称性）与通信领域的源编码（压缩）和信道编码（纠错）思想结合。
*   **无需额外开销**：与剪枝不同，对称性是预定义的结构，不需要传输复杂的索引图，降低了协议开销。
*   **理论支撑**：提供了关于轨道大小与方差缩减之间关系的理论分析。

### 8. 不足与局限
*   **信道模型较单一**：实验仅基于内存无记忆的 AWGN 信道，未考虑更复杂的衰落信道（Fading）或突发错误（Burst errors）。
*   **模型架构局限**：实验仅在小型 DeepCNN 上进行，尚未在 ResNet、ViT 等现代主流大模型上验证其对称性的有效性。
*   **任务范围**：目前仅限于图像分类任务，未探讨在回归、检测或生成任务中的表现。
*   **对称性限制**：强制对称可能会引入一定的模型偏差（Bias），对于某些本身不具备对称特征的数据集，可能会导致初始训练准确率略低于无约束模型。

（完）

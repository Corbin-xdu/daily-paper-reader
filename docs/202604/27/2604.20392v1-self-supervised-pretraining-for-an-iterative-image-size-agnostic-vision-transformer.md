---
title: Self-supervised pretraining for an iterative image size agnostic vision transformer
title_zh: 针对迭代式图像尺寸无关视觉 Transformer 的自监督预训练
authors: "Nedyalko Prisadnikov, Danda Pani Paudel, Yuqian Fu, Luc Van Gool"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20392v1"
tags: ["query:cv"]
score: 9.0
evidence: 具有自蒸馏机制的迭代式图像尺寸无关视觉Transformer
tldr: 针对视觉Transformer（ViT）在处理大尺寸图像时计算效率低且难以扩展的问题，本文提出了一种基于自监督学习（SSL）的迭代式图像尺寸无关视觉Transformer预训练框架。该方法借鉴仿生视网膜机制，通过迭代处理固定大小的多尺度补丁，并结合改进的DINO自蒸馏目标和高效的积分图像补丁提取技术，实现了在恒定计算预算下处理任意分辨率图像的能力，在ImageNet-1K等任务上表现出色。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20392v1/fig-001.webp\", \"caption\": \"Fig. 3: Main architecture for sequential-to-global pretraining. The student network iteratively updates its state tokens, computing the distillation loss at each step against a static teacher embedding. Stop-gradients prevent backpropagation through time.\", \"page\": 6, \"index\": 1, \"width\": 688, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.20392v1/fig-002.webp\", \"caption\": \"Fig. 4: Combining multi-zoom patches with focused grid of patches for informative context for the transformer. a) Shows the input image with the bounding-boxes of the multi-zoom crops (blue) and the foveal grid crops (red) overlayed, b) shows the extracted grid patches, and c) shows the multi-zoom patches. Note that the patches are allowed to span outside the image area by filling them with zero padding.\", \"page\": 8, \"index\": 2, \"width\": 583, \"height\": 228}]"
motivation: 传统的ViT在自监督预训练中受限于低分辨率，且计算量随图像尺寸增加而剧增，难以作为处理高分辨率图像的基础模型。
method: 提出一种基于DINO自蒸馏的序列到全局SSL框架，利用迭代处理多尺度补丁的架构和积分图像补丁提取法实现分辨率无关性。
result: 在ImageNet-1K及下游分类任务中取得了具有竞争力的性能，且计算开销不随输入图像分辨率的增加而改变。
conclusion: 该研究证明了通过迭代式架构和自监督学习，可以构建出既高效又具备分辨率鲁棒性的通用视觉编码器。
---

## 摘要
视觉 Transformer (ViT) 在自监督学习 (SSL) 领域占据主导地位。虽然它们已被证明在大规模预训练中非常有效，但其计算效率较低，且随图像尺寸的扩展性较差。因此，像 DINO 这样的基础模型被限制在低分辨率处理上。最近一种受视网膜中央凹启发的 Transformer 通过迭代处理固定大小的多缩放补丁上下文，实现了分辨率无关性。该模型通过监督学习展示了良好的前景，利用了无需随时间反向传播的序列化、类循环过程。为了释放其作为基础骨干网络的潜力，我们引入了一种基于 DINO 自蒸馏目标的新型序列到全局 SSL 框架。在高效的积分图像补丁提取方法的支持下，我们的方法实现了图像尺寸无关视觉编码器的大规模预训练。我们在 ImageNet-1K 和下游分类任务上取得了具有竞争力的性能，且无论输入分辨率如何，都能保持恒定的计算预算。

## Abstract
Vision Transformers (ViTs) dominate self-supervised learning (SSL). While they have proven highly effective for large-scale pretraining, they are computationally inefficient and scale poorly with image size. Consequently, foundational models like DINO are constrained to low-resolution processing. A recent foveal-inspired transformer achieves resolution agnosticism by iteratively processing a fixed-size context of multi-zoom patches. This model demonstrated promising results via supervised learning, utilizing a sequential, recurrent-like process without backpropagation through time. To unlock its potential as a foundational backbone, we introduce a novel sequential-to-global SSL framework based on DINO's self-distillation objective. Supported by an efficient integral-image patch extraction method, our approach enables large-scale pretraining for image-size agnostic vision encoders. We achieve competitive performance on ImageNet-1K and downstream classification tasks, maintaining a constant computational budget regardless of input resolution.

---

## 论文详细总结（自动生成）

这是一份关于论文《Self-supervised pretraining for an iterative image size agnostic vision transformer》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的视觉 Transformer (ViT) 在处理高分辨率图像时面临两个瓶颈：一是计算复杂度随图像尺寸呈平方级增长；二是由于采用固定像素大小的补丁（如 16x16），在不同分辨率下补丁包含的语义信息会发生偏移（分布偏移），导致模型在超高分辨率下性能崩溃。
*   **研究背景**：现有的自监督学习（SSL）模型（如 DINO）通常被限制在低分辨率（如 224x224）预训练。为了使 ViT 成为真正的图像尺寸无关（Image-size Agnostic）基础模型，需要一种既能保持恒定计算量，又能通过自监督方式学习全局表征的架构。

### 2. 方法论
该论文提出了一种**序列到全局（Sequential-to-Global）的自监督预训练框架**，核心思想是模拟人类视觉的“中央凹”机制，通过多次迭代观察局部细节来构建全局理解。
*   **核心架构**：基于迭代式视觉 Transformer，包含多个可学习的 `CLS` 状态令牌（State Tokens）作为内部记忆。模型在每一步处理固定数量的“多缩放补丁”（Multi-zoom Patches），并更新状态令牌。
*   **关键技术细节**：
    *   **序列到全局蒸馏**：改进 DINO 目标。学生网络通过 8 步迭代处理局部视图，每一步的输出状态都尝试匹配教师网络（处理全局视图）的静态嵌入。
    *   **停止梯度（No BPTT）**：在迭代步骤之间切断梯度流，避免了随时间反向传播（BPTT）带来的巨大计算开销。
    *   **密集中央凹网格（Foveal Grid）**：在注视点周围提取固定比例（而非固定像素）的密集补丁网格，提供高分辨率上下文。
    *   **积分图像（Integral Image）提取法**：利用积分图像技术，将任意尺寸补丁的提取和缩放复杂度降低到 $O(1)$（相对于输入图像尺寸），彻底解决了预训练中的数据加载瓶颈。
    *   **强化学习策略**：使用 GRPO（群组相对策略优化）算法训练注视策略（Gaze Policy），让模型学会“该看哪里”以最大化分类置信度。

### 3. 实验设计
*   **数据集**：主要在 **ImageNet-1K** 上进行预训练和线性评估。为了验证泛化性，还使用了细粒度分类数据集 **CUB-200-2011**（鸟类）和 **Oxford 102 Flowers**（花卉）。
*   **Benchmark 与对比方法**：
    *   对比了标准的 **DINOv1**（ViT-Small 架构）。
    *   对比了之前的**监督学习版本**的迭代式模型。
    *   评估模式包括：标准 ViT 模式（全局处理）、随机注视模式（Random Gazes）和学习策略模式（Learned Policy）。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 **Google Cloud Platform (GCP)** 提供的计算资源。
*   **具体参数**：预训练在 ImageNet-1K 上进行，Batch Size 为 1024。
*   **训练时长**：主实验未明确说明具体的 GPU 型号和总时长，但在消融实验中提到使用了“轻量级设置”（100 epoch）以加速实验过程。

### 5. 实验数量与充分性
*   **实验规模**：
    *   完成了 ImageNet-1K 的完整预训练及线性探测评估。
    *   进行了跨分辨率评估（从 256 到 2048 分辨率），验证了模型对大尺寸图像的鲁棒性。
    *   **消融实验**：针对“中央凹网格大小”和“状态令牌数量”进行了详细对比。
*   **充分性评价**：实验设计较为充分，特别是通过对比“随机注视”与“学习策略”，客观证明了主动视觉策略的有效性。同时，跨分辨率的对比实验有力地支持了其“图像尺寸无关”的核心主张。

### 6. 主要结论与发现
*   **性能竞争性**：在 ViT 模式下，ViT-Small 等效模型达到 76.1% 的 Top-1 准确率，与 DINO 相当。
*   **迭代有效性**：在 8 步迭代模式下，通过学习到的策略可达到 75.0% 的准确率，远超随机注视（71.7%）。
*   **分辨率鲁棒性**：当评估分辨率提升至 1024 以上时，标准 ViT 性能急剧下降，而该模型保持了稳定的性能，在超高分辨率下比监督基线高出 10% 以上。
*   **计算效率**：模型维持了严格的 $O(1)$ 计算足迹（相对于图像像素数），推理延迟不随分辨率增加而显著波动。

### 7. 优点
*   **架构创新**：成功将 DINO 的空间蒸馏扩展到了时间/序列维度，且无需昂贵的 BPTT。
*   **工程优化**：引入积分图像技术解决了高分辨率补丁提取的效率问题，具有很强的实用价值。
*   **生物启发**：较好地模拟了人类视觉从局部到全局的认知过程，为处理超大分辨率图像（如卫星图、医疗影像）提供了新思路。

### 8. 不足与局限
*   **训练阶段耦合**：任务头（Task Head）和注视策略（Policy）是顺序训练的。如果任务头在随机注视下没学好，策略网络就无法获得高质量的奖励信号，存在“鸡生蛋”的优化难题。
*   **性能微差距**：虽然迭代模式很高效，但在标准分辨率下，其绝对准确率仍略低于全图处理的 ViT 模式。
*   **应用限制**：目前主要验证了分类任务，在需要精细空间坐标的检测或分割任务上的表现尚待验证。

（完）

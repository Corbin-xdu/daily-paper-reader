---
title: "ESsEN: Training Compact Discriminative Vision-Language Transformers in a Low-Resource Setting"
title_zh: ESsEN：在低资源环境下训练紧凑型判别式视觉-语言 Transformer
authors: "Clayton Fields, Casey Kennington"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18452v1"
tags: ["query:cv"]
score: 9.0
evidence: 在低资源环境下训练紧凑型视觉语言Transformer
tldr: 针对边缘设备对轻量化视觉语言模型的需求，本文受儿童语言学习启发，研究了低资源环境下的模型训练。提出了ESsEN模型，通过结合双塔编码器架构与传统卷积网络，在大幅减少参数量的同时保持了高性能，为低资源视觉语言建模提供了高效解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18452v1/fig-001.webp\", \"caption\": \"Table 4: Results Auxiliary Architecture Study Parameter counts are for encoder modules without classification heads. All values are derived from the dev split for each task.\", \"page\": 7, \"index\": 1, \"width\": 667, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2604.18452v1/fig-002.webp\", \"caption\": \"Figure 1: Simple visual representation of two-tower and one-tower encoder models.\", \"page\": 3, \"index\": 2, \"width\": 461, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2604.18452v1/fig-003.webp\", \"caption\": \"Table 2: Results for Part 1 of Backbone Architecture Study using an image encoders with traditional transformer architectures. Parameter counts are for encoder modules without classification heads. All values are derived from the dev split for each task.\", \"page\": 5, \"index\": 3, \"width\": 663, \"height\": 192}]"
motivation: 现有的视觉语言模型参数量巨大且训练资源消耗高，难以应用于边缘设备或独立机器人平台。
method: 采用双塔编码器架构，并融入卷积神经网络以提升参数效率，同时探索了跨模态融合模块的多样化设计。
result: 实验表明双塔架构在低资源判别任务中优于单塔，且ESsEN模型仅需极少参数即可达到与其他大型模型相当的性能。
conclusion: 本研究证明了紧凑型视觉语言模型在低资源下训练的可行性，降低了该领域的研究门槛。
---

## 摘要
视觉-语言建模正迅速普及，可用模型列表不断扩大。在大多数情况下，这些视觉-语言模型的参数量达到数百亿，这对于某些需求是必要的，但在许多情况下（例如在边缘设备或独立机器人平台上），更小的模型是必需的。遗憾的是，关于生产轻量级模型或使用小数据集训练它们的研究很少。受儿童发育过程中语言学习进程和数据稀疏性的启发，本文以系统的方式实现了这两个目标。我们证明，在低资源环境下处理判别式英语任务时，双塔编码器模型优于单塔编码器。我们还表明，将传统的卷积网络整合到双塔 Transformer 架构中，有助于产生参数高效的视觉-语言模型。最后，我们展示了双塔编码器的跨模态融合模块在产生相同结果的同时，其形状和大小可以有显著差异。此外，我们提出了 ESsEN，这是一种紧凑的视觉-语言模型，可以用相对较少的资源进行端到端训练，在多项任务上的表现与其它模型相当，而参数量仅为其它模型的一小部分。我们在此展示的实验结果和工具使视觉-语言建模能够被更广泛的研究人员所使用。

## Abstract
Vision-language modeling is rapidly increasing in popularity with an ever expanding list of available models. In most cases, these vision-language models have parameters in the tens of billions, which is necessary for some needs, but in many cases smaller models are necessary (e.g., on edge devices or independent robotic platforms). Unfortunately, there is little research in producing light-weight models or in training them with small datasets. Inspired by the language learning progression and data sparsity in child development, in this paper, we address both of these goals in a systematic fashion. We show that two-tower encoder models are superior to one-tower encoders in low-resource settings for discriminative English tasks. We show also that incorporating traditional convolutional networks into the two-tower transformer architecture can help produce parameter efficient vision-language models. Finally, we show that the cross-modal fusion module of two-tower encoders can vary significantly in shape and size while producing the same results. In addition, we present ESsEN, a compact vision-language model that can be trained end-to-end with relatively few resources that performs as well on several tasks with only a fraction of the parameters compared to other models. The experimental results and the tools we present here make vision-language modeling more accessible to a wider variety of researchers.

---

## 论文详细总结（自动生成）

这是一份关于论文《ESsEN: Training Compact Discriminative Vision-Language Transformers in a Low-Resource Setting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的视觉-语言（VL）模型通常拥有数百亿参数，且依赖海量互联网抓取的数据集进行训练，这导致其在边缘设备或独立机器人平台上的部署变得极其困难。
*   **研究动机**：
    *   **资源受限**：大型模型对算力、存储和电力的需求在商业和科研上往往不可持续。
    *   **发育启发**：受儿童语言习得启发（儿童仅需约500万词汇量即可掌握基本语言），研究者希望探索在“低资源”和“小数据集”环境下，如何训练出高效的判别式（非生成式）VL模型。
    *   **学术门槛**：降低VL研究的硬件门槛，使更多资源有限的研究者能参与其中。

### 2. 论文提出的方法论
*   **核心思想**：通过系统性实验验证，结合**双塔编码器架构**与**传统卷积神经网络（CNN）**，构建一个参数量极小但性能稳健的VL模型。
*   **关键技术细节**：
    *   **架构选择**：对比了单塔（One-tower，图文混合输入）与双塔（Two-tower，图文独立编码后融合）架构，最终采用双塔结构。
    *   **ESsEN 模型构成**：
        *   **文本塔**：采用 **ELECTRA-Small**（约13.7M参数），因其在小型模型中表现出的高效判别能力。
        *   **视觉塔**：创新性地引入了 **EfficientNet-b2**（CNN架构，约7.7M参数），而非传统的视觉 Transformer（ViT），以提升参数效率。
        *   **融合模块**：使用 6 层跨模态 Transformer 层，通过跨注意力（Cross-attention）机制融合双塔特征。
    *   **预训练任务**：采用标准的掩码语言模型（MLM）和图像-文本匹配（ITM）进行端到端训练。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：仅使用 MSCOCO 和 Visual Genome，合并后约 600 万个图像-文本对，但仅包含 18 万张唯一图像（约 30GB 数据），远小于主流模型使用的亿级数据集。
    *   **微调与评估（Benchmark）**：
        1.  **NLVR2**：视觉推理任务（判断关于两张图的描述是否正确）。
        2.  **SNLI-VE**：视觉蕴含任务（判断图文关系：蕴含、矛盾或中立）。
        3.  **RefCOCO**：指代理解任务（在图像中定位文字描述的特定物体）。
*   **对比方法**：
    *   **内部对比**：不同规模的单塔 vs 双塔模型；不同骨干网络（DeiT, Swin, ResNet, EfficientNet）。
    *   **外部对比**：MiniVLM（52M参数）、PixelBERT（110M参数）以及传统的 WAC 模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 块 NVIDIA L40s GPU**。
*   **训练细节**：
    *   预训练步数：实验过程中分别进行了 50k、100k 和最终的 150k 步训练。
    *   批大小（Batch Size）：根据模型大小在 512 到 704 之间调整。
    *   学习率：统一使用 1e-4。
*   **评价**：该配置属于典型的实验室级/个人研究级算力，验证了模型在低资源下的可训练性。

### 5. 实验数量与充分性
*   **实验组数**：论文进行了三阶段系统实验：
    1.  **架构对比**：4 组模型对比（单塔 vs 双塔，不同参数量级）。
    2.  **骨干网络搜索**：9 组 Transformer 组合 + 3 组异构网络（CNN）组合。
    3.  **辅助架构搜索**：6 组关于融合层深度（4-12层）和宽度（维度192-384）的消融实验。
*   **充分性评价**：实验设计逻辑严密，遵循“先定大框架，再选骨干，最后微调组件”的流程。通过控制变量法确保了对比的公平性（如在相同参数量级下对比单塔与双塔）。

### 6. 论文的主要结论与发现
*   **双塔胜出**：在低资源环境下，双塔编码器在 NLVR2 和 RefCOCO 等任务上的表现显著优于单塔编码器（领先幅度达两位数百分点）。
*   **CNN 的价值**：在小型 VL 模型中，使用 EfficientNet 等 CNN 作为视觉编码器，比同等规模的 ViT 变体更有效。
*   **融合层冗余性**：跨模态融合层的层数和维度在一定范围内变化时，对最终下游任务的准确率影响较小，这暗示了融合模块存在参数压缩的空间。
*   **ESsEN 性能**：最终提出的 ESsEN 模型（39.2M 参数）在 NLVR2 上的表现（71.1%）接近参数量大其数倍的模型（如 PixelBERT 的 71.7%）。

### 7. 优点（亮点）
*   **极高的参数效率**：ESsEN 仅用约 40M 的参数量就达到了主流轻量级模型的性能。
*   **端到端可训练性**：不依赖复杂的知识蒸馏或剪枝技术，直接从随机初始化或标准预训练权重开始端到端训练。
*   **跨架构融合**：成功证明了 CNN（EfficientNet）与 Transformer（ELECTRA）在双塔 VL 架构中的互补性。
*   **开源贡献**：提供了 Renaissance 建模平台，降低了 VL 研究的门槛。

### 8. 不足与局限
*   **任务局限性**：仅关注判别式任务（分类、匹配、定位），未涉及当前主流的生成式任务（如图像描述生成、多模态对话）。
*   **性能上限**：虽然在同类小型模型中领先，但与百亿参数的 SOTA 模型相比，绝对精度仍有较大差距。
*   **数据多样性**：预训练数据仅限于英文和特定数据集，未验证在多语言或更广泛领域数据上的泛化能力。
*   **偏差风险**：由于数据集较小且来源于特定标注，模型可能存在与训练数据相关的社会偏见或分布偏差。

（完）

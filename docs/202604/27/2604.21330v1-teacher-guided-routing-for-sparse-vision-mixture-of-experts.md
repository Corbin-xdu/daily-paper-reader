---
title: Teacher-Guided Routing for Sparse Vision Mixture-of-Experts
title_zh: 稀疏视觉混合专家模型的教师引导路由
authors: "Masahiro Kada, Ryota Yoshihashi, Satoshi Ikehata, Rei Kawakami, Ikuro Sato"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21330v1"
tags: ["query:cv"]
score: 8.5
evidence: 用于高效稀疏视觉混合专家的教师引导路由
tldr: 针对稀疏混合专家模型（MoE）在训练中因梯度阻断导致的路由不稳定和优化困难问题，本文提出了TGR-MoE。该方法利用预训练稠密教师模型的中间表示构建教师路由，为学生路由提供伪监督信号。通过这种教师引导机制，TGR-MoE在训练初期就能实现知识引导的专家选择，显著提升了路由一致性和模型准确率，在ImageNet-1K等数据集上表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21330v1/fig-001.webp\", \"caption\": \"Table 2. Comparison of Top-1 accuracy (%) across datasets and model scales. For V-MoE and the proposed TGR-MoE, results are reported as K=1 / K=2 (left / right) in top-K routing. Models with K = 1 and K = 2 are trained independently. For all other baselines without a slash, the reported values correspond to K=1 or an equivalent computational cost.\", \"page\": 6, \"index\": 1, \"width\": 964, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2604.21330v1/fig-002.webp\", \"caption\": \"Figure 3. Comparison of routing consistency during training. (Top) Agreement rate with the final epoch. (Bottom) Agreement rate between consecutive epochs, evaluated every 5 epochs. The proposed method significantly reduces routing fluctuations and achieves more stable expert selection, averaged over all MoE layers.\", \"page\": 6, \"index\": 2, \"width\": 496, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.21330v1/fig-003.webp\", \"caption\": \"Figure 2. Detailed architecture of the proposed TGR-MoE, illustrated for layer i. The teacher provides a routing signal Ldistill to guide the student router, with the additional balancing loss Lload and the regularization loss Lent.\", \"page\": 3, \"index\": 3, \"width\": 494, \"height\": 276}]"
motivation: 稀疏MoE路由器的梯度仅来源于选中的专家，导致梯度阻断和路由分配频繁波动，难以学习到最优的专家选择策略。
method: 提出TGR-MoE，利用预训练稠密教师模型的中间特征生成路由伪标签，对学生路由器的专家选择进行监督和稳定。
result: 在ImageNet-1K和CIFAR-100上的实验表明，该方法在保持推理速度的同时，显著提高了分类精度和路由稳定性。
conclusion: 教师引导的路由机制能有效解决稀疏MoE的训练不稳定问题，是提升大规模稀疏模型性能的有效手段。
---

## 摘要
深度学习的最新进展由日益增长的模型规模所驱动，但随之而来的计算成本已成为关键瓶颈。稀疏混合专家（MoE）通过为每个输入仅激活一小部分专家，提供了一种有效的解决方案，在不牺牲推理速度的情况下实现了高扩展性。尽管有效，但稀疏 MoE 的训练存在特有的优化困难。由于路由仅通过前向传播中选定的专家接收有效梯度，它会遭受梯度阻塞，且从未选路径中获取的信息极少。这种有限且高度局部化的反馈使得路由难以学习到合适的专家选择评分，并常导致不稳定的路由动态，例如训练期间专家分配的剧烈波动。为了解决这一问题，我们提出了 TGR-MoE：稀疏视觉混合专家模型的教师引导路由。这是一种简单而有效的方法，利用来自预训练稠密教师模型的监督信号来稳定路由学习。TGR-MoE 从教师模型的中间表示构建教师路由，并将其路由输出作为学生路由的伪监督，从而抑制训练过程中频繁的路由波动，并实现从训练初期开始的知识引导型专家选择。在 ImageNet-1K 和 CIFAR-100 上的广泛实验表明，TGR 一致地提高了准确性和路由一致性，同时即使在高度稀疏的配置下也能保持稳定的训练。

## Abstract
Recent progress in deep learning has been driven by increasingly large-scale models, but the resulting computational cost has become a critical bottleneck. Sparse Mixture of Experts (MoE) offers an effective solution by activating only a small subset of experts for each input, achieving high scalability without sacrificing inference speed. Although effective, sparse MoE training exhibits characteristic optimization difficulties. Because the router receives informative gradients only through the experts selected in the forward pass, it suffers from gradient blocking and obtains little information from unselected routes. This limited, highly localized feedback makes it difficult for the router to learn appropriate expert-selection scores and often leads to unstable routing dynamics, such as fluctuating expert assignments during training. To address this issue, we propose TGR-MoE: Teacher-Guided Routing for Sparse Vision Mixture-of-Experts, a simple yet effective method that stabilizes router learning using supervision derived from a pretrained dense teacher model. TGR-MoE constructs a teacher router from the teacher's intermediate representations and uses its routing outputs as pseudo-supervision for the student router, suppressing frequent routing fluctuations during training and enabling knowledge-guided expert selection from the early stages of training. Extensive experiments on ImageNet-1K and CIFAR-100 demonstrate that TGR consistently improves both accuracy and routing consistency, while maintaining stable training even under highly sparse configurations.
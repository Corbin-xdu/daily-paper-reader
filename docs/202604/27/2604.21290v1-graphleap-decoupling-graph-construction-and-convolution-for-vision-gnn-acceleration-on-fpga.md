---
title: "GraphLeap: Decoupling Graph Construction and Convolution for Vision GNN Acceleration on FPGA"
title_zh: GraphLeap：解耦图构建与卷积以在 FPGA 上加速视觉图神经网络
authors: "Anvitha Ramachandran, Dhruv Parikh, Viktor Prasanna"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21290v1"
tags: ["query:cv"]
score: 8.5
evidence: 通过解耦图构建来加速视觉图神经网络
tldr: 视觉图神经网络（ViG）通过动态构建kNN图捕捉图像特征，但每层构图带来的高计算开销和顺序依赖严重限制了推理速度。本文提出GraphLeap，通过解耦构图与卷积过程，利用上一层特征预构建当前层图结构，实现构图与消息传递的并行处理。基于此，作者设计了首个端到端FPGA加速器，通过流水线设计和并行优化，在保持精度的同时显著提升了ViG的推理性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21290v1/fig-001.webp\", \"caption\": \"Fig. 1: Overall architecture of GraphLeap, featuring the Graph Construction Engine and Feature Update Engine and preprocessing and postprocessing modules.\", \"page\": 3, \"index\": 1, \"width\": 446, \"height\": 378}, {\"url\": \"assets/figures/arxiv/2604.21290v1/fig-002.webp\", \"caption\": \"TABLE VI: End-to-End Inference Latency (ms) and Speedup Analysis.\", \"page\": 8, \"index\": 2, \"width\": 1060, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2604.21290v1/fig-003.webp\", \"caption\": \"Fig. 5: End-to-End Inference Latency (ms) for ViG and GraphLeap variants (image resolution 224× 224) across hardware platforms.\", \"page\": 8, \"index\": 3, \"width\": 941, \"height\": 465}, {\"url\": \"assets/figures/arxiv/2604.21290v1/fig-004.webp\", \"caption\": \"Fig. 2: Dataflow for the Graph Construction Engine and the Feature Update Engine, including the structure of the Graph Convolution Engine.\", \"page\": 5, \"index\": 4, \"width\": 932, \"height\": 656}]"
motivation: 视觉图神经网络中每层动态构建kNN图的计算开销巨大且存在顺序依赖，成为制约推理速度的主要瓶颈。
method: 提出GraphLeap方法，通过解耦构图与特征更新，利用前一层特征预先构建下一层图结构，实现两者的并行执行。
result: 在Alveo U280 FPGA上，GraphLeap相比CPU实现最高95.7倍加速，相比GPU实现最高8.5倍加速。
conclusion: 该研究证明了通过算法与硬件协同设计，可以有效解决ViG的计算瓶颈，实现高效的实时视觉图神经网络推理。
---

## 摘要
视觉图神经网络 (ViGs) 将图像表示为补丁标记 (patch tokens) 的图，从而实现自适应的、特征驱动的邻域。与具有固定网格偏差的 CNN 或具有全局标记交互的 Vision Transformer 不同，ViG 依赖于动态图卷积：在每一层，通过对当前补丁特征进行 k-最近邻 (kNN) 搜索来构建依赖于特征的图，随后进行消息传递。这种逐层图构建是主要的瓶颈，在 CPU 和 GPU 上消耗了 50% 到 95% 的图卷积时间，其复杂度随补丁数量 N 呈 O(N^2) 增长，并在图构建和特征更新之间创建了顺序依赖关系。我们引入了 GraphLeap，这是一种简单的重新表述，通过跨层解耦图构建与特征更新来消除这种依赖关系。GraphLeap 使用前一层的特征构建的图在第 l 层执行特征更新，同时使用当前层的特征为第 l+1 层构建图。这种单层前瞻 (one-layer-lookahead) 的图构建方式实现了图构建与消息传递的并发执行。尽管使用前一层特征可能会引入轻微的精度下降，但只需进行几个周期的轻量级微调即可恢复原始精度。基于 GraphLeap，我们提出了首个用于视觉 GNN 的端到端 FPGA 加速器。我们的流式、层流水线设计将 kNN 图构建引擎与特征更新引擎重叠，利用了节点级和通道级并行性，并实现了高效的片上数据流，无需显式的边特征实例化。在 Alveo U280 FPGA 上对各向同性和金字塔型 ViG 模型进行的评估表明，GraphLeap 相比 CPU 基准实现了高达 95.7 倍的加速，相比 GPU 基准实现了 8.5 倍的加速，证明了实时视觉 GNN 推理的可行性。

## Abstract
Vision Graph Neural Networks (ViGs) represent an image as a graph of patch tokens, enabling adaptive, feature-driven neighborhoods. Unlike CNNs with fixed grid biases or Vision Transformers with global token interactions, ViGs rely on dynamic graph convolution: at each layer, a feature-dependent graph is built via k-nearest-neighbor (kNN) search on current patch features, followed by message passing. This per-layer graph construction is the main bottleneck, consuming 50--95\% of graph convolution time on CPUs and GPUs, scaling as $O(N^2)$ with the number of patches $N$, and creating a sequential dependency between graph construction and feature updates.   We introduce GraphLeap, a simple reformulation that removes this dependency by decoupling graph construction from feature update across layers. GraphLeap performs the feature update at layer $\ell$ using a graph built from the previous layer's features, while simultaneously using the current layer's features to construct the graph for layer $\ell+1$. This one-layer-lookahead graph construction enables concurrent graph construction and message passing. Although using prior-layer features can introduce minor accuracy degradation, lightweight fine-tuning for a few epochs is sufficient to recover the original accuracy. Building on GraphLeap, we present the first end-to-end FPGA accelerator for Vision GNNs. Our streaming, layer-pipelined design overlaps a kNN graph construction engine with a feature update engine, exploits node- and channel-level parallelism, and enables efficient on-chip dataflow without explicit edge-feature materialization. Evaluated on isotropic and pyramidal ViG models on an Alveo U280 FPGA, GraphLeap achieves up to $95.7\times$ speedup over CPU and $8.5\times$ speedup over GPU baselines, demonstrating the feasibility of real-time Vision GNN inference.